@CHARAMOVE_DATA_1(ARG, ARG:2)
LOCAL = 1
SELECTCASE ARG
	;연동설정 (RETURN=의존하는 캐릭터, RESULT:1~3=대등하게 연동되는 캐릭터)
	CASE 1
	;특수생활시간 설정 (CFLAG:LOCAL:생활시간보정 을 설정)
	CASE 3
	;이동경향 (±~10=가기 쉽다/가기 어렵다, -100=가지 않는다)
	CASE 4
		SELECTCASE ARG:2
			;よく居る
			CASE P2경내, P10툇마루
				RETURN 5
			;他所
			CASE P30삼요정의집・목욕탕, P39몽환유적・욕실, P56아운의거처
				RETURN -100
			CASE P502향림당
				RETURN 3
		ENDSELECT
	;출현장소
	CASE 7
		SELECTCASE ARG:2
			CASE 벚꽃길토리이, 신사경내, 신사본당, 신사툇마루
				RETURN 1
			CASE 광장, 상가거리, 술집소로, 주택가
				RETURN 1
			CASE 향림당
				RETURN 1
		ENDSELECT
	;인력설정 (CFLAG:LOCAL:인력점을 설정한다)
	CASE 8
ENDSELECT

@EXIST_캐릭터데이터1
#LOCALSIZE 1
#LOCALSSIZE 1

;オブジェクト本体
@캐릭터데이터1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "통칭"
	CALLF MAKE_STR(V_NAME, "레이무")

;여기서부터 의상 설정
CASE "오늘의 파자마"
	CALLF MAKE_STR(V_NAME, "파자마(주반) 세트")

CASE "평상복：액세서리"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("액세서리", "소매"))
CASE "평상복：모자"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("모자", "리본"))
CASE "평상복：신발"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("신발", "신발"))
CASE "평상복：양말"
	IF FLAG:2 > 0
		CALLF MAKE_INT(V_NAME, CLOTHES_ID("양말", "니삭스"))
	ELSE
		CALLF MAKE_INT(V_NAME, CLOTHES_ID("양말", "양말"))
	ENDIF
CASE "평상복：치마"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("치마", "치마"))
CASE "평상복：상반신윗도리1"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신윗도리1", "하쿠레이의 무녀옷"))

CASE "고유커맨드"
	CALLF MAKE_STR(V_NAME, "운세뽑기계약")
CASE "고유커맨드 실행 가능"
	CALLF MAKE_INT(V_NAME, !FLAG:운세뽑기계약)

;요리 관련
CASE "요리：좋아하는 맛"
	CALLF MAKE_STR(V_NAME, "쌀/양식/이파리/가벼운/담백/검소/품위있는/가정적/푹신푹신/" + "단/시큼한/" * 2 + "봄철/센베이/" * 3)
CASE "요리：싫어하는 맛"
	CALLF MAKE_STR(V_NAME, "매운/요력/" * 2 + "그로테스크/수수께끼 요리/" * 5)
CASE "요리：조수 효과"
	CALLF MAKE_STR(V_NAME, "담백한 맛/특기요리＊일식＊/")
;선물 관련
CASE "감성：좋아함" ;JP판 수정요망, 존재하지 않는 형용사 蓮華蝶の. 연꽃나비(蓮花蝶の) 오타로 추정
	CALLF MAKE_STR(V_NAME, "빨강빛/질서/귀여운/일본풍/" + "신령/주술/부적/여우의/봄의/영원한/" * 3 + "蓮華蝶の/벛꽃의/무녀의/낙원의/봉마의/바늘/불제봉/" * 5)
CASE "감성：싫어함"
	CALLF MAKE_STR(V_NAME, "혼돈/서양풍/로리타/매니악/" + "손목호구/기계/" * 3 + "요괴/" * 5 + "모리야의/요괴의/" * 10)

CASE "고유 속옷 형용사"
	CALLF MAKE_STR(V_NAME, "작고 붉은 리본이 프런트에 달린")
CASE "고유 하반신속옷명"
	CALLF MAKE_STR(V_NAME, "프런트 리본 팬티")
CASE "고유 상반신속옷"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 상반신속옷명"
	CALLF MAKE_STR(V_NAME, "리본 브래지어")
CASE "고유 속옷 착용 조건"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "고유 속옷 옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)

;농민 관련
;신사는 토지를 가지고 있음、칸다
CASE "농민：설명"
	CALLF MAKE_STR(V_NAME, "쌀 농가")
CASE "농민：작물"
	CALLF MAKE_STR(V_NAME, "쌀")
CASE "농민：밭의 종류"
	CALLF MAKE_STR(V_NAME, "논")
CASE "농민：밭의 맵"
	CALLF MAKE_STR(V_NAME, "하쿠레이 신사")

CASE "농민：맵 ID"
	CALLF MAKE_INT(V_NAME, 0)
CASE "농민：장소 ID"
	CALLF MAKE_INT(V_NAME, 22)
CASE "농민：보수"
	CALLF MAKE_INT(V_NAME, 400)
CASE "농민：금리"
	CALLF MAKE_INT(V_NAME, 5)

;여기는 의뢰 ID가 관련하지 않는 정보
;의뢰 발생율 스카2/일반3/고유 5
CASE "의뢰 없음 발생율"
	CALLF MAKE_INT(V_NAME, 2)
CASE "일반의뢰발생율"
	CALLF MAKE_INT(V_NAME, 3)
CASE "고유의뢰발생율"
	CALLF MAKE_INT(V_NAME, 5)
CASE "고유의뢰최종 요소"
	CALLF MAKE_INT(V_NAME, 103)
CASEELSE
	;고유의뢰관련의 데이터에 바톤 터치
	SIF STRCOUNT(O_DATA, "의뢰")
		CALLF 고유의뢰1(ARG, O_DATA, V_NAME)

;고유 카운터 테스트


ENDSELECT

;-------------------------------------------------
;고유요리설정
;DISH_NAME          요리명
;RESULTS            요리의 종류(경식/주식/디저트）
;CALL 재료설정("")  재료의 설정。"범용"을 넣으면 자동으로 범용 식재료가 된다
;DISH_TASTE         맛의 설정
;DISH_COMMENT       (임의）한줄 코멘트
;DISH_TIME          (임의）조리 소요 시간
;DISH_MAXLIMIT      (임의）요리의 지속 시간
;-------------------------------------------------
@SPECIAL_MENU_1
DISH_NAME = 홍백만쥬
RESULTS = 디저트
CALL 재료설정("감미재")
DISH_TASTE = 일식/만쥬/단/품위있는/예술적/쫄깃한/축복/
DISH_COMMENT = 경사 때 먹는、경사스러운 만쥬。[축복]의 효과가 있다。
RETURN 1

;IMAGE_I1_特殊服装(CHARA)-------------------------------------------------------------------
;キャラクター特有の服装表示条件
;エフェクトを共有する服装差分はこちらで指定する
;使用時注意点:
;	RESULTSに特殊服装を格納すること
;	書式は %服装%%特殊服装% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊服装(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:스탠딩종류
			;顔絵
			IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
				IF DAY:2 == 4
					RESULTS = _冬服
				ENDIF
			ENDIF
		ELSE
			;立ち絵
			IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
				IF DAY:2 == 4
					RESULTS = _冬服
				ENDIF
			ENDIF
		ENDIF
	ELSE
		;服
		IF FLAG:스탠딩종류
			;顔絵
			IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
				IF CURRENT_CLOTHES_SET(CHARA) == 55
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ELSEIF CHECK_CLO("bra_visible")
					RESULTS = _下着
				ELSEIF DAY:2 == 4
					RESULTS = _冬服
				ENDIF
			ENDIF
		ELSE
			;立ち絵
			IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
				IF CURRENT_CLOTHES_SET(CHARA) == 55
					RESULTS = _バニー
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
					RESULTS = _変T
				ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
					RESULTS = _セーラー
				ELSEIF CHECK_CLO("bra_visible") && EQUIP:CHARA:하반신속옷2 != 0
					RESULTS = _下着
				ELSEIF CHECK_CLO("bra_visible") && EQUIP:CHARA:하반신속옷2 == 0
					RESULTS = _ノーパン下着
				{
				ELSEIF (!EQUIP:CHARA:상반신윗도리1 && !EQUIP:CHARA:상반신윗도리2 && !EQUIP:CHARA:외투 &&
            			!EQUIP:CHARA:기모노 && !EQUIP:CHARA:원피스 &&
						!EQUIP:CHARA:레오타드 && !EQUIP:CHARA:보디슈트) &&
						EQUIP:CHARA:하반신속옷2 != 0
				}
					RESULTS = _ノーブラ下着
				ELSEIF DAY:2 == 4
					RESULTS = _冬服
				ENDIF
			ENDIF
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF CHECK_NUDE(CHARA)
		;裸
		IF FLAG:스탠딩종류
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ELSE
		;服
		IF FLAG:스탠딩종류
			;顔絵
		ELSE
			;立ち絵
		ENDIF
	ENDIF
ENDIF

;IMAGE_I1_体形(CHARA)-----------------------------------------------------------------------
;キャラクター特有の体形表示条件
;エフェクトを共有する体形差分はこちら
;服装を指定しても自動で表示されるので注意
;使用時注意点:
;	RESULTSに体形を格納すること
;	書式は %体形%%服装% 適宜"_"等入れること
;	"巨乳"は本編で実装済み %体形%巨乳%服装% で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I1_体形(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1) && FLAG:거유차이표시
			IF TALENT:CHARA:가슴사이즈 > 0
				RESULTS = 巨乳_
			ELSEIF TALENT:CHARA:가슴사이즈 < 0
				RESULTS = 貧乳_
			ENDIF
		ENDIF
	ELSE
		;立ち絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1) && FLAG:거유차이표시
			IF TALENT:CHARA:가슴사이즈 > 0
				RESULTS = 巨乳_
			ELSEIF TALENT:CHARA:가슴사이즈 < 0
				RESULTS = 貧乳_
			ENDIF
		ENDIF
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I1_特殊表情(CHARA)--------------------------------------------------------------------
;キャラクター特有の表情表示条件
;時間停止中でも維持する表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"通常","笑顔","怒り","発情","睡眠"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊表情(CHARA)
#DIM CHARA

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I1_特殊表情COM(CHARA, コマンド)-------------------------------------------------------
;直前のコマンドで変化するキャラクター特有の表情表示条件
;時間停止中は変化しない表情はこちら
;使用時注意点:
;	RESULTS:0に表情を格納
;	RESULTS:1にマウスオーバー時の表情を格納すること
;	"オナニー","愛撫","性交"は本編で実装済み
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊表情COM(CHARA, コマンド)
#DIM CHARA
#DIM コマンド

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I1_差分(CHARA)-----------------------------------------------------------------------
;キャラクター特有の差分表示条件
;裸やエフェクトを共有しない画像はこちら
;使用時注意点:
;	RESULTSに差分を格納すること
;	書式は {CHARA}%差分% 適宜"_"等入れること
;-------------------------------------------------------------------------------------------
@IMAGE_I1_差分(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	SIF DAY:2 == 4
		RESULTS = _冬服
ENDIF

;IMAGE_I1_特殊照れ(CHARA)--------------------------------------------------------------------
;キャラクター特有の照れエフェクト表示条件
;使用時注意点:
;	RESULTSに特殊照れを格納すること
;	書式は %特殊照れ%照れ
;	"裸"は本編で実装済み %特殊照れ%裸照れ で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊照れ(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
			IF CURRENT_CLOTHES_SET(CHARA) == 55
				RESULTS = バニー
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
				RESULTS = 変T
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
				RESULTS = セーラー
			ELSEIF DAY:2 == 4
				RESULTS = 冬服
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
ENDIF

;IMAGE_I1_特殊吐息(CHARA)--------------------------------------------------------------------
;キャラクター特有の吐息エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊吐息を格納すること
;	書式は %特殊吐息%吐息
;	"裸"は本編で実装済み %特殊吐息%裸吐息 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊吐息(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
			IF CURRENT_CLOTHES_SET(CHARA) == 55
				RESULTS = バニー
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
				RESULTS = 変T
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
				RESULTS = セーラー
			ELSEIF DAY:2 == 4
				RESULTS = 冬服
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I1_特殊汗(CHARA)--------------------------------------------------------------------
;キャラクター特有の汗エフェクト表示条件
;使用時注意点:
;	RESULTSに特殊汗を格納すること
;	書式は %特殊汗%汗
;	"裸"は本編で実装済み %特殊汗%裸汗 で追記可能
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊汗(CHARA)
#DIM CHARA
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
			IF CURRENT_CLOTHES_SET(CHARA) == 55
				RESULTS = バニー
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 62
				RESULTS = 変T
			ELSEIF CURRENT_CLOTHES_SET(CHARA) == 74
				RESULTS = セーラー
			ELSEIF DAY:2 == 4
				RESULTS = 冬服
			ENDIF
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ENDIF

;IMAGE_I1_特殊エフェクトチェック(CHAR, エフェクト)--------------------------------------------
;キャラクター特有のエフェクト判定  特殊エフェクトを直接指定したい場合に参照
;引数 "エフェクト" に固有のエフェクトが含まれるかを判定する
;例の通り、STRFINDを使用してください
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊エフェクトチェック(CHARA, エフェクト)
#DIM CHARA
#DIMS エフェクト

IF CFLAG:CHARA:대체적용
	;差し替え画像
ELSE
	;通常画像
	SIF STRFIND(エフェクト, "すやすや照れ") != -1
		RETURN 1
ENDIF

RETURN 0

;IMAGE_I1_特殊エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有のエフェクト表示条件
;使用時注意点:
;	RESULTS配列に特殊エフェクトを格納
;	RETURNで追加するエフェクト数を返却すること
;-------------------------------------------------------------------------------------------
@IMAGE_I1_特殊エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
	ELSE
		;立ち絵
	ENDIF
ELSE
	;通常画像
	IF FLAG:스탠딩종류 && CHECK_BLUSH(CHARA) && CFLAG:CHARA:수면
		;顔絵
		RESULTS:エフェクト数 = すやすや照れ
		エフェクト数 ++
	ELSE
		;立ち絵
	ENDIF
ENDIF

RETURN エフェクト数

;IMAGE_I1_自慰エフェクト(CHARA)--------------------------------------------------------------------
;キャラクター特有の自慰エフェクト表示条件
@IMAGE_I1_自慰エフェクト(CHARA)
#DIM CHARA
#DIM DYNAMIC エフェクト数 = 0
VARSET RESULTS

IF CFLAG:CHARA:대체적용
	;差し替え画像
	IF FLAG:스탠딩종류
		;顔絵
		IF GROUPMATCH(CFLAG:CHARA:대체적용, 1)
			RESULTS:エフェクト数 = 別顔_AFTERTRAニー_1
			SIF TALENT:CHARA:가슴사이즈 > 0 && FLAG:거유차이표시
				RESULTS = 別顔_AFTERTRAニー巨乳_1
			エフェクト数 += 1
		ENDIF
	ELSE
		;立ち絵
	ENDIF
ELSE
	;活動時間中は通常画像
	SIF BETWEENTIME(CFLAG:CHARA:기상시간, CFLAG:CHARA:취침시간)
		RETURN
	;通常画像
	IF FLAG:스탠딩종류
		;顔絵
		;AFTERTRAニーは寝間着
		RESULTS:エフェクト数 = 顔絵_AFTERTRAニー_1
		SIF TALENT:CHARA:가슴사이즈 > 0 && FLAG:거유차이표시
			RESULTS = 顔絵_AFTERTRAニー巨乳_1
		エフェクト数 += 1
	ELSE
		;立ち絵
	ENDIF
ENDIF

RETURN エフェクト数
;********************************************************************************************
