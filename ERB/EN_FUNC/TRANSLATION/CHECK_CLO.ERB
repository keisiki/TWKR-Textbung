@CHECK_CLO(ARGS, ARG = -1)
#FUNCTION

SIF ARG == -1
	ARG = TARGET

SELECTCASE ARGS
    CASE "belly_visible"
    {
        RETURNF !EQUIP:ARG:상반신윗도리1 && !EQUIP:ARG:상반신윗도리2 && !EQUIP:ARG:외투
            && !EQUIP:ARG:기모노 && !EQUIP:ARG:원피스 && !EQUIP:ARG:레오타드 && !EQUIP:ARG:보디슈트
    }
    CASE "bra_visible"
    {
        RETURNF !EQUIP:ARG:상반신윗도리1 && !EQUIP:ARG:상반신윗도리2 && !EQUIP:ARG:외투
            && !EQUIP:ARG:기모노 && !EQUIP:ARG:원피스 && !EQUIP:ARG:레오타드 && !EQUIP:ARG:보디슈트
            && (EQUIP:ARG:상반신속옷1 || EQUIP:ARG:상반신속옷2)
    }
    CASE "bra_overridden"
        LOCAL = CURRENT_CLOTHES_SET(ARG)
        IF LOCAL == 100
            RETURNF CFLAG:ARG:FAVORITE_OVERRIDE_UNDERWEAR
        ELSE
            RETURNF GET_INT(ARG, "의상세트", LOCAL, "OVERRIDE_UNDERWEAR_TOP")
        ENDIF
    CASE "panties_overridden"
        LOCAL = CURRENT_CLOTHES_SET(ARG)
        IF LOCAL == 100
            RETURNF CFLAG:ARG:9128 ;FAVORITE_OVERRIDE_UNDERWEAR
        ELSE
            RETURNF GET_INT(ARG, "의상세트", LOCAL, "OVERRIDE_UNDERWEAR_BOTTOM")
        ENDIF
ENDSELECT
RETURNF 0

@CHECK_PANTIES(ARGS, ARG = -1)
#FUNCTION
SIF ARG == -1
	ARG = TARGET

SELECTCASE ARGS
    CASE "can_pilfer"
        RETURNF EQUIP:ARG:0 && ARG && EQUIP:ARG:하반신속옷2 != 0 && GET_INT(ARG, "하반신속옷_비켜놓기가능", EQUIP:ARG:하반신속옷2, "콜렉션 표시 가능")
    CASE "can_shift_aside"
        RETURNF EQUIP:ARG:0 && ARG && EQUIP:ARG:하반신속옷2 != 0 && GET_INT(ARG, "하반신속옷_비켜놓기가능", EQUIP:ARG:하반신속옷2, "콜렉션 표시 가능")
ENDSELECT


@CURRENT_CLOTHES_SET(ARG)
#FUNCTION
SIF CFLAG:ARG:의상일시변경
    RETURNF CFLAG:ARG:의상일시변경

RETURNF CFLAG:ARG:기본복장세트