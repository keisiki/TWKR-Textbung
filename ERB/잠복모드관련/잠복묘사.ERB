@잠복묘사

;一行目 潜伏場所、現在のモード、잠복율を表示
IF GETBIT(FLAG:잠복,2)
	IF BASE:MASTER:잠복율 >= 70
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("赤")
		PRINTFORM 会話モード
		RESETCOLOR
		SETCOLOR COLOR("緑")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSEIF BASE:MASTER:잠복율 >= 40
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("赤")
		PRINTFORM 会話モード
		RESETCOLOR
		SETCOLOR COLOR("黄")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSE
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("赤")
		PRINTFORM 会話モード
		RESETCOLOR
		SETCOLOR COLOR("赤")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ENDIF
ELSEIF GETBIT(FLAG:잠복,1)
	IF BASE:MASTER:잠복율 >= 70
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("黄")
		PRINTFORM 探索モード
		RESETCOLOR
		SETCOLOR COLOR("緑")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSEIF BASE:MASTER:잠복율 >= 40
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("黄")
		PRINTFORM 探索モード
		RESETCOLOR
		SETCOLOR COLOR("黄")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSE
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("黄")
		PRINTFORM 探索モード
		RESETCOLOR
		SETCOLOR COLOR("赤")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ENDIF
ELSE
	IF BASE:MASTER:잠복율 >= 70
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("緑")
		PRINTFORM 潜伏モード
		RESETCOLOR
		SETCOLOR COLOR("緑")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSEIF BASE:MASTER:잠복율 >= 40
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("緑")
		PRINTFORM 潜伏モード
		RESETCOLOR
		SETCOLOR COLOR("黄")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ELSE
		PRINTFORM %NAME_HIDING_PLACE()%
		SETCOLOR COLOR("緑")
		PRINTFORM 潜伏モード
		RESETCOLOR
		SETCOLOR COLOR("赤")
		PRINTFORML （잠복율 {BASE:MASTER:잠복율}％）
		RESETCOLOR
	ENDIF
ENDIF
;二行目 情景描写
;IF GETBIT(FLAG:잠복,2)
;	IF BASE:MASTER:잠복율 >= 70
;	ELSEIF BASE:MASTER:잠복율 >= 40
;	ELSE
;	ENDIF
;ELSEIF GETBIT(FLAG:잠복,1)
;	IF BASE:MASTER:잠복율 >= 70
;	ELSEIF BASE:MASTER:잠복율 >= 40
;	ELSE
;	ENDIF
;ELSE
;	IF BASE:MASTER:잠복율 >= 70
;	ELSEIF BASE:MASTER:잠복율 >= 40
;	ELSE
;	ENDIF
;ENDIF
