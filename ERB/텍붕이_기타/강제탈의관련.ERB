@강제탈의해제가능(ARG)
#FUNCTION
IF CFLAG:ARG:현재위치 != CFLAG:ARG:초기위치
    RETURNF 0
ENDIF
IF TALENT:ARG:당신의노예
    IF CFLAG:ARG:강제탈의 == 3 && !CFLAG:ARG:시간정지들킴
    ELSE
        RETURNF 0
    ENDIF
ENDIF
IF !SHIRAHU(ARG)
    RETURNF 0
ENDIF
IF TBCFLAG:ARG:강제탈의해제금지
    RETURNF 0
ENDIF
IF RAND:3 != 0
    RETURNF 0
ENDIF
RETURNF 1

@강제탈의해제(ARG)
IF CFLAG:ARG:현재위치 == CFLAG:MASTER:현재위치
    PRINTFORML %ARG는% 옷장에서 새 옷을 꺼내입었다
ENDIF
CFLAG:ARG:강제탈의 = 0
CALL CLOTHES_SETTING_TRAIN(TARGET)