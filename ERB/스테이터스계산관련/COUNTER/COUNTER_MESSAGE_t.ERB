@EVENT_COUNTER_MESSAGE
SIF TARGET <= 0
	RETURN 0
CALL DATUI_MESSAGE
SIF TCVAR:피임
	CALL SET_HININ(TARGET)
RESULT = 0
TRYCALLFORM EVENT_COUNTE_MESSAGE{TCVAR:20}
SIF RESULT
	PRINTL
IF CFLAG:TARGET:구상셀렉터
	TRYCALLFORM M_KOJO_K{NO:TARGET}_{CFLAG:TARGET:구상셀렉터}
ELSE
	TRYCALLFORM M_KOJO_K{NO:TARGET}
ENDIF
SIF FLAG:대사색깔
	TRYCALLFORM M_KOJO%RESULTS%_COLOR_K{NO:TARGET}

;------------------------------------------
;1~9 커뮤니케이션
;------------------------------------------
;거리가 가깝다
@EVENT_COUNTE_MESSAGE1
PRINTFORML 어쩐지 %CALLNAME:TARGET%의 거리가 가깝다…
RETURN 1

;치라 보고
@EVENT_COUNTE_MESSAGE2
PRINTFORML %타겟으로%부터 시선을 느낀다…
SIF !RAND:4
	PRINTFORML %조교자와% 시선이 마주치자 %타겟은% \@ TALENT:TARGET:연모 ?  수줍게 웃었다 # 황급히 시선을 돌렸다\@
RETURN 1

;이야기
@EVENT_COUNTE_MESSAGE3
PRINTFORML %타겟은% %CALLNAME:PLAYER%에 말을 건네 왔다…
RETURN 1

;좋은 냄새
@EVENT_COUNTE_MESSAGE4
PRINTFORML %타겟으로%부터 가볍게 좋은 냄새가 했다…
RETURN 1

;안이고 후 (? )
@EVENT_COUNTE_MESSAGE9

;양쪽 모두의 구멍에 사정하고 있었을 경우, 한편 2분의 1
IF 충전율(TARGET,1) > 0 & 충전율(TARGET,2) > 0 & RAND:2 == 0
	IF 충전율(TARGET,3) >= 1000
		PRINTFORML %CALLNAME:TARGET%의 두 구멍으로부터는, 한없이 \@ TALENT:MASTER:농후정액? 굉장한 성취를 발하는# \@정액이 흘러넘치고 있다……
	ELSEIF 충전율(TARGET,3) >= 100
		PRINTFORML %CALLNAME:TARGET%의 두 구멍으로부터 \@ TALENT:MASTER:농후정액?  끈적끈적한# \@정액이 쏟아졌다……
	ELSEIF 충전율(TARGET,3) >= 50
		PRINTFORML %CALLNAME:TARGET%의 두 구멍으로부터 \@ TALENT:MASTER:농후정액?  끈적끈적한# \@정액이 쏟아졌다……
	ENDIF
ELSE
	IF 충전율(TARGET, 1) >= 1000
		PRINTFORML %CALLNAME:TARGET%의 질구로부터는 숨길 수 없을 만큼 \@ TALENT:MASTER:농후정액?  끈적끈적한# \@정액이 흘러넘치고 있다…
	ELSEIF 충전율(TARGET, 1) >= 100
		PRINTFORML %CALLNAME:TARGET%의 질구로부터 \@ TALENT:MASTER:농후정액?  끈적끈적한# \@정액이, 허벅지를 타고 떨어졌다…
	ELSEIF 충전율(TARGET, 1) >= 50
		PRINTFORML %CALLNAME:TARGET%의 자궁에서 \@ TALENT:MASTER:농후정액?  끈적끈적한# \@정액이, 울컥 굽이쳤다…
	ENDIF

	IF 충전율(TARGET, 2) >= 1000
		PRINTFORML %CALLNAME:TARGET%의 항문으로부터 정액이 \@ TALENT:MASTER:농후정액? 덩어리가 되어# \@흘러넘치며 떨어졌다…
	ELSEIF 충전율(TARGET, 2) >= 100
		PRINTFORML %CALLNAME:TARGET%의 장을 \@ TALENT:MASTER:농후정액? 농후한# \@정액이 채우고 있다…
	ELSEIF 충전율(TARGET, 2) >= 50
		PRINTFORML %CALLNAME:TARGET%의 장에 \@ TALENT:MASTER:농후정액? 농후한# \@정액이 흡수되어 간다…
	ENDIF
ENDIF


RETURN 1
;------------------------------------------
;10~19 베리소프트
;------------------------------------------
;접근해 온다
@EVENT_COUNTE_MESSAGE11
PRINTFORM %타겟은% 
;심약계
IF CFLAG:TARGET:성격경향 == 1
	PRINTFORM 흠칫흠칫 
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORM 기분이 언짢은 듯
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORM 즐거운 모습으로 
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORM 자연스럽게 
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORM 태연히 
ENDIF
PRINTFORML %CALLNAME:PLAYER% 곁에 접근해 왔다…
RETURN 1

;웃는 얼굴을 띤다
@EVENT_COUNTE_MESSAGE12
PRINTFORM %타겟은% %CALLNAME:PLAYER%에 
;심약계
IF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 대해, 수줍어하면서 미소 짓고 있다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 향해, 입꼬리를 살짝 풀고 있다…
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORML 횻토코 가면을 드러내며 고개를 돌려 왔다…
	ELSE
		PRINTFORML 즐겁게 웃는 얼굴을 돌려 왔다…
	ENDIF
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 향해 희미하게 미소 짓고 있다…
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORML 향해 희미한 미소를 띠고 있다…
ENDIF
RETURN 1

;상관해 갖고 싶어한다
@EVENT_COUNTE_MESSAGE13
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
;심약계
IF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 옷의 소매를, 몰래 잡아당기고 있다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 옆구리를, 손가락으로 쿡쿡 찔러 온다…
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 팔을 잡아, 자신의 팔과 얽혀 온다…
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 팔에, 자신의 팔을 맞대고 있다…
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORML 어깨에 손을 뻗어서는, 곧바로 움츠리며 …
ENDIF
RETURN 1

;몸을 문질러 댄다
@EVENT_COUNTE_MESSAGE14
PRINTFORM %타겟은% 
;심약계
IF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 흠칫거리는 모습으로, %CALLNAME:PLAYER%에 몸을 바짝 다가왔다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 무엇인가 말에, % CALLNAME:PLAYER%의 어깨에 손을 얹어 왔다…
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 횻토코 가면을 띄우고
	ELSE
		PRINTFORM 기쁜 듯한 모습으로 
	ENDIF
	PRINTFORML %CALLNAME:PLAYER%에 기대어 왔다…
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 늠름한 표정인 채, %CALLNAME:PLAYER%의 바로 옆에 왔다…
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORML 평상시 그대로의 표정으로, %CALLNAME:PLAYER%에 바싹 다가왔다…
ENDIF
RETURN 1

;응시해 온다
@EVENT_COUNTE_MESSAGE15
PRINTFORM %타겟은% 
;심약계
IF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 수줍은 얼굴을 뺨에 띠며 %조교자를% 응시하고 있다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 왠지 입술을 삐죽이고, %조교자를% 노려보고 있다…
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 횻토코 가면을 띄우고 
	ELSE
		PRINTFORM 넋을 잃고서 
	ENDIF
	PRINTFORML %조교자를% 응시하고 있다…
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 부드러운 미소와 함께 %조교자를% 응시하고 있다…
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORML 말하기 어려운 표정으로, %조교자를% 응시하고 있다…
ENDIF
RETURN 1

;어깨에 턱이상
@EVENT_COUNTE_MESSAGE16
PRINTFORML %타겟은% 뒤로부터 %CALLNAME:PLAYER%의 목에 팔을 돌려, 어깨에 턱을 얹었다…
RETURN 1

;차입
@EVENT_COUNTE_MESSAGE17
PRINTFORML %타겟은% %CALLNAME:PLAYER%에게 간식을 가져와 주었다…
RETURN 1
;------------------------------------------
;20~29 유혹
;------------------------------------------
;요염한 행동
@EVENT_COUNTE_MESSAGE20
PRINTFORM %타겟은% 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 부끄러운 듯이 뺨을 붉게 물들이고 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 도발하듯이 히죽 웃으며 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고 
		ELSE
			PRINTFORM 상냥한 미소를 띠고 
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 쑥스러운 듯하게 하면서 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 입꼬리를 느슨하게 하고 
	ENDIF
ENDIF
PRINTFORML 요염한 행동으로 유혹해왔다…
RETURN 1

;응시한다
@EVENT_COUNTE_MESSAGE21
PRINTFORM %타겟은% %조교자를% 
;그 외
IF CFLAG:TARGET:성격경향 == 5 || (!FLAG:카운터지문제어 && (TARGET != 57 || (TARGET == 57 && TALENT:연모)))
	PRINTFORM 촉촉한 눈으로 뜨겁게 
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORM 눈을 열심히 부릅뜨고 
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORM 교태를 뒤섞은 날카로움으로 
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 횻토코 가면을 띄워 
	ELSE
		PRINTFORM 녹는 것 같은 미소를 띠며 
	ENDIF
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORM 애정의 시선으로 
ENDIF
PRINTFORML 응시하고 있다…
RETURN 1

;몸을 접댄다
@EVENT_COUNTE_MESSAGE22
PRINTFORM %타겟은% %CALLNAME:PLAYER%에 
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 엉겨들듯 온몸을 접근해왔다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 조심스럽지만, 제대로 몸을 붙여왔다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 억지로 접근해, 이쪽의 어깨에 머리를 맡겨 온다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 횻토코 가면을 띄우면서 
	ELSE
		PRINTFORM 기쁜듯하게 미소를 띠고 
	ENDIF
	PRINTFORML , 끈적끈적과 응석부려 온다 
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 살짝 접할 정도로의 거리에 들러 왔다…
ENDIF
RETURN 1

;치마걷어 올리기
@EVENT_COUNTE_MESSAGE23
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORM %타겟은% 빈틈을 찔러 
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORM 뒤로부터 몰래 가까워진 %타겟은% 
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORM 눈앞에 단지 %타겟이% 무슨 예고도 없고 
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORM 스윽 모여 온 %타겟이% 
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 노인 가면을 띄우고 
	ELSE
		PRINTFORM 미소를 띠고 
	ENDIF
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORM %타겟은% 남몰래 %CALLNAME:PLAYER%의 하반신에 손을 뻗어 
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 치마를 걷어 붙였다
IF !EQUIP:PLAYER:하반신속옷2
	PRINTFORM 치마 속에 아무것도 입지 않은 하반신을
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:PLAYER:하반신속옷2, PLAYER) %%조사처리(PANTSNAME(EQUIP:PLAYER:하반신속옷2, PLAYER),"를")%
ENDIF
PRINTL 확실히 들켜 버렸다…
RETURN 1

;입김을 내뿜는다
@EVENT_COUNTE_MESSAGE24
PRINTFORML %타겟은% %CALLNAME:PLAYER%의 배후에 소리없이 다가와 귓전에 뜨거운 숨결을 내뿜었다…
IF FLAG:카운터지문제어
	PRINTFORM 놀라 되돌아 보는 %CALLNAME:PLAYER%의 눈에 
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 몇 발짝 물러선 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 우쭐거린 표정의 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 횻토코 가면을 띄운 
		ELSE
			PRINTFORM 즐거운 듯이 웃는 
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 조금 수줍어하고 있는 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 태연한 표정의 
	ENDIF
	PRINTFORML %타겟이% 비친다
ENDIF
RETURN 1

;엉덩이를어루만진다
@EVENT_COUNTE_MESSAGE25
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 엉덩이를 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 흠칫흠칫한 소프트 터치로 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 꽉 움켜쥐고 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고 
		ELSE
			PRINTFORM 즐겁게 
		ENDIF
		PRINTFORM 살살 
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 맛사지를 하듯이 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 표면을 가볍게 
	ENDIF
ENDIF
PRINTFORML 쓰다듬고 있고 왔다…
RETURN 1

;팔 달라붙어
@EVENT_COUNTE_MESSAGE26
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 팔
IF !FLAG:카운터지문제어
	PRINTFORML 에 안겨들었다
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 을 살며시 잡고 꼭 껴안았다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 을 힘껏 끌어안았다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORM 을 쑤욱 이끌어,
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 뺨을 붉게 한 무표정으로
	ENDIF
	PRINTFORM 팔을 걸어 왔다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 을 잡고, 손을 잡아 왔다
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
	PRINTFORML 을 제대로 꼭 껴안았다
ENDIF
SIF TALENT:TARGET:가슴사이즈 > 0
	PRINTFORML %CALLNAME:PLAYER%의 팔에 %CALLNAME:TARGET%의 부드러운 가슴이 짓눌렸다…
RETURN 1

;골짜기를 보인다
@EVENT_COUNTE_MESSAGE27
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 눈앞에서, 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 수줍음에 뺨을 붉히면서도 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 당당히 자랑하듯 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 불쑥 이가면을 흔들어 
		ELSE
			PRINTFORM 즐거운 모습으로 
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 눈을 치켜 뜨고 %조교자를% 응시하면서 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 어딘지 모르게 기쁜 듯이 
	ENDIF
ENDIF
LOCAL = 0
LOCAL:1 = 0
FOR LOCAL,1,CHARANUM
	SIF CFLAG:PLAYER:현재위치 == CFLAG:LOCAL:현재위치 && LOCAL != TARGET
		LOCAL:1 ++
	SIF TCVAR:LOCAL:친해져 >= 2
		LOCAL:1 --
NEXT
IF GETBIT (TEQUIP:TARGET:1,0) && !LOCAL:1
	SIF !FLAG:카운터지문제어
		PRINTFORM 대담하게 
	PRINTFORM 옷을 벗고, 
ENDIF
IF TALENT:TARGET:가슴사이즈 >= 1
	PRINTFORML \@ TALENT:TARGET:가슴사이즈 > 1 ?  풍만한 흉 # 흉 \@의 골짜기를 과시해 왔다……
ELSEIF TALENT:TARGET:가슴사이즈 == 0
	PRINTFORML 가슴을 모아 골짜기를 만들어 과시해 왔다……
ENDIF
RETURN 1

;엉덩이 즈리
@EVENT_COUNTE_MESSAGE28
PRINTFORM %타겟은% 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 당황하도록 하면서도, 천천히 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 빙글빙글 문지르듯, 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM 숨을 헐떡이면서, 
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 눈을 숙여 어색한 듯이 하면서도, 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
	ENDIF
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 엉덩이에 옷 너머로 페니스를 꽉 눌러 왔다…
RETURN 1

;------------------------------------------
;30~44 착의 성희롱
;------------------------------------------
;키스
@EVENT_COUNTE_MESSAGE30
SELECTCASE RAND:4
	CASE 0
		PRINTFORM %타겟은% %CALLNAME:PLAYER%
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORM 에 얼굴을 접근해 갑자기 입술을 빼앗았다……
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 에 얼굴을 대며,  가냘프게 입술을 포갰다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 의 턱을 손가락으로 잡고 억지로 입술을 빼앗았다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 에, 노인 가면을 향하면서
				ELSE
					PRINTFORM 의 얼굴을 넋을 잃고 바라봐
				ENDIF
				PRINTFORML 입술을 바쳐 왔다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 의 입술에 자신의 입술을 살며시 거듭해 왔다
		ENDIF
	CASE 1
		PRINTFORM %타겟은% %CALLNAME:PLAYER%
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 에 가볍게 접하는 것 같은 키스를 했다……
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 에 키스해줘, 얼굴을 진적으로 한 뒤 곧바로 떨어졌다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 의 눈을 확실히 응시해, 입술을 접근해왔다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
				PRINTFORM 의 입술의 감촉을 손가락으로 확인해
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 뺨을 붉게 한 무표정으로
				ENDIF
				PRINTFORML 살며시 키스해줘 왔다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 의 입술에, 탁같은 키스를 해 왔다
		ENDIF
	CASE 2
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML %타겟은% %CALLNAME:PLAYER%에 끈적끈적하고 농후한 키스를 했다……
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 입술을 거듭한 %타겟이%, 흠칫흠칫%CALLNAME:PLAYER%의 입술을 비집고 혀를 넣어 왔다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 억지로 입술을 포개어 오는 %CALLNAME:TARGET%의 혀가, %CALLNAME:PLAYER%의 입안을 유린한다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 노인 가면을 띄운다
				ELSE
					PRINTFORM 넋을 잃고 있다
				ENDIF
				PRINTFORML %타겟은% %CALLNAME:PLAYER%의 구강에 혀를 내며 왔다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% 입술을 겹친 채, 혀를 천천히 입안에 침입시켜 왔다
		ENDIF
	CASE 3
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || CFLAG:TARGET:성격경향 == 4 || !FLAG:카운터지문제어
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 뺨에 손을 대며 키스를 했다……
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML %타겟은% 흠칫흠칫%CALLNAME:PLAYER%의 뺨에 손을 포개며,  입술을 바쳐 왔다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 턱을 잡아, 제대로 입술을 거듭해 왔다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 노인 가면을 띄우면서, 무표정하게 뺨을 붉히고
				ELSE
					PRINTFORM %타겟은% 기쁜 듯이 웃어
				ENDIF
				PRINTFORML , %CALLNAME:PLAYER%의 뺨을 양손으로 끼우며 키스해줘 왔다
		ENDIF
ENDSELECT
RETURN 1

;뒤로부터 안겨든다
@EVENT_COUNTE_MESSAGE31
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML %조교자는% 등 뒤로 %CALLNAME:TARGET%에 안아 지쳤다
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 등 뒤에서 가까워진 %타겟이%, 흠칫흠칫 %CALLNAME:PLAYER%에 안겨들었다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML %CALLNAME:PLAYER%의 등 뒤에서 슬쩍 접근해온 %타겟이%, 마음껏 꼭 껴안아 왔다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORM %타겟이%
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 노인 가면을 띄워
	ELSE
		PRINTFORM 쿡쿡 웃으면서
	ENDIF
	PRINTFORML , 등 뒤에서 %CALLNAME:PLAYER%에 안겨들었다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML %타겟은% %조교자를% 뒤로부터 살며시 껴안아 왔다
ENDIF
SIF TALENT:TARGET:가슴사이즈 > 0
	PRINTFORML %CALLNAME:PLAYER%의 등에 %CALLNAME:TARGET%의 부드러운 가슴을 꽉 누를 수 있다…
RETURN 1

;앞에서 안겨든다
@EVENT_COUNTE_MESSAGE32
PRINTFORM %타겟은% %CALLNAME:PLAYER%
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORM 를 꼭 껴안고
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORM 에 흠칫흠칫 달라붙어
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORM 를 도발하면서 웃음
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	IF TARGET == 57 && !TALENT:연모
		PRINTFORM 에 불쑥 이 면을 향해라
	ELSE
		PRINTFORM 에 환하게 미소지어 써라
	ENDIF
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORM 를 상냥하게 껴안아
ENDIF
IF TALENT:TARGET:체형 > TALENT:PLAYER:체형
	PRINTFORML 그\@ TALENT:TARGET:가슴사이즈 > 0 ?  풍만한 흉 # 흉 \@으로 %CALLNAME:PLAYER%의 얼굴을 쌌다…
ELSE
	PRINTFORML 숨을 거칠게 하면서 몸을 접근해왔다…
ENDIF
;IF CFLAG:우후후 && TALENT:TARGET:가슴사이즈 >= 0
;	PRINTFORML %CALLNAME:MASTER%의 
RETURN 1

;가슴을 비비게 한다
@EVENT_COUNTE_MESSAGE33
PRINTFORM %타겟은% 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 흠칫흠칫 하면서도 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 입술을 삐쭉 내밀어서 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고 
		ELSE
			PRINTFORM 만면의 미소로 
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 살짝 몸을 굳어지게 한이면서 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 즐거운 모습으로 
	ENDIF
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 손을 잡아 자신의 가슴에 이끌었다
PRINTFORML %CALLNAME:TARGET%의 심장의 고동이 손으로부터 전해져 온다…
RETURN 1

;고간뒤진다
@EVENT_COUNTE_MESSAGE34
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 \@ TALENT:PLAYER:2 & 2 ? 고간 # 비소 \@
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 에 손을 신섬 뒤지기 시작했다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 에 흠칫흠칫 손을 뻗어 손대고 있다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 를 몹시 거칠게 어루만지고 문지르기 시작했다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 를 상냥한 손놀림으로 어루만지기 시작했다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 를 감싸듯이 접해 왔다…
ENDIF
RETURN 1

;치마안을 보인다
@EVENT_COUNTE_MESSAGE35
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1 || TALENT:수치심 > 0
		PRINTFORM 눈물을 눈초리에 띄웠다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 입가에 미소를 띠었다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 불쑥 이 면을 띄웠다
		ELSE
			PRINTFORM 즐거운 표정의
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 얼굴을 붉게 했다
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 알려지고 로 한 표정으로
	ENDIF
ENDIF
PRINTFORML %타겟은% \@ TALENT:수치심 > 0 ? 흠칫흠칫 # \@자신의 %SHOW_BOTTOM(TARGET, 1)%를 걷어 붙여
PRINTFORM %CALLNAME:PLAYER%의 눈앞에 
IF TEQUIP:TARGET:50 == MASTER
	PRINTFORM %조교자와%의 결합부
ELSEIF 충전율(TARGET, 1) > 100 || 충전율(TARGET, 2) > 100
	PRINTFORM 정액을 분출해 흘리는 고간
ELSEIF 충전율(TARGET, 1) > 50 || 충전율(TARGET, 2) > 50
	IF !EQUIP:하반신속옷2
		PRINTFORM 정액을 흘리는 고간
	ELSE
		PRINTFORM 정액을 스며들게 한 %PANTS_DESCRIPTION(EQUIP:하반신속옷2, TARGET) %%PANTSNAME(EQUIP:하반신속옷2, TARGET)%
	ENDIF
ELSEIF !EQUIP:하반신속옷2
	PRINTFORM %SHOW_BOTTOM(TARGET, 1)% 안의 아무것도 입지 않은 하반신
ELSE
	PRINTFORM %PANTS_DESCRIPTION(EQUIP:하반신속옷2, TARGET) %%PANTSNAME(EQUIP:하반신속옷2, TARGET)%
ENDIF
PRINTFORML 를 노출…
RETURN 1

;뒤로부터 가슴 만지작 만지작
@EVENT_COUNTE_MESSAGE36
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 등 뒤로부터 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 허약한 힘으로 포근포근하고 
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 강한 힘으로 물컹물컹 
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM 보통 힘으로 몰랑몰랑 
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 부드러운 손놀림으로 
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 그다지 힘을 집중하지 않고 
	ENDIF
ENDIF
PRINTFORML 가슴을 주무르는 대로라고 왔다…
RETURN 1

;엿강탈
@EVENT_COUNTE_MESSAGE39
PRINTFORML %타겟은% %CALLNAME:MASTER%의 엿을 빼앗았다……
RETURN 1
;------------------------------------------
;40~49 애원
;------------------------------------------

@EVENT_COUNTE_MESSAGE41
PRINTFORM %타겟은% \@ BEDROOM() ? 위로 향해 되어# \@조금 다리를 열어,
IF TALENT:TARGET:처녀 == 1 || TALENT:TARGET:처녀 == -1
	PRINTFORM 긴장한 표정으로 
ELSE
	SIF EX:V절정직전
		PRINTFORM 화살도 방패도 참을 수 없는 모습으로 
	PRINTFORM 음부를 넓혀 
ENDIF
PRINTFORML %마스터를% 유혹했다…

;==================================================
;후배위 V삽입 애원
;==================================================
;상대 주도로 당신이 V에 삽입 당한다
;==================================================
@EVENT_COUNTE_MESSAGE43

;PRINTFORM %타겟은% \@ BEDROOM() ? 넙죽 엎드리며# 벽에 손을 붙어\@ 
;(이)라고→에서의 흐름에 위화감을 기억하므로 지문의 제시
;PRINTFORM %타겟은% \@ TALENT:동물귀? 귀를 삑삑하고 뛰게 한이면서# \@\@ BEDROOM() ? 엎드린 자세로 되어# 벽에 손을 붙어\@
PRINTFORM %타겟은% \@ TALENT:동물귀? 귀를 삑삑하고 뛰게 한이면서# \@
IF !INROOM(CFLAG:PLAYER:현재위치) && !(BATHROOM() && ITEM:20)
	PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ? 엉덩이를 쑥 내밀어 # 벽에 손을 붙어 \@
ELSE
	PRINTFORM 엎드린 자세로 되어 
ENDIF
IF TALENT:TARGET:처녀 == 1 || TALENT:TARGET:처녀 == -1
	PRINTFORM 긴장한 표정으로 
ELSE
	SIF EX:V절정직전
		PRINTFORM 화살도 방패도 참을 수 없는 모습으로 
ENDIF
PRINTFORML %HIP(TARGET)%를 흔들어 %마스터를% 유혹했다…
SIF CFLAG:TARGET:생해금 == 2 && ESTRUS_CYCLE(TARGET) == 150
	PRINTFORM %CALLNAME:TARGET%의 자궁은 준비를 갖추어지게 해 %CALLNAME:MASTER%의 \@ TALENT:MASTER:동물귀 ? 교배 # 유전자 \@를 요구하고 뜨겁게 쑤시고 있다…

;==================================================
;후배위 A삽입 애원
;==================================================
;상대 주도로 당신이 A에 삽입 당한다
;==================================================
@EVENT_COUNTE_MESSAGE44

PRINTFORM %타겟은% \@ TALENT:동물귀? 귀를 삑삑하고 뛰게 한이면서# \@
IF !INROOM(CFLAG:PLAYER:현재위치) && !(BATHROOM() && ITEM:20)
	PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  엉덩이를 쑥 내밀어 # 벽에 손을 붙어 \@
ELSE
	PRINTFORM 엎드린 자세로 되어
ENDIF
PRINTFORML \@ BEDROOM() ? 엉덩이를 열어 # %HIP(TARGET)%를 흔들어 \@%마스터를% 유혹했다…

;@EVENT_COUNTE_MESSAGE45
;PRINTFORM %타겟은% 
;
;PRINTFORML 위로 향해 되어 가랑이를 열어 
;PRINTFORML 넙죽 엎드리며 엉덩이를 향해라 
;지문 부족에 고민하는 작자를 위해 써 보았다
;PRINTFORML 부끄럽기 때문에 빨리 해와 졸라 왔다
;PRINTFORML 조금 등지면서 여자에게 창피를 주지 말고와 졸라 왔다
;------------------------------------------
;50~69 탈의 성희롱(애무)
;------------------------------------------
;수음
@EVENT_COUNTE_MESSAGE50
IF TALENT:PLAYER:2 & 2
	IF (TCVAR:PLAYER:12 != 1 && CFLAG:TARGET:우후후 != 2) || (!TCVAR:PLAYER:이기초 && CFLAG:TARGET:우후후 == 2)
		SIF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
			TEQUIP:PLAYER:3 = 1
		PRINTFORM %타겟은% 
		IF TEQUIP:PLAYER:50 == TARGET
			PRINTFORM %CALLNAME:MASTER%에 허리를 쳐 붙이면서 
		ELSEIF TEQUIP:PLAYER:51 == TARGET
			PRINTFORM %CALLNAME:MASTER%의 애널을 범하면서 
		ELSEIF TEQUIP:식스나인
			PRINTFORM %CALLNAME:MASTER%의 얼굴에 걸친 채로 
		ELSE
			PRINTFORM %CALLNAME:MASTER%의 얼굴을 응시하면서 
		ENDIF
		PRINT ,
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORM 격렬하고 
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 힘없이도 적확하게 
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 강한 힘으로 억지로 
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 가볍게 잡으면서보다 빨리 
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 조금 강하게 해에 
		ENDIF
		PRINTFORML 남성기를 훑어내고 사정으로 이끌었다…
	ELSE
		PRINTFORM %타겟은% 
		IF TEQUIP:PLAYER:50 == TARGET
			PRINTFORM %CALLNAME:MASTER%에 허리를 쳐 붙이면서 
		ELSEIF TEQUIP:PLAYER:51 == TARGET
			PRINTFORM %CALLNAME:MASTER%의 애널을 범하면서 
		ELSEIF TEQUIP:식스나인
			PRINTFORM %CALLNAME:MASTER%의 얼굴에 걸친 채로 
		ENDIF
		PRINT ,
		IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
			TEQUIP:PLAYER:3 = 1
			PRINTFORM 속옷으로부터 남성기를 꺼내 
		ELSE
			PRINTFORM 노출시킨 남성기에 
		ENDIF
		IF FLAG:카운터지문제어
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORM 흠칫흠칫 약한 힘으로 
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORM 꽉 강하게 해에 
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				PRINTFORM 가벼운 힘으로 
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORM 상냥하게 
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORM 느슨하게 잡듯이 
			ENDIF
		ENDIF
		PRINTFORML 손가락을 걸어 개 문질러 광을 냈다…
	ENDIF
ELSE
	PRINTFORM %타겟은% 
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM %CALLNAME:PLAYER%의 속옷을 비켜 놓아 이슬 악어 되었다
	ENDIF
	PRINTFORM %CALLNAME:PLAYER%의 비소를 
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 미끌미끌과 힘없이 
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 쭉 먹혀들게 하도록 
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 고육에와도라고 놀도록 
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 형을 확인하고 나서 
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 스윽 어루만지고 나서 
		ENDIF
	ENDIF
	PRINTFORML 손가락첩자 냈다…
ENDIF
RETURN 1

;펠라티오
@EVENT_COUNTE_MESSAGE51
IF TALENT:PLAYER:2 & 2
	PRINTFORM %타겟은% 
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM 속옷으로부터 꺼낸 남성기
	ELSE
		PRINTFORM 노출시킨 남성기
	ENDIF
	IF TALENT:TARGET:연모
		SELECTCASE RAND:5
			CASE 0
				PRINTFORML 를 입으로 입에 물어 빨기 시작하는……
			CASE 1
				PRINTFORML 에 한껏 침을 도 해 달라붙어서 놓지 않았다……
			CASE 2
				PRINTFORML 의 끝에 키스를 했다……
			CASE 3
				PRINTFORML 에 몹시 사랑스럽게 뺨을 비볐다……
			CASE 4
				PRINTFORML 를 목구멍까지 질네 붐비었다……
		ENDSELECT
	ELSEIF TALENT:TARGET:애욕
		SELECTCASE RAND:4
			CASE 0
				PRINTFORML 를 목구멍까지 질네 붐비었다……
			CASE 1
				PRINTFORML 에 탐내는 것처럼 달라붙어서 놓지 않았다……
			CASE 2
				PRINTFORML 의 끝에 혓바닥을 기어 갔다……
			CASE 3
				PRINTFORML 에 한껏 침을 도 해 빨기 시작하는…………

		ENDSELECT
	ELSE
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 를 입으로 입에 물어 빨기 시작하는……
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 에 흠칫흠칫 얼굴을 대어, 쪼옥 쪽 빨기 시작했다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 를 조용히 질네 붐비어, 격렬하고 머리를 흔들며면서 혀를 걸기 시작했다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 에 가볍게 입맞춤하고 나서, 끈적끈적 혀를 기게 한 시작했다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 를 숨을 끊으면서 질네 붐비었다
		ENDIF
	ENDIF
ELSE
	PRINTFORM %타겟은% 
	IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
		TEQUIP:PLAYER:3 = 1
		PRINTFORM %CALLNAME:PLAYER%의 속옷을 비켜 놓아,
	ENDIF
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORML 이슬 악어 된%CALLNAME:PLAYER%의 비소에 혓바닥을 기어 갔다…
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1 || CFLAG:TARGET:성격경향 == 4
		PRINTFORML %CALLNAME:PLAYER%의 비소를 정중하게 빨기 시작했다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML %CALLNAME:PLAYER%의 음순을 밀어 헤치고 비두를 없는 시작했다…
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML %CALLNAME:PLAYER%의 비소를 개와 같이 열심임으로 빨고 있다
	ENDIF
ENDIF
RETURN 1

;가랑이
@EVENT_COUNTE_MESSAGE52
IF TALENT:PLAYER:2 & 2
	PRINTFORM %타겟은% %CALLNAME:PLAYER%
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 에 흠칫흠칫 걸쳐
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 를 기쁨으로서 덮치기이면서
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 에 노인 가면을 향한하기 시작해
			ELSE
				PRINTFORM 에 생긋 웃은하기 시작해
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 에 신중하게 걸쳐,
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 에 즐거운 표정을 향하여
		ENDIF
	ELSE
		PRINTFORM 에
	ENDIF
	PRINTFORM 말타기가 되어 %TINKO(PLAYER)%를 %SLIT(TARGET)%로 비비기 시작했다…
ELSE
	PRINTFORM %타겟은% 
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 수줍음에 뺨을 붉게 마음에 끌려라
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 기쁜듯이 코와 있고 미소를 띠고
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 띄우면서
			ELSE
				PRINTFORM 즐거운 듯이 미소지으면서
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 조금 부끄러운 듯이 하면서
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 어딘지 모르게 기쁜듯하게
		ENDIF
	ENDIF
	PRINTFORM 두 명의 성기를 거듭해 비비기 시작했다…
ENDIF
RETURN 1

;파이즈리
@EVENT_COUNTE_MESSAGE53
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 속옷으로부터 꺼냈다
ELSEIF TEQUIP:PLAYER:0 & 2
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 노출시켰다
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
ENDIF
PRINTFORM %TINKO(PLAYER)%를
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 벌벌 떨이면서,
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 히죽 웃어
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고
		ELSE
			PRINTFORM 기쁜듯하게 미소
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 상냥하고
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 어딘지 모르게 즐거운 듯이
	ENDIF
ENDIF
PRINTFORM 가슴으로 사이에 두어 비비기 시작했다…
RETURN 1

;풋잡
@EVENT_COUNTE_MESSAGE54
IF TEQUIP:PLAYER:0 > 3 || (TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3)
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 국부를 짓밟아 비비기 시작했다…
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은%
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 스스로의 다리가 젖는 감촉에, 드물고 재미있을 것 같은 표정을 띠고 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %조교자가% 얼굴을 찡그릴 때, 그 다리의 움직임이 격렬해져 갔다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 띄우고
			ELSE
				PRINTFORM 쿡쿡하고 웃음
			ENDIF
			PRINTFORML 면서, 쾌감을 주도록(듯이) 다리를 움직이고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 반 기가 막힌 표정이지만, 자주(잘) 보면(자) 즐거운 듯이도 보인다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 즐거운 표정으로, 조금씩 다리를 움직여, 쾌감을 꺼내게 하려 하고 있다
		ENDIF
	ENDIF
ELSE
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 남성기를 짓밟아 비비기 시작했다…
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은%
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 조금 걱정스러운 표정으로, 그런데도 다리의 움직임을 멈추는 기색은 없다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 히죽히죽 웃으면서, 슬슬 다리를 꽉 눌러 온다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면
			ELSE
				PRINTFORM 기쁜 듯한 미소
			ENDIF
			PRINTFORML 를 띄워 쾌감을 주려고 다리를 문질러 온다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 그 표정은 마음속으로부터 솟구치는 것을 억제 당하지 않았다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 어딘지 모르게 즐거운 듯이, 다리를 움직이고 있다
		ENDIF
	ENDIF
ENDIF
RETURN 1

;애널애무
@EVENT_COUNTE_MESSAGE55
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 아누스를 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 주름의 한 개 한 개를 쓰다듬듯이
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		;PRINTFORM 꾸욱과 항문을 관철하도록(듯이)
		PRINTFORM 관철하도록(듯이) 문지르면서
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		;PRINTFORM 고육에  어루만지고 켜도록(듯이)
		PRINTFORM 어루만지고 켜도록(듯이) 고육에
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		;PRINTFORM 쿡쿡과 찌르도록(듯이)
		PRINTFORM 쿡쿡과 쿡쿡 찌르도록(듯이)
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		;PRINTFORM 표면을 가볍게 만지도록(듯이)
		PRINTFORM 표면을 가볍게 비비도록(듯이)
	ENDIF
ENDIF
PRINTFORML 손가락으로 애무했다…
RETURN 1

;애널빨기
@EVENT_COUNTE_MESSAGE56
PRINTFORML %타겟은% %CALLNAME:PLAYER%의 아누스를 정중하게 빨기 시작했다…
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORML 쪼옥 쪽 입구를 핥아서는, 때때로 뜨거운 숨결을 흘리고 있다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML 다만 빨 뿐만 아니라, 혀를 항강에 대해 넣어 열심히 희롱하고 있다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML 와 열심히 혀를 기게 해, 쾌락을 꺼내려고 온다
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML 구멍만으로는 없고, 둔부도 핥고 돌리고 있다
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORML 가볍게 혀를 닿게 해 또 떼어 놓는 것을, 몇 번도 반복해 온다
	ENDIF
ENDIF
RETURN 1

;손가락삽입
@EVENT_COUNTE_MESSAGE57
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 비소에 천천히 손가락을 삽입했다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 비렬에, 흠칫흠칫 손가락을 쑤셔 왔다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 질구에 손가락을 찔러 붐비어, 격렬하게 움직이기 시작했다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 질내에 포함하게 한 손가락으로, 주름을 정중하게 비비어 세우고 있다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 비렬에 손가락을 삽입해, 안을 위를 덧써 왔다
ENDIF
RETURN 1

;가슴애무
@EVENT_COUNTE_MESSAGE58
IF TEQUIP:PLAYER:1 & 1 && !TEQUIP:PLAYER:4
	TEQUIP:PLAYER:4 = 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 가슴팍을 피부 지우게 해
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
ENDIF
IF TALENT:PLAYER:2 & 1
	PRINTFORM 가슴을,
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 부끄러운 듯 부드럽게 
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 히죽 웃어 슬슬
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 뺨을 붉게 한 무표정으로
			ELSE
				PRINTFORM 즐거운 듯이 웃어
			ENDIF
			PRINTFORM 물컹물컹하고
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 크기를 확인하도록(듯이)
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 알려지고 로 한 표정으로 포근포근하고
		ENDIF
	ENDIF
	PRINTFORML 유하기 만…
ELSE
	PRINTFORM 유두를
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 흠칫흠칫 하면서
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			;PRINTFORM 히죽히죽 웃으면서
			PRINTFORM 히죽히죽 하면서
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 뺨을 붉게 한 무표정으로
			ELSE
				PRINTFORM 즐거운 듯이 미소지으면서
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 아픔을 주지 않게 신중하게
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 호기심을 만족시키려고
		ENDIF
	ENDIF
	PRINTFORML 손가락으로 깎지 않아로 사리 냈다…
ENDIF
RETURN 1

;유두빨기
@EVENT_COUNTE_MESSAGE59
IF TEQUIP:PLAYER:1 & 1 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:4 = 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 가슴팍을은 만
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
ENDIF
PRINTFORM 유두
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 에 빨아 당겨 왔다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 에 흠칫흠칫 입술을 접근하고 달라붙어 왔다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 를 입술로 질네, 강한 힘으로 들이마시기 시작했다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 에 빨아들이면서, 입안에서 혀를 움직이고 있다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 를 혀로 빨고 돌렸다
ENDIF
RETURN 1

;딥키스
@EVENT_COUNTE_MESSAGE60
PRINTFORM %타겟은% %CALLNAME:PLAYER%
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 의 얼굴을 잡아 입맞춤해 정열적으로 혀를 휘감아 왔다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 와 입술을 거듭해 흠칫흠칫 혀를 보내 왔다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 의 입술을 빼앗아, 혀를 밀고 들어가 입안을 유린해 온다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 에 입술을 바쳐 서로의 혀를 서로 들이마시도록(듯이) 재촉해 왔다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 에 사랑을 속삭인 후순을 빼앗아, 혀를 침입시켜 왔다
ENDIF
RETURN 1


;오폭 삽입
@EVENT_COUNTE_MESSAGE61
PRINTFORML 관능에 넋을 잃어 격렬하게 %TINKO(PLAYER)%를 문지르고 있었던 %CALLNAME:TARGET%의 %SLIT(TARGET, 1)%에, 주르룩 귀두가 미끄러져 들어갔다
PRINTFORML 뜨거운 미끄러짐에 이끌려\@ TALENT:처녀 ? %" 순결의 증거를 용이하게 찢어"%# \@ 육봉은 순식간에 꿀단지의 안쪽 깊게까지 도달했다…

;------------------------------------------
;70~79 탈의 성희롱(강요)
;------------------------------------------
;커닐링구스강제
@EVENT_COUNTE_MESSAGE70
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 얼굴에 
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 천천히 걸쳐 입가에 비소를 꽉 눌렀다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML 흠칫흠칫 걸치면서, 비소를 바쳐 왔다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML 비소를 꽉 눌러 빨리 빨도록(듯이) 재촉 해 온다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML 걸친 뒤, 스스로 음순을 나누어 열어 애원 하고 있다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML 체중을 걸치지 않게, 비소를 보내 왔다
ENDIF
RETURN 1

;펠라치오 강제
@EVENT_COUNTE_MESSAGE71
PRINTFORML %타겟은% %CALLNAME:PLAYER%의 입으로 %TINKO(TARGET)%에 봉사시켰다…
IF FLAG:카운터지문제어
	PRINTFORM %타겟은% 
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORML 변명 없음 그렇게 하면서도, 열락에 군침을 흘려 허덕이고 있다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML 히죽히죽 웃으면서, %CALLNAME:PLAYER%의 목을 밀어 올려 왔다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고
		ELSE
			PRINTFORM 기쁜 듯한 표정으로
		ENDIF
		PRINTFORML , %CALLNAME:PLAYER%의 머리를 어루만지고 있는
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML 노 깨는 것 같은 표정이지만, 쾌감에는 이길 수 없는 것 같다
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORML 기분이 좋은 듯 신음성을 높이고 있다
	ENDIF
ENDIF
RETURN 1

;애널 봉사
@EVENT_COUNTE_MESSAGE72
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 얼굴에 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 흠칫흠칫
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 억지로
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM 천천히
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 가볍고
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 스윽
	ENDIF
ENDIF
PRINTFORML 걸쳐 입가에 아누스를 꽉 눌렀다…
RETURN 1

;
@EVENT_COUNTE_MESSAGE73
IF !TFLAG:60
	IF TALENT:TARGET:가슴사이즈 >= 0
		PRINTFORM %타겟은% 
		IF TALENT:TARGET:연모
			SELECTCASE RAND:8
				CASE 0
					PRINTFORM 행복한 표정을 띠며, 
				CASE 1
					PRINTFORM 황홀이 되면서, 
				CASE 2
					PRINTFORM 황홀한 표정을 띠며, 
				CASE 3
					PRINTFORM 숨을 헐떡이면서, 
				CASE 4
					PRINTFORM 사랑을 속삭이면서, 
				CASE 5
					PRINTFORM 사랑하는 것 같은 시선을 이쪽으로 향하면서, 
				CASE 6
					PRINTFORM 흥분으로 유두를 단단하게 하면서, 
				CASE 7
					IF TALENT:TARGET:헌신적
						PRINTFORM 봉사의 쾌감에 만취한 표정을 띠며, 
					ELSE
						PRINTFORM 가슴에의 쾌감에 기다려 애태워, 
					ENDIF
			ENDSELECT
			SELECTCASE RAND:5
				CASE 0
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 양측으로부터 %OPPAI_DESCRIPTION(TARGET)%로 상냥하게 쌌다……
				CASE 1
					PRINTFORML %CALLNAME:MASTER%의 얼굴에 초조하게 하는 것처럼 %OPPAI_DESCRIPTION(TARGET)%를 칠했다……
				CASE 2
					PRINTFORML %CALLNAME:MASTER%의 얼굴에 %OPPAI_DESCRIPTION(TARGET)%를 꽉 꽉 눌렀다……
				CASE 3
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 애무했다……
				CASE 4
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 사이에 두어 붐비었다……
			ENDSELECT
		ELSEIF TALENT:TARGET:사모
			IF TARGET == 57
				PRINTFORM 노인 가면을 띄우고 
			ELSE
				SELECTCASE RAND:4
					CASE 0
						PRINTFORM 기쁜 듯한 표정을 띠며, 
					CASE 1
						PRINTFORM 수줍어하면서, 
					CASE 2
						PRINTFORM 쑥스러운 듯하게 하면서, 
					CASE 3
						PRINTFORM 뺨을 붉게 물들여, 
				ENDSELECT
			ENDIF
			SELECTCASE RAND:5
				CASE 0
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 양측으로부터 %OPPAI_DESCRIPTION(TARGET)%로 상냥하게 쌌다……
				CASE 1
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 움켜 쥐어 초조하게 하는 것처럼 %OPPAI_DESCRIPTION(TARGET)%를 칠했다……
				CASE 2
					PRINTFORML %CALLNAME:MASTER%의 얼굴에 %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌렀다……
				CASE 3
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 애무했다……
				CASE 4
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 사이에 두어 붐비었다……
			ENDSELECT
		ELSE
			IF TARGET == 57
				PRINTFORM 불쑥 이 면을 띄우고 
			ELSE
				SELECTCASE RAND:4
					CASE 0
						PRINTFORM 부끄러운 것 같은 표정을 띠며, 
					CASE 1
						PRINTFORM 수줍어하면서, 
					CASE 2
						PRINTFORM 수줍으면서, 
					CASE 3
						PRINTFORM 뺨을 진적에 물들여, 
				ENDSELECT
			ENDIF
			SELECTCASE RAND:3
				CASE 0
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 움켜 쥐어 %OPPAI_DESCRIPTION(TARGET)%를 칠했다……
				CASE 1
					PRINTFORML %CALLNAME:MASTER%의 얼굴에 %OPPAI_DESCRIPTION(TARGET)%로 꽉 눌렀다……
				CASE 2
					PRINTFORML %CALLNAME:MASTER%의 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 사이에 두어 붐비었다……
			ENDSELECT
		ENDIF
	ELSE
		PRINTFORML %타겟은% %CALLNAME:MASTER%의 얼굴을 움켜 쥐어 %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌렀다……
	ENDIF
ELSE
	IF TALENT:TARGET:가슴사이즈 >= 0
		SELECTCASE RAND:3
			CASE 0
				PRINTFORML %타겟은% %CALLNAME:MASTER%의 옆 얼굴을 %OPPAI_DESCRIPTION(TARGET)%로 애무했다……
			CASE 1
				PRINTFORML %타겟은% %CALLNAME:MASTER%의 옆 얼굴에 %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌렀다……
			CASE 2
				PRINTFORML %타겟은% %CALLNAME:MASTER%의 옆 얼굴에 %OPPAI_DESCRIPTION(TARGET)%를 칠했다……
		ENDSELECT
	ELSE
		PRINTFORML %타겟은% %CALLNAME:MASTER%의 옆 얼굴에 %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌렀다……
	ENDIF
ENDIF
RETURN 1

;젖 맞추어라
@EVENT_COUNTE_MESSAGE74
IF TALENT:PLAYER:2 == 2
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 몸에 
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 흠칫흠칫 몸을 의지하면서 
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 맞혀라면 말 하는 듯 
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 기분 좋게 시키고 싶다고 하듯이 
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 안겨들듯이 
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 태연한 표정으로 
		ENDIF
	ENDIF
	PRINTFORM %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌러 왔다…
ELSE
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 유방에 %OPPAI_DESCRIPTION(TARGET)%를 꽉 눌러 왔다…
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은% 
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 기분 좋은 듯한 한숨을 흘리면서도, %조교자가% 기분이 좋아져서 있을까 걱정인 것 같다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 히죽히죽 웃으면서, 스윽스윽과 서로의 유두를 서로 문지르고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 유두와 유방을 각각 서로 문지르면서 달콤한 한숨을 흘리고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 유두가 스칠 때마다 퍼뜩 몸을 떨고 있다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 즐거운 듯이 서로의 유방을 서로 겹치며 몸을 흔들고 있다
		ENDIF
	ENDIF
ENDIF
RETURN 1

;타와시아라이
@EVENT_COUNTE_MESSAGE75
PRINTFORML %타겟은% 
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 흠칫흠칫 하면서도,
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 히죽히죽 웃으면서,
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 흔들어
		ELSE
			PRINTFORM 어딘지 모르게 즐거운 듯이,
		ENDIF
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM %CALLNAME:PLAYER%의 얼굴을 응시하면서,
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 태연하게,
	ENDIF
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 팔에 걸터앉아 성기를 비벼대고 있다…
RETURN 1

;강제 보지 애무
@EVENT_COUNTE_MESSAGE76
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 손가락을 질네라고 빨고 돌린 후, 스스로의 
IF TALENT:TARGET:처녀
	PRINTFORML 비소에 이끌었다……
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은% 
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 일순간, 무서워 하듯 어깨를 진동시키면서 %CALLNAME:PLAYER%의 손가락의 움직임에 몸을 맡기며 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 조금 몸을 굳어지게 한이면서도, 입가에 미소를 띠며 빨리 움직이라고 재촉 해 온다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 조금 어깨를 움츠려 그런데도 %CALLNAME:PLAYER%의 손가락의 움직임에 단 소리를 높이고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 각오를 결정한 표정으로 %CALLNAME:PLAYER%의 애무를 받아들이고 있다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 꿈틀 몸을 진동시키고 나서, 몸이 힘을 빼 손가락이 움직이는 것을 기다리고 있다
		ENDIF
	ENDIF
ELSE
	PRINTFORML 비렬에 이끌었다……
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML %타겟은% %조교자가% 질강에 손가락을 다하고 넣은 순간 크게 떨려, 조심스럽게 단 소리를 높이기 시작했다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 입에 물고 붐빈 손가락을 자극하도록(듯이), 허리를 흔들어 %타겟은% 열락을 탐내고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 자극하기 쉽게 음순을 연%타겟이%, 안타까울만큼 달게 우기 시작했다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% 손가락의 감촉에 허덕이면서 %CALLNAME:PLAYER%의 고간에 애달픈 듯한 시선을 보내고 있다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML %타겟은% 단 소리를 높이면서, 큐 응 두근 질구를 단단히 조여, 쾌감을 전해 온다
		ENDIF
	ENDIF
ENDIF
RETURN 1

;젖을 들이마시게 한다
@EVENT_COUNTE_MESSAGE77
PRINTFORML %타겟은% %CALLNAME:PLAYER%의 입에, 완전히 단단해진 %NIPPLE_DESCRIPTION(TARGET)%를 포함하게 해 왔다……
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORML %타겟은% 변명 없음 그런 표정을 띠면서도, %CALLNAME:PLAYER%의 흡인에 단 소리를 높이고 있다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML 아이를 어르는 것 같은 말씨로, %타겟이% 도발해 온다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM %타겟은%
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고 
		ELSE
			PRINTFORM 기쁜듯이 뺨을 느슨해지게 했다
		ENDIF
		PRINTFORML , %조교자가% 유두를 감교 보고 할 때, 이쪽의 머리를 쓰다듬고 있고 왔다
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML %타겟은% %CALLNAME:PLAYER%에 젖을 들이마셔질 때, 행복한 허덕여소리를 지르고 있다
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORML %조교자는% 아무것도 말하지 않고 다만 머리를 계속 어루만지는 %CALLNAME:TARGET%에, 욕정을 기억 유두를 더욱 자극했다
	ENDIF
ENDIF
SIF TALENT:TARGET:모유체질
	PRINTFORML 그 끝에서는 단 모유가 배어 나오고 하고 있다……
RETURN 1

;엉덩이 즈리
@EVENT_COUNTE_MESSAGE78
PRINTFORM %타겟은% 
IF FLAG:카운터지문제어
	;강경세계
	IF CFLAG:TARGET:성격경향 == 2
		PRINTFORML %CALLNAME:PLAYER%의 엉덩이를 강하게 잡으면(자), 고육의 사이에 빙글빙글 페니스를 켜고 왔다…
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML 억제할 수 없는 흥분에 당황하면서, %CALLNAME:PLAYER%의 엉덩이에 더듬거리게 페니스를 문질러 왔다…
	;그 외
	ELSE
		PRINTFORML 흥분한 모습으로 미끌미끌과%CALLNAME:PLAYER%의 엉덩이에 페니스를 문질러 왔다…
	ENDIF
ELSE
	PRINTFORML 흥분한 모습으로 미끌미끌과 %CALLNAME:PLAYER%의 엉덩이에 페니스를 문질러 왔다…
ENDIF
RETURN 1

;------------------------------------------
;80~89 성교
;------------------------------------------
;==================================================
;정상위 V삽입된다
;==================================================
;상대주도정상위로 당신이 V에 삽입된다
;==================================================
@EVENT_COUNTE_MESSAGE80
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORM 속옷을 비켜 놓은 뒤,
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 꿀꺽 군침을 삼켜
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 싫은 것 같은 미소를 띠며
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 흔들어
			ELSE
				PRINTFORM 기쁜듯하는 미소를 띠고
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 이쪽에 미소지어 써라
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 어딘지 모르게 즐거운 표정으로
		ENDIF
	ENDIF
	PRINTFORML 비소에 %TINKO(TARGET)%를 비틀어 넣고, 허리를 움직이기 시작했다…
ELSE
	PRINTFORM 비소에 정면에서 %TINKO(TARGET)%를 비틀어 넣고,
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 쾌락에 군침을 흘리면서, 흠칫흠칫
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 히죽 웃었다와 사양 없고 거침없이
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 띄우고
			ELSE
				PRINTFORM 기쁜듯이 웃어
			ENDIF
			PRINTFORM 쾌락을 주려고
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 쾌감에 참기 (위해)때문에 눈을 감고면서
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 즐거운 표정으로 서로를 즐길 수 있는 듯
		ENDIF
	ENDIF
	PRINTFORML 허리를 움직이기 시작했다…
ENDIF
IF TALENT:MASTER:처녀 && TALENT:2 >= 1 && TALENT:PLAYER:처녀 != -1
	PRINTFORML 결합부로부터는 %CALLNAME:PLAYER%의 파과의 피가 흐르기 시작하고 있다…
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 변명 없음 그런 표정을 띠면서, 그런데도 %CALLNAME:TARGET%의 허리의 움직임은 멈추지 않는다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 드물고 솔직한 미소를 띠며%타겟은% 즐거운 듯이 이쪽을 응시하고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 감에 견디지 않는다고 한 모습으로, 한 번 움직임을 멈춘%타겟은%, 상냥하게 움직이기 시작했다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% 일순간 경직된 후, 천천히 허리를 움직이기 시작했다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 황홀한 표정의 %타겟은%, 그대로허리를 계속 움직이고 있다
		ENDIF
	ENDIF
ELSE
	PRINTFORML 결합부로부터는 %타겟과% %CALLNAME:PLAYER%의 체액이 넘쳐 나오고 하고 있다…
ENDIF
RETURN 1

;==================================================
;후배위 V삽입된다
;==================================================
;상대주도후배위로 V에 당신이 삽입된다
;==================================================
@EVENT_COUNTE_MESSAGE83
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%에 뒤로부터 지워져 속옷을 비켜 놓으면(자),
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM %CALLNAME:PLAYER%의 모습을 물으면서 신중하게
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 허리를 빈틈없이 누르고 붐비어 억지로
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM %조교자를% 꽉 꼭 껴안으면서
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 귓전으로 %CALLNAME:PLAYER%에 사랑을 속삭여 걸치면서
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 흥분에 숨을 헐떡이면서
		ENDIF
	ENDIF
	PRINTFORML 비소에 %TINKO(TARGET)%를 삽입해, 허리를 움직이기 시작했다…
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 비소에 등 뒤로부터 %TINKO(TARGET)%를 밀어넣으면(자),
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM %CALLNAME:PLAYER%에 사과하면서도 격렬하고
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM %CALLNAME:PLAYER%의 엉덩이를 잡아 격렬하고
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 기분 좋음 그렇게 소리를 높이면서
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM %조교자를% 신경쓰면서도 격렬하고
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 쾌감을 탐내도록(듯이) 격렬하고
		ENDIF
	ENDIF
	PRINTFORML 허리를 움직이기 시작했다…
ENDIF
IF TALENT:MASTER:처녀 && TALENT:2 >= 1 && TALENT:PLAYER:처녀 != -1
	PRINTFORML 결합부로부터는 %CALLNAME:PLAYER%의 파과의 피가 흐르기 시작하고 있다…
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 변명 없음 그런 표정을 띠면서도, %타겟이% 허리의 움직임을 멈출 것은 없었다…
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %타겟은% 뜻밖에도 %조교자를% 신경쓰는 인사하면(자), 허리의 움직임을 무리가 없는 정도로 느슨하게했다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 처녀를 받은 기쁨을 말로 전하면서, 스피드를 느슨하게해 상냥하게 허리를 움직였다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% 한 번 허리를 끊어 %조교자가% 안정될 때까지 기다리고 나서, 다시 천천히 움직이기 시작했다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML %타겟은% 기쁜듯이 %CALLNAME:PLAYER%에 웃어 걸친 후, 곧바로 다시 허리를 거절하기 시작했다
		ENDIF
	ENDIF
ELSE
	PRINTFORML 결합부로부터는 %타겟과% %CALLNAME:PLAYER%의 체액이 넘쳐 나오고 하고 있다…
ENDIF
RETURN 1

;==================================================
;허리 모습 V접수
;==================================================
;상대 주도로 당신이 V에 삽입된다
;==================================================
;
@EVENT_COUNTE_MESSAGE84
IF RAND:2
	IF TEQUIP:MASTER:체위 == 1
		LOCALS = 정면에서
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, %CALLNAME:PLAYER%의 표정을 즐거운 듯이 관찰했다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, 조심스럽게 기분이 좋은지 어떤지 물어 왔다…
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, %조교자가% 느끼는 모습을 실황하는것 같이 소리에 내 듣게 해 왔다…
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, %CALLNAME:PLAYER%의 얼굴을 가만히 응시하고 있다…
			ELSE
				PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, %CALLNAME:PLAYER%에 웃어 걸쳐 왔다…
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 질내를%LOCALS%밀어 올리면서, 애정이 가득찬 시선으로 %CALLNAME:PLAYER%의 얼굴을 응시했다…
		ENDIF
	ELSE
		LOCALS = 
		IF TEQUIP:MASTER:체위 == 2
			LOCALS = 뒤로부터
		ENDIF
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 허리를 다시 잡으면(자),%LOCALS%질의 깊게 페니스를 빙글빙글 꽉 눌러 왔다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 허리를 다시 잡으면(자) , 평상시와는 다른 짐승과 같이 격렬함으로%LOCALS%페니스를 찔러 넣어 왔다…
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %타겟은% %조교자를% %LOCALS%로부터 범하면서, 좀 더 큰 소리를 높이도록(듯이) 요구했다…
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 허리를 다시 잡으면(자), %CALLNAME:PLAYER%의 키에 기대고 걸리도록(듯이) 하면서 허리를 움직여 왔다…
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 허리를 다시 잡으면(자), %조교자를% 조금이라도 기분 좋게 하려고%LOCALS%질 안쪽을 조금씩 자극했다…
		ENDIF
	ENDIF
ELSE
	IF TEQUIP:MASTER:체위 == 1
		LOCALS = 정상위로
	ELSEIF TEQUIP:MASTER:체위 == 2
		LOCALS = 후배위로
	ELSE
		LOCALS = 
	ENDIF
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORML %타겟은% %CALLNAME:PLAYER%에 삽입한 채로%LOCALS%격렬하게 허리를 부딪히고 있다…
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 모습을 끊임없이 신경쓰면서도,%LOCALS%격렬하게 허리를 부딪히고 있다…
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 허리를 도망칠 수 없도록 제대로 억누르며, %LOCALS%격렬하게 허리를 부딪히고 있다…
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML %타겟은% 쾌감의 천를 억제하는 일도 하지 않고, 기분 좋음 그렇게 소리를 높이면서%LOCALS%격렬하게 허리를 부딪히고 있다…
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML %타겟은% 쾌감으로 소리가 새는 것을 필사적으로 참으면서,%LOCALS%격렬하게 허리를 부딪히고 있다…
	ENDIF
ENDIF
RETURN 1

;==================================================
;삽입
;==================================================
;상대 주도로 당신이 V에 삽입 당한다
;==================================================
@EVENT_COUNTE_MESSAGE85

PRINTFORML %타겟은% 흠뻑 젖어 있는 비렬에 %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 가져다 대며,  앉아 왔다…
IF FLAG:카운터지문제어
	PRINTFORM %타겟은%
	IF TALENT:TARGET:처녀 == 1
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 눈에 눈물을 고이면서 아픔을 견뎌 처녀를 바칠 수 있던 것을 기뻐하고 있는 것 같다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 아픔의 너무 괴로운 울음을 흘리면서, 그런데도 어딘가 기쁜듯이 뺨을 느슨해지게 하고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 너무나 아파서 움직일 수 없는 모습이면서,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을
			ELSE
				PRINTFORM 만면의 미소를
			ENDIF
			PRINTFORML 띄워 희열에 잠겨 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 눈물로 미소지어, 프로포즈로 밖에 생각되지 않는 대사를 말했다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 아픔에 눈살을 찌푸리면서, 기쁜듯하는 분위기를 감돌게 하고 있다
		ENDIF
	ELSE
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 변명 없음 그렇게 하면서도, 걸친 채로 허리를 상하에 곤두세우고 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 히죽히죽 웃으면서 허리를 회전시켜,시에 초조하게 하고 나서 또 움직여, 열락을 탐내고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML %CALLNAME:PLAYER%에 결합부를 쬐려고, 다리를 열어 음순을 나누어 열어, 허리를 전후 시키고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %CALLNAME:PLAYER%에의 애정을 말로 하면서, 그것을 확인하도록(듯이) 허리를 털고 있다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 어딘지 모르게 즐거운 듯이 허리를 흔들어, 열락을 탐내고 있다
		ENDIF
	ENDIF
ENDIF
RETURN 1

;==================================================
;정말좋아 hold
;==================================================
@EVENT_COUNTE_MESSAGE86
PRINTFORM 사정이 가까운 일을 감지한 %타겟은%,
IF TEQUIP:TARGET:체위 == 3
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORML 묵직하게 체중을 실으며 %TINKO(PLAYER)%를 깊숙한 곳까지 질불공평 보고, 질내사정을 졸랐다…
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1
		PRINTFORML 허리를 밀착시키면서, 질내에 사정 해 주었으면 하면 간절히원함 하고 있다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML 한껏 허리를 가라앉혀, 질내에 내게 해 줄 테니까 감사하라고 속삭여 온다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML 한계까지 앉아, 짜 취하기 때문이라고
		IF TARGET == 57 && !TALENT:연모
			PRINTFORM 노인 가면을 띄우고
		ELSE
			PRINTFORM 웃어
		ENDIF
		PRINTFORML 질강으로 %TINKO(PLAYER)%를 잡고 붙여 왔다
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML 앉아 %TINKO(PLAYER)%를 자신의 최안쪽까지 이끌어, 사정을 졸라 왔다
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%에
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORM 제대로 안아 다하면서 양 다리로 허리를 안아,
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 흠칫흠칫 편 손과 다리로 열심히 매달려
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 동작할 수 없게 팔과 다리를 걸어 와
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM 약한 치카라인데 거절해 풀리지 않게 안아 붙어
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM 빈틈없이 손과 다리를 걸어 밀착해,
	ENDIF
	PRINTFORML 질내사정을 졸랐다…
ENDIF
RETURN 1

;A삽입
@EVENT_COUNTE_MESSAGE87
PRINTFORML %타겟은% 애널에 %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 가져다 대며,  앉아 왔다…
IF FLAG:카운터지문제어
	PRINTFORM %타겟은%
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORML 부끄러운 듯 얼굴을 붉히면서도, 부정의 장소에서의 열락에 몸을 진동시키고 있다
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML 항문에 넣게 하고라고 기뻐하는 변태와%조교자를% 따지면서, 자신이 쾌락에 빠지고 있다
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML 아누스에서의 희열을 정직하게 토로하면서, %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 탐내고 있다
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML 마지못해서와 머리를 흔들며면서도, 그 표정에는 제대로 쾌락의 색을 간파할 수 있다
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORML 애널로 받아들인 %TINKO(PLAYER)%에, 기분이 좋은 듯 허덕이고 있다
	ENDIF
ENDIF
RETURN 1

;==================================================
;허리 모습 V
;==================================================
;당신이 상대에게 V에 삽입 당한다
;==================================================
@EVENT_COUNTE_MESSAGE88
PRINTFORM %타겟은%
SELECTCASE RAND:3
	CASE 2
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 조금씩 허리를 움직여, 질벽을 뜨는 페니스의 감촉에 만취해 있다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 흠칫흠칫 허리를 흔들어서는, 달고 뜨거운 숨결을 흘리고 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %TINKO(PLAYER)%를 단단히 조여 질내의 구석구석까지 열락을 맛보고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORML 꾸준히 하고 자궁구로 귀두에 키스해줘,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORML 노인 가면을 띄워 질압을 높여 온다
			ELSE
				PRINTFORML 즐거운 듯이 미소를 흘리고 있다
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML %TINKO(PLAYER)%의 형태를 확인하도록(듯이), 염인가 까는 허리를 털고 있다
		ENDIF
	CASE 1
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 천천히 긴 스토로크로 허리를 움직이면서, %CALLNAME:PLAYER%에 기분이 좋을까 물어 왔다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 천천히 길게 허리를 흔들어, 걱정스러운 듯이 기분 좋게 할 수 있고 있어 %CALLNAME:PLAYER%에 물어 봐 왔다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 미끌미끌 질강으로 %TINKO(PLAYER)%를 빨도록(듯이) 움직여, 기분이 좋을 것이라고 웃고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 느긋하게 허리를 움직여,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 보이고
			ELSE
				PRINTFORM 즐거운 듯이
			ENDIF
			PRINTFORML 기분이 좋지요로 단정해 왔다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 이러한 편이 길게 연결되고 있을 수 있다로부터와 초조하게 하도록(듯이) 천천히 움직이고 있다
		ENDIF
	CASE 0
		IF FLAG:카운터지문제어
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORM 미안해요와 몇 번이나 말하면서,
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORM 내가 기분이 좋다고 웃으면서
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				PRINTFORM 함께 기분 좋아지자고 해
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORM 기쁜 눈물을 띄우면서
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORM 입가에 미소를 띠며
			ENDIF
		ENDIF
		PRINTFORML 열중하며 허리를 털고 있다…
ENDSELECT
RETURN 1

;==================================================
;허리 모습 A
;==================================================
;상대 주도로 당신이 A에 삽입 당한다
;==================================================
@EVENT_COUNTE_MESSAGE89
PRINTFORM %타겟은%
SELECTCASE RAND:3
	CASE 2
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 조금씩 허리를 움직여, 장벽을 도려내는 페니스의 감촉에 만취해 있다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 흠칫흠칫 허리를 흔들어서는, 달고 뜨거운 숨결을 흘리고 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML %TINKO(PLAYER)%를 단단히 조여 항강으로부터의 열락을 맛보고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 항문을 꽉 잡으면서 허리를 거절해,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORML 노인 가면을 띄우고 있다
			ELSE
				PRINTFORML 즐거운 듯이 미소를 흘리고 있다
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 자신의 행동에 곤혹하면서도, 교성을 주면서 쾌감을 탐내고 있다
		ENDIF
	CASE 1
		;그 외
		IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
			PRINTFORML 천천히 긴 스토로크로 허리를 움직이면서, %CALLNAME:PLAYER%에 기분이 좋을까 물어 왔다…
		;심약계
		ELSEIF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 천천히 길게 허리를 흔들어, 걱정스러운 듯이 기분 좋게 할 수 있고 있어 %CALLNAME:PLAYER%에 물어 봐 왔다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 미끌미끌 장벽으로 %TINKO(PLAYER)%를 빨도록(듯이) 움직여, 기분이 좋을 것이라고 웃고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 느긋하게 허리를 움직여,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 보이고
			ELSE
				PRINTFORM 즐거운 듯이
			ENDIF
			PRINTFORML 기분이 좋지요로 단정해 왔다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 아누스로부터의 쾌감을 거절하도록(듯이), 천천히 한 속도로 허리를 털고 있다
		ENDIF
	CASE 0
		IF FLAG:카운터지문제어
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORM 미안해요와 몇 번이나 말하면서,
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORM 나는 기분이 좋다고 웃으면서
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				PRINTFORM 함께 기분 좋아지자고 해
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORM 이를 악물면서도
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORM 입가에 미소를 띠며
			ENDIF
		ENDIF
		PRINTFORML 열중하며 허리를 털고 있다…
ENDSELECT
RETURN 1

;==================================================
;후배위 A삽입된다
;==================================================
;상대 주도로 당신이 A에 삽입된다
;==================================================
@EVENT_COUNTE_MESSAGE90
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 속옷을 비켜 놓으면(자), 등 뒤로부터 애널에 %TINKO(TARGET)%를 나사 포함허리를 움직이기 시작했다…
ELSE
	PRINTFORML %타겟은% 등 뒤로부터%CALLNAME:PLAYER%의 애널에 %TINKO(TARGET)%를 비틀어 넣고, 허리를 움직이기 시작했다…
ENDIF
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML 결합부로부터는 %타겟과% %CALLNAME:PLAYER%의 체액이 넘쳐 나오고 하고 있다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML %타겟은% 평상시의 흠칫흠칫 한 모습이 거짓말과 같이, 짐승과 같은 허리 사용으로 %CALLNAME:PLAYER%의 애널을 탐내고 있다
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML %타겟은% 항문으로 느끼는 변태와 웃으면서, 자신도 열락에 %TINKO(TARGET)%를 경련시킨다
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 엉덩이가 기분이 좋으면 기쁜듯이 고하면서 허리를 흔들고 있다
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML %타겟은% 미안한 듯 하면서도, %CALLNAME:PLAYER%의 애널의 감촉을 맛보고 있다
ENDIF
RETURN 1
;==================================================
;이라마치오 된다
;==================================================
;상대 주도로 당신의 입에 삽입된다
;==================================================
;
@EVENT_COUNTE_MESSAGE91
PRINTFORM %타겟은%
IF FLAG:카운터지문제어
	;심약계
	IF CFLAG:TARGET:성격경향 == 1
		PRINTFORM 사양하면서도 가차 없이
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORM 히죽 웃어 격렬하고
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORM 기분 좋고 해 주었으면 한다고 말하면서
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORM %CALLNAME:PLAYER%에 확인하고 나서
	;그 외
	ELSEIF CFLAG:TARGET:성격경향 == 5
		PRINTFORM 두를 잡아
	ENDIF
ENDIF
PRINTFORML 난폭하게 %CALLNAME:PLAYER%의 목까지 %TINKO(TARGET)%를 찔러 넣었다…
RETURN 1

;==================================================
;애널개발허가를 요구한다(현재 상태로서는 표시되지 않는다
;==================================================
@EVENT_COUNTE_MESSAGE92
;심약계
IF CFLAG:TARGET:성격경향 == 1
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORM 
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
;그 외
ELSEIF CFLAG:TARGET:성격경향 == 5
ENDIF
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 애널을 개발해 보고 싶다고 말하기 시작했다…
	PRINTFORML [0]미안해요 진짜 좀 봐주세요
	PRINTFORML [1]쉽게 해
	$LOOP
	INPUT
	SELECTCASE RESULT
		CASE 0
			PRINTFORMW %타겟은% 유감스럽다…
		CASE 1
			PRINTFORMW %타겟은% 눈을 빛낼 수 있었다…
			CFLAG:TARGET:애널개발허가 = 1
		CASEELSE
			GOTO LOOP
	ENDSELECT
RETURN 1

;==================================================
;허리 모습역A
;==================================================
;상대 주도로 당신이 A에 삽입된다
;==================================================
@EVENT_COUNTE_MESSAGE93
IF RAND:2
	IF TEQUIP:MASTER:체위 == 1
		LOCALS = 정면에서
	ELSEIF TEQUIP:MASTER:체위 == 2
		LOCALS = 뒤로부터
	ELSE
		LOCALS = 
	ENDIF
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORML %타겟은% %조교자를% %LOCALS%강하게 꼭 껴안으면서, 큰 스토로크로 %CALLNAME:PLAYER%의 애널을 탐내고 있다…
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1
		PRINTFORML %타겟은% %조교자를% %LOCALS%강하게 꼭 껴안으면서, 숨을 할딱이며 %CALLNAME:PLAYER%의 애널을 탐내고 있다…
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML %타겟은% %조교자를% %LOCALS%강하게 꼭 껴안으면서, 용서가 없는 허리 사용으로 %CALLNAME:PLAYER%의 애널을 탐내고 있다…
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML %타겟은% %조교자를% %LOCALS%강하게 꼭 껴안으면서, 기분 좋음 그렇게 %CALLNAME:PLAYER%의 애널을 탐내고 있다…
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML %타겟은% %조교자를% %LOCALS%강하게 꼭 껴안으면서, 열에 들뜬 것처럼%CALLNAME:PLAYER%의 애널을 탐내고 있다…
	ENDIF
ELSE
	IF TEQUIP:MASTER:체위 == 1
		IF !BEDROOM(CFLAG:PLAYER:현재위치) && !(BATHROOM() && ITEM:20)
			LOCALS = 대면입위로
		ELSE
			LOCALS = 정상위로
		ENDIF
	ELSEIF TEQUIP:MASTER:체위 == 2
		IF !INROOM(CFLAG:PLAYER:현재위치) && !(BATHROOM() && ITEM:20)
			LOCALS = 배면입위로
		ELSE
			LOCALS = 후배위로
		ENDIF
	ELSE
		LOCALS = 
	ENDIF
	;그 외
	IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 애널에 삽입한 채로%LOCALS%격렬하게 허리를 부딪히고 있다…
	;심약계
	ELSEIF CFLAG:TARGET:성격경향 == 1
		PRINTFORML %타겟은% 때때로 %CALLNAME:PLAYER%에 사과하면서도,%LOCALS%항문에 격렬하게 허리를 부딪히고 있다…
	;강경세계
	ELSEIF CFLAG:TARGET:성격경향 == 2
		PRINTFORML %타겟은% %조교자를% 몇 번이나 매도하면서,%LOCALS%항문에 격렬하게 허리를 부딪히고 있다…
	;솔직함계
	ELSEIF CFLAG:TARGET:성격경향 == 3
		PRINTFORML %타겟은% 페니스로 느끼는 쾌락에 얼굴을 느슨해지게 한이면서,%LOCALS%항문에 격렬하게 허리를 부딪히고 있다…
	;진면목계
	ELSEIF CFLAG:TARGET:성격경향 == 4
		PRINTFORML %타겟은% 평상시의 성실한 모습이 거짓말과 같이 흐트러지면서,%LOCALS%항문에 격렬하게 허리를 부딪히고 있다…
	ENDIF
ENDIF
RETURN 1

;==================================================
;정상위 A삽입된다
;==================================================
;상대 주도로 당신이 A에 삽입된다
;==================================================
@EVENT_COUNTE_MESSAGE94
IF TEQUIP:PLAYER:0 & 2 && !TEQUIP:PLAYER:3
	TEQUIP:PLAYER:3 = 1
	PRINTFORML %타겟은% %CALLNAME:PLAYER%에 정면에서 엎어졌다와 속옷을 비켜 놓아 애널에 %TINKO(TARGET)%를 나사 포함허리를 움직이기 시작했다…
ELSE
	PRINTFORML %타겟은% %CALLNAME:PLAYER%에 정면에서 엎어졌다와 애널에 %TINKO(TARGET)%를 나사 붐비어 허리를 움직이기 시작했다…
ENDIF
;그 외
IF CFLAG:TARGET:성격경향 == 5 || !FLAG:카운터지문제어
	PRINTFORML %타겟은% %조교자를% 꼭 껴안으면서, 격렬하게 허리를 털고 있다…
;심약계
ELSEIF CFLAG:TARGET:성격경향 == 1
	PRINTFORML %타겟은% %CALLNAME:PLAYER%에 몇 번이나 사과하고 있지만, 그 얼굴은 열락으로 다 느슨해지고 있다…
;강경세계
ELSEIF CFLAG:TARGET:성격경향 == 2
	PRINTFORML %타겟은% 애널을 범해지는 %CALLNAME:PLAYER%의 표정을 히죽히죽 하면서 관찰하고 있다…
;솔직함계
ELSEIF CFLAG:TARGET:성격경향 == 3
	PRINTFORML %타겟은% 기분 좋음 그렇게 소리를 높이면서, %CALLNAME:PLAYER%의 다리를 강한 힘으로 안아 격렬하게 허리를 털고 있다…
;진면목계
ELSEIF CFLAG:TARGET:성격경향 == 4
	PRINTFORML %타겟은% %조교자를% 신경쓰면서도, 애널을 탐내는 허리의 움직임을 제지당하지 않고 있다…
ENDIF
RETURN 1

;------------------------------------------
;100~119 삽입중 전용
;------------------------------------------
@EVENT_COUNTE_MESSAGE100
CALL EVENT_COUNTE_MESSAGE32
@EVENT_COUNTE_MESSAGE101
CALL EVENT_COUNTE_MESSAGE33
@EVENT_COUNTE_MESSAGE102
CALL EVENT_COUNTE_MESSAGE35
@EVENT_COUNTE_MESSAGE103
CALL EVENT_COUNTE_MESSAGE55
@EVENT_COUNTE_MESSAGE104
CALL EVENT_COUNTE_MESSAGE58
@EVENT_COUNTE_MESSAGE105
CALL EVENT_COUNTE_MESSAGE59
@EVENT_COUNTE_MESSAGE106
CALL EVENT_COUNTE_MESSAGE60
@EVENT_COUNTE_MESSAGE107
CALL EVENT_COUNTE_MESSAGE77
@EVENT_COUNTE_MESSAGE108
CALL EVENT_COUNTE_MESSAGE86
@EVENT_COUNTE_MESSAGE109
CALL EVENT_COUNTE_MESSAGE88
@EVENT_COUNTE_MESSAGE110
CALL EVENT_COUNTE_MESSAGE89
@EVENT_COUNTE_MESSAGE111
@EVENT_COUNTE_MESSAGE112
@EVENT_COUNTE_MESSAGE113
;------------------------------------------
;120~139 껴안기모드
;------------------------------------------
;머리 어루만지고
@EVENT_COUNTE_MESSAGE120
PRINTFORML %CALLNAME%는 %CALLNAME:MASTER%의 머리를 응석부리게 하도록(듯이) 상냥하게 어루만졌다…
RETURN 1

;미용사나무
@EVENT_COUNTE_MESSAGE121
PRINTFORML %CALLNAME%는 %CALLNAME:MASTER%의 머리카락에 수 쿠시를 들어갈 수 있었다…
RETURN 1

;안아 붙어 있는
@EVENT_COUNTE_MESSAGE122
PRINTFORM %CALLNAME%는 %CALLNAME:MASTER%에 정면에서 안아 붙으면(자),
IF TALENT:MASTER:가슴사이즈 >= 1
	PRINTFORML %CALLNAME:MASTER%의 풍만한 가슴의 골짜기에 얼굴을 묻었다…
ELSE
	PRINTFORML 응석부리도록(듯이) 해 미끌미끌과 뺨을 문질러 왔다
ENDIF
RETURN 1

;꼭 껴안고
@EVENT_COUNTE_MESSAGE123
PRINTFORM %CALLNAME%는 %CALLNAME:MASTER%
IF RAND:2
	IF TALENT:가슴사이즈 >= 1
		PRINTFORML 를 정면에서 꼭 껴안으면(자), 풍만한 가슴의 골짜기에 %CALLNAME:MASTER%의 얼굴을 파묻히게 했다…
	ELSE
		PRINTFORML 의 키에 팔을 돌리면(자), 서로의 체온을 서로 느끼도록(듯이) 꽉 강하게 꼭 껴안아 왔다…
	ENDIF
ELSE
	PRINTFORM 를 등 뒤로부터 꼭 껴안으면(자), 즐거운 모습으로 %CALLNAME:MASTER%의 몸을 철썩철썩하고 손대어 돌렸다…
ENDIF
RETURN 1

;뺨 어루만지고
@EVENT_COUNTE_MESSAGE124
PRINTFORML %CALLNAME%는 정면에서 %CALLNAME:MASTER%의 얼굴을 들여다 봐 붐비면(자), 뺨을 상냥하게 어루만지면서 가만히%마스터를% 응시해 왔다…
RETURN 1

;간질임
@EVENT_COUNTE_MESSAGE125
PRINTFORM %CALLNAME%는 히죽 미소를 띠면(자), %CALLNAME:MASTER%의
SELECTCASE RAND:4
	CASE 0
		PRINT 목덜미
	CASE 1
		PRINT 옆구리
	CASE 2
		PRINT 발바닥
	CASE 3
		PRINT 겨드랑 밑
ENDSELECT
PRINTFORML 를 간질여 왔다!
PRINTFORML %마스터는% 지나친 간지러움에 소리를 높여 웃어 굴렀다…
RETURN 1

;무릎베개시킨다
@EVENT_COUNTE_MESSAGE126
PRINTFORML %CALLNAME%는 %마스터를% 억지로 정좌시키면(자), %CALLNAME:MASTER%의 무릎 위로 뒹굴어 머리를 쓰다듬듯이 요구해 왔다…
RETURN 1

;냄새를 냄새 맡는다
@EVENT_COUNTE_MESSAGE127
PRINTFORML %CALLNAME%는 %CALLNAME:MASTER%에 안아 붙은 채로, 슨슨과 코를 울려%CALLNAME:MASTER%의 몸의 냄새를 냄새 맡아 왔다…
RETURN 1

;말을 건넨다
@EVENT_COUNTE_MESSAGE128
IF RAND:2
	PRINTFORML %CALLNAME%는 %CALLNAME:MASTER%에 안아 대한 채로 말을 건네 왔다…
ELSE
	PRINTFORML %CALLNAME%는 %마스터와% 몸을 밀착시킨 채로 말을 건네 왔다…
ENDIF
RETURN 1

;목덜미 빨고
@EVENT_COUNTE_MESSAGE129
PRINTFORML %CALLNAME%는 %CALLNAME:MASTER%에 안아 붙은 채로, %CALLNAME:MASTER%의 목덜미를 혀로 더듬으며 주어 왔다…
RETURN 1

;==================================================
;옷을벗게한다
;==================================================
@DATUI_MESSAGE
VARSET LOCAL
IF TCVAR:TARGET:22 == 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의
	IF ((TEQUIP:MASTER:0 & 9) > 0) && ((TEQUIP:MASTER:0 & 6) > 0)
		PRINTFORML %SHOW_BOTTOM(MASTER, 9)%과%SHOW_하반신속옷(MASTER, 6)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:0 & 9) > 0) && ((TEQUIP:MASTER:0 & 6) == 0) 
		PRINTFORML %SHOW_BOTTOM(MASTER, 9)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:0 & 9) == 0) && ((TEQUIP:MASTER:0 & 6) > 0)
		PRINTFORML %SHOW_하반신속옷(MASTER, 6)%를 떼어냈다
	ENDIF
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은%
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 꿀꺽 침을 삼켜%CALLNAME:PLAYER%의 하반신을 응시하고 있다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 히죽 웃어, 하반신노출한 채 부끄럽지 않을까 물어 봐 온다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 흔들어
			ELSE
				PRINTFORM 황홀한 표정으로
			ENDIF
			PRINTFORML , 매우 깨끗하다면 중얼거림, 응시해 온다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 눈을 숙여 의복을 깨끗이 접었다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 태연하게 한 표정으로, 아직 지금부터와 조금 오니 것을 중얼거리고 있다
		ENDIF
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 2
	IF TEQUIP:PLAYER:0 & 1
		PRINTFORM %타겟은% %CALLNAME:PLAYER%의 %SHOW_BOTTOM(MASTER, 1)%안에 손을 돌진해
		PRINTFORML 중에 신고 있는%SHOW_하반신속옷(MASTER, 6)%를 끌어내렸다
		IF FLAG:카운터지문제어
			PRINTFORM %타겟은%
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORML 미안해요라고 말하면서, 손에 넣은 속옷을 가만히 응시하고 있다
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORML 건방진 속옷이라고 불만을 말하면서, 손에 넣은 속옷을 넓히고 있다
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				PRINTFORM 사랑스러운 속옷이구나와
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 불쑥 이 면을 흔들어
				ELSE
					PRINTFORM 미소를 띠며
				ENDIF
				PRINTFORML , 품에 끝내려고 한다
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORML 속옷을 보면서 후훗하고 미소를 흘렸다
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORML 피식 작게 웃어, 그 속옷을 과시해 온다
			ENDIF
		ENDIF
	ELSEIF TEQUIP:PLAYER:0 & 8
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 %SHOW_BOTTOM(MASTER, 9)%과%SHOW_하반신속옷(MASTER, 6)%를 떼어냈다
		IF FLAG:카운터지문제어
			PRINTFORM %타겟은%
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORML 꿀꺽 침을 삼켜%CALLNAME:PLAYER%의 하반신을 응시하고 있다
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORML 히죽 웃어, 하반신노출한 채 부끄럽지 않을까 물어 봐 온다
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 노인 가면을 흔들어
				ELSE
					PRINTFORM 황홀한 표정으로
				ENDIF
				IF TALENT:MASTER:성별 == 2
					PRINTFORML , 매우 멋있다고 중얼거림, 응시해 온다
				ELSE
					PRINTFORML , 매우 깨끗하다면 중얼거림, 응시해 온다
				ENDIF
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORML 눈을 숙여 의복을 깨끗이 접었다
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORML 태연하게 한 표정으로, 아직 지금부터와 조금 오니 것을 중얼거리고 있다
			ENDIF
		ENDIF
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:22 == 3
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 %SHOW_BOTTOM(MASTER, 9)%를 벗겨냈다
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은%
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 미안해요라고 말하면서,
			IF TEQUIP:MASTER:0 & 6
				PRINTFORML 속옷 모습의 %조교자를% 가만히 응시하고 있다
			ELSE
				PRINTFORML 비소를 노출 %조교자를% 가만히 응시하고 있다
			ENDIF
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 건방진 복장이라고 불만을 말하면서,
			IF TEQUIP:MASTER:0 & 6
				PRINTFORML 속옷 모습의 %조교자를% 가만히 응시하고 있다
			ELSE
				PRINTFORML 비소를 노출 %조교자를% 가만히 응시하고 있다
			ENDIF
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TEQUIP:MASTER:0 & 6
				PRINTFORM 사랑스러운 속옷이구나와
			ELSE
				PRINTFORM 깨끗한 몸이구나와
			ENDIF
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 흔들어
			ELSE
				PRINTFORM 미소를 띠며
			ENDIF
			IF TEQUIP:MASTER:0 & 6
				PRINTFORML 속옷 모습의 %조교자를% 가만히 응시하고 있다
			ELSE
				PRINTFORML 비소를 노출 %조교자를% 가만히 응시하고 있다
			ENDIF
			
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			IF TEQUIP:MASTER:0 & 6
				PRINTFORM 속옷 모습의 %조교자를% 보면서
			ELSE
				PRINTFORM 비소를 노출 %조교자를% 보면서
			ENDIF
			PRINTFORML 후훗하고 미소를 흘렸다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 피식 작게 웃어,
			IF TEQUIP:MASTER:0 & 6
				PRINTFORML 속옷 모습의 %조교자를% 가만히 관찰하고 있다
			ELSE
				PRINTFORML 비소를 노출 %조교자를% 가만히 관찰하고 있다
			ENDIF
		ENDIF
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT 한층 더
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:23 == 1
	IF ((TEQUIP:TARGET:0 & 9) > 0) && ((TEQUIP:TARGET:0 & 6) > 0)
		PRINTFORM %타겟은% %SHOW_BOTTOM(TARGET, 9)%과 %PANTS_DESCRIPTION(EQUIP:하반신속옷2, TARGET) %%PANTSNAME(EQUIP:하반신속옷2, TARGET)%를,
	ELSEIF (TEQUIP:TARGET:0 & 9) > 0
		PRINTFORM %타겟은% %SHOW_BOTTOM(TARGET, 9)%를
	ELSEIF (TEQUIP:TARGET:0 & 6) > 0
		PRINTFORM %PANTS_DESCRIPTION(EQUIP:하반신속옷2, TARGET) %%PANTSNAME(EQUIP:하반신속옷2, TARGET)%를
	ENDIF
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 수치에 뺨을 붉혀
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 히죽 미소를 띠며
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 흔들어
			ELSE
				PRINTFORM 기쁜듯이 웃어
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 볼 수 있는 일을 신경쓰면서
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 알려지고 로 한 표정으로
		ENDIF
	ENDIF
	PRINTFORML 벗어 던졌다
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 2
	IF TEQUIP:TARGET:0 & 1
		PRINTFORM %타겟은% %SHOW_BOTTOM(TARGET, 1)%안에 신고 있는%SHOW_하반신속옷(TARGET, 6)%를,
		IF FLAG:카운터지문제어
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORM 흠칫흠칫 부끄러운 듯
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORM 히죽 웃어 대담하게
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 불쑥 이 면을 흔들어
				ELSE
					PRINTFORM 기쁜듯하게 미소지으면서
				ENDIF
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORM 심호흡 한 후 단숨에
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORM 알려지고 로 한 표정으로
			ENDIF
		ENDIF
		PRINTFORML 벗어 던졌다
	ELSEIF TEQUIP:TARGET:0 & 8
		PRINTFORM %타겟은% %SHOW_BOTTOM(TARGET, 9)%를,
		IF FLAG:카운터지문제어
			;심약계
			IF CFLAG:TARGET:성격경향 == 1
				PRINTFORM 수치에 뺨을 붉혀
			;강경세계
			ELSEIF CFLAG:TARGET:성격경향 == 2
				PRINTFORM 히죽 미소를 띠며
			;솔직함계
			ELSEIF CFLAG:TARGET:성격경향 == 3
				IF TARGET == 57 && !TALENT:연모
					PRINTFORM 노인 가면을 흔들어
				ELSE
					PRINTFORM 기쁜듯이 웃어
				ENDIF
			;진면목계
			ELSEIF CFLAG:TARGET:성격경향 == 4
				PRINTFORM 볼 수 있는 일을 신경쓰면서
			;그 외
			ELSEIF CFLAG:TARGET:성격경향 == 5
				PRINTFORM 알려지고 로 한 표정으로
			ENDIF
		ENDIF
		PRINTFORML %SHOW_하반신속옷(TARGET, 6)%마다 벗어 던졌다
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:23 == 3
	PRINTFORM %타겟은% %SHOW_BOTTOM(TARGET, 1)%를
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 흠칫흠칫 부끄러운 듯
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 히죽 웃어 대담하게
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 불쑥 이 면을 흔들어
			ELSE
				PRINTFORM 기쁜듯하게 미소지으면서
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 심호흡 한 후 단숨에
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 알려지고 로 한 표정으로
		ENDIF
	ENDIF
	PRINTFORML 벗어 던졌다
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT 한층 더
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:24 == 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
	IF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) > 0)
		PRINTFORML %SHOW_TOP(MASTER, 2)%과%SHOW_상반신속옷(MASTER, 2)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_TOP(MASTER, 2)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:1 & 6) == 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_상반신속옷(MASTER, 2)%를 떼어냈다
	ENDIF
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은% 
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 기 레이와 중얼거리고 나서, 얼굴을 붉히며 떨어졌다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 봐 주기 때문에 감사 해 주시라고 모르는체해, 가만히 응시하고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 보고 있어 즐겁다고 말해,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 띄우고
			ELSE
				PRINTFORM 기쁜듯이
			ENDIF
			PRINTFORML 응시하고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 가슴에 손을 저나라고로부터 의복을 깨끗이 접었다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 이것으로 좋아, 라고 구강으로 말을 굴려, 위에서 아래까지 바라보고 있다
		ENDIF
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:24 == 2
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 
	IF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) > 0)
		PRINTFORML %SHOW_TOP(MASTER, 2)%과%SHOW_상반신속옷(MASTER, 2)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_TOP(MASTER, 2)%를 떼어냈다
	ELSEIF ((TEQUIP:MASTER:1 & 6) == 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_상반신속옷(MASTER, 2)%를 떼어냈다
	ENDIF
	IF FLAG:카운터지문제어
		PRINTFORM %타겟은% 
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORML 기 레이와 중얼거리고 나서, 얼굴을 붉히며 떨어졌다
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORML 봐 주기 때문에 감사 해 주시라고 모르는체해, 가만히 응시하고 있다
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 보고 있어 즐겁다고 말해,
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 노인 가면을 띄우고 
			ELSE
				PRINTFORM 기쁜듯이 
			ENDIF
			PRINTFORML 응시하고 있다
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORML 가슴에 손을 저나라고로부터 의복을 깨끗이 접었다
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORML 이것으로 좋아, 라고 구강으로 말을 굴려, 위에서 아래까지 바라보고 있다
		ENDIF
	ENDIF
	LOCAL = 1
ENDIF
IF LOCAL
	PRINT 한층 더 
	LOCAL = 0
ENDIF
IF TCVAR:TARGET:25 == 1
	PRINTFORM %타겟은% 
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 부끄러운 듯 얼굴을 붉히고 
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 과시하듯 웃으면서 
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 보면 좋겠다고 
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 아이 가면을 띄우고 
			ELSE
				PRINTFORM 기쁜 듯이 
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 당신에게라면 상관없다고 
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 알려지고 로 한 표정으로 
		ENDIF
	ENDIF
	IF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) > 0)
		PRINTFORML , %SHOW_TOP(TARGET, 2)%과 %SHOW_상반신속옷(TARGET, 2)%를 벗어 던졌다
	ELSEIF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML , %SHOW_TOP(TARGET, 2)%를 벗어 던졌다
	ELSEIF ((TEQUIP:MASTER:1 & 6) == 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML ,%SHOW_상반신속옷(TARGET, 2)%를 벗어 던졌다
	ENDIF
	LOCAL = 1
ELSEIF TCVAR:TARGET:25 == 2
	PRINTFORM %타겟은% 
	IF FLAG:카운터지문제어
		;심약계
		IF CFLAG:TARGET:성격경향 == 1
			PRINTFORM 부끄러운 듯 얼굴을 붉혀
		;강경세계
		ELSEIF CFLAG:TARGET:성격경향 == 2
			PRINTFORM 피부를 과시하듯 웃으면서,
		;솔직함계
		ELSEIF CFLAG:TARGET:성격경향 == 3
			PRINTFORM 보면 좋겠다고
			IF TARGET == 57 && !TALENT:연모
				PRINTFORM 아이 가면을 띄우고
			ELSE
				PRINTFORM 기쁜 듯이
			ENDIF
		;진면목계
		ELSEIF CFLAG:TARGET:성격경향 == 4
			PRINTFORM 당신에게라면 상관없다고
		;그 외
		ELSEIF CFLAG:TARGET:성격경향 == 5
			PRINTFORM 알려지고 로 한 표정으로,
		ENDIF
	ENDIF
	IF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) > 0)
		PRINTFORML , PRINTFORML %조사처리(SHOW_TOP(TARGET, 2),"를")% 벗고, %조사처리(SHOW_상반신속옷(TARGET, 2),"를")% 벗어 던졌다
	ELSEIF ((TEQUIP:MASTER:1 & 6) > 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_TOP(TARGET, 2)%를  드러냈다
	ELSEIF ((TEQUIP:MASTER:1 & 6) == 0) && ((TEQUIP:MASTER:1 & 1) == 0)
		PRINTFORML %SHOW_상반신속옷(TARGET, 2)%를 벗어 던졌다
	ENDIF
	LOCAL = 1
ENDIF

;==================================================
;지문의 분기 조건
;==================================================
@소질계산(ARG)
#FUNCTION
#DIM 심약계
#DIM 강경세계
#DIM 솔직함계
#DIM 진면목계

심약계 = 0
강경세계 = 0
솔직함계 = 0
진면목계 = 0

;겁쟁이
IF TALENT:ARG:담력 == -1
	심약계 += 5
;다부짐
ELSEIF TALENT:ARG:담력 == 1
	강경세계 += 1
ENDIF

;솔직함
IF TALENT:ARG:태도 == -1
	솔직함계 += 2
;반항적
ELSEIF TALENT:ARG:태도 == 1
	강경세계 += 1
ENDIF

;얌전하다
IF TALENT:ARG:응답 == -1
	심약계 += 1
;거만함
ELSEIF TALENT:ARG:응답 == 1
	강경세계 += 1
ENDIF

;자존심 낮음
IF TALENT:ARG:자존심 == -1
	심약계 += 1
;자존심 높음
ELSEIF TALENT:ARG:자존심 == 1
	강경세계 += 1
ENDIF

;츤데레
IF TALENT:ARG:츤데레
	IF TALENT:ARG:연모 || TALENT:ARG:음란 || TALENT:ARG:복종
		솔직함계 += 1
	ELSE
		강경세계 += 1
	ENDIF
ENDIF

;자제심
SIF TALENT:ARG:자제심
	진면목계 += 1

;보수적
IF TALENT:ARG:성적흥미 == -1
	진면목계 += 1
;호기심
ELSEIF TALENT:ARG:성적흥미 == 1
	솔직함계 += 1
ENDIF

;음침
IF TALENT:ARG:명랑／음침 == -1
	심약계 += 1
;명랑
ELSEIF TALENT:ARG:명랑／음침 == 1
	솔직함계 += 1
ENDIF

;정조무관심
IF TALENT:ARG:정조 == -1
	솔직함계 += 1
;정조 관념
ELSEIF TALENT:ARG:정조 == 1
	진면목계 += 1
ENDIF

;억압
IF TALENT:ARG:자기애 == -1
	심약계 += 1
;해방
ELSEIF TALENT:ARG:자기애 == 1
	솔직함계 += 1
ENDIF

;수치심 없음
IF TALENT:ARG:수치심 == -1
	솔직함계 += 1
;수줍음
ELSEIF TALENT:ARG:수치심 == 1
	심약계 += 1
ENDIF

;헌신적
SIF TALENT:ARG:헌신적
	진면목계 += 2

;쾌감에 부정
IF TALENT:ARG:쾌감응답 == -1
	진면목계 += 1
;쾌감에 솔직
ELSEIF TALENT:ARG:쾌감응답 == 1
	솔직함계 += 1
ENDIF

;새드
SIF TALENT:ARG:새드
	강경세계 += 2
	
;마조
SIF TALENT:ARG:마조
	심약계 += 2

;심약계
IF 심약계 > 강경세계 && 심약계 > 솔직함계 && 심약계 > 진면목계 && !(TALENT:ARG:성별기호 == 1 && TALENT:MASTER:성별 == 2) && !(TALENT:ARG:성별기호 == 2 && TALENT:MASTER:성별 == 1) && TALENT:ARG:성별기호 != 3
	RETURNF 1
;강경세계
ELSEIF 강경세계 > 솔직함계 && 강경세계 > 심약계 && 강경세계 > 진면목계 && !(TALENT:ARG:성별기호 == 1 && TALENT:MASTER:성별 == 2) && !(TALENT:ARG:성별기호 == 2 && TALENT:MASTER:성별 == 1) && TALENT:ARG:성별기호 != 3
	RETURNF 2
;솔직함계
ELSEIF 솔직함계 > 강경세계 && 솔직함계 > 심약계 && 솔직함계 > 진면목계 && !(TALENT:ARG:성별기호 == 1 && TALENT:MASTER:성별 == 2) && !(TALENT:ARG:성별기호 == 2 && TALENT:MASTER:성별 == 1) && TALENT:ARG:성별기호 != 3
	RETURNF 3
;진면목계
ELSEIF 진면목계 > 강경세계 && 진면목계 > 심약계 && 진면목계 > 솔직함계 && !(TALENT:ARG:성별기호 == 1 && TALENT:MASTER:성별 == 2) && !(TALENT:ARG:성별기호 == 2 && TALENT:MASTER:성별 == 1) && TALENT:ARG:성별기호 != 3
	RETURNF 4
ELSE
;그 외
	RETURNF 5
ENDIF

@SET_HININ(ARG)
IF 임신가부(ARG) && CFLAG:ARG:생해금 < 2 && !TALENT:ARG:무지 && !TCVAR:ARG:만취
	IF CHK_HININ_LOVE(ARG) > CHK_HININ_REASON(ARG,"COUNTER") * 6 / 5
		IF TCVAR:ARG:발정
			PRINTFORM 암컷의 본능에 자극을 받아 %CALLNAME:MASTER%의 아이를 낳고 싶어서 견딜 수 없게 되었다
		ELSE
			PRINTFORM %마스터를% 가만히 봐, 아기 만들어 버리자인가, 라고 말하자
			CFLAG:ARG:생해금 = 2
		ENDIF
	ELSEIF TALENT:ARG:조합지식
		;TCVAR:ARG:피임 = 2
		TCVAR:ARG:피임약 = 1
		PRINTFORM 필을 마시면(자),
	ELSEIF CHK_HININ_LOVE(ARG) > CHK_HININ_REASON(ARG,"COUNTER") && CFLAG:ARG:생리주기 != 7
		PRINTFORM 생으로 해버릴까, 라고 말하자
	ELSE
		;TCVAR:ARG:피임 = 1
		TEQUIP:PLAYER:콘돔 = 1
		PRINTFORM %CALLNAME:MASTER%에 콘돔을 씌우자,
	ENDIF
ENDIF
