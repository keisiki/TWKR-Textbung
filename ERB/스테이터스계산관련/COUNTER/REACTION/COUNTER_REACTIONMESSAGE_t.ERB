;질성교
@EVENT_COUNTE_MESSAGE500
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %CALLNAME:TARGET%의 육벽은 %CALLNAME:MASTER%의 %TINKO(PLAYER)%에 얽혀 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 탐을 내며 덤벼 온다…
	;정말좋아 hold
	CASE 2
		PRINTFORMW %타겟은% %CALLNAME:PLAYER%에게 부둥켜안기며 두 발로 허리를 껴안았다…
	;성교 과시하고
	CASE 3
		PRINTFORMW %타겟은% 상체를 다른 곳으로 돌려%CALLNAME:PLAYER%에게 결합부를 과시하면서 %조사처리(TINKO(PLAYER),"를")% 훑어 내고 있다…
	CASE 4
		PRINTFORMW %CALLNAME:PLAYER%의 정자를 요구해, %CALLNAME:TARGET%의 자궁이 내려 온 것 같다…
	CASE 5
		PRINTFORMW %타겟은% 무의식 중에 허리를 띄워%마스터를% 보다 깊게 받아들이려 하고 있다…
	CASE 6
		PRINTFORMW %CALLNAME:TARGET%의 자궁 입구는 빠끔빠끔 개폐해, %CALLNAME:PLAYER%의 사정을 애타게 기다리고 있다…
	CASE 7
		PRINTFORMW %타겟은% 손가락을 얽히면서 %CALLNAME:MASTER%의 손을 잡아 왔다…
ENDSELECT
RETURN 1
;애널 성교
@EVENT_COUNTE_MESSAGE501
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW 뜨거운 점액으로 채워진%CALLNAME:TARGET%의 장내에서, 육벽이 %조사처리(TINKO(PLAYER),"를")% 잡고 붙이고 있다…
	CASE 2
		PRINTFORMW %타겟은% 손가락을 얽히면서 %CALLNAME:MASTER%의 손을 잡아 왔다…
ENDSELECT
RETURN 1
;수음
@EVENT_COUNTE_MESSAGE502
;현상 허가를 받으려면  동정상실 필수
;IF NOWEX:PLAYER:사정 && TALENT:PLAYER:비동정 <= 0 && !TALENT:무지 && CFLAG:생해금 == 2
IF NOWEX:PLAYER:사정 && !TALENT:무지 && CFLAG:생해금 == 2
	PRINTFORML %타겟은% %CALLNAME:MASTER%에 걸쳐, 질 구멍에 %TINKO(PLAYER)%를 꽉 누르면서 단숨에 속도를 올려 %TINKO(PLAYER)%를 훑어 냈다 …
	
ELSEIF NOWEX:PLAYER:사정
	PRINTFORML %타겟은% 넘쳐 나오는 쿠퍼액을 %TINKO(PLAYER)%전체에 넓히면(자) 단숨에 속도를 올려 %TINKO(PLAYER)%를 훑어 냈다 …
ELSE
	PRINTFORML %타겟은% %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 잡아 천천히 상하시키고 있다…
ENDIF
RETURN 1

;펠라치오
@EVENT_COUNTE_MESSAGE503
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %타겟은% %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 질네, 입술로 귀두를 훑어 내고 있다…
	CASE 2
		PRINTFORMW %타겟은% 장대 뿐만이 아니라 구슬에까지 혀를 더듬으며, %CALLNAME:MASTER%의 성기 전체에 봉사했다…
	;딥스로트
	CASE 3
		PRINTFORMW %타겟은% 한계까지 발기한 %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 근원까지 삼켜
		PRINTFORMW 혀와 목을 걸어 애무했다…
	CASE 4
		PRINTFORMW %타겟은% %CALLNAME:MASTER%에의 봉사를 계속하면서, 스스로의 비소뒤졌다…
ENDSELECT
RETURN 1

;파이즈리
@EVENT_COUNTE_MESSAGE504
SELECTCASE TCVAR:21
	CASE 1
		IF TALENT:TARGET:가슴사이즈 > 0
			PRINTFORMW %타겟은% %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 가슴 속에 끼웠다…
		ELSE
			PRINTFORMW %타겟은% %TINKO(PLAYER)%의 반짝이는 귀두에 오돌토톨한 유두를 비벼대고 있다…
		ENDIF
	;세로 파이즈리, 젖성교
	CASE 2
		PRINTFORMW %조교자는% %CALLNAME:TARGET%의 풍만한 가슴의 틈새에 %TINKO(PLAYER)%를 찔러 넣어 허리를 털었다…
	;골짜기를 만든다
	CASE 3
		IF TALENT:TARGET:가슴사이즈 < 0
			PRINTFORMW %타겟은% 평평한 가슴을 열심히 대어 어떻게든 %TINKO(PLAYER)%를 사이에 두려 하고 있다…
		ELSE
			PRINTFORMW %타겟은% 가슴을 대어 만든 골짜기에서 %TINKO(PLAYER)%를 사이에 두어 상반신을 격렬하게 움직이고 있다…
		ENDIF
	;파이즈리 펠라
	CASE 4
		PRINTFORMW 한층 더 %타겟은%, 남은 귀두를 입에 물어 혀로 애무했다…
ENDSELECT
RETURN 1

;가랑이
@EVENT_COUNTE_MESSAGE505
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %타겟은% 허벅지로 %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 끼워 허리를 움직였다…
	;수음 추가
	CASE 2
		PRINTFORMW %타겟은% 허벅지로 %CALLNAME:MASTER%의 %TINKO(PLAYER)%를 끼워 남은 귀두를 손가락으로 괴롭혔다 …
ENDSELECT
RETURN 1

;억지로 질성교
@EVENT_COUNTE_MESSAGE506
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW %타겟은% %CALLNAME:PLAYER%로부터 달아나려고 몸을 비틀었다…
	CASE 2
		PRINTFORMW %타겟은% 울상으로 째려보고 있다…
ENDSELECT
RETURN 1


;유두고문
@EVENT_COUNTE_MESSAGE510
PRINTFORM %타겟은% %CALLNAME:PLAYER%의 유두
SELECTCASE TCVAR:21
	CASE 1
		PRINTFORMW 를 손가락으로 집어, 동글동글 자극했다…
	CASE 2
		PRINTFORMW 에 혓바닥을 기게 했다…
ENDSELECT

RETURN 1

;구슬 비비어
@EVENT_COUNTE_MESSAGE600
IF TCVAR:21 == 1
	IF SOURCE:가학
		PRINTFORMW %타겟은% %CALLNAME:PLAYER%의 음낭에 손을 뻗어 비비어 풀어 왔다…
	ELSE
		PRINTFORMW %타겟은% %CALLNAME:PLAYER%의 음낭에 손을 뻗어 상냥하게 비비어 풀어 왔다…
	ENDIF
ELSE
	PRINTFORMW %타겟은% 음낭을 물고 입 안에서 데굴데굴 구슬을 굴리고 있다
ENDIF
RETURN 1

;애널애무
@EVENT_COUNTE_MESSAGE601
IF ABL:PLAYER:A감각 < 2
	PRINTFORMW %타겟은% %CALLNAME:PLAYER%의 아누스를 손가락으로 자극하고 있다…
ELSE
	PRINTFORMW %타겟은% %CALLNAME:PLAYER%의 아누스에 손가락을 삽입해 직장까지 휘저었다…
ENDIF
RETURN 1