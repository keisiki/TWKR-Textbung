@EVENT_COUNTER_COMBINATION
VARSET LOCAL
;만들어 써라
;LOCAL:100~
FOR LOCAL, 1, GET_TARGETNUM() + 1
	SIF TARGET:LOCAL <= 0
		CONTINUE
	SIF TCVAR:(TARGET:LOCAL):20
		LOCAL:(100 + TCVAR:(TARGET:LOCAL):20) ++
NEXT