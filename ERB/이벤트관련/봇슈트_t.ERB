@봇슈트
;팬티몰수, 수집

;SIF !CFLAG:TARGET:팬티2 && STRLENS(CSTR:MASTER:12)
SIF !CFLAG:TARGET:팬티2 && FLAG:팬티수송
	CALL 팬티몰수(TARGET)
;요리 가지고 있으면(자) 먹어진다
IF  DISHTYPE
	SIF DISHASSI == TARGET
		RETURN 0
	SIF CFLAG:TARGET:동행
		RETURN 0
	SIF DISH_DRAG()
		RETURN 0
	LOCAL:1 = 0
	IF DISHTYPE == 3
		SIF TIME_PROGRESS(TCVAR:디저트공복시각)
			LOCAL:1 = 1
	ELSE
		SIF TIME_PROGRESS(TCVAR:TARGET:공복시각)
			LOCAL:1 = 1
	ENDIF
	IF FLAG:20 && LOCAL:1 && RAND:5 == 0 && SELECTCOM != 413
		PRINTL 
		PRINTFORMW %타겟은% %마스터가% 가지고 있던＜%EX_DISHNAME()%＞를 찾아냈다
		PRINTFORMW 
		PRINTFORMW 
		PRINTFORMW 만든 요리를 먹을 수 있어 버렸다
		SIF DISH_RADICAL()
			CALL MESSAGE_REACTION_TASTE
		CALL KOJO_MESSAGE_SEND("EVENT", 14, TARGET,-1,-1)
		CALL 식사(TARGET)
		CALL RESET_DISH
	ENDIF
ENDIF

@팬티몰수(ARG)
;팬티몰수, 수집
LOCAL:1 = RAND:2
SIF FLAG:발원내용 == 202
	LOCAL:1 = RAND:3
IF  !LOCAL:1 && CFLAG:ARG:노팬티 != 2
	PRINTL  
	PRINTFORMW %CALLNAME:ARG%에 보고 나무랄 수 있는 신체 검사를 받았다…
	PRINTFORMW 
	PRINTFORMW 
	PRINTFORMW 
	IF TALENT:MASTER:팬티밀수 && RAND:10
		PRINTFORMW 그러나 %마스터는% 교묘하게 %CALLNAME:ARG%의 눈을 속였다
	ELSE
		PRINTFORMW 훔친 팬티를 몰수당했다
		SIF !CFLAG:약점잡음
			PRINTFORMW %CALLNAME:ARG%에게 약점을 잡혔다
			CFLAG:ARG:약점잡음 += 5
		CALL KOJO_MESSAGE_SEND("EVENT", 9, ARG, 0,1)
		;CSTR:MASTER:12 =
		VARSET PANTS_TEMP
		FLAG:팬티수송 = 0
	ENDIF
ENDIF
