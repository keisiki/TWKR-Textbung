@DAILY_CONDITION_EV0
SIF RAND:8 > 0
	RETURN -1
SIF DAY <= 30
	RETURN -1
SIF AVERAGE_YOGORE() > 800
	RETURN -1

CALL DAILY_EV0
RETURN 1

@DAILY_EV0
PRINTFORML %마스터가% 자는 사이에 %GET_MAPNAME(MAIN_MAP)%에 요정 대군이 몰려왔다!
IF RAND:10 < ABL:MASTER:전투능력 && AT_HOME(MASTER) == 1
	PRINTFORML 요정들은 %GET_MAPNAME(MAIN_MAP)% 안을 어지럽히고 있었지만
	PRINTFORML 도중에 눈치챈 %마스터가% 나오자마자, 쏜살같이 도망쳤다
	FOR LOCAL, MINROOM(), MAXROOM
		IF RAND:10 <= ABL:MASTER:전투능력
			YOGORE:LOCAL = MIN(YOGORE:LOCAL + 1000 + RAND:4000, 6000)
		ENDIF
	NEXT
;블랙썬더를 10개 이상 소지
ELSEIF ITEM:120 >= 10 && AT_HOME(MASTER) == 1
	;최대 9개(살). 당신의 몫은 남겨 둔다
	PRINTFORML %마스터가% 눈을 떠보니 요정들은 블랙썬더를 우물우물 하고 있었다
	PRINTFORML %마스터는% 요정들의 입 주위를 닦아주고 돌아가라고 타일렀다
	CALL LOST_ITEM("블랙썬더", RAND:9 + 1, 1)
ELSE
	PRINTFORML 요정들은 %GET_MAPNAME(MAIN_MAP)% 안을 어지럽혀 나갔다
	FOR LOCAL, MINROOM(), MAXROOM
		YOGORE:LOCAL = MIN(YOGORE:LOCAL + 1000 + RAND:4000, 6000)
	NEXT
ENDIF
PRINTW