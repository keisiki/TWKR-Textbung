@DAILY_CONDITION_EV4

SIF RAND:7 > 0
	RETURN -1
CALL DAILY_EV4
RETURN 1

@DAILY_EV4
CALL FISHER_YATES_SHAFFLE(CHARANUM - 1)
FOR LOCAL:10, 0, CHARANUM - 1
	LOCAL = SHAFFLE_ARRAY:(LOCAL:10) + 1
	IF ABL:LOCAL:친밀 >= 5 && !CFLAG:LOCAL:신사거주 && !TALENT:LOCAL:사망 && !CFLAG:LOCAL:출금
		PRINTFORMW 걷잡을 수 없는 사고에 빠져있던 %조사처리(CALLNAME:LOCAL,"는")%, 어느새 %마스터를% 생각하고 있었다는 것을 깨달았다
		PRINTFORMW %조사처리(CALLNAME:LOCAL,"는")% 내일 %마스터를% 만나러 가기로 한 것 같다
		CFLAG:LOCAL:다음날내방 = 1
		BREAK
	ENDIF
NEXT

