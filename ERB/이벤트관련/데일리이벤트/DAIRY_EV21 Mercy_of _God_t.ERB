@DAIRY_CONDITION_EV21
#DIM YOME
SIF !FLAG:자식기원
	RETURN -1
SIF FLAG:획득팬티총수 < 100
	RETURN -1
YOME = FLAG:자식기원

SIF RAND:30 > 0
	RETURN -1
SIF TALENT:YOME:임신
	RETURN -1
SIF !TALENT:YOME:비생물 && !TALENT:YOME:봉래인 && !TALENT:YOME:신령
	RETURN -1
SIF CFLAG:YOME:아이인원수 > 0
	RETURN -1
CALL DAIRY_EV21(YOME)

RETURN 1


@DAIRY_EV21(YOME)
#DIM YOME
PRINTFORMW 팬티님은 자식 없는 %조사처리(CALLNAME:YOME,"를")% 불쌍히 여겼다……
CALL BUFF_BASE(MASTER, BASE_체력, 600)
CALL BUFF_BASE(MASTER, BASE_기력, 600)
CALL COLORMESSAGE(@"%CALLNAME:YOME%의 체력과 기력이 일시적으로 크게 강화되었다", C_YELLOW, 1,1)
CALL BUFF_BASE(MASTER, BASE_정력, 200)
CALL COLORMESSAGE(@"%CALLNAME:MASTER%의 정력이 일시적으로 강화되었다", C_YELLOW, 1,1)
IF !TALENT:MASTER:농후정액
	TALENT:MASTER:농후정액 = 1
	CLEARBIT TFLAG:일일일회, 8
	CALL COLORMESSAGE(@"%마스터는%【농후정액】이 되었다", C_YELLOW, 1,1)
	BASE:MASTER:정력 = MAXBASE:MASTER:정력
ENDIF
PRINTW 
CFLAG:YOME:다음날내방 = 1
CFLAG:YOME:자식보정 ++