@V_C절정
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORML [C절정]
    IF 가타겟아니야 == 1
    PRINTFORML 벽 뒤의 창녀는 클리로부터의 절정에 몸을 바들바들 떨고 있다
    ELSE
    PRINTFORML %가타겟은% 클리로부터의 절정에 몸을 바들바들 떨고 있다
    ENDIF
    PRINTFORML C절정경험 +1
    PRINTFORML 절정경험 +1
    ENDIF
    EXP:가타겟:C절정경험++
    EXP:가타겟:절정경험++
    가쾌C = 0
@V_V절정
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORML [V절정]
    IF 가타겟아니야 == 1
        PRINTFORML 질내로부터 쏟아지는 쾌감에 벽 반대편에서 움찔움찔거리는게 느껴진다
    ELSE
    PRINTFORML 질내로부터 쏟아지는 쾌감에 %가타겟의% 몸이 떨린다
    ENDIF
    PRINTFORML V절정경험 +1
    PRINTFORML 절정경험 +1
    ENDIF
    EXP:가타겟:V절정경험++
    EXP:가타겟:절정경험++
    가쾌V = 0
@V_A절정
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORML [A절정]
    IF 가타겟아니야 == 1
        PRINTFORML 등골을 타고오르는 오싹한 쾌감에 벽 넘어에서 야릇한 신음소리가 들린다...
    ELSE
    PRINTFORML 등골을 타고오르는 오싹한 쾌감에 %가타겟의% 신음소리도 높아져갔다
    ENDIF
    PRINTFORML A절정경험 +1
    PRINTFORML 절정경험 +1
    ENDIF
    EXP:가타겟:A절정경험++
    EXP:가타겟:절정경험++
    가쾌A = 0
@V_B절정
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORML [B절정]
    IF 가타겟아니야 == 1
        PRINTFORML 구멍 너머의 몸이 약하게 떨리고 있다...
    ELSE
    PRINTFORML %가타겟의% 유두가 파르르 떨리고 있다
    ENDIF
    PRINTFORML B절정경험 +1
    PRINTFORML 절정경험 +1
    ENDIF
    EXP:가타겟:B절정경험++
    EXP:가타겟:절정경험++
    가쾌B = 0
@V_M절정
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        PRINTFORML [M절정]
        IF 가타겟아니야 == 1
            PRINTFORML 구멍너머 보이는 입술이 파르르 떨고 있다
        ELSE
        PRINTFORML %가타겟은% 입술을 파르르 떨고 있다
        ENDIF
        PRINTFORML M절정경험 +1
        PRINTFORML 절정경험 +1
    ENDIF
    EXP:가타겟:M절정경험++
    EXP:가타겟:절정경험++
    가쾌M = 0

@V_고통각인(ARG)
IF MARK:가타겟:고통각인 < ARG&&ARG <= 3
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        IF 가타겟아니야 == 1
            PRINTFORML 벽 넘어의 상대는 고통각인 LV{ARG}을 취득했다
        ELSE
        PRINTFORML %CALLNAME:가타겟%은 고통각인 LV{ARG}을 취득했다
        ENDIF
    ENDIF
    MARK:가타겟:고통각인 = ARG
ENDIF

@V_쾌락각인(ARG)
IF MARK:가타겟:쾌락각인 < ARG&&ARG <= 3
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        IF 가타겟아니야 == 1
            PRINTFORML 벽 넘어의 상대는 쾌락각인 LV{ARG}을 취득했다
        ELSE
        PRINTFORML %CALLNAME:가타겟%은 쾌락각인 LV{ARG}을 취득했다
        ENDIF
    ENDIF
    MARK:가타겟:쾌락각인 = ARG
ENDIF

@V_반발각인(ARG)
IF MARK:가타겟:반발각인 < ARG&&ARG <= 3
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        IF 가타겟아니야 == 1
            PRINTFORML 벽 넘어의 상대는 반발각인 LV{ARG}을 취득했다
        ELSE
        PRINTFORML %CALLNAME:가타겟%은 반발각인 LV{ARG}을 취득했다
        ENDIF
    ENDIF
    MARK:가타겟:반발각인 = ARG
ENDIF

@V_아나타사정(ARG)
;ARG = 사정량
IF ARG >= 10000
    ARG = 10000
ENDIF
IF 가아사정 >= 10000
    PRINTFORML [당신 사정]
    IF V_V삽입 == 1
        가자궁정자 += ARG
    ELSEIF V_A삽입 == 1
        가애널정자 += ARG
    ELSEIF 가펠라치오 == 1
        가구내정자 += ARG
    ENDIF
    가아사정 = 0
    가아발기 -= ARG
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORML {ARG}만큼의 정액이 뿌려졌다...
    가아체력 -= ARG
    IF 가아기력 > 0
        가아기력 -= ARG
    ELSE
        가아체력 -= ARG
    ENDIF
ENDIF

@V_약물중독
#DIM 공용변수
공용변수 = 0
IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
PRINTFORML [!약물 과다 투여!]
PRINTFORML %가타겟은% 몸을 덜덜떨며 괴로워했다...
ENDIF
SETCOLOR 0xFF0000
SELECTCASE RAND:11
    CASE 0
        공용변수 = 50 * (RAND:5 + 1)
        IF 가체력 >= 공용변수
            가체력 -= 공용변수
        ELSE
            가체력 = 0
        ENDIF
        IF MAXBASE:가타겟:체력 - 공용변수 >= 200
            MAXBASE:가타겟:체력 -= 공용변수
        ELSE
            MAXBASE:가타겟:체력 = 200
        ENDIF
        SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        PRINTFORML %가타겟의% 체력이 감소해버렸다..
    CASE 1
        공용변수 = 50 * (RAND:5 + 1)
        IF 가기력 >= 공용변수
            가기력 -= 공용변수
        ELSE
            가기력 = 0
        ENDIF
        IF MAXBASE:가타겟:기력 - 공용변수 >= 200
            MAXBASE:가타겟:기력 -= 공용변수
        ELSE
            MAXBASE:가타겟:기력 = 200
        ENDIF
        SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        PRINTFORML %가타겟의% 기력이 감소해버렸다..
    CASE 2
        공용변수 = 50 * (RAND:5 + 1)
        CFLAG:가타겟:호감도 -= 공용변수
        SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
        PRINTFORML %가타겟의% 호감도가 감소해버렸다..
    CASE 3
		IF ABL:가타겟:기교 != 0
			ABL:가타겟:기교 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 기교가 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
    CASE 4
		IF ABL:가타겟:교양 != 0
			ABL:가타겟:교양 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 교양이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
    CASE 5
		IF ABL:가타겟:전투능력 != 0
			ABL:가타겟:전투능력 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 전투능력이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
    CASE 6
		IF ABL:가타겟:요리기능 != 0
			ABL:가타겟:요리기능 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 요리기능이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
	CASE 7	
		IF ABL:가타겟:화술기능 != 0
			ABL:가타겟:화술기능 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 화술기능이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
	CASE 8
		IF ABL:가타겟:친밀 != 0
			ABL:가타겟:친밀 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 친밀이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
	CASE 9
		IF ABL:가타겟:음악기능 != 0
			ABL:가타겟:음악기능 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 음악기능이 감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF
	CASE 10
        IF ABL:가타겟:봉사정신 != 0
		    ABL:가타겟:봉사정신 -= 1
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의%  감소해버렸다..
        ELSE
            SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
            PRINTFORML %가타겟의% 호감도가 감소해버렸다..
            CFLAG:가타겟:호감도 -= 50
        ENDIF		
ENDSELECT
RESETCOLOR
IF TALENT:가타겟:애욕&&(CFLAG:가타겟:호감도 < 3000)
    SETCOLOR 0xFF0000
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
	PRINTFORMW %가타겟은% %CALLNAME:MASTER%와의 일을 서서히 잊어버려서, [애욕]을 잃어버렸습니다
    TALENT:가타겟:애욕 = 0
    RESETCOLOR
ENDIF
IF TALENT:가타겟:연모&&(CFLAG:가타겟:호감도 < 3000||CFLAG:가타겟:신뢰도 < 700||ABL:가타겟:친밀<10)
    SETCOLOR 0xFF0000
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
	PRINTFORMW %가타겟은 %CALLNAME:MASTER%를 좋아하던 감정을 서서히 잊어버려서, [연모]를 잃어버렸습니다
	TALENT:가타겟:연모 = 0
    RESETCOLOR
ENDIF
IF TALENT:가타겟:사모&&(CFLAG:가타겟:호감도 < 1500||ABL:가타겟:친밀 < 5||CFLAG:가타겟:신뢰도 < 250)
    SETCOLOR 0xFF0000
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
	PRINTFORMW %가타겟은% %CALLNAME:MASTER%와 있었던 일을 모조리 까먹어버려서, [사모]를 잃어버렸습니다
	TALENT:가타겟:사모 = 0
    RESETCOLOR
ENDIF
IF TALENT:가타겟:연인 == 2&&!TALENT:가타겟:연모&&!TALENT:가타겟:사모&&!TALENT:가타겟:애욕&&CFLAG:가타겟:호감도 < 500&&CFLAG:가타겟:신뢰도 < 100
    IF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
	PRINTFORMW %가타겟은% %CALLNAME:MASTER%와 사랑했던 지난날을 모조리 잊어버렸습니다...
    PRINTFORMW %가타겟은% 손가락에 끼워져 있는 반지를 의아하게 바라보고는 빼서 저 멀리 던져버렸습니다
    SETCOLOR 0xFF0000
	PRINTFORMW %가타겟은% 더이상 [아내]가 아닙니다
    ENDIF
	TALENT:가타겟:연인 = 0
    RESETCOLOR
ENDIF
IF MARK:가타겟:반발각인 > 0||RAND:3 == 0
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
	PRINTFORMW %가타겟은% %마스터를% 서서히 잊어가면서 그에 따른 분노도 사라져갑니다
    SETCOLOR 0xFFFF00
    SIF ((!FLAG:창녀묘사간소화&&아저씨조교 == 1)||아저씨조교 == 0)&&조교말하지마 == 0
    PRINTFORMW %가타겟의% 반발각인이 0으로 떨어졌습니다
	MARK:가타겟:반발각인 = 0
    RESETCOLOR
ENDIF
RESETCOLOR
가약물 = 0