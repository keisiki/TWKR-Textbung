@V_아저씨_ABLE
IF 가상조교지역 == 1||가상조교지역 == 2
    IF 아저씨명수 < 3
        IF RAND:(5 + 아저씨명수) == 0
            RETURN 1
        ENDIF
    ENDIF
ENDIF
CALL V_개입
RETURN 0

@V_아저씨
;대연회장이나 스트립바에서 놀고 있으면 아저씨(혹은 아가씨?)가 나타나 행위를 함께한다!
PRINTFORML 돌연, 다른 사람이 옆으로 다가왔다!
아저씨명수++


@V_개입
가조교자 '= "모르는사람"
IF 가상조교지역 == 1&&아저씨명수 > 0&&RAND:3 == 0
    CALL V_COM100("실행")
ENDIF

가조교자 = CALLNAME:MASTER