;-----이하 쾌감처리----
;ARG = 조교강도
@V_C쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 250
    TIMES 증가량 , 1.5
ENDIF
SELECTCASE ABL:가타겟:C감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
SELECTCASE ABL:가타겟:욕망
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.1
    CASE IS < 4
        TIMES 증가량 , 1.2
    CASE IS < 6
        TIMES 증가량 , 1.3
    CASE IS < 8
        TIMES 증가량 , 1.4
    CASE IS >= 10
        TIMES 증가량 , 1.5
ENDSELECT
SELECTCASE 가욕정/1000
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.2
    CASE IS < 4
        TIMES 증가량 , 1.4
    CASE IS < 6
        TIMES 증가량 , 1.6
    CASE IS < 8
        TIMES 증가량 , 1.8
    CASE IS >= 10
        TIMES 증가량 , 2.0
ENDSELECT
가쾌C증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
CALL V_욕정(증가량/10)


@V_V쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 250
    TIMES 증가량 , 1.5
ENDIF
SELECTCASE ABL:가타겟:V감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
SELECTCASE ABL:가타겟:욕망
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.1
    CASE IS < 4
        TIMES 증가량 , 1.2
    CASE IS < 6
        TIMES 증가량 , 1.3
    CASE IS < 8
        TIMES 증가량 , 1.4
    CASE IS >= 10
        TIMES 증가량 , 1.5
ENDSELECT
SELECTCASE 가욕정/1000
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.2
    CASE IS < 4
        TIMES 증가량 , 1.4
    CASE IS < 6
        TIMES 증가량 , 1.6
    CASE IS < 8
        TIMES 증가량 , 1.8
    CASE IS >= 10
        TIMES 증가량 , 2.0
ENDSELECT
가쾌V증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
CALL V_욕정(증가량/10)


@V_A쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 250
    TIMES 증가량 , 1.5
ENDIF
SELECTCASE ABL:가타겟:A감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
SELECTCASE ABL:가타겟:욕망
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.1
    CASE IS < 4
        TIMES 증가량 , 1.2
    CASE IS < 6
        TIMES 증가량 , 1.3
    CASE IS < 8
        TIMES 증가량 , 1.4
    CASE IS >= 10
        TIMES 증가량 , 1.5
ENDSELECT
SELECTCASE 가욕정/1000
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.2
    CASE IS < 4
        TIMES 증가량 , 1.4
    CASE IS < 6
        TIMES 증가량 , 1.6
    CASE IS < 8
        TIMES 증가량 , 1.8
    CASE IS >= 10
        TIMES 증가량 , 2.0
ENDSELECT
가쾌A증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
CALL V_욕정(증가량/10)

@V_B쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 250
    TIMES 증가량 , 1.5
ENDIF
SELECTCASE ABL:가타겟:B감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
SELECTCASE ABL:가타겟:욕망
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.1
    CASE IS < 4
        TIMES 증가량 , 1.2
    CASE IS < 6
        TIMES 증가량 , 1.3
    CASE IS < 8
        TIMES 증가량 , 1.4
    CASE IS >= 10
        TIMES 증가량 , 1.5
ENDSELECT
SELECTCASE 가욕정/1000
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.2
    CASE IS < 4
        TIMES 증가량 , 1.4
    CASE IS < 6
        TIMES 증가량 , 1.6
    CASE IS < 8
        TIMES 증가량 , 1.8
    CASE IS >= 10
        TIMES 증가량 , 2.0
ENDSELECT
가쾌B증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
CALL V_욕정(증가량/10)


@V_M쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 250
    TIMES 증가량 , 1.5
ENDIF
SELECTCASE ABL:가타겟:M감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
SELECTCASE ABL:가타겟:욕망
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.1
    CASE IS < 4
        TIMES 증가량 , 1.2
    CASE IS < 6
        TIMES 증가량 , 1.3
    CASE IS < 8
        TIMES 증가량 , 1.4
    CASE IS >= 10
        TIMES 증가량 , 1.5
ENDSELECT
SELECTCASE 가욕정/1000
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.2
    CASE IS < 4
        TIMES 증가량 , 1.4
    CASE IS < 6
        TIMES 증가량 , 1.6
    CASE IS < 8
        TIMES 증가량 , 1.8
    CASE IS >= 10
        TIMES 증가량 , 2.0
ENDSELECT
가쾌M증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
CALL V_욕정(증가량/10)

@V_고통(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:통각 == 1
    TIMES 증가량 , 2.0
ELSEIF TALENT:가타겟:통각 == -1
    TIMES 증가량 , 0.5
ENDIF
가고통증가 += 증가량
가반감증가 += 증가량/50
IF TALENT:가타겟:마조
    가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
    CALL V_욕정(증가량/20)
ENDIF

@V_욕정(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    증가량 += 500
    TIMES 증가량 , 2.5
ENDIF
IF TALENT:가타겟:쾌감응답 == 1
    TIMES 증가량 , 2.0
ELSEIF TALENT:가타겟:쾌감응답 == -1
    TIMES 증가량 , 0.5
ENDIF
가욕정증가 += 증가량
IF !TALENT:가타겟:연모
    가반감증가 += 증가량/50
ENDIF
IF TALENT:가타겟:감정결여
    TIMES 증가량 , 0.1
ENDIF
가윤활증가 += (증가량 / 50)

@V_순종(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:응답 == 1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:태도 == 1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:태도 == -1
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:담력 == -1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:감정결여
    TIMES 증가량 , 0.1
ENDIF
가순종증가 += 증가량


@V_굴복(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:자존심 == 1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:자존심 == 1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:담력 == 1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:감정결여
    TIMES 증가량 , 0.1
ENDIF
가굴복증가 += 증가량
IF !TALENT:가타겟:연모
    가반감증가 += 증가량/50
ENDIF

@V_치정(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:눈에띄고싶어함 == 1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:수치심 == 1
    TIMES 증가량 , 1.2
ELSEIF TALENT:가타겟:수치심 == 1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:담력 == -1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:감정결여
    TIMES 증가량 , 0.1
ENDIF
가치정증가 += 증가량
IF !TALENT:가타겟:연모
    가반감증가 += 증가량/20
ENDIF
CALL V_욕정(증가량/50)

@V_공포(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    TIMES 증가량 , 1.1
ENDIF
IF TALENT:가타겟:눈에띄고싶어함 == 1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:애같음 == 1
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:유치 == 1
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:무관심 == 1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:담력 == 1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:담력 == -1
    TIMES 증가량 , 1.5
ENDIF
IF TALENT:가타겟:감정결여
    TIMES 증가량 , 0.5
ENDIF
가공포증가 += 증가량
가반감증가 += 증가량/25

@V_약물(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:중독되기쉬움
    TIMES 증가량 , 2.0
ELSEIF TALENT:가타겟:약독내성
    TIMES 증가량 , 0.1
ENDIF
가약물증가 += 증가량
CALL V_욕정(증가량/5)
가반감증가 += 증가량/10

@V_선도(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:도착적
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:새드
    TIMES 증가량 , 1.2
ENDIF
가선도증가 += 증가량
SIF TALENT:가타겟:새드
    가욕정증가 += 증가량/2

@V_반감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF GETBIT(가약물효과,1) == 1
    TIMES 증가량 , 1.1
ENDIF
SELECTCASE MARK:가타겟:반발각인
    CASE 1
        TIMES 증가량 , 1.5
    CASE 2
        TIMES 증가량 , 2.0
    CASE 3
        TIMES 증가량 , 2.5
ENDSELECT
IF TALENT:가타겟:정조 == 1
    TIMES 증가량 , 1.2
ELSEIF TALENT:가타겟:응답 == -1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:통각 == 1
    TIMES 증가량 , 1.2
ELSEIF TALENT:가타겟:통각 == -1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:응답 == -1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:담력 == -1
    TIMES 증가량 , 0.8
ENDIF
IF TALENT:가타겟:연인 == 2
    TIMES 증가량 , 0.2
ELSEIF TALENT:가타겟:연인 == 1
    TIMES 증가량 , 0.4
ELSEIF TALENT:가타겟:연모 == 1
    TIMES 증가량 , 0.5
ELSEIF TALENT:가타겟:애욕 == 1
    TIMES 증가량 , 0.5
ELSEIF TALENT:가타겟:섹프 == 1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:사모 == 1
    TIMES 증가량 , 0.8
ENDIF
가반감증가 += 증가량

@V_윤활(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:젖기쉬움 == -1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:젖기쉬움 == 1
    TIMES 증가량 , 1.2
ENDIF
가윤활증가 += 증가량

@V_습득(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:습득속도 == -1
    TIMES 증가량 , 0.8
ELSEIF TALENT:가타겟:습득속도 == 1
    TIMES 증가량 , 1.2
ENDIF
IF TALENT:가타겟:성적흥미 == -1
    TIMES 증가량 , 0.9
ELSEIF TALENT:가타겟:성적흥미 == 1
    TIMES 증가량 , 1.1
ENDIF
가습득증가 += 증가량

@V_아나타봉사(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
가아사정증가 += 증가량
SIF 가아발기 <= 10000
    가아발기증가 += 증가량

;----------
@V_소스처리
DRAWLINE
IF 가쾌C증가 > 0
    PRINTFORML 쾌 C {가쾌C}+ {가쾌C증가}={가쾌C + 가쾌C증가}
    가쾌C += 가쾌C증가
    가총쾌C증가 += 가쾌C증가
    
ENDIF
IF 가쾌V증가 > 0
    IF 가윤활 < 3000
        CALL V_고통(100)
    ENDIF
    PRINTFORML 쾌 V {가쾌V}+ {가쾌V증가}={가쾌V + 가쾌V증가}
    가쾌V += 가쾌V증가
    가총쾌V증가 += 가쾌V증가
    
ENDIF
IF 가쾌A증가 > 0
    IF 가윤활 < 3000
        CALL V_고통(100)
    ENDIF
    PRINTFORML 쾌 A {가쾌A}+ {가쾌A증가}={가쾌A + 가쾌A증가}
    가쾌A += 가쾌A증가
    가총쾌A증가 += 가쾌A증가
    
ENDIF
IF 가쾌B증가 > 0
    PRINTFORML 쾌 B {가쾌B}+ {가쾌B증가}={가쾌B + 가쾌B증가}
    가쾌B += 가쾌B증가
    가총쾌B증가 += 가쾌B증가
    
ENDIF
IF 가쾌M증가 > 0
    PRINTFORML 쾌 M {가쾌M}+ {가쾌M증가}={가쾌M + 가쾌M증가}
    가쾌M += 가쾌M증가
    가총쾌M증가 += 가쾌M증가
    
ENDIF
IF 가윤활증가 > 0
    PRINTFORML 윤활 {가윤활}+ {가윤활증가}={가윤활+ 가윤활증가}
    가윤활 += 가윤활증가
    가총윤활증가 += 가윤활증가
    
ENDIF
IF 가고통증가 > 0
    PRINTFORML 고통 {가고통}+ {가고통증가}={가고통 + 가고통증가}
    가고통 += 가고통증가
    가총고통증가 += 가고통증가
    
ENDIF
IF 가욕정증가 > 0
    PRINTFORML 욕정 {가욕정}+ {가욕정증가}={가욕정 + 가욕정증가}
    가욕정 += 가욕정증가
    가총욕정증가 += 가욕정증가
    
ENDIF
IF 가굴복증가 > 0
    PRINTFORML 굴복 {가굴복}+ {가굴복증가}={가굴복 + 가굴복증가}
    가굴복 += 가굴복증가
    가총굴복증가 += 가굴복증가
    
ENDIF
IF 가치정증가 > 0
    PRINTFORML 치정 {가치정}+ {가치정증가}={가치정 + 가치정증가}
    가치정 += 가치정증가
    가총치정증가 += 가치정증가
    
ENDIF
IF 가습득증가 > 0
    PRINTFORML 습득 {가습득}+ {가습득증가}={가습득 + 가습득증가}
    가습득 += 가습득증가
    가총습득증가 += 가습득증가
    
ENDIF
IF 가공포증가 > 0
    PRINTFORML 공포 {가공포}+ {가공포증가}={가공포 + 가공포증가}
    가공포 += 가공포증가
    가총공포증가 += 가공포증가
    
ENDIF
IF 가약물증가 > 0
    PRINTFORML 약물 {가약물}+ {가약물증가}={가약물 + 가약물증가}
    가약물 += 가약물증가
    가총약물증가 += 가약물증가
    
ENDIF
IF 가선도증가 > 0
    PRINTFORML 선도 {가선도}+ {가선도증가}={가선도 + 가선도증가}
    가선도 += 가선도증가
    가총선도증가 += 가선도증가
    
ENDIF
IF 가반감증가 > 0
    PRINTFORML 반감 {가반감}+ {가반감증가}={가반감 + 가반감증가}
    가반감 += 가반감증가
    가총반감증가 += 가반감증가
ENDIF
IF 가아발기증가 > 0
    IF 가아발기증가 + 가아발기 >= 10000
        가아발기 = 10000
    ELSE
        가아발기 += 가아발기증가
    ENDIF
ENDIF
IF 가아사정증가 > 0
    IF 가아사정증가 + 가아사정 >= 10000
        가아사정 = 10000
    ELSE
        가아사정 += 가아사정증가
    ENDIF
ENDIF


@V_경험처리
IF 가쾌C증가/500 > 0
    PRINTFORML C경험 + {가쾌C증가/500+1}
    EXP:가타겟:C경험 += 가쾌C증가/500 + 1
ENDIF
IF 가쾌V증가/500 > 0
    PRINTFORML V경험 + {가쾌V증가/500+1}
    EXP:가타겟:V경험 += 가쾌V증가/500 + 1
ENDIF
IF 가쾌A증가/500 > 0
    PRINTFORML A경험 + {가쾌A증가/500+1}
    EXP:가타겟:A경험 += 가쾌A증가/500 + 1
ENDIF
IF 가쾌B증가/500 > 0
    PRINTFORML B경험 + {가쾌B증가/500+1}
    EXP:가타겟:B경험 += 가쾌B증가/500 + 1
ENDIF
IF 가쾌M증가/500 > 0
    PRINTFORML M경험 + {가쾌M증가/500+1}
    EXP:가타겟:M경험 += 가쾌M증가/500 + 1
ENDIF

@V_절정처리
#DIM 공용변수
DRAWLINE
공용변수 = 0
IF 가쾌C >= 10000
    CALL V_C절정
ENDIF
IF 가쾌V >= 10000
    CALL V_V절정
ENDIF
IF 가쾌A >= 10000
    CALL V_A절정
ENDIF
IF 가쾌B >= 10000
    CALL V_B절정
ENDIF
IF 가쾌M >= 10000
    CALL V_M절정
ENDIF
IF 가아사정 >= 10000
    IF V_V삽입 == 1
        공용변수 += 300
        공용변수 += ABL:가타겟:기교 * 75
        공용변수 += ABL:가타겟:봉사정신 * 50
        공용변수 += ABL:가타겟:욕망 * 50
        공용변수 += ABL:가타겟:질 * 50
        공용변수 += ABL:가타겟:정액중독 * 75
        공용변수 += TALENT:가타겟:음호 * 150
    ELSEIF V_A삽입 == 1
        공용변수 += 300
        공용변수 += ABL:가타겟:기교 * 75
        공용변수 += ABL:가타겟:봉사정신 * 50
        공용변수 += ABL:가타겟:욕망 * 50
        공용변수 += ABL:가타겟:애널 * 50
        공용변수 += ABL:가타겟:정액중독 * 75
        공용변수 += TALENT:가타겟:항문광 * 150
    ELSEIF 가펠라치오 == 1
        공용변수 += 300
        공용변수 += ABL:가타겟:기교 * 75
        공용변수 += ABL:가타겟:봉사정신 * 55
        공용변수 += ABL:가타겟:욕망 * 50
        공용변수 += ABL:가타겟:혀 * 50
        공용변수 += ABL:가타겟:정액중독 * 100
        공용변수 += TALENT:가타겟:혀놀림 * 100
    ENDIF
    CALL V_아나타사정(공용변수)
    공용변수 = 0
ENDIF
IF 가고통증가 >= 8000
    CALL V_고통각인(3)
ELSEIF 가고통증가 >= 5000
    CALL V_고통각인(2)
ELSEIF 가고통증가 >= 2000
    CALL V_고통각인(1)
ENDIF
IF 가반감증가 >= 8000
    CALL V_반발각인(3)
ELSEIF 가반감증가 >= 5000
    CALL V_반발각인(2)
ELSEIF 가반감증가 >= 2000
    CALL V_반발각인(1)
ENDIF
IF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 9000
    CALL V_쾌락각인(3)
ELSEIF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 7000
    CALL V_쾌락각인(2)
ELSEIF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 5000
    CALL V_쾌락각인(1)
ENDIF
IF 가약물 >= 10000
    CALL V_약물중독
ENDIF
;----
@V_증가초기화
가쾌C증가 = 0
가쾌B증가 = 0
가쾌V증가 = 0
가쾌A증가 = 0
가쾌M증가 = 0
가윤활증가 = 0
가습득증가 = 0
가순종증가 = 0
가욕정증가 = 0
가굴복증가 = 0
가치정증가 = 0
가고통증가 = 0
가공포증가 = 0
가약물증가 = 0
가선도증가 = 0
가반감증가 = 0
가아발기증가 = 0
가아사정증가 = 0
가아체력감소량 = 0
가아기력감소량 = 0
