;-----이하 쾌감처리----
;ARG = 조교강도
@V_C쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:C감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
가쾌C증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


@V_V쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:V감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
가쾌V증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


@V_A쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:A감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
가쾌A증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


@V_B쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:B감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
가쾌B증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


@V_M쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:M감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
가쾌M증가 += 증가량
가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


@V_고통(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
IF TALENT:가타겟:통각 == 1
    TIMES 증가량 , 2.0
ELSEIF TALENT:가타겟:통각 == -1
    TIMES 증가량 , 0.5
ENDIF
가고통증가 += 증가량
가반감증가 += 증가량/50
SIF TALENT:가타겟:마조
    가윤활증가 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))


;----------
@V_소스처리
DRAWLINE
IF 가쾌C증가 > 0
    가쾌C += 가쾌C증가
    PRINTFORML 쾌 C + {가쾌C증가}
ENDIF
IF 가쾌V증가 > 0
    IF 가윤활 < 3000
        CALL V_고통(100)
    ENDIF
    가쾌V += 가쾌V증가
    PRINTFORML 쾌 V + {가쾌V증가}
ENDIF
IF 가쾌A증가 > 0
    IF 가윤활 < 3000
        CALL V_고통(100)
    ENDIF
    가쾌A += 가쾌A증가
    PRINTFORML 쾌 A + {가쾌A증가}
ENDIF
IF 가쾌B증가 > 0
    가쾌B += 가쾌B증가
    PRINTFORML 쾌 B + {가쾌B증가}
ENDIF
IF 가쾌M증가 > 0
    가쾌M += 가쾌M증가
    PRINTFORML 쾌 M + {가쾌M증가}
ENDIF
IF 가윤활증가 > 0
    가윤활 += 가윤활증가
    PRINTFORML 윤활 + {가윤활증가}
ENDIF
IF 가고통증가 > 0
    가고통 += 가고통증가
    PRINTFORML 고통 + {가고통증가}
ENDIF

@V_경험처리
IF 가쾌C증가/500 > 0
    PRINTFORML C경험 + {가쾌C증가/500+1}
    EXP:가타겟:C경험 += 가쾌C증가/500 + 1
ENDIF
IF 가쾌V증가/500 > 0
    PRINTFORML V경험 + {가쾌V증가/500+1}
    EXP:가타겟:V경험 += 가쾌V증가/500 + 1
ENDIF
IF 가쾌A증가/500 > 0
    PRINTFORML A경험 + {가쾌A증가/500+1}
    EXP:가타겟:A경험 += 가쾌A증가/500 + 1
ENDIF
IF 가쾌B증가/500 > 0
    PRINTFORML B경험 + {가쾌B증가/500+1}
    EXP:가타겟:B경험 += 가쾌B증가/500 + 1
ENDIF
IF 가쾌M증가/500 > 0
    PRINTFORML M경험 + {가쾌M증가/500+1}
    EXP:가타겟:M경험 += 가쾌M증가/500 + 1
ENDIF

@V_절정처리
DRAWLINE
IF 가쾌C >= 10000
    CALL V_C절정
ENDIF
IF 가쾌V >= 10000
    CALL V_V절정
ENDIF
IF 가쾌A >= 10000
    CALL V_A절정
ENDIF
IF 가쾌B >= 10000
    CALL V_B절정
ENDIF
IF 가쾌M >= 10000
    CALL V_M절정
ENDIF
IF 가고통증가 >= 8000
    CALL V_고통각인(3)
ELSEIF 가고통증가 >= 5000
    CALL V_고통각인(2)
ELSEIF 가고통증가 >= 2000
    CALL V_고통각인(1)
ENDIF
IF 가반감증가 >= 8000
    CALL V_반발각인(3)
ELSEIF 가반감증가 >= 5000
    CALL V_반발각인(2)
ELSEIF 가반감증가 >= 2000
    CALL V_반발각인(1)
ENDIF
IF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 9000
    CALL V_쾌락각인(3)
ELSEIF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 7000
    CALL V_쾌락각인(2)
ELSEIF 가쾌C증가+가쾌V증가+가쾌A증가+가쾌B증가+가쾌M증가 >= 5000
    CALL V_쾌락각인(1)
ENDIF
;----
@V_증가초기화
가쾌C증가 = 0
가쾌B증가 = 0
가쾌V증가 = 0
가쾌A증가 = 0
가쾌M증가 = 0
가윤활증가 = 0
가습득증가 = 0
가순종증가 = 0
가욕정증가 = 0
가굴복증가 = 0
가치정증가 = 0
가고통증가 = 0
가공포증가 = 0
가약물증가 = 0
가선도증가 = 0
가반감증가 = 0