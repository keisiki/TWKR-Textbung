[SKIPSTART]
;---------------------
;약물커맨드 템플릿
;---------------------
;이러한 형식대로 추가하시면 됩니다
@D_COM100_ABLE
#DIM 값
값 = 1000
;해당하는 약 효과 이름을 넣으세요
IF 무조건임신약 == 0
    ;여기에 커맨드 이름을 넣으세요
    PRINTFORMLC [100]무조건 임신! 약({값}\\)
    가커맨드개행++
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_COM100
#DIM DYNAMIC 강도
#DIM 값
;가격을 입력
값 = 1000
;해당하는 약 효과를 넣으세요
IF 무조건임신약 == 0&&MONEY >= 값
    MONEY -= 값
    ;감소량을 입력하세요
    가체력감소량 = 100
    가기력감소량 = 300
    ;대사를 입력하세요
    PRINTFORML 잠시후 주사기가 담긴 쟁반을 든 직원이 들어와 %가타겟의% 팔에 주사하였다...
    SETCOLOR 0xFFFF00
    PRINTFORML %가타겟은% 임신하기 쉬운 몸이 되어버렸다
    ;소스처리를 하세요
    강도 += 2000
    무조건임신약 = 1
    ;처리된 소스를 입력하세요
    CALL V_약물(강도)
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_HELP100
;해당 약의 설명을 넣어주세요
PRINTFORML 임신을 거의 확정시키는 약
PRINTFORML 난자가 정자를 향해 헤엄친다고 한다
PRINTFORML 재료는 아마 사랑을 이루지 못한 처녀의 생리혈이라던가...
[SKIPEND]

;------------------------------------
@D_COM100_ABLE
#DIM 값
값 = 1500
;여기에 커맨드 이름을 넣으세요
IF 무조건임신약 == 0
    PRINTFORMLC [100]무조건 임신! 약({값}\\)
    가커맨드개행++
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_COM100
#DIM DYNAMIC 강도
#DIM 값
값 = 1500
IF 무조건임신약 == 0&&MONEY >= 값
    MONEY -= 값
    ;감소량을 입력하세요
    가체력감소량 = 100
    가기력감소량 = 300
    ;대사를 입력하세요
    PRINTFORML 잠시후 주사기가 담긴 쟁반을 든 직원이 들어와 %가타겟의% 팔에 주사하였다...
    SETCOLOR 0xFFFF00
    PRINTFORML %가타겟은% 임신하기 쉬운 몸이 되어버렸다
    ;소스처리를 하세요
    강도 += 2000
    무조건임신약 = 1
    ;처리된 소스를 입력하세요
    CALL V_약물(강도)
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_HELP100
;해당 약의 설명을 넣어주세요
PRINTFORML 임신을 거의 확정시키는 약
PRINTFORML 난자가 정자를 향해 헤엄친다고 한다
PRINTFORML 재료는 아마 사랑을 이루지 못한 처녀의 생리혈이라던가...

@D_COM101_ABLE
#DIM 값
값 = 2000
;해당하는 약 효과 이름을 넣으세요
IF GETBIT(가약물효과,1) == 0
    ;여기에 커맨드 이름을 넣으세요
    PRINTFORMLC [101] 미마약({값}\\)
    가커맨드개행++
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_COM101
#DIM DYNAMIC 강도
#DIM 값
값 = 2000
;해당하는 약 효과를 넣으세요
IF GETBIT(가약물효과,1) == 0&&MONEY >= 값
    MONEY -= 값
    ;감소량을 입력하세요
    가체력감소량 = 100
    가기력감소량 = 300
    ;대사를 입력하세요
    PRINTFORML 잠시후 주사기가 담긴 쟁반을 든 직원이 들어와 %가타겟의% 팔에 주사하였다...
    SETCOLOR 0xFFFF00
    PRINTFORML %가타겟은% 참기 어려운 욕망을 느끼기 시작했다... 
    ;소스처리를 하세요
    강도 += 2500
    SETBIT 가약물효과, 1
    ;처리된 소스를 입력하세요
    CALL V_약물(강도)
    CALL V_욕정(강도*2)
    CALL V_반감(강도/2)
    RETURN 1
ELSE
    RETURN 0
ENDIF

@D_HELP101
;해당 약의 설명을 넣어주세요
PRINTFORML 미약과 마약을 합친 괴상한 약
PRINTFORML 마약의 힘으로 신경을 교란시켜 도파민이 쌓이게 하고, 이에 더해 미약의 힘으로 쾌락을 탐하게 한다
PRINTFORML 재료는 아마 잊혀진 악령의 애액과 모유를 섞은거라고...

@D_COM110_ABLE
#DIM 값
값 = 1000
;해당하는 약 효과 이름을 넣으세요
;여기에 커맨드 이름을 넣으세요
PRINTFORMLC [110]원기회복약({값}\\)
가커맨드개행++
RETURN 1


@D_COM110
#DIM DYNAMIC 강도
#DIM 값
값 = 500
;해당하는 약 효과를 넣으세요
IF MONEY >= 값&&(가체력 < MAXBASE:가타겟:체력||가기력 < MAXBASE:가타겟:기력)
    MONEY -= 값
    ;대사를 입력하세요
    PRINTFORML 잠시후 알약이 담긴 쟁반을 든 직원이 들어와 %가타겟의% 입에 집어넣었다...
    SETCOLOR 0xFFFF00
    PRINTFORML %가타겟의% 체력이 500, 기력이 250만큼 회복되었다 
    ;소스처리를 하세요
    강도 += 1500
    강도 += 가약물/5
    ;처리된 소스를 입력하세요
    CALL V_약물(강도)
    가체력 += 500
    IF 가체력 > MAXBASE:가타겟:체력
        가체력 = MAXBASE:가타겟:체력
    ENDIF
    가기력 += 250
    IF 가기력 > MAXBASE:가타겟:기력
        가기력 = MAXBASE:가타겟:기력
    ENDIF
    RETURN 1
ELSE
    IF MONEY < 값
        PRINTFORML 돈이 부족하다...
    ELSEIF (가체력 == MAXBASE:가타겟:체력||가기력 == MAXBASE:가타겟:기력)
        PRINTFORML %가타겟은% 아직 팔팔하다
    ENDIF
    RETURN 0
ENDIF

@D_HELP110
;해당 약의 설명을 넣어주세요
PRINTFORML 체력과 기력이 떨어졌습니까? 그런 당신을 위한 원기회복정!
PRINTFORML 본 알약을 1개 섭취한 즉시 체내의 신진대사를 촉진, 피로와 상처를 회복시켜줍니다!
PRINTFORML 
PRINTFORML 경고 : 본 알약을 과다하게 사용할경우 간 혹은 신장에 악 영향을 끼칠수 있습니다