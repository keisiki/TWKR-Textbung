@COM763
DRAWLINE
PRINTFORML 누구의 방으로 갈까?
CALL 창녀목록(2)
PRINTL [999]돌아간다
INPUT
LOCAL = RESULT
IF LOCAL == 999
    RETURN -1
ELSEIF (TALENT:LOCAL:창녀 == 1||TALENT:LOCAL:렌탈창녀 == 1)&&(CFLAG:LOCAL:창관일 == 0||CFLAG:LOCAL:창관일 == 1)
    FLAG:개인실입장 = LOCAL
    CFLAG:LOCAL:현재위치 = 1530
    CFLAG:LOCAL:우후후 = 1
    CFLAG:LOCAL:수면 = 0
    CFLAG:MASTER:우후후 = LOCAL
ENDIF

@COM_ABLE763
SIF FLAG:USERCOM시작 == 0
	RETURN 0
IF !매음굴지역판정(CFLAG:MASTER:현재위치)
    RETURN 0
ENDIF
IF CFLAG:MASTER:현재위치 != 1530
    RETURN 0
ENDIF
IF FLAG:개인실입장
    RETURN 0
ENDIF
RETURN 1