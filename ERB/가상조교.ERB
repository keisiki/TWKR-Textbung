;========
;마개조 시작
;========
@가상조교(ARG)
;ARG = 상대
;여기선 초기화만
가체력 = MAXBASE:ARG:체력
가기력 = MAXBASE:ARG:기력
가타겟 = ARG
EXP:가타겟:매춘경험++
DRAWLINE
CALL 가상조교루틴(ARG)
RETURN 0

@가상조교루틴(ARG)
;실제 처리는 여기서
SIF 가기력 < 0
    가기력 = 0
IF 가상조교시간 >= 120
    ;시간 마감 처리
    PRINTFORMW 시간이 끝났다...
    RETURN 0
ENDIF
CALL 가상조교상태창(ARG)
PRINTL
DRAWLINE
;커맨드표시
CALL 가커맨드
;루프
INPUT
IF RESULT == 999
    PRINTFORMW %CALLNAME:가타겟%을 뒤로하고 개인실에서 나왔다
    RETURN 가상조교시간
ENDIF
;V_COM100
TRYCALLFORM V_COM{RESULT}
PRINTFORML
RESTART

@가상조교상태창(ARG)
#DIM 체력백분율
#DIM 기력백분율
체력백분율 = (가체력 * 100)/MAXBASE:ARG:체력
기력백분율 = (가기력 * 100)/MAXBASE:ARG:기력
PRINTFORML 조교 대상 : %CALLNAME:ARG%  조교자 : %CALLNAME:MASTER%  남은 시간 : {60 - 가상조교시간}분
PRINTFORML (조교경험 : {EXP:ARG:조교경험} 호감도 : {CFLAG:ARG:호감도} 신뢰도 : {CFLAG:ARG:신뢰도}  이상경험 : {EXP:ARG:이상경험})
PRINTFORM 체력
IF 체력백분율 >= 70
    SETCOLOR 0x19FF19
ELSEIF 체력백분율 >= 50
    SETCOLOR 0xFF8000
ELSEIF 체력백분율 >= 30
    SETCOLOR 0xFFFF00
ELSE
    SETCOLOR 0xFF0000
ENDIF
PRINTFORM [
REPEAT (체력백분율/5)
    PRINTFORM *
REND
REPEAT 20 - (체력백분율/5)
    PRINTFORM .
REND
PRINTFORM ]
RESETCOLOR
PRINTFORML  ({가체력}/{MAXBASE:ARG:체력})
PRINTFORM 기력
IF 기력백분율 >= 70
    SETCOLOR 0x19FF19
ELSEIF 기력백분율 >= 50
    SETCOLOR 0xFF8000
ELSEIF 기력백분율 >= 30
    SETCOLOR 0xFFFF00
ELSE
    SETCOLOR 0xFF0000
ENDIF
PRINTFORM [
REPEAT (기력백분율/5)
    PRINTFORM *
REND
REPEAT 20 - (기력백분율/5)
    PRINTFORM .
REND
PRINTFORM ]
RESETCOLOR
PRINTFORM  ({가기력}/{MAXBASE:ARG:기력})
PRINTL
;-----
PRINTFORM 쾌 C Lv{가쾌C/1000}[
REPEAT ((가쾌C % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가쾌C % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가쾌C)
PRINTFORM {가쾌C}


PRINTFORM 쾌 V Lv{가쾌V/1000}[
REPEAT ((가쾌V % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가쾌V % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가쾌V)
PRINTFORM {가쾌V}

PRINTFORM 쾌 A Lv{가쾌A/1000}[
REPEAT ((가쾌A % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가쾌A % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가쾌A)
PRINTFORM {가쾌A}

PRINTL

PRINTFORM 쾌 B Lv{가쾌B/1000}[
REPEAT ((가쾌B % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가쾌B % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가쾌B)
PRINTFORM {가쾌B}


PRINTFORM 윤활 Lv{가윤활/1000}[
REPEAT ((가윤활 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가윤활 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가윤활)
PRINTFORM {가윤활}


PRINTFORM 습득 Lv{가습득/1000}[
REPEAT ((가습득 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가습득 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가습득)
PRINTFORM {가습득}


PRINTL

PRINTFORM 순종 Lv{가순종/1000}[
REPEAT ((가순종 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가순종 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가순종)
PRINTFORM {가순종}

PRINTFORM 욕정 Lv{가욕정/1000}[
REPEAT ((가욕정 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가욕정 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가욕정)
PRINTFORM {가욕정}


PRINTFORM 굴복 Lv{가굴복/1000}[
REPEAT ((가굴복 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가굴복 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가굴복)
PRINTFORM {가굴복}


PRINTL

PRINTFORM 치정 Lv{가치정/1000}[
REPEAT ((가치정 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가치정 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가치정)
PRINTFORM {가치정}

PRINTFORM 고통 Lv{가고통/1000}[
REPEAT ((가고통 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가고통 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가고통)
PRINTFORM {가고통}

PRINTFORM 공포 Lv{가공포/1000}[
REPEAT ((가공포 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가공포 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가공포)
PRINTFORM {가공포}

PRINTL

PRINTFORM 약물 Lv{가약물/1000}[
REPEAT ((가약물 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가약물 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가약물)
PRINTFORM {가약물}

PRINTFORM 선도 Lv{가선도/1000}[
REPEAT ((가선도 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가선도 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가선도)
PRINTFORM {가선도}

PRINTFORM 반감 Lv{가반감/1000}[
REPEAT ((가반감 % 1000)/100)
    PRINTFORM *
REND
REPEAT 10 - ((가반감 % 1000)/100)
    PRINTFORM .
REND
PRINTFORM ]
CALL 가파라메터띄기(가반감)
PRINTFORM {가반감}

PRINTL

@가파라메터띄기(ARG)
IF ARG / 1000 > 0
    ;1회 띄어쓴다
    PRINT  
ELSEIF ARG / 100 > 0
    ;2회 띄어쓴다
    PRINT   
ELSEIF ARG / 10 > 0
    ;3회 띄어쓴다
    PRINT    
ELSE
    ;4회 띄어쓴다
    PRINT     
ENDIF

@가커맨드
#DIM DYNAMIC 카운트
;커맨드표를 띄운다 일단은 취소만
PRINTL
FOR LOCAL, 100, 300
    RESULT = 0
    TRYCALLFORM V_COM{LOCAL}_ABLE
    SIF RESULT == 1
        카운트++
    IF 카운트 % 3 == 0
        PRINTL
    ENDIF
NEXT
DRAWLINE
PRINTFORML [999] 그만둔다


;------이하 커맨드-----
@V_COM100_ABLE
PRINTFORML [100]애무
RETURN 1

@V_COM100
#DIM DYNAMIC 강도
가상조교시간++
;감소량
CALL 가체력감소(10)
CALL 가기력감소(50)
PRINTFORMW %마스터는% %가타겟의% 몸을 애무하기 시작했다...
강도 += ABL:MASTER:기교 * 10
강도 += TALENT:MASTER:능숙한손가락 * 50
강도 += TALENT:MASTER:혀놀림 * 50
CALL V_C쾌감(강도)
CALL V_B쾌감(강도/2)

@V_COM101_ABLE
PRINTFORML [101]커널링구스
RETURN 1

@V_COM101
#DIM DYNAMIC 강도
가상조교시간++
CALL 가체력감소(5)
CALL 가기력감소(80)
PRINTFORML %마스터는% %가타겟의% 음부를 혀로 핥기 시작했다...
강도 += ABL:MASTER:기교 * 15
강도 += TALENT:MASTER:혀놀림 * 50
CALL V_C쾌감(강도)

@V_COM102_ABLE
;여기에 커맨드 이름을 넣으세요
PRINTFORML [102]손가락 삽입
RETURN 1

@V_COM102
#DIM DYNAMIC 강도
가상조교시간++
;감소량을 입력하세요
CALL 가체력감소(10)
CALL 가기력감소(50)
;대사를 입력하세요
PRINTFORMW %마스터는% %가타겟의% 몸을 애무하기 시작했다...
;소스처리를 하세요
강도 += ABL:MASTER:기교 * 10
강도 += TALENT:MASTER:능숙한손가락 * 50
강도 += TALENT:MASTER:혀놀림 * 50
;처리된 소스를 입력하세요
CALL V_C쾌감(강도)

[SKIPSTART]
;이러한 형식대로 추가하시면 됩니다
@V_COM100_ABLE
;여기에 커맨드 이름을 넣으세요
PRINTFORML [100]애무한다
RETURN 1

@V_COM100
#DIM DYNAMIC 강도
가상조교시간++
;감소량을 입력하세요
CALL 가체력감소(10)
CALL 가기력감소(50)
;대사를 입력하세요
PRINTFORMW %마스터는% %가타겟의% 몸을 애무하기 시작했다...
;소스처리를 하세요
강도 += ABL:MASTER:기교 * 10
강도 += TALENT:MASTER:능숙한손가락 * 50
강도 += TALENT:MASTER:혀놀림 * 50
;처리된 소스를 입력하세요
CALL V_C쾌감(강도)
[SKIPEND]

;-----이하 쾌감처리----
;ARG = 조교강도
@V_C쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:C감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
PRINTFORML C경험 + {증가량/500+1}
EXP:가타겟:C경험 += 증가량/500 + 1
가쾌C += 증가량
PALAM:가타겟:윤활 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
IF 가쾌C >= 10000
    CALL V_C절정
ENDIF

@V_V쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:V감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
PRINTFORML V경험 + {증가량/500+1}
EXP:가타겟:V경험 += 증가량/500 + 1
가쾌V += 증가량
PALAM:가타겟:윤활 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
IF 가쾌V >= 10000
    CALL V_V절정
ENDIF

@V_A쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:A감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
PRINTFORML C경험 + {증가량/500+1}
EXP:가타겟:C경험 += 증가량/500 + 1
가쾌A += 증가량
PALAM:가타겟:윤활 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
IF 가쾌A >= 10000
    CALL V_A절정
ENDIF

@V_B쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:B감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
PRINTFORML B경험 + {증가량/500+1}
EXP:가타겟:B경험 += 증가량/500 + 1
가쾌B += 증가량
PALAM:가타겟:윤활 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
IF 가쾌B >= 10000
    CALL V_B절정
ENDIF

@V_M쾌감(ARG)
#DIM DYNAMIC 증가량
증가량 = ARG
SELECTCASE ABL:가타겟:M감각
    CASE 0
        TIMES 증가량 , 1.0
    CASE IS < 2
        TIMES 증가량 , 1.5
    CASE IS < 4
        TIMES 증가량 , 2.0
    CASE IS < 6
        TIMES 증가량 , 2.5
    CASE IS >= 6
        TIMES 증가량 , 3.0
ENDSELECT
PRINTFORML M경험 + {증가량/500+1}
EXP:가타겟:M경험 += 증가량/500 + 1
가쾌M += 증가량
PALAM:가타겟:윤활 += (증가량 / 50) + ((TALENT:가타겟:젖기쉬움+2)*(RAND:50+1))
IF 가쾌M >= 10000
    CALL V_M절정
ENDIF

@V_C절정
    PRINTFORML [C절정]
    PRINTFORML %가타겟은% 클리로부터의 절정에몸을 바들바들 떨고 있다
    PRINTFORML C절정경험 +1
    PRINTFORML 절정경험 +1
    EXP:가타겟:C절정경험++
    EXP:가타겟:절정경험++
    가쾌C = 0
@V_V절정
    PRINTFORML [V절정]
    PRINTFORML 질내로부터 쏟아지는 쾌감에 %가타겟의% 몸이 떨린다
    PRINTFORML V절정경험 +1
    PRINTFORML 절정경험 +1
    EXP:가타겟:V절정경험++
    EXP:가타겟:절정경험++
    가쾌V = 0
@V_A절정
    PRINTFORML [A절정]
    PRINTFORML 등골을 타고오르는 오싹한 쾌감에 %가타겟의% 신음소리도 높아져갔다
    PRINTFORML A절정경험 +1
    PRINTFORML 절정경험 +1
    EXP:가타겟:A절정경험++
    EXP:가타겟:절정경험++
    가쾌A = 0
@V_B절정
    PRINTFORML [B절정]
    PRINTFORML %가타겟의% 유두가 파르르 떨리고 있다
    PRINTFORML B절정경험 +1
    PRINTFORML 절정경험 +1
    EXP:가타겟:B절정경험++
    EXP:가타겟:절정경험++
    가쾌B = 0
@V_M절정
    PRINTFORML [M절정]
    PRINTFORML %가타겟은% 입술을 파르르 떨고 있다
    PRINTFORML M절정경험 +1
    PRINTFORML 절정경험 +1
    EXP:가타겟:M절정경험++
    EXP:가타겟:절정경험++
    가쾌M = 0

@가체력감소(ARG)
IF 가체력 < ARG
    가체력 = 0
ELSE
    가체력 -= ARG
ENDIF

@가기력감소(ARG)
IF 가기력 < ARG
    CALL 가체력감소(ARG - 가기력)
    가기력 = 0
ELSE
    가기력 -= ARG
ENDIF