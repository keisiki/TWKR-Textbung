;요소명→ID변환용 함수의 존부 확인용 함수
;변환 가능(ID와 요소명의 관계가 항상 변화하지 않는다)이라면 설치
;상황에 의해 이름이 바뀌는 것 같은 것은 불가능
;WEAPONID는 변환 가능
@EXIST_하반신속옷_비켜놓기가능_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;요소명→ID변환용 함수
;필요하면 각 오브젝트로와 같이  작성
@하반신속옷_비켜놓기가능_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;작성하는 변수명
#DIMS C_NAME, OBJ_ID_LAST + 1 ;데이터 보유
#DIM  O_ID                    ;참조용
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "하반신속옷_비켜놓기가능", O_ID, "이름")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



;존부 함수
;F함수는 TRYCALL계를 사용할 수 없기 때문에 통상 함수로 존부 배열을 만들어버린다
@EXIST_하반신속옷_비켜놓기가능0
#LOCALSIZE 1
#LOCALSSIZE 1

;오브젝트 본체
@하반신속옷_비켜놓기가능0(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	;STR형의 일시 변수를 정의하는 명령
	CALLF MAKE_STR(V_NAME, "노팬티")
CASE "입다"
;노팬티를 신는다고는
	SIF (TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:섹프 || ABL:ARG:노출벽 >= 8) && !RAND:5
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능1
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "트렁크스")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "남자용품의")
CASE "비쳐보이는"
	CALLF MAKE_INT(V_NAME, 1)
CASE "갈아입히기 가능"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "T셔츠"))
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능2

@하반신속옷_비켜놓기가능2(ARG, O_DATA, V_NAME)
#FUNCTION
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "드로워즈")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "완만한 구조의")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능3
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "쿠마짱 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "마스코트의 프린트가 사랑스러운")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능4
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "물방울 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "아이 같은 무늬의")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능5
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "줄무늬 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "영덩이의 라인이 강조되는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능6
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "프릴 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "사랑스러운 프릴이 쓰인")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능7
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "민무늬 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "꾸밈없는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSEIF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	IF TALENT:ARG:소악마 || TALENT:ARG:섹프
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능10
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "로레그 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "심플한 디자인의")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능11
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "로라이즈 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "엉덩이의 균열부가 보일 것 같은")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF !RAND:(2 - TALENT:ARG:연모)
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능12
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "하이레그 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "아슬아슬하게 째진")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF !RAND:(2 - TALENT:ARG:연모)
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	SELECTCASE RAND:(1 + 승부속옷판정(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능13
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "레이스 팬티(흰색)")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "청초하지만 어렴풋이 비쳐보이는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF !(TALENT:ARG:소악마 || TALENT:ARG:연모) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:연모 - 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	SELECTCASE (RAND:2 + 승부속옷판정(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "레이스 브라(흰색)"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	IF !(TALENT:ARG:소악마 || TALENT:ARG:연모) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "콜렉션 해금 조건"
	CALLF MAKE_STR(V_NAME, "해금：연모")
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능14
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	;STR형의 일시 변수를 정의하는 명령
	CALLF MAKE_STR(V_NAME, "레이스 팬티(흑)")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "어른의 색기가 넘치는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF !(TALENT:ARG:소악마 || TALENT:ARG:연모) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:연모 - 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	SELECTCASE (RAND:2 + 승부속옷판정(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "레이스 브라(흑)"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	IF !(TALENT:ARG:소악마 || TALENT:ARG:연모) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "콜렉션 해금 조건"
	CALLF MAKE_STR(V_NAME, "해금：연모")
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능15
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "T 백")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "엉덩이가 들어나는 대담한")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "입다"
	SIF (TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모) && !RAND:(3 -  TALENT:ARG:연모 - 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	SELECTCASE RAND:(1 + 승부속옷판정(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	SIF TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모
		CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
	CALLF MAKE_STR(V_NAME, "해금：연모")
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능16
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "끈 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "가는 끈으로 허리에 매인")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF (TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모) && !RAND:(2 -  TALENT:ARG:연모)
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	SELECTCASE RAND:(1 + 승부속옷판정(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	SIF TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모
		CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
	CALLF MAKE_STR(V_NAME, "해금：연모")
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능17
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "가죽 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "세로 지퍼가 달린")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "입다"
	SIF (TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && ABL:ARG:새드끼 > 4 && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	IF !RAND:2
		CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "벨트 브라"))
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF

CASE "콜렉션 표시 가능"
	SIF TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프
		CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
	IF (TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && ABL:ARG:새드끼 <= 4
		CALLF MAKE_STR(V_NAME, "해금：새드끼 5")
	ELSE
		CALLF MAKE_STR(V_NAME, "해금：연모")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능18
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_BOTTOM(ARG)%")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_ADJ(ARG)%")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_FRONT(ARG))
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TRANSPARENCY_BACK(ARG))
CASE "입다"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_ABLE(ARG))
CASE "관련 속옷"
	;固有ブラ
	IF GET_INT(ARG, "캐릭터데이터", ARG, "고유 상반신속옷")
		CALLF MAKE_INT(V_NAME, 13)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
;우선 이번은 무조건으로 던지지만 본래는 캐릭터데이터함수에 건네주어 반환값을 봐야 함
;그렇다면 착용 조건의 유무까지 설정할 수 있지만, 다시 전캐릭터 분의 설정이 필요하게 된다
;거기까지 하는 것 큰 일이겠지
CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능19
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "쇼츠")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "소녀다운 깜찍한")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능20
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "스캔티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "기장이 극히 짧은")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	SIF FLAG:섹시팬티추가 && !RAND:(2 - TALENT:ARG:연모)
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	SELECTCASE RAND:(1 + 승부속옷판정(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	SIF FLAG:섹시팬티추가
		CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능21
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "훈도시")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "엉덩이에 꽉 죄인")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "입다"
	SIF FLAG:섹시팬티추가 && !RAND:(3 + 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "사라시"))

CASE "콜렉션 표시 가능"
	SIF FLAG:섹시팬티추가
		CALLF MAKE_INT(V_NAME, 1)
CASE "콜렉션 해금 조건"
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능22
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "G스트링")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "극소 면적의 끈과 다름없는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "입다"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:연모 - 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	SELECTCASE RAND:(1 + 승부속옷판정(ARG))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "콜렉션 해금 조건"
	IF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지) && ABL:ARG:노출벽 >= 4
		CALLF MAKE_STR(V_NAME, "해금：연모")
	ELSEIF ABL:ARG:노출벽 < 4
		CALLF MAKE_STR(V_NAME, "해금：노출벽 4")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능23
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "시스루 쇼츠")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "중요한 부분이 비치는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - TALENT:ARG:연모 - 승부속옷판정(ARG))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	SELECTCASE (RAND:2 + 승부속옷판정(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "시스루 브라"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "콜렉션 해금 조건"
	IF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지) && ABL:ARG:노출벽 >= 4
		CALLF MAKE_STR(V_NAME, "해금：연모")
	ELSEIF ABL:ARG:노출벽 < 4
		CALLF MAKE_STR(V_NAME, "해금：노출벽 4")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능24
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "오픈 쇼츠")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "휑하게 치부를 드러낸")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(4 - TALENT:ARG:연모 - (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "관련 속옷"
	SELECTCASE (RAND:2 + 승부속옷판정(ARG))
		CASE 2
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "베이비돌"))
		CASE 1
			CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "톱리스 브라"))
		CASEELSE
			CALLF MAKE_INT(V_NAME, 0)
	ENDSELECT

CASE "콜렉션 표시 가능"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || TALENT:ARG:연모 || TALENT:ARG:섹프) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "콜렉션 해금 조건"
	IF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지) && ABL:ARG:노출벽 >= 4
		CALLF MAKE_STR(V_NAME, "해금：연모")
	ELSEIF ABL:ARG:노출벽 < 4
		CALLF MAKE_STR(V_NAME, "해금：노출벽 4")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능25
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "반창고")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "세로로 찰싹 붙이기만 한")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "입다"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지) && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "반창고"))

CASE "콜렉션 표시 가능"
	IF !(FLAG:섹시팬티추가 && ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "콜렉션 해금 조건"
	IF ABL:ARG:노출벽 < 4
		CALLF MAKE_STR(V_NAME, "해금：노출벽 4")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능26
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "드로워즈(가랑이 열림)")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "배변용으로 가랑이가 크게 벌어진")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "입다"
	IF FLAG:섹시팬티추가 && (TALENT:ARG:섹프 && ABL:ARG:노출벽 >= 5 || TALENT:ARG:무지) && !RAND:4
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)

CASE "콜렉션 표시 가능"
	IF !FLAG:섹시팬티추가
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:섹프 && ABL:ARG:노출벽 >= 5 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "콜렉션 해금 조건"
	IF TALENT:ARG:무지
	ELSEIF TALENT:ARG:섹프 && ABL:ARG:노출벽 >= 5
	ELSEIF TALENT:ARG:섹프
		CALLF MAKE_STR(V_NAME, "해금：노출벽 5")
	ELSEIF ABL:ARG:노출벽 >= 5
		CALLF MAKE_STR(V_NAME, "해금：섹프")
	ENDIF
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능27
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "정조대")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "튼튼한 열쇠가 잠긴 금속제의")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "정조대 브라"))
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능28
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "촉수 속옷")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "안쪽에서 뭔가 꿈틀꿈틀 움직이는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "촉수 브라"))
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능29
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "바이브 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "원통형의 이물이 장치된")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능30
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "두 구멍 바이브 팬티")
CASE "묘사"
	CALLF MAKE_STR(V_NAME, "두 개의 봉이 가랑이에서 방방 굽이치는")
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능31
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "비키니 보텀")
;하반신속옷2의 20
CASE "갈아입히기 가능"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "비키니 탑"))
ENDSELECT



@EXIST_하반신속옷_비켜놓기가능32
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기가능32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "마이크로 보텀")
;하반신속옷2の21
CASE "갈아입히기 가능"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "관련 속옷"
	CALLF MAKE_INT(V_NAME, CLOTHES_ID("상반신속옷1", "마이크로 탑"))
ENDSELECT

@승부속옷판정(ARG)
#FUNCTION
IF !TALENT:ARG:무지
	SIF (CFLAG:ARG:기정사실 & 합의_우후후 || TALENT:ARG:연모) && CFLAG:ARG:다음날내방
		RETURNF 1
	IF DAY:2 == 1
		SELECTCASE DAY:3
			CASE 14
				SIF TALENT:ARG:연모
					RETURNF 1
		ENDSELECT
	ELSEIF DAY:2 == 4
		SELECTCASE DAY:3
			CASE 14, 24, 25
				SIF TALENT:ARG:연모
					RETURNF 1
		ENDSELECT
	ENDIF
ENDIF
RETURNF 0

