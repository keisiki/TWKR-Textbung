;하반신속옷_비켜놓기불가
;요소명→ID변환용 함수의 존부 확인용 함수
;변환 가능(ID와 요소명의 관계가 항상 변화하지 않는다)이라면 설치
;상황에 의해 이름이 바뀌는 것 같은 것은 불가능
@EXIST_하반신속옷_비켜놓기불가_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;요소명→ID변환용 함수
;필요하면 각 오브젝트로와 같이  작성
@하반신속옷_비켜놓기불가_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;작성하는 변수명
#DIMS C_NAME, OBJ_ID_LAST + 1 ;데이터 보유
#DIM  O_ID                    ;참조용
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "하반신속옷_비켜놓기불가", O_ID, "이름")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_하반신속옷_비켜놓기불가1
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "샅바")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 1
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가2
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "마에바리")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 2
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가3
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "반창고")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 3
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가4
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "스패츠")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 4
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 1)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가5
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "정조대")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 5
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가6
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "라비아 피어스")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 6
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT


@EXIST_하반신속옷_비켜놓기불가7
#LOCALSIZE 1
#LOCALSSIZE 1

@하반신속옷_비켜놓기불가7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "클리 피어스")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「하반신속옷1」")
;구장비 처리와의 조회용, 하반신속옷1의 7
CASE "비쳐보이는(앞)"
	CALLF MAKE_INT(V_NAME, 0)
CASE "비쳐보이는(뒤)"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



