;상반신속옷_탈의불가
;요소명→ID변환용 함수의 존부 확인용 함수
;변환 가능(ID와 요소명의 관계가 항상 변화하지 않는다)이라면 설치
;상황에 의해 이름이 바뀌는 것 같은 것은 불가능
@EXIST_상반신속옷_탈의불가_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;요소명→ID변환용 함수
;필요하면 각 오브젝트로와 같이  작성
@상반신속옷_탈의불가_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;작성하는 변수명
#DIMS C_NAME, OBJ_ID_LAST + 1 ;데이터 보유
#DIM  O_ID                    ;참조용
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "상반신속옷_탈의불가", O_ID, "이름")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF




@EXIST_상반신속옷_탈의불가1
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "브래지어")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 1
CASE "붙인다"
	IF TALENT:ARG:유치 || TALENT:ARG:애같음
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가2
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "사라시")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 2
CASE "붙인다"
	IF TALENT:ARG:유치 || TALENT:ARG:애같음
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 3)
ENDSELECT



@EXIST_상반신속옷_탈의불가3
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "스포츠브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 3
CASE "붙인다"
	SIF !RAND:(1 + (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가4
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "캐미숄")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 4
CASE "붙인다"
	CALLF MAKE_INT(V_NAME, 1)
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가5
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가5(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "반창고")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 5
CASE "붙인다"
	IF !(ABL:ARG:노출벽 >= 4)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지) && !RAND:3
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
		CALLF MAKE_INT(V_NAME, 0)
	ENDIF
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가6
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "니프레스(별 모양)")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 6
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가7
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "니프레스(하트형)")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 7
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT



@EXIST_상반신속옷_탈의불가8
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가8(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "유두 피어스")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 8
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT



@EXIST_상반신속옷_탈의불가9
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가9(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "벨트 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 9
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 99)
ENDSELECT

@EXIST_상반신속옷_탈의불가10
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "T셔츠")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 10
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT

@EXIST_상반신속옷_탈의불가11
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "레이스 브라(흰색)")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 11
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_상반신속옷_탈의불가12
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "레이스 브라(흑)")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 12
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_상반신속옷_탈의불가13
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, @"%UNIQUE_UNDERWARE_TOP(ARG)%")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 13
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, UNIQUE_UNDERWARE_TOP_THICK(ARG))
ENDSELECT

@EXIST_상반신속옷_탈의불가14
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "시스루 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 14
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_상반신속옷_탈의불가15
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가15(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "톱리스 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 15
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 0)
ENDSELECT

@EXIST_상반신속옷_탈의불가16
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가16(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "튜브탑 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
CASE "붙인다"
	IF TALENT:ARG:유치 || TALENT:ARG:애같음
		CALLF MAKE_INT(V_NAME, 0)
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT

@EXIST_상반신속옷_탈의불가17
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가17(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "정조대 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 17
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 99)
ENDSELECT

@EXIST_상반신속옷_탈의불가18
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가18(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "촉수 브라")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 18
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 3)
ENDSELECT

@EXIST_상반신속옷_탈의불가19
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가19(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "비키니 탑")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 19
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT

@EXIST_상반신속옷_탈의불가20
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가20(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "마이크로 탑")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
;구장비 처리와의 조회용, 상반신속옷1의 20
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 2)
ENDSELECT

@EXIST_상반신속옷_탈의불가21
#LOCALSIZE 1
#LOCALSSIZE 1

@상반신속옷_탈의불가21(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "베이비돌")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「상반신속옷1」")
CASE "붙인다"
	IF !(TALENT:ARG:걸레 || TALENT:ARG:소악마 || IS_LOVER(ARG)) && (TALENT:ARG:유치 || TALENT:ARG:애같음 || TALENT:ARG:체형 < 0 || TALENT:ARG:무지)
		CALLF MAKE_INT(V_NAME, 0)
	ELSEIF !RAND:(3 - (2 * 승부속옷판정(ARG)))
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
CASE "옷감 두께"
	CALLF MAKE_INT(V_NAME, 1)
ENDSELECT
