;보디슈트
;요소명→ID변환용 함수의 존부 확인용 함수
;변환 가능(ID와 요소명의 관계가 항상 변화하지 않는다)이라면 설치
;상황에 의해 이름이 바뀌는 것 같은 것은 불가능
@EXIST_보디슈트_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;요소명→ID변환용 함수
;필요하면 각 오브젝트로와 같이  작성
@보디슈트_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;작성하는 변수명
#DIMS C_NAME, OBJ_ID_LAST + 1 ;데이터 보유
#DIM  O_ID                    ;참조용
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "보디슈트", O_ID, "이름")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_보디슈트1
#LOCALSIZE 1
#LOCALSSIZE 1

@보디슈트1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "보디슈트")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「보디슈트」")
;구장비 처리와의 조회용, 보디슈트의 1
ENDSELECT



@EXIST_보디슈트2
#LOCALSIZE 1
#LOCALSSIZE 1

@보디슈트2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "러버 슈트")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「보디슈트」")
;구장비 처리와의 조회용, 보디슈트의 2
ENDSELECT



@EXIST_보디슈트3
#LOCALSIZE 1
#LOCALSSIZE 1

@보디슈트3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "라이더 슈트")
CASE "장비부위"
	CALLF MAKE_STR(V_NAME, "「보디슈트」")
;구장비 처리와의 조회용, 보디슈트의 3
ENDSELECT
