;캐캐릭터리스트
;요소명→ID변환용 함수의 존부 확인용 함수
;변환 가능(ID와 요소명의 관계가 항상 변화하지 않는다)이라면 설치
;상황에 의해 이름이 바뀌는 것 같은 것은 불가능
@EXIST_캐릭터리스트_ID
#LOCALSIZE 1
#LOCALSSIZE 1

;요소명→ID변환용 함수
;필요하면 각 오브젝트로와 같이  작성
@캐릭터리스트_ID(ARG:0, ARGS:0, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS V_NAME                  ;작성하는 변수명
#DIMS C_NAME, OBJ_ID_LAST + 1 ;데이터 보유
#DIM  O_ID                    ;참조용
IF ARGS:0 == "RESET"
	VARSET C_NAME
	FOR O_ID, 1, OBJ_ID_LAST + 1
		C_NAME:O_ID = %GET_STR(, "캐릭터리스트", O_ID, "이름")%
	NEXT
ELSE
	O_ID = FINDELEMENT(C_NAME, ESCAPE(ARGS:0), , , 1)
	CALLF MAKE_INT(V_NAME, O_ID)
ENDIF



@EXIST_캐릭터리스트1
#LOCALSIZE 1
#LOCALSSIZE 1

;오브젝트 본체
@캐릭터리스트1(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "존재하는 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "존재")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_캐릭터리스트2
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트2(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "주인공 이외의캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF ARG == MASTER || !CHECK_CHARA(ARG, "존재")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT


@EXIST_캐릭터리스트3
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트3(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "함락않다 보고 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "함락")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT



@EXIST_캐릭터리스트4
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트4(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "깨끗한교제 취득 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "깨끗한교제")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT





@EXIST_캐릭터리스트6
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트6(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "주인공의 약점을 잡는 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "약점잡음")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_캐릭터리스트7
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트7(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "주인공에게 약점이 있는 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "약점잡힘")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_캐릭터리스트8
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트8(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "반발각인 떡캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "반발각인")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_캐릭터리스트9
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트9(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "시간각인 떡캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "시간각인")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT



@EXIST_캐릭터리스트10
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트10(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "동실에 있는 캐릭터리스트")
CASE "판정"
	SIF CHECK_CHARA(ARG, "동실")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT


@EXIST_캐릭터리스트11
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트11(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "말떡캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "구상실장상황")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_캐릭터리스트12
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트12(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "임신중 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "임신")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_캐릭터리스트13
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트13(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "육아중 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "육아중")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

@EXIST_캐릭터리스트14
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트14(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "아이 딸린 사람 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	SIF CHECK_CHARA(ARG, "아이 딸린 사람")
		CALLF MAKE_INT(V_NAME, 1)
ENDSELECT

;종족
@EXIST_캐릭터리스트22
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트22(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "무녀 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "무녀")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트23
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트23(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "요정 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "요정")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트24
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트24(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "신령 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "신령")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트25
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트25(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "츠쿠모가미 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "츠쿠모가미")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트26
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트26(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "오니 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "오니")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트27
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트27(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "텐구 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "텐구")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트28
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트28(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "봉래인 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "봉래인")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트29
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트29(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "요괴 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "요괴")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

@EXIST_캐릭터리스트30
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트30(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "마법사 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "마법사")
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT

;복합 조건계


@EXIST_캐릭터리스트31
#LOCALSIZE 1
#LOCALSSIZE 1

;오브젝트 본체
@캐릭터리스트31(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "갈아입히기 가능 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
;갈아입히기일이 생긴다
	IF ARG == MASTER
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF !CHECK_CHARA(ARG, "존재") || !CHECK_CHARA(ARG, "동실")
	ELSEIF FLAG:시간정지 == 1
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF 수면시간(ARG)
	ELSEIF CHECK_CHARA(ARG, "함락")
		CALLF MAKE_INT(V_NAME, 1)
	ELSEIF CHECK_CHARA(ARG, "약점잡음")
	ELSEIF CHECK_CHARA(ARG, "약점잡힘")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_캐릭터리스트32
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트32(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "평상복 모방 가능 캐릭터리스트")
CASE "SHOP 표시"
	CALLF MAKE_INT(V_NAME, 1)
CASE "판정"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "존재")
	ELSEIF CHECK_CHARA(ARG, "약점잡음")
	ELSEIF CHECK_CHARA(ARG, "함락") || CHECK_CHARA(ARG, "약점잡힘")
		CALLF MAKE_INT(V_NAME, 1)
	ELSE
	ENDIF
ENDSELECT



@EXIST_캐릭터리스트33
#LOCALSIZE 1
#LOCALSSIZE 1

@캐릭터리스트33(ARG, O_DATA, V_NAME)
#FUNCTION
#LOCALSIZE 1
#LOCALSSIZE 1
#DIMS O_DATA
#DIMS V_NAME
SELECTCASE O_DATA
CASE "이름"
	CALLF MAKE_STR(V_NAME, "TARGET 리스트")
CASE "판정"
	IF ARG == MASTER
	ELSEIF !CHECK_CHARA(ARG, "존재") || !CHECK_CHARA(ARG, "동실")
	ELSE
		CALLF MAKE_INT(V_NAME, 1)
	ENDIF
ENDSELECT
