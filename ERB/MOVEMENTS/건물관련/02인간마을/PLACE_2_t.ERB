@KICKOUT_2()
#FUNCTION
;SELECTCASE CHK_INHOME(CFLAG:MASTER:현재위치)
;	CASE 12
;		RETURNF 107
;ENDSELECT
;RETURNF 101

@BATHCHECK_2(ARG)
#FUNCTION
SELECTCASE CHK_INHOME(CFLAG:MASTER:현재위치)
ENDSELECT
RETURNF 0

@MAP_UNIQUEMESSAGE_2
IF WITH_MOB()
	RETURN 1
ELSE
	SELECTCASE CFLAG:MASTER:현재위치
		CASE 205
			IF INRANGE(TIME, 540, 1020)
				PRINTFORML 보기에도 의욕이 없어 보이는 캇파가 삭도의 접수를 하고 있다
			ELSE
				PRINTFORML 삭도역은 닫혀 있다
			ENDIF
		CASE 213
			PRINTFORML 점내에는 많은 책장이 줄지어 있다
		CASE 222
			IF TIME >= 480 && TIME <= 720 || TIME >= 780 && TIME <= 840 && FLAG: 70 != 1
				PRINTDATA
					DATAFORM 서당의 아이들은 %CALLNAME:TARGET%의 수업을 받고 있다…
				ENDDATA
				PRINTL
			ELSEIF TIME >= 720 && TIME <= 780 && FLAG:70 != 1
				PRINTDATA
					DATAFORM 서당의 아이들은 %타겟과% 식사하고 있다…
				ENDDATA
				PRINTL
			ELSEIF TIME >= 480 && TIME <= 840 && FLAG:70 == 1 && !CFLAG:MASTER:우후후
				PRINTDATA
					DATAFORM 서당의 아이들은 정지해 있다…
				ENDDATA
				PRINTL
			ELSEIF TIME >= 480 && TIME <= 840 && FLAG:70 == 1 && CFLAG:MASTER:우후후
				PRINTDATA
					DATAFORM %타겟은% 서당의 아이들의 앞에서 습격당하고 있다…
				ENDDATA
				PRINTL
			ENDIF
		CASE 203 TO 206
			SIF !RAND:50 && INRANGE(TIME, 540, 1020)
				PRINTFORML 약봉지를 든 수염의 사나이가, 행인에게 무언가 열심히 말하고 있다.
	ENDSELECT
ENDIF
RETURN 1


;-----------------------------------------
;비자택시에 갈아넣는 장소 ID를 돌려주는 함수
;ARG, 대상ID
;RETURNF, 갈아넣고가 끝난 대상ID
;-----------------------------------------

@MAP_SEARCH_REPLACEMENT_2(ARG)
#FUNCTION
SELECTCASE ARG
	CASE 201 TO 207, 219, 220
		RETURNF 210
	CASE 225
		RETURNF 260
	CASE 213, 215, 216, 226, 227
		RETURNF 220
	CASE 211, 212, 217, 218
		RETURNF 230
	CASE 222, 224
		RETURNF 240
	CASE 223, 228
		RETURNF 250
	CASE 208 TO 210, 214, 221, 230 TO 237
		RETURNF 270
ENDSELECT
RETURNF 0



@MAP2_AREANAME(PLACE_ID)
#FUNCTIONS
#LOCALSIZE 1
#LOCALSSIZE 1
#DIM PLACE_ID
RETURNF "마을"
