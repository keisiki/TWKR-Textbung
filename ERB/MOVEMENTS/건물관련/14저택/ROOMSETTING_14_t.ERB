@ROOMSETTING_14
VARSET ROOMDATA
FOR LOCAL, 1,100
	SIF INRANGE(LOCAL, 14,19)|| INRANGE(LOCAL, 24,29)|| INRANGE(LOCAL, 34,39)
		ROOMDATA:LOCAL |= 장소_이불
	SIF INRANGE(LOCAL, 1,8) || INRANGE(LOCAL, 13,44) ||GROUPMATCH(LOCAL, 11,45)
		ROOMDATA:LOCAL |= 장소_옥내
	SIF GROUPMATCH(LOCAL, 8,44)
		ROOMDATA:LOCAL |= 장소_목욕탕
	SIF GROUPMATCH(LOCAL, 6,30,40)
		ROOMDATA:LOCAL |= 장소_주방
	SIF GROUPMATCH(LOCAL, 9,41)
		ROOMDATA:LOCAL |= 장소_훈련
	SIF GROUPMATCH(LOCAL, 22,32)
		ROOMDATA:LOCAL |= 장소_공부
	SIF GROUPMATCH(LOCAL, 2,3,4,5,44,45)||INRANGE(LOCAL, 14,22)|| INRANGE(LOCAL, 24,32)|| INRANGE(LOCAL, 34,42)
		ROOMDATA:LOCAL |= 장소_휴식
	SIF GROUPMATCH(LOCAL, 9,10,12,31)
		ROOMDATA:LOCAL |= 장소_창공
	SIF GROUPMATCH(LOCAL, 9,10,12,31)
		ROOMDATA:LOCAL |= 장소_달구경
	SIF GROUPMATCH(LOCAL, 14,31)
		ROOMDATA:LOCAL |= 장소_하늘의소리
	SIF GROUPMATCH(LOCAL, 9)
		ROOMDATA:LOCAL |= 장소_채소밭
	SIF GROUPMATCH(LOCAL, 10,44)
		ROOMDATA:LOCAL |= 장소_물긷기
	SIF GROUPMATCH(LOCAL, 12,31)
		ROOMDATA:LOCAL |= 장소_전망
	SIF GROUPMATCH(LOCAL, 11)
		ROOMDATA:LOCAL |= 장소_좁다
	SIF GROUPMATCH(LOCAL, 9,10,44,13,23,33,43)
		ROOMDATA:LOCAL |= 장소_넓은
	SIF GROUPMATCH(LOCAL, 3,4)||INRANGE(LOCAL, 14,20)||INRANGE(LOCAL, 24,30)||INRANGE(LOCAL, 34,40)
		ROOMDATA:LOCAL |= 장소_선잠_가능
	SIF GROUPMATCH(LOCAL, 11)
		ROOMDATA:LOCAL |= 장소_머물지않는다
	SIF GROUPMATCH(LOCAL, 7,8,42,44)
		ROOMDATA:LOCAL |= 장소_신체측정
	SIF INRANGE(LOCAL, 14,19)|| INRANGE(LOCAL, 24,29)|| INRANGE(LOCAL, 34,39)||GROUPMATCH(LOCAL, 45)
		ROOMDATA:LOCAL |= 장소_원룸
	SIF GROUPMATCH(LOCAL, 11)
		ROOMDATA:LOCAL |= 장소_과소
	SIF INRANGE(LOCAL, 14,19)|| INRANGE(LOCAL, 24,29)|| INRANGE(LOCAL, 34,39)
		ROOMDATA:LOCAL |= 장소_Private
	SIF GROUPMATCH(LOCAL, 44)
		ROOMDATA:LOCAL |= 장소_공공장소
	SIF INRANGE(LOCAL, 14,19)|| INRANGE(LOCAL, 24,29)|| INRANGE(LOCAL, 34,39)
		ROOMDATA:LOCAL |= 장소_거실
NEXT
;;
IF ITEM:주택 ==2
	MAXROOM = 1424
ELSEIF ITEM:주택 ==3
	MAXROOM = 1434
ELSEIF ITEM:주택 ==4
	MAXROOM = 1446
ELSE
	MAXROOM = 1446
ENDIF
ENTRANCE = 1401
피난처 = 1402

SIF GET_MAPID(거점_술벌레) != MAIN_MAP
	거점_술벌레 = 1406
IF ITEM:주택 ==2
	MAP_PAGES = 1
ELSEIF ITEM:주택 ==3
	MAP_PAGES = 2
ELSEIF ITEM:주택 ==4
	MAP_PAGES = 3
ENDIF

MAPNAME:1 = %CALLNAME:MASTER%의 저택 1-2층
MAPNAME:2 = %CALLNAME:MASTER%의 저택 2-3층
MAPNAME:3 = %CALLNAME:MASTER%의 저택 지하-1층

CALL 저택방이름설정

@DEFAULT_OWNER_14
Default초기위치 = 1414
거점_주인 = 0