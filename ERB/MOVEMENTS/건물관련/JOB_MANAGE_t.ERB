;-----------------------------------------
;일·연회 관련
;-----------------------------------------
@ENKAI_MAP_SETTING(ARG)
SELECTCASE ARG



ENDSELECT
;-----------------------------------------
;직장, 연회회장의 장소 ID를 돌려주는 함수
;ARGS,"연회"or"일"
;TCHARA, ARGS=="일"의 경우가 일하는 캐릭터 ID
;출력, 직장, 연회회장의 장소 ID
;-----------------------------------------
@GET_JOBPLACE(ARGS, TCHARA = 0)
#FUNCTION
#DIM 대상ID
#DIM 대상맵
#DIM TCHARA
#DIM OUTID
IF ARGS == "연회"
	대상ID = FLAG:32
ELSEIF ARGS == "일" && TCHARA != 0
	대상ID = CFLAG:TCHARA:직장
ELSE
	RETURNF 0
ENDIF
RETURNF GET_MAP_REPLACEMENT(대상ID)
