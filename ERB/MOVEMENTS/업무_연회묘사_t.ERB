@일정경묘사(ARG)
;ARG는 캐릭터 캐릭터별의 정경묘사에 부디.
IF (TALENT:ARG:창녀 || TALENT:ARG:렌탈창녀) && 매음굴지역판정(CFLAG:ARG:현재위치)
	SELECTCASE CFLAG:ARG:창관일
		CASE 1
			;개인실
			;대사없음
		CASE 2
			;스트립쇼
			SELECTCASE RAND:3
				CASE 0
					PRINTFORMW %ARG는% 아슬아슬한 지점까지 옷을 벗어보이고 있다
				CASE 1
					PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 힐끔힐끔 바라보며#요염하게\@ 봉춤을 추고 있다
				CASE 2
					PRINTFORMW %ARG는% 거의 헐벗은 모습으로 춤을 추고 있다
			ENDSELECT
		;3은 더미데이터
		CASE 4,5
			;대연회장
			SELECTCASE RAND:3
				CASE 0
					PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 힐끔힐끔 바라보며#부지런히\@ 손님들에게 봉사하고 있다.
				CASE 1
					PRINTFORMW %ARG는% 지나가던 손님에게 몸을 더듬어지고 있다.
				CASE 2
					PRINTFORMW %ARG는% 손님들에게 아양을 떨고 있다
			ENDSELECT
	ENDSELECT
ELSEIF CFLAG:ARG:직종 == 48
	PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 신경 쓰면서 # 바쁘게\@ 연회 준비를 하고 있다.
ELSEIF CFLAG:ARG:직종 == 50
	PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 신경 쓰면서 # 묵묵히\@ 연회 정리를 하고 있다.
ELSEIF CFLAG:ARG:직종 == 46
	PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %CALLNAME:MASTER%에 가끔식 시선을 돌리며 # \@놀고 있다.
ELSEIF CFLAG:ARG:직종 == 52
	SELECTCASE ARG
		;어쨌거나 여자 모임 ※나중에 캐릭터마다 개성있게 하고 싶기 때문에 분기만 만들어 둔다
		CASE 4
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 유유코와 최근 무서웠던 일을 이야기해 노란 비명을 지르고 있다.
			ENDSELECT
		CASE 26
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 미마와 함께 염라에의 험담으로 분위기를 살리고 있다.
			ENDSELECT
		CASE 32
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 신키와 유행의 네일 아트에 대해 말하고 있다.
			ENDSELECT
		CASE 55
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 에이린에 안티 에이징의 화제를 털고 있다.
			ENDSELECT
		CASE 66
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 유카리의 접시로부터 닭꼬치를 군것질하고 있다.
			ENDSELECT
		CASE 72
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 카나코와 즉 친구인 것을 서로 맹세하고 있다.
			ENDSELECT
		CASE 102
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 오키나와 반짝반짝의 많이 붙은 스맛폰을 서로 보이고 있다.
			ENDSELECT
		CASE 124
			SELECTCASE RAND:2
			CASE 0
				PRINTFORML %ARG는%, 여자들과 떠들썩하게 담소하고 있다.
			CASEELSE ;KR판 구잔재.
				PRINTFORML %ARG는%, 뱌쿠렌과 서로의 머리카락의 예쁨을 칭찬하고 서로 뜯고.
			ENDSELECT
	ENDSELECT
		;여자 모임 여기까지
ELSE
	SELECTCASE ARG
		CASE 30
			IF INRANGE(TIME, CLOCK_TO_TIME(9, 0), CLOCK_TO_TIME(11, 59))
				PRINTFORML %ARG는% 엄숙히 %조사처리(GET_JOBNAME(ARG), "를")% 진행하고 있다.
			ELSEIF INRANGE(TIME, CLOCK_TO_TIME(12, 0), CLOCK_TO_TIME(12, 59))
				PRINTFORML %ARG는% %조사처리(GET_JOBNAME(ARG), "를")% 취하고 있다.
			ELSE
				PRINTFORML %ARG는% 담담하게 %조사처리(GET_JOBNAME(ARG), "를")% 진행하고 있다.
			ENDIF
		CASE 54
			PRINTFORMW %ARG는% 책에 박힌 얼굴을 들지도 않는다…
		CASE 57
			PRINTFORMW %ARG는% 한가롭게 %조사처리(GET_JOBNAME(ARG), "를")% 하고 있다.
		CASE 67
			IF TIME >= 720 && TIME <= 780
				PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 신경 쓰면서 # \@아이들과 %조사처리(GET_JOBNAME(ARG), "를")% 취하고 있다.
			ELSE
				PRINTFORMW %ARG는% \@ TALENT:ARG:연모 ? %마스터를% 신경 쓰면서 # \@%조사처리(GET_JOBNAME(ARG), "를")% 하고 있다.
			ENDIF
		CASE 70
			IF DAY % 7 == 3 || DAY % 7 == 4
				PRINTFORML %ARG는% 책을 읽으면서 %조사처리(GET_JOBNAME(ARG), "를")% 하고 있다.
			ELSE
				PRINTFORML %ARG는% 내용을 확인하면서 정중하게 %조사처리(GET_JOBNAME(ARG), "를")% 하고 있다.
			ENDIF
		CASEELSE
			PRINTFORML %ARG는% 기분 좋게 %조사처리(GET_JOBNAME(ARG), "를")% 해내고 있다.
	ENDSELECT
ENDIF
@연회묘사(ARG)
;FLAG:연회개최플래그를 사용해 연회 종별로 나누어 써도 괜찮을지도

IF TCVAR:ARG:만취 == 0
	IF CFLAG:ARG:직종 == 49 && TALENT:ARG:연모 == 1
		PRINTFORMW %ARG는% %CALLNAME:MASTER%에 가끔씩 눈길을 주면서 연회를 즐기고 있다.
	ELSE
		PRINTFORML %ARG는% 연회를 즐기고 있다.
	ENDIF
ELSE
	IF TALENT:ARG:연모 == 1
		PRINTFORMW %ARG는% %CALLNAME:MASTER%에 가끔씩 눈길을 주면서 만취하고 있다.
	ELSE
		PRINTFORML %ARG는% 만취해서 녹초가 되어 있다.
	ENDIF
ENDIF
IF CFLAG:ARG:직종 == 51 && (BASE:ARG:술기운 > (MAXBASE:ARG:술기운 / 10) * 8)
	PRINTW 　취기는 꽤 남은 것 같다…
ELSEIF CFLAG:ARG:직종 == 51 && (BASE:ARG:술기운 > (MAXBASE:ARG:술기운 / 10) * 6)
	PRINTW 　취기가 해소되려면 아직 시간이 필요할 것 같다…
ELSEIF CFLAG:ARG:직종 == 51
	PRINTW 　조금 있으면 취기가 깰 것 같다…
ELSEIF CFLAG:ARG:직종 == 49 && (BASE:ARG:술기운 > (MAXBASE:ARG:술기운 / 10) * 6) 
	IF TALENT:ARG:감정결여 && RAND:4
		ELSEIF !RAND:10
			IF !RAND:2
				PRINTFORMW 　%CALLNAME:ARG%의 눈이 감겨있다…
				PRINTFORMW 　나쁜 술버릇을 가진 것 같다
					TALENT:ARG:15 = -1
			ELSE
				PRINTFORMW 　%ARG는% 무척 즐거워 보인다
				PRINTFORMW 　좋은 느낌으로 취기가 올라온 것 같다
					TALENT:ARG:15 = 1
			ENDIF
		ELSE
			IF TALENT:ARG:15 == -1
				PRINTFORMW 　%ARG는% 몸을 비비 꼬고 있다…
			ELSEIF TALENT:ARG:15 == 1
				PRINTFORMW 　%ARG는% 의미도 없이 웃고 있다…
			ELSE
				PRINTW 　슬슬 취기에서 회복된 것 같다…
			ENDIF
	 	ENDIF
ELSEIF CFLAG:ARG:직종 == 49 && (BASE:ARG:술기운 > (MAXBASE:ARG:술기운 / 10) * 4)
	PRINTW 　조금 기분이 업되었다…
ELSEIF TALENT:ARG:술내성 == -2 || CFLAG:ARG:임신자각상태 == 1
	IF BASE:ARG:일량 >= (MAXBASE:ARG:일량 / 3) * 2
		PRINTW 　술은 마시지 않고 담소하고 있다…
	ELSEIF BASE:ARG:일량 >= (MAXBASE:ARG:일량 / 3)
		IF CFLAG:ARG:임신자각상태 == 1
			PRINTW 　정신을 바짝 차리고 술을 마시지 않으며 즐기고 있다…
		ELSEIF TALENT:ARG:술내성 == -2
			PRINTW 　여전히 술은 마시지 않는 것 같다…
		ENDIF
	ELSEIF TALENT:ARG:술내성 == -2 || CFLAG:ARG:임신자각상태 == 1
		PRINTW 　술에는 일절 손대지 않을 태세다…
	ENDIF
ELSEIF CFLAG:ARG:직종 == 49 
	PRINTW 　아직 쌩쌩한 편이다…
ENDIF
