@붉은실발동
IF TALENT:MASTER:붉은실&&(RAND:20==0)
	SETCOLOR C_RED
	PRINTFORML %CALLNAME:MASTER%의 운명의 붉은 실이 빛나고 있습니다.
	SETCOLOR C_PINK
	PRINTFORML %타겟은% %CALLNAME:MASTER%에게서 운명을 느낀 듯하다.
	RESETCOLOR
	SIF !RELATION:TARGET:MASTER
		RELATION:TARGET:MASTER = 100
	RELATION:TARGET:MASTER += 20
	SIF !RELATION:MASTER:TARGET
		RELATION:MASTER:TARGET = 100
	RELATION:MASTER:TARGET += 20
ENDIF