;-------------------------------------------------
;A펄을 넣는다
;-------------------------------------------------
@COM127
;-------------------------------------------------
;COM별 FLAG의 설정
;-------------------------------------------------
;행위자
TCVAR:116 = TARGET
;봉사 쾌락 플래그
TCVAR:108 = 1

CSTR:PLAYER:10 += @"{TARGET}/"
;TCVAR:2 사정 개소 플래그（-1=콘돔 1=질내 2=애널 3=자위 4=펠라티오 5=파이즈리 6=스마타 7=풋잡 8=체표 9=애널 봉사 10=도구 11=촉수 12=A고문 13=V고문）
TCVAR:PLAYER:2 = 12
;강도
TCVAR:PLAYER:4 = 300

;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비 체력, 기력
DOWNBASE:체력 += 100
DOWNBASE:기력 += 150

SOURCE:PLAYER:쾌A = 200
SOURCE:성행동 = 300
SOURCE:달성 = 200
SOURCE:정복 = 800
SOURCE:불결 = 200
SOURCE:반감 = 100

;-------------------------------------------------
;특수 소스 처리
;-------------------------------------------------
;억압, 저항 소유에는, 억울소스 추가
SIF TALENT:자기애 < 0 || TALENT:저항
	SOURCE:울굴 += 400

;-------------------------------------------------
;더러움의 처리
;-------------------------------------------------
;대상의 손⇔조교자의A의 더러움이 이동
STAIN:1 |= STAIN:PLAYER:4
STAIN:PLAYER:4 |= STAIN:1

;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;동성애  레즈라면 반환값 1 동성애자라면 반환값 2
IF HETEROSEX(TARGET,PLAYER)
	EXP:(50 + HETEROSEX(TARGET,PLAYER)) += 7
	EXP:PLAYER:(50 + HETEROSEX(TARGET,PLAYER)) += 7
ENDIF

STR:0 = A펄을 넣는다

CALL PEARL_INSERT(TARGET, 5)

PRINTFORML %CALLNAME:TARGET%에 애널 펄을 넣었다
PRINTFORMW {TFLAG:펄기한-TIME}분간 나오지 않게 힘내자

FOR LOCAL, 0, CHARANUM
	CALL ENDUFUFU(LOCAL)
NEXT
TFLAG:106 = 0

RETURN 1

@CAN_COM127(ARG)
SKIPDISP ARG
PRINTL A펄을 넣는다
;-------------------------------------------------
;실행 판정 처리
;-------------------------------------------------

RETURN 1
;-------------------------------------------------
;실행 판정
;-------------------------------------------------
@COM_ABLE127
;A펄을 넣을 수 있는 실행 판정
SIF !TFLAG:100
	RETURN 0
;일괄관리
SIF GLOBAL_COMABLE(127)
	RETURN RESULT
;하반신 착의（조교자）
SIF TEQUIP:PLAYER:0 & 4
	RETURN 0
;수면간중
SIF CFLAG:장난 > 1
	RETURN 0
;정지중은 불가
SIF FLAG:70 == 1
	RETURN 0
;SIF ABL:새드끼 < 2
;	RETURN 0
;SIF EXP:PLAYER:A경험 < 10
;	RETURN 0

; 사용 불가 처리
RETURN 0
