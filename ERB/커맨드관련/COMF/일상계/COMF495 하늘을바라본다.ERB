;-------------------------------------------------
;空を眺める
;日常系コマンド、レベル1
;-------------------------------------------------
@COM495

TIME += 1

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE495
;一括管理
SIF GLOBAL_COMABLE(495)
	RETURN RESULT
SIF !TFLAG:100
	RETURN 0
;停止中は不可
SIF FLAG:70
	RETURN 0
;ウフフ中は不可
SIF CFLAG:우후후
	RETURN 0
;睡眠イタズラ中は不可
SIF CFLAG:장난
	RETURN 0
;添い寝中
SIF CFLAG:동침중
	RETURN 0
;デート中
;SIF CHK_DATENOW(CFLAG:デート中) && CFLAG:現在位置 != OMANEKIBEYA()
;	RETURN 0
RETURN 1