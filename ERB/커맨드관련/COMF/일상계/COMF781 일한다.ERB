;-------------------------------------------------
;追う
;日常系コマンド、レベル1
;-------------------------------------------------
@COM781

CALL 직장일처리
RETURN RESULT

;-------------------------------------------------
;実行判定
;-------------------------------------------------
@COM_ABLE781
SIF FLAG:USERCOM시작 == 0
	RETURN 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
IF MAIN_MAP == FLAG:직장지역
	IF FLAG:직장위치1 != CFLAG:MASTER:현재위치
		RETURN 0
	ENDIF
ELSE
	IF FLAG:직장위치2 != CFLAG:MASTER:현재위치
		RETURN 0
	ENDIF
ENDIF
IF FLAG:70&&BASE:MASTER:TSP < 180
	RETURN 0
ENDIF
RETURN 1

