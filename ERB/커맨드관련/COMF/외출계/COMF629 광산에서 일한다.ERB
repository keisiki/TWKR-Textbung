@COM629
#DIM DYNAMIC 성공률
#DIM DYNAMIC 컷
#DIM DYNAMIC 보수
DRAWLINE
PRINTFORML 광산에서 열심히 일했다
성공률 = 20
성공률 += RAND:11
성공률 += MIN(ABL:MASTER:전투능력,15)
성공률 += MIN(TALENT:MASTER:채집Lv,5)
성공률 += MIN(ABL:MASTER:기교,5)
성공률 += MIN(EXP:MASTER:도구사용경험/20,5)
성공률 += MIN(EXP:MASTER:전투경험/100,10)
성공률 += MIN(EXP:MASTER:채집경험/20,10)
컷 = 60
컷 += RAND:11
컷 += MIN(100-((BASE:MASTER:체력*100)/MAXBASE:MASTER:체력)/2,40)
컷 += MIN(100-((BASE:MASTER:기력*100)/MAXBASE:MASTER:기력)/2,20)
LOCAL = RAND:컷+5

보수 = 1000
DOWNBASE:MASTER:체력 = 150
DOWNBASE:MASTER:기력 = 300
SELECTCASE 성공률 - LOCAL 
	CASE IS < 0
		보수 += 1000
		보수 *= 2
		DOWNBASE:MASTER:체력 += 250
		DOWNBASE:MASTER:기력 += 500
		PRINTFORMW 그다지 잘 못한것 같다...
	CASE IS < 10
		보수 += 1500
		보수 *= 5
		DOWNBASE:MASTER:체력 += 300
		DOWNBASE:MASTER:기력 += 600
		PRINTFORMW 그럭저럭 잘 한듯 하다
	CASE IS < 15
		보수 += 2000
		보수 *= 10
		DOWNBASE:MASTER:체력 += 350
		DOWNBASE:MASTER:기력 += 700
		PRINTFORMW 꽤 잘한거 같다는 기분이 든다
	CASE IS > 20
		보수 += 2000
		보수 *= 15
		DOWNBASE:MASTER:체력 += 400
		DOWNBASE:MASTER:기력 += 800
		PRINTFORMW 척척 해낼수 있었다!
ENDSELECT
PRINTFORML 보수로 {보수}원 받았다
MONEY += 보수
TIME += 180
SELECTCASE RAND:3
	CASE 0
		CALL AddEXP("전투경험" , MASTER, 1)
	CASE 1
		CALL AddEXP("채집경험" , MASTER, 1)
	CASE 2
		;꽝
ENDSELECT
RETURN 1

@COM_ABLE629
SIF FLAG:USERCOM시작 == 0
	RETURN 0
SIF FLAG:70
	RETURN 0
SIF ((CFLAG:MASTER:현재위치 == 860&&MAIN_MAP != 8)||(CFLAG:MASTER:현재위치 == 855&&MAIN_MAP == 8))
	RETURN 1
SIF CFLAG:우후후
	RETURN 0
;RETURN 1
