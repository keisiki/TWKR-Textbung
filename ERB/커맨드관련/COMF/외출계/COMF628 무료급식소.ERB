@COM628
PRINTFORML 식판에 나누어 주는 밥을 받아 적당한 곳에서 먹어치웠다
CALL TB외식하기
TIME += 40

@COM_ABLE628
;SIF GLOBAL_COMABLE(627)
;	RETURN RESULT
;貸本屋ではない場所
SIF DISH_NAME != ""
	RETURN 0
SIF FLAG:USERCOM시작 == 0
	RETURN 0
IF TCVAR:MASTER:공복시각 != 0
	IF !TIME_PROGRESS(TCVAR:MASTER:공복시각+120)
		RETURN 0
	ENDIF
ENDIF
SIF FLAG:70
	RETURN 0
SIF CFLAG:우후후
	RETURN 0
SIF ((CFLAG:MASTER:현재위치 == 220&&MAIN_MAP != 2)||(CFLAG:MASTER:현재위치 == 219&&MAIN_MAP == 2))||(CFLAG:MASTER:현재위치 == 1207)||(CFLAG:MASTER:현재위치 == 840&&MAIN_MAP != 8)||(CFLAG:MASTER:현재위치 == 811&&MAIN_MAP == 8)
	RETURN 1

;RETURN 1

@TB외식하기(품질)
#DIMS DYNAMIC 품질
FLAG:오늘밥 += 1
FLAG:충치진행도 += RAND:3
CALL RESET_DISH
FLAG:요리 = 1
DISH_TYPE = 주식
DISH_NAME = 급식
DISH_BASEPOINT = 300
CALL 식사(MASTER)
CALL RESET_DISH
TCVAR:MASTER:공복시각 = 1440 * DAY + TIME + 180
SIF TALENT:MASTER:대식가
	TCVAR:MASTER:공복시각 -= 150

;탄 단 지 나 비
SELECTCASE 품질
	CASE "영양"
		CALL 영양소증가(95,23,16,13,36)
	CASE "저급"
		CALL 영양소증가(70,10,8,20,5)
	CASE "평범"
		CALL 영양소증가(80,16,12,19,9)
	CASE "좋음"
		CALL 영양소증가(86,24,15,25,15)
	CASE "호화로움"
		CALL 영양소증가(95,35,20,26,30)
ENDSELECT
