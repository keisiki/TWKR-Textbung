@COM628
PRINTFORML 식판에 나누어 주는 밥을 받아 적당한 곳에서 먹어치웠다
FLAG:오늘밥 += 1
CALL 영양소증가(95,23,16,13,36)
FLAG:충치진행도 += RAND:3
CALL SET_DISHNAME("MAINDISH")
DISHQUALITY = 3
CALL 식사(MASTER)
CALL RESET_DISH
TCVAR:MASTER:공복시각 = 1440 * DAY + TIME + 180
SIF TALENT:MASTER:대식가
	TCVAR:MASTER:공복시각 -= 150
TIME += 40

@COM_ABLE628
;SIF GLOBAL_COMABLE(627)
;	RETURN RESULT
;貸本屋ではない場所
SIF DISHNAME != ""
	RETURN 0
SIF FLAG:USERCOM시작 == 0
	RETURN 0
IF TCVAR:MASTER:공복시각 != 0
	IF !TIME_PROGRESS(TCVAR:MASTER:공복시각+120)
		RETURN 0
	ENDIF
ENDIF
SIF ((CFLAG:MASTER:현재위치 == 220&&MAIN_MAP != 2)||(CFLAG:MASTER:현재위치 == 219&&MAIN_MAP == 2))||(CFLAG:MASTER:현재위치 == 1207)
	RETURN 1
;RETURN 1
