;賭場を探す
@COM634
DRAWLINE
PRINTFORML 조금 걸어 들어가니 용 모습의 담뱃대가 늘어져 있는 폐옥이 발견되었다.
PRINTFORMW 희미하게 들려오는 환호성에 이끌려 %마스터는%는 건물 안으로 발을 들여놓았다.
CALL TOBA_SETUP
CALL 賭場_導入
SIF NEMUKE() >= 700
	PRINTFORM 졸음이 한계를 맞이하고 있다
PRINTFORMW %마스터는% 도박장을 떠났습니다
TIME += 10

@COM_ABLE634
;一括管理
SIF GLOBAL_COMABLE(634)
	RETURN RESULT
SIF !TOBA_PLACE(CFLAG:MASTER:현재위치)
	RETURN 0
SIF TFLAG:망상중
	RETURN 0
;時間停止中
SIF FLAG:70
	RETURN 0
;時間に余裕が無い
;活動限界を700としてその20分前
SIF NEMUKE() >= 700
	RETURN 0
RETURN 1

@TOBA_PLACE(ARG)
#FUNCTION
;秘天崖
SIF ARG == GET_MAP_REPLACEMENT(739)
	RETURNF 1
RETURNF 0
