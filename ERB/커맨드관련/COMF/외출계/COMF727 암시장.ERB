@COM727
PRINTFORML 바닥을 두드리자 안에서 목소리가 흘러나온다...
PRINTFORML "들어 오려면, 입장료를 내"
PRINTFORML 입장료는 10000원인듯 하다...
PRINTFORML 들어갈까?
CALL ASK_YN
IF RESULT
	RETURN -1
ELSE
	MONEY -= 10000
    PRINTFORML "좋아, 들어와"
	CALL 암시장
	TIME += 60
ENDIF


@COM_ABLE727
;SIF GLOBAL_COMABLE(727)
;	RETURN RESULT
;貸本屋ではない場所
SIF MONEY < 10000
	RETURN 0
SIF !암시장조건()
	RETURN 0
SIF FLAG:USERCOM시작 == 0
	RETURN 0
SIF FLAG:70
	RETURN 0
SIF CFLAG:우후후
	RETURN 0
SIF ((CFLAG:MASTER:현재위치 == 220&&MAIN_MAP != 2)||(CFLAG:MASTER:현재위치 == 219&&MAIN_MAP == 2))
	RETURN 1
;RETURN 1
