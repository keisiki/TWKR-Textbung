;-------------------------------------------------
;에이프런을 댄다
;특수 커맨드
;-------------------------------------------------
@COM190
;착탈의처리 장비시는 소스, 경험의 가산은 실시하지 않는다
TEQUIP:에이프런 = !TEQUIP:에이프런
RETURN 1
;-------------------------------------------------
;장착 계속의 처리
;-------------------------------------------------
@EQUIP_COM190(ARG)
;에이프런을 한다

;조수 조교시는, 에이프런을 해제해 함수 종료
IF ASSIPLAY
	TEQUIP:MASTER:에이프런 = 0
	RETURN 0
ENDIF

PRINTFORML ＜%마스터가% 에이프런중＞
;-------------------------------------------------
;소스 추가 처리
;-------------------------------------------------
;소비 체력, 기력
DOWNBASE:ARG:체력 += 10
DOWNBASE:ARG:기력 += 10

SOURCE:ARG:사랑 += 1000
SOURCE:ARG:공순 += 5000
SOURCE:ARG:일탈 += 500


;-------------------------------------------------
;경험의 처리
;-------------------------------------------------
;애정경험
EXP:ARG:애정경험 += RAND:2
RETURN 1

;-------------------------------------------------
;실행 판정
;-------------------------------------------------
@COM_ABLE190
;에이프런을 대는 실행 판정
SIF !TFLAG:100
	RETURN 0
;일괄관리
SIF GLOBAL_COMABLE(190)
	RETURN RESULT
;정지 중에는 불가
SIF FLAG:70 == 1
	RETURN 0
;아이템「에이프런」을 가지고 있는인가, 아이템 없음 설정이 아니면 안 돼
SIF ITEM:23 == 0 && NOITEM == 0
	RETURN 0
;노예가 연모나 친애가 아니면 안 돼
SIF !TALENT:연모
	RETURN 0
;해제는 언제라도 가능
SIF TEQUIP:34
	RETURN 1
;조수는 안 돼
SIF ASSIPLAY
	RETURN 0
;순종이 3이상 없으면 안 돼
SIF ABL:순종 < 3
	RETURN 0
;야외플레이중은 안 돼
SIF TEQUIP:29
	RETURN 0
;수치플레이중은 안 돼
SIF TEQUIP:30
	RETURN 0
;목욕탕플레이중은 안 돼
SIF TEQUIP:31
	RETURN 0
RETURN 1