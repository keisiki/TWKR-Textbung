;ARG - 현재 위치
@지역특수커맨드조건
LOCAL:1 = 0
FOR LOCAL,0,1000
    TRYCALLFORM TSC_ABLE{LOCAL}()
    IF RESULT == 2
        LOCAL:1++
    ENDIF
    RESULT = 0
NEXT
RETURN LOCAL:1

@지역특수커맨드본체
CALL 지역특수커맨드조건
PRINTFORML 현재 위치에서 실행 가능한 커맨드 갯수 : {RESULT}개
LOCAL:1 = 0
FOR LOCAL,0,1000
    RESULT = 0
    TRYCALLFORM TSC_ABLE{LOCAL}()
    IF RESULT == 2
        CALLFORM TSC_NAME{LOCAL}
        PRINTFORMLC %RESULTS,16,RIGHT%[{LOCAL,3,RIGHT}]
        LOCAL:1 ++
    ENDIF
    IF LOCAL:1 % 3 == 0&&LOCAL:1 != 0
        PRINTL
    ENDIF
NEXT
PRINTL
DRAWLINE
PRINTFORML %"돌아간다",16,RIGHT%[999]
$LOOP
INPUT
IF RESULT == 999
    RETURN -1
ELSE
    LOCAL = RESULT
    TRYCALLFORM TSC_ABLE{LOCAL}()
    IF RESULT == 2
        CALLFORM TSC_{LOCAL}
        RETURN RESULT
    ELSE
        GOTO LOOP
    ENDIF
ENDIF