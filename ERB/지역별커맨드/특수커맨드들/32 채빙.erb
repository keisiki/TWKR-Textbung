
@TSC_32
REPEAT 5
	REUSELASTLINE  채취중.%"." * COUNT%
	TWAIT 150, 1
REND
LOCAL = 5
LOCAL += MIN(5,ABL:MASTER:기교 / 2)
LOCAL += RAND:5
LOCAL *= 4
PRINTFORML 얼음 {LOCAL}개를 채취했다!
ITEM:얼음 += LOCAL
TIME += 60
DOWNBASE:MASTER:기력 == 150

@TSC_NAME32
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 얼음 채취
RESULTS = %커맨드이름%

@TSC_ABLE32

IF !GROUPMATCH(CFLAG:MASTER:현재위치, GET_MAP_REPLACEMENT(814),GET_MAP_REPLACEMENT(343),GET_MAP_REPLACEMENT(703),GET_MAP_REPLACEMENT(802))
    RETURN 0
ENDIF
SIF GET_MONTH() != "겨울의 달"
	RETURN 0
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2

