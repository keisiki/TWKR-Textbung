
@TSC_29
PRINTFORML 약방 안에 들어왔다...
$LOOP
DRAWLINE
CALL EX_SHOPPING(GETNUM(ITEM, "녹즙"), "녹즙", "유기농", 0, 1, 1)
CALL EX_SHOPPING(GETNUM(ITEM, "청주"), "청주", "술이 곧 보약이다! 술로 만든 보약을 사왓!", 0, 1, 2)
CALL EX_SHOPPING(GETNUM(ITEM, "우콘드링크"), "우콘드링크", "이걸 먹고 술로 만든 보약을 더 먹어!", 0, 1, 3)
DRAWLINE
PRINTFORML [999] - 돌아간다
$LOOP1
INPUT
IF RESULT == 999
	RETURN
ELSEIF RESULT
	SELECTCASE RESULT
		CASE 1
			CALL ITEM_CARTON(GETNUM(ITEM, "녹즙"),0,1,0)
		CASE 2
			CALL ITEM_CARTON(GETNUM(ITEM, "청주"),0,1,0)
		CASE 3
			CALL ITEM_CARTON(GETNUM(ITEM, "우콘드링크"),0,1,0)
	ENDSELECT
ELSE
	GOTO LOOP1
ENDIF
GOTO LOOP

@TSC_NAME29
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 약방
RESULTS = %커맨드이름%

@TSC_ABLE29

IF !GROUPMATCH(CFLAG:MASTER:현재위치,GET_MAP_REPLACEMENT(203))
    RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2
