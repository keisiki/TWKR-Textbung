
@TSC_37
PRINTFORML 돈을 넣으면 히나 인형 제작 키트가 나오는 모양이다...
PRINTFORML [0] 5000원을 넣어 1개 뽑는다
PRINTFORML [9] 그만둔다
INPUT
IF RESULT == 9
	RETURN
ELSE
	IF MONEY < 5000
		PRINTFORMW 돈이 없다
	ELSE
		ITEM:인스턴트히나인형 ++
		MONEY -= 5000
	ENDIF
ENDIF
RETURN

@TSC_NAME37
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 히나인형 가판대
RESULTS = %커맨드이름%

@TSC_ABLE37

IF MAIN_MAP == 2
	IF CFLAG:현재위치 != P202중앙광장
		RETURN 0
	ENDIF
ELSE
	IF CFLAG:현재위치 != 210
		RETURN 0
	ENDIF
ENDIF
IF ITEM:인스턴트히나인형 || 건치의여신
	RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2


