
@TSC_21
PRINTFORML 가까이 다가가니 산 송장 같이 서 있던 무언가가 일어나 접객을 시작했다
DRAWLINE
PRINTFORML 무엇을 살까? (소지금 : {MONEY})
PRINTFORML [1] - 냥냥제 안심귀가 스크롤 ({냥냥가격(500000)})
PRINTFORML [2] - 선단 ({냥냥가격(100000)})
PRINTFORML [999] - 돌아간다
$LOOP
INPUT
IF RESULT == 999
	RETURN
ELSEIF RESULT
	SELECTCASE RESULT
		CASE 1
			IF MONEY >= 냥냥가격(500000) && !FLAG:냥냥제안심귀가스크롤
				FLAG:냥냥제안심귀가스크롤++
				MONEY -= 냥냥가격(500000)
			ELSEIF FLAG:냥냥제안심귀가스크롤
				PRINTFORML 1인 1개가 원칙이라고 한다...
			ELSE
				PRINTFORML 돈이 부족하다
			ENDIF
		CASE 2
			IF MONEY >= 냥냥가격(100000)
				ITEM:선단++
				MONEY -= 냥냥가격(100000)
			ELSE
				PRINTFORML 돈이 부족하다
			ENDIF
		;CASE 3
			
	ENDSELECT
ELSE
	GOTO LOOP
ENDIF
RESTART

@TSC_NAME21
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 냥냥몰
RESULTS = %커맨드이름%

@TSC_ABLE21

IF MAIN_MAP == 1
	IF CFLAG:MASTER:현재위치 != 121
		RETURN 0
	ENDIF
ELSEIF CFLAG:MASTER:현재위치 != 150
	RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2


@냥냥가격(ARG)
#FUNCTION
LOCAL = 물가적용(ARG)
SELECTCASE MARK:[[청아]]:반발각인
	CASE 1
		TIMES LOCAL, 1.2
	CASE 2
		TIMES LOCAL, 1.4
	CASE 3
		TIMES LOCAL, 1.6
ENDSELECT
IF CFLAG:[[청아]]:호감도 >= 2000
	TIMES LOCAL, 0.8
ENDIF