
@TSC_35
PRINTFORML 책을 읽기 시작했다...
PRINTFORML 어려운 내용을 천천히 해석해 나가고 있다
LOCAL = 10
LOCAL += ABL:MASTER:교양 / 2
IF TFLAG:엑스트라인텔리
	LOCAL += 5
ENDIF
번역된책:4 += LOCAL
IF 번역된책:4 >= 번역된책:5
	번역된책:4 = 번역된책:5
ENDIF
PRINTFORML 현재의 내용이해도 : {번역된책:4  * 100/ 번역된책:5}％
TIME += 60
DOWNBASE:MASTER:기력 += 300
EXP:MASTER:학습경험 += 5
IF FLAG:훈련강화
	EXP:MASTER:학습경험 += 3
ENDIF
IF TFLAG:엑스트라인텔리
	EXP:MASTER:학습경험 += 1
ENDIF

IF 번역된책:4 >= 번역된책:5
	PRINTFORML 완벽히 이해 했다!
	CALL 번역본코드해석
	CALL 번역본리셋
ELSE
	RETURN
ENDIF




@TSC_NAME35
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 번역된 책을 읽는다
RESULTS = %커맨드이름%

@TSC_ABLE35

IF !번역된책:0
    RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2



