[SKIPSTART]
@TSC_35


@TSC_NAME35
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 번역된 책을 읽는다
RESULTS = %커맨드이름%

@TSC_ABLE53

IF 1
    RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2

[SKIPEND]

