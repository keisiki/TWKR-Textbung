
@TSC_30
CALL 냉장고()
RETURN 1

@TSC_NAME30
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 냉장고
RESULTS = %커맨드이름%

@TSC_ABLE30

IF !ITEM:냉장고
    RETURN 0
ENDIF
IF CFLAG:현재위치 != CFLAG:MASTER:현재위치
	RETURN 0
ENDIF
SIF CFLAG:우후후 == 2
	RETURN 0
RETURN 2

