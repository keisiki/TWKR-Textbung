
@TSC_28
PRINTFORML 개발용 디버그 함수
PRINTFORML 일반 유저는 사용하지 않는걸추천
;-----------------------------------
;이하는 테스트 내용
;-----------------------------------
PRINTFORML %타겟을% 취침시킵니까?
CALL ASK_YN()
IF !RESULT
	CFLAG:수면 = 1
	;CALL CHARA_SLEEP, TARGET, 1
	TCVAR:수면제강도 = 3
	CALL SET_SLEEP(1,TARGET,0)
ENDIF
;-----------------------------------
@TSC_NAME28
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 디버그
RESULTS = %커맨드이름%

@TSC_ABLE28

[IF_DEBUG]
RETURN 2
[ENDIF]
RETURN 0

