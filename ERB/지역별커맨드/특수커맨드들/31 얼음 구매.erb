
@TSC_31

PRINTFORML 「얼음 필요해?」
CALL EX_SHOPPING(GETNUM(ITEM, "얼음"), "얼음 5개", "꽝꽝 얼어있다", 0, 5, 1)
CALL EX_SHOPPING(GETNUM(ITEM, "얼음"), "얼음 10개", "10개가 통째로 붙어있다는건 무시하자", 0, 10, 2)
CALL EX_SHOPPING(GETNUM(ITEM, "얼음"), "얼음 20개", "하루치 냉장고 연료", 0, 20, 3)
CALL EX_SHOPPING(GETNUM(ITEM, "얼음"), "얼음 50개", "멀티팩", 0, 5, 4)
PRINTFORML [9] 돌아간다
INPUT
IF RESULT == 9
	RETURN
ELSE
	SELECTCASE RESULT
		CASE 1
			CALL ITEM_CARTON(GETNUM(ITEM, "얼음"),0,5,0)
		CASE 2
			CALL ITEM_CARTON(GETNUM(ITEM, "얼음"),0,10,0)
		CASE 3
			CALL ITEM_CARTON(GETNUM(ITEM, "얼음"),0,20,0)
		CASE 4
			CALL ITEM_CARTON(GETNUM(ITEM, "얼음"),0,50,0)
		CASEELSE
			RESTART
	ENDSELECT
ENDIF
RESTART

@TSC_NAME31
#DIMS DYNAMIC 커맨드이름
커맨드이름 = 얼음 구매
RESULTS = %커맨드이름%

@TSC_ABLE31

IF CFLAG:[[치르노]]:현재위치 != CFLAG:MASTER:현재위치
    RETURN 0
ENDIF
;기력 0
SIF BASE:MASTER:기력 <= 0
	RETURN 0
SIF CFLAG:우후후 == 2
	RETURN 0
;정지 중에는 불가
SIF FLAG:70
	RETURN 0
RETURN 2

