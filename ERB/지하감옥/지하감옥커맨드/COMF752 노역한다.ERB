@COM752
#DIM DYNAMIC 벌이
DRAWLINE
TFLAG:193 = RAND:5 - 2
벌이 = 3 * ABL:MASTER:기교
벌이 += ABL:MASTER:손가락 * 3
벌이 += RAND:5 + 1
SELECTCASE TFLAG:193
    CASE -2
        벌이 *= 10
    CASE -1
        벌이 *= 20
    CASE 0
        벌이 *= 50
    CASE 1
        벌이 *= 100
    CASE 2
        벌이 *= 200
ENDSELECT
IF 벌이 >= 10000
    벌이 = 10000
ENDIF
MONEY += 벌이
PRINTFORML 열심히 작업대에서 작업을 해, {벌이}원 받을수 있었다!
TIME += 60
DOWNBASE:MASTER:기력 += 100
DOWNBASE:MASTER:체력 += 50
RETURN 1

@COM_ABLE752

IF 지하감옥커맨드가부() == 0
    RETURN 0
ENDIF
IF CFLAG:MASTER:현재위치 != 1209
    RETURN 0
ENDIF
RETURN 1