
@MESSAGE_탈의윗도리
IF SHOW_TOP(TARGET) != ""
	IF TEQUIP:3 == 0; && EQUIP:TARGET:상반신속옷1
		PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? 의 # %조사만처리(CALLNAME:TARGET,"가")% \@
		IF EQUIP:TARGET:상반신윗도리1
			IF EQUIP:TARGET:원피스
				PRINTFORM %조사처리(CLOTHNAME(17, EQUIP:TARGET:상반신윗도리1),"와")%
				SIF EQUIP:TARGET:상반신윗도리2
					PRINTFORM %조사처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"와")%
				SIF EQUIP:TARGET:기모노
					PRINTFORM %조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"와")%
				PRINTFORM %조사처리(CLOTHNAME(13, EQUIP:TARGET:원피스),"를")% \@ FLAG:70 ? 벗기자 # 벗자 \@, 
			ELSE
				PRINTFORM %조사처리(CLOTHNAME(17, EQUIP:TARGET:상반신윗도리1),"를")%
				IF EQUIP:TARGET:상반신윗도리2
					PRINTFORM \@ FLAG:70 ? 벗기자 # 벗자 \@%CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2)%
					SIF EQUIP:TARGET:기모노
						PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"와")% %조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"를")%
					SIF !EQUIP:TARGET:기모노
						PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"를")
					PRINTFORM \@ FLAG:70 ? 풀어 헤치자 # 풀자 \@ 
				ELSEIF EQUIP:TARGET:기모노
					PRINTFORM \@ FLAG:70 ? 벗기자 # 벗자 \@%조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"를")% \@ FLAG:70 ? 풀어 헤치자 # 풀자 \@, 
				ELSE
					PRINTFORM \@ FLAG:70 ? 벗기자 # 벗자 \@、
				ENDIF
			ENDIF
		ELSEIF EQUIP:TARGET:상반신윗도리2
			PRINTFORM %CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2)%
			IF EQUIP:TARGET:원피스
				SIF EQUIP:TARGET:기모노
					PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"와")% %조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"를")%
				SIF !EQUIP:TARGET:기모노
					PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"를")%
				PRINTFORM 옷매무새가 흐트러진 %조사처리(CLOTHNAME(13, EQUIP:TARGET:원피스),"를")% \@ FLAG:70 ? 벗기자 # 벗자 \@、
			ELSE
				SIF EQUIP:TARGET:기모노
					PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"와")% %조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"를")%
				SIF !EQUIP:TARGET:기모노
					PRINTFORM %조사만처리(CLOTHNAME(16, EQUIP:TARGET:상반신윗도리2),"를")%
				PRINTFORM \@ FLAG:70 ? 풀어 헤치자 # 풀자 \@ 
			ENDIF
		ELSEIF EQUIP:TARGET:기모노
			PRINTFORM %조사처리(CLOTHNAME(14, EQUIP:TARGET:기모노),"를")%
			IF EQUIP:TARGET:원피스
				PRINTFORM 옷매무새가 흐트러진 %조사처리(CLOTHNAME(13, EQUIP:TARGET:원피스),"를")% \@ FLAG:70 ? 벗기자 # 벗자 \@、
			ELSE
				PRINTFORM  \@ FLAG:70 ? 풀어 헤치자 # 풀자 \@ 
			ENDIF
		ELSE ;ワンピのみ
			PRINTFORM %조사처리(CLOTHNAME(13, EQUIP:TARGET:원피스),"를")% \@ FLAG:70 ? 벗기자 # 벗자 \@、
		ENDIF

	; 	IF SHOW_TOP(TARGET, 1) != ""
	; 		PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? 의 # %조사만처리(CALLNAME:TARGET,"가")% \@ %SHOW_TOP(TARGET, 1)%를 \@ FLAG:70 ? 풀어해치면 # 풀어해치게 하면 \@ 
	; 	ELSE
	; 		PRINTFORM %CALLNAME:TARGET%\@ FLAG:70 ? 의 # %조사만처리(CALLNAME:TARGET,"가")% \@ %SHOW_TOP(TARGET, 2)%를 \@ FLAG:70 ? 벗기면 # 벗으면 \@ 
	; 	ENDIF
		
		IF SHOW_상반신속옷(TARGET) != ""
			PRINTFORM %OPPAI_DESCRIPTION(TARGET)%
			IF EQUIP:TARGET:레오타드 || EQUIP:TARGET:보디슈트 || EQUIP:TARGET:원피스 || EQUIP:TARGET:기모노
				PRINTFORM %조사만처리(OPPAI_DESCRIPTION(TARGET),"을")% 감싸고 있는 %SHOW_상반신속옷(TARGET)%
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM %조사만처리(SHOW_상반신속옷(TARGET),"과")% %조사처리(SLIT(TARGET),"가")% 
				ELSEIF !EQUIP:TARGET:치마
					PRINTFORM %조사만처리(SHOW_상반신속옷(TARGET),"과")% %조사처리(SHOW_BOTTOM(TARGET, 14),"가")% 
				ELSE
					PRINTFORMW %조사만처리(SHOW_상반신속옷(TARGET),"가")% 
				ENDIF
				PRINTFORMW 공공연히 드러났다
			ELSEIF EQUIP:TARGET:상반신속옷1
				PRINTFORMW %조사만처리(OPPAI_DESCRIPTION(TARGET),"을")% 감싼 %조사처리(SHOW_상반신속옷(TARGET),"가")% 공공연히 드러났다
			ELSEIF EQUIP:TARGET:하반신속옷2
				PRINTFORMW %조사만처리(OPPAI_DESCRIPTION(TARGET),"가")% 벌어진 %조사처리(SHOW_TOP(TARGET,1),"로")%부터 들여다 보이고 있다
			ENDIF
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%
			IF EQUIP:TARGET:레오타드 || EQUIP:TARGET:보디슈트 || EQUIP:TARGET:원피스 || EQUIP:TARGET:기모노
				IF SHOW_BOTTOM(TARGET) == ""
					PRINTFORM %조사만처리(OPPAI_DESCRIPTION(TARGET, 1),"와")% %조사처리(SLIT(TARGET),"가")% 
				ELSEIF !EQUIP:TARGET:치마
					PRINTFORM %조사만처리(OPPAI_DESCRIPTION(TARGET, 1),"와")% %조사처리(SHOW_BOTTOM(TARGET, 14),"가")% 
				ELSE
					PRINTFORM %조사만처리(OPPAI_DESCRIPTION(TARGET, 1),"가")% 
				ENDIF
			ENDIF
			PRINTFORMW 공공연히 드러났다
		ENDIF
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%의 %조사처리(OPPAI_DESCRIPTION(TARGET, 1),"가")% 공공연히 드러났다
ENDIF

IF PALAM:TARGET:욕정 >= PALAMLV:3
	IF PALAM:TARGET:욕정 >= PALAMLV:6
		PRINTFORM 그 %조사처리(NIPPLE_DESCRIPTION(TARGET),"는")% 
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:2 == 0
				PRINT 마치 유욕에 대한 기대에 떨듯이 
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에게 가슴으로 장난쳐달라고 조르는 것 같이 
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 쾌감에 의해 추잡하게 부풀어 올라 
		ELSEIF RAND:3 == 0
			PRINT 힘차게 핑 솟아올라
		ELSEIF RAND:2 == 0
			PRINT 유륜에서 봉긋하게 부풀어 올라 
		ELSE
			PRINT 유륜째로 볼록해져
		ENDIF
		PRINTFORMW \@ EQUIP:TARGET:상반신속옷1 ? %SHOW_상반신속옷(TARGET)%의 천을 밀어 올리고 # \@있다…
	ELSEIF PALAM:TARGET:욕정 >= PALAMLV:3
		PRINTFORM 그 %조사처리(NIPPLE_DESCRIPTION(TARGET),"는")% 
		IF RAND:3 == 0
			PRINT 봉긋 솟아올라 
		ELSEIF RAND:2 == 0
			PRINT 완전히 딱딱해져 
		ELSE
			PRINT 발기해 
		ENDIF
		PRINTFORMW \@ EQUIP:TARGET:상반신속옷1 ? %SHOW_상반신속옷(TARGET)%의 천을 밀어 올리고 # \@있다…
	ENDIF
ENDIF

SIF !((EQUIP:TARGET:레오타드 || EQUIP:TARGET:보디슈트 || EQUIP:TARGET:원피스 || EQUIP:TARGET:기모노) && !(EQUIP:TARGET:치마 || EQUIP:TARGET:바지))
	RETURN
IF (EQUIP:TARGET:하반신속옷1 || EQUIP:TARGET:하반신속옷2)
	IF PALAM:윤활 >= PALAMLV:3
		PRINTFORMW %조사처리(UNDER_SKIRT_DESCRIPTION(TARGET),"가")% 애액으로 얼룩져 있다…
	ELSEIF PALAM:윤활 >= PALAMLV:2
		PRINTFORMW %조사처리(UNDER_SKIRT_DESCRIPTION(TARGET),"가")% 애액으로 축축하다…
	ENDIF
ENDIF

	
@MESSAGE_탈의하속옷
IF EQUIP:TARGET:하반신속옷2
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? %조사만처리(CALLNAME:TARGET,"가")% # %조사만처리(CALLNAME:TARGET,"로")%부터\@ %조사처리(PANTSNAME(EQUIP:TARGET:하반신속옷2, TARGET),"를")% 
	IF PALAM:TARGET:윤활 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 벗자 # 벗기자 \@ 
		IF GET_INT(TARGET, "하반신속옷_비켜놓기가능", EQUIP:TARGET:하반신속옷2, "비쳐보이는") && !RAND:2
			PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 질척질척 온통 질퍽거리고 있어 %조사처리(PANTSNAME(EQUIP:TARGET:하반신속옷2, TARGET),"는")% 애액을 흠뻑 들이마셔 묵직해지고 있다…
		ELSE
			PRINTFORMW %조사처리(SLIT(TARGET),"로")%부터 흘러넘친 애액이 질질 허벅지를 타내려 오고 있다…
		ENDIF
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 벗으면 # 벗기면 \@ 
		IF GET_INT(TARGET, "하반신속옷_비켜놓기가능", EQUIP:TARGET:하반신속옷2, "비쳐보이는") && !RAND:2
			PRINTFORMW %조사처리(SLIT(TARGET),"와")% %PANTSNAME(EQUIP:TARGET:하반신속옷2, TARGET)%의 사이에 애액이 실을 늘어뜨린다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 분명히 젖고 있다…
		ENDIF
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 벗기자 # 벗자 \@ %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 촉촉하게 습기차 있다…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 벗었다… # 벗겼다…\@
	ENDIF
ELSEIF EQUIP:TARGET:하반신속옷1
	PRINTFORM %CALLNAME:TARGET%\@ !FLAG:70 ? %조사만처리(CALLNAME:TARGET,"가")% # %조사만처리(CALLNAME:TARGET,"로")%부터\@ %조사처리(SHOW_하반신속옷(TARGET, 3),"를")% 
	IF PALAM:TARGET:윤활 >= PALAMLV:4
		PRINTFORM \@ !FLAG:70 ? 벗자 # 벗기자 \@ 
		IF GET_INT(TARGET, "하반신속옷_비켜놓기불가", EQUIP:TARGET:하반신속옷1, "비쳐보이는") && !RAND:2
			PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 질척질척 온통 질퍽거리고 있어 %조사처리(SHOW_하반신속옷(TARGET, 3),"는")% 애액에 흠뻑 젖어 묵직하게 무거워지고 있다…	
		ELSE
			PRINTFORMW %조사처리(SLIT(TARGET),"로")%부터 흘러넘친 애액이 줄줄 허벅지를 타내려 오고 있다…
		ENDIF
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:3
		PRINTFORM \@ !FLAG:70 ? 벗자 # 벗기자 \@ 
		IF GET_INT(TARGET, "하반신속옷_비켜놓기불가", EQUIP:TARGET:하반신속옷1, "비쳐보이는") && !RAND:2
			PRINTFORMW %조사처리(SLIT(TARGET),"와")% %SHOW_하반신속옷(TARGET, 3)%의 사이에 애액이 실을 늘어뜨린다…
		ELSE
			PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 분명히 젖어 있다…
		ENDIF
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:2
		PRINTFORMW \@ !FLAG:70 ? 벗자 # 벗기자\@ %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 촉촉하게 습기가 차 있었다…
	ELSE
		PRINTFORMW \@ !FLAG:70 ? 벗었다… # 벗겼다…\@
	ENDIF
ELSE
	IF PALAM:TARGET:윤활 >= PALAMLV:4
		PRINTFORMW %CALLNAME:TARGET%의 비밀스런 장소는 질척질척 온통 질퍽대고 있어 %조사처리(SLIT(TARGET),"로")%부터 흘러넘친 애액이 줄줄 허벅지를 타내려 오고 있다…
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:3
		PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 분명히 젖어 있다…
	ELSEIF PALAM:TARGET:윤활 >= PALAMLV:2
		PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"는")% 촉촉하게 습기가 차 있었다…
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 %조사처리(SLIT(TARGET),"가")% 드러났다…
	ENDIF
ENDIF

SELECTCASE CFLAG:TARGET:임신경과일수
	CASE IS >= 60
		IF TALENT:TARGET:체형 == -2
			PRINTFORMW 해산일이 코앞인 만삭의 배는 어린 몸에는 언밸런스할 정도로 부풀어 올라 있다
		ELSE
			PRINTFORMW 해산일이 코앞인 만삭의 배는 크게 부풀어 올라 있다
		ENDIF
	CASE IS >= 40
		PRINTFORMW %CALLNAME:TARGET%의 몸매는 예전보다 둥그스름해지고, 배는 뚜렷하게 부풀어 올랐다
	CASE IS >= 30
		PRINTFORMW 부풀어 오르기 시작한 %CALLNAME:TARGET%의 배를 보고, 재차 %타겟을% 임신시켰다는 것을 실감했다
	CASE IS >= 20
		PRINTFORMW %CALLNAME:TARGET%의 배는 아직 부풀어 오르진 않았지만, 그 몸 안에 자신의 아이를 품고 있다고 생각하니 감회가 새롭다
ENDSELECT

@MESSAGE_탈의상
IF SHOW_TOP(TARGET) != ""
	IF SHOW_TOP(TARGET, 1) != ""
		PRINTFORM \@ FLAG:70 ? # %@"%타겟이% "%\@%조사처리(SHOW_TOP(TARGET,1),"를")% 풀어 헤치\@ FLAG:70 ? 고, # 고선 \@ 
		IF (SHOW_상반신속옷(TARGET) != SHOW_TOP(TARGET,1)) && (SHOW_상반신속옷(TARGET) != "")
			SIF !EQUIP:TARGET:상반신속옷1 || !EQUIP:TARGET:상반신속옷2
				PRINTFORM %조사처리(SHOW_상반신속옷(TARGET),"를")% \@ FLAG:70 ? 벗겨내어 # 벗어던져 \@, 
;		ELSE
;			SIF ! EQUIP:TARGET:상반신속옷1 || ! EQUIP:TARGET:상반신속옷2
;				PRINTFORM %SHOW_상반신속옷(TARGET)%를 탈\@ FLAG:70? 하지만 해 #\@,
		ENDIF
		PRINTFORML %조사처리(OPPAI_DESCRIPTION(TARGET, 1),"를")% 드러냈다
	ELSE
		PRINTFORM \@ FLAG:70 ? %조사처리(CALLNAME:TARGET,"로")%부터 # %타겟이% \@ %SHOW_TOP(TARGET,2)%
		IF SHOW_상반신속옷(TARGET) != SHOW_TOP(TARGET,1) && SHOW_상반신속옷(TARGET) != SHOW_TOP(TARGET,2)
			SIF !EQUIP:TARGET:상반신속옷1 || !EQUIP:TARGET:상반신속옷2
				PRINTFORM %조사만처리(SHOW_TOP(TARGET,2),"와")% %조사처리(SHOW_상반신속옷(TARGET),"를")% 
		ELSE
			PRINTFORM %조사만처리(SHOW_TOP(TARGET,2),"를")% 
		ENDIF
		PRINTFORML \@ FLAG:70 ? 벗어던지자 # 벗자 \@, %조사처리(OPPAI_DESCRIPTION(TARGET, 1),"가")% 드러났다
	ENDIF
ELSE
	PRINTFORMW %CALLNAME:TARGET%의 %조사처리(OPPAI_DESCRIPTION(TARGET, 1),"가")% 드러났다
ENDIF

IF PALAM:TARGET:욕정 >= PALAMLV:3
	IF PALAM:TARGET:욕정 >= PALAMLV:6
		PRINTFORM 그 %조사처리(NIPPLE_DESCRIPTION(TARGET),"는")%, 
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:2 == 0
				PRINT 마치 유욕에 대한 기대에 떨듯이 
			ELSE
				PRINTFORM %CALLNAME:PLAYER%에게 가슴으로 장난쳐달라고 조르는 것 같이 
			ENDIF
		ENDIF
		IF RAND:4 == 0
			PRINT 쾌감에 의해 싫어질 정도로 부풀어 올라 
		ELSEIF RAND:3 == 0
			PRINT 힘차게 핑 솟아올라 
		ELSEIF RAND:2 == 0
			PRINT 유륜에서 봉긋하게 부풀어 올라 
		ELSE
			PRINT 유륜째로 볼록해져 
		ENDIF
		PRINTFORMW 분위기를 살리고 있다…
	ELSEIF PALAM:TARGET:욕정 >= PALAMLV:3
		PRINTFORM 그 %조사처리(NIPPLE_DESCRIPTION(TARGET),"는")%, 
		IF RAND:3 == 0
			PRINT 봉긋 솟아올라 
		ELSEIF RAND:2 == 0
			PRINT 완전히 딱딱해져 
		ELSE
			PRINT 발기해 
		ENDIF
		PRINTFORMW , 자기 주장을 하고 있다…
	ENDIF
ENDIF

@MESSAGE_탈의하(ARG)
SIF !(EQUIP:TARGET:치마 || EQUIP:TARGET:바지)
	RETURN
SIF (!ARG && EQUIP:TARGET:치마) || EQUIP:TARGET:바지
	PRINTFORM \@ EQUIP:TARGET:바지 ? %조사처리(SHOW_BOTTOM(TARGET),"를")% # %조사처리(SHOW_BOTTOM(TARGET, 1),"를")%\@ \@ FLAG:70 ? 벗자 # 벗어던지자 \@ 

PRINTFORM %UNDER_SKIRT_DESCRIPTION(TARGET)%
IF (EQUIP:TARGET:하반신속옷1 || EQUIP:TARGET:하반신속옷2)
	IF PALAM:윤활 >= PALAMLV:3
		PRINTFORMW %조사만처리(UNDER_SKIRT_DESCRIPTION(TARGET),"는")% 애액으로 얼룩져 있었다…
	ELSEIF PALAM:윤활 >= PALAMLV:2
		PRINTFORMW %조사만처리(UNDER_SKIRT_DESCRIPTION(TARGET),"가")% 애액으로 습기차 있다…
	ELSE
		PRINTFORMW %조사만처리(UNDER_SKIRT_DESCRIPTION(TARGET),"를")% 볼 수 있었다…
	ENDIF
ELSE
	PRINTFORMW %조사만처리(UNDER_SKIRT_DESCRIPTION(TARGET),"를")% 볼 수 있었다…
ENDIF
