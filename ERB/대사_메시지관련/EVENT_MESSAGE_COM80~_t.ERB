;-------------------------------------------------
;80, 수음
;-------------------------------------------------
@MESSAGE_COM80
VARSET LOCAL
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	IF NOWEX:MASTER:사정
		IF LOCAL == TARGET
			PRINTFORML 시간을 멈추어 범하고 있는 %CALLNAME:TARGET%의 \@ MASTER_POSE(SET_V,SET_P,1) ? 바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 손에 잡게 했다
		ELSEIF LOCAL
			PRINTFORML 시간을 멈추어 범하고 있는 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아 %CALLNAME:TARGET%의 손에 잡게 했다
		ELSE
			PRINTFORML 시간이 멈추어 있는 %CALLNAME:TARGET%의 손에 %TINKO(PLAYER)%를 잡게 했다
		ENDIF
			PRINTFORML %CALLNAME:TARGET%의 손을 사용해, 사정 직전의 %TINKO(PLAYER)%를 전후로 해 있었다…
	ELSE
		PRINTFORML %조교자는% 시간이 멈추어 있는 %CALLNAME:TARGET%의 손에 %TINKO(PLAYER)%를 잡게 해 켰다 …
	ENDIF
ELSEIF CFLAG:MASTER:장난 == 2
	IF NOWEX:MASTER:사정
		IF LOCAL == TARGET
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:TARGET%의 \@ MASTER_POSE(SET_V,SET_P,1) ? 바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 손에 잡게 했다
		ELSEIF LOCAL
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 손에 잡게 했다
		ELSE
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:TARGET%의 손에 %TINKO(PLAYER)%를 잡게 했다
		ENDIF
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 전후로 해 있었다…
	ELSE
		PRINTFORML %조교자는% 자고 있는 %CALLNAME:TARGET%의 손에 %TINKO(PLAYER)%를 잡게 해 켰다 …
	ENDIF
ELSE
	IF NOWEX:MASTER:사정
		IF LOCAL == TARGET
			PRINTFORML 사정 직전에 \@ MASTER_POSE(SET_V,SET_P,1) ? 바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아 %CALLNAME:TARGET%에 잡게 했다…
		ELSEIF LOCAL
			PRINTFORML 사정 직전에 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아 %CALLNAME:TARGET%에 잡게 했다…
		ELSE
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 %CALLNAME:TARGET%에 잡게 했다…
		ENDIF
		IF ABL:기교 > 3
			IF ABL:정액중독 >= 5
				PRINTFORML %타겟은% 격렬하고 %TINKO(PLAYER)%를 훑어내면서 요도구의 앞에서 입을 열고 정액을 기다려 짓고 있다…
			ELSE
				PRINTFORML %타겟은% 손에 익은 손놀림으로 %TINKO(PLAYER)%의 민감한 부분을 찾아내고 %조교자를% 사정으로 이끌었다…
			ENDIF
		ELSE
			PRINTFORML %타겟은% 당황하면서도 요도구의 부분을 가만히 응시하며, %TINKO(PLAYER)%를 전후로 해 어휘라고 있다…
		ENDIF
	ELSE
		PRINTFORM %타겟은% 
		;연모나 친애
		SIF TALENT:연모
			PRINT 사랑하는 것 같은 표정으로, 
		;음란한가 창부
		SIF TALENT:음란
			PRINT 스스로 
		SIF CFLAG:패배자 && !TALENT:연모 && !TALENT:음란
			PRINTFORM 외면하면서 
		PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%에 손가락을 걸어 켜고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;81, 펠라치오
;-------------------------------------------------
@MESSAGE_COM81
VARSET LOCAL
;청소용
IF TCVAR:MASTER:112 == 1
	LOCAL = MASTER_POSE(SET_V,SET_P,1)
ELSEIF TCVAR:MASTER:112 == 2
	LOCAL = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF MASTER_POSE(SET_V,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_V,SET_P,1)
ELSEIF MASTER_POSE(SET_A,SET_P,1)
	LOCAL:1 = MASTER_POSE(SET_A,SET_P,1)
ENDIF
IF FLAG:70 == 1
	;청소
	IF LOCAL
		PRINTFORML 시간이 멈춘%CALLNAME:LOCAL%의 안에 한껏 사정하고 나서 %TINKO(PLAYER)%를 뽑아,
		PRINTFORML 정액의 부착한 %TINKO(PLAYER)%를 %CALLNAME:TARGET%의 입에 찔러 넣어 %TINKO(PLAYER)%를 청소시켰다…
	ELSEIF NOWEX:MASTER:사정
		IF LOCAL:1 == TARGET
			PRINTFORML 시간을 멈추어 범하고 있는 %CALLNAME:TARGET%의\@ MASTER_POSE(SET_V,SET_P,1) ?  바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 입에 찔러 넣고
		ELSEIF LOCAL
			PRINTFORML 시간을 멈추어 범하고 있는 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 입에 찔러 넣고
		ELSE
			PRINTFORML 시간이 멈추어 있는 %CALLNAME:TARGET%의 입에 %TINKO(PLAYER)%를 찔러 넣고
		ENDIF
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 목구멍에 꽉 눌렀다…
	ELSE
		PRINTFORML %조교자는% 시간이 멈추어 있는 %CALLNAME:TARGET%의 입에 %TINKO(PLAYER)%를 찔러 넣어 혀에 문지르도록(듯이) 허리를 움직였다…
	ENDIF
	SIF TALENT:키스미경험
		PRINTFORML 키스의 경험도 없는 %CALLNAME:TARGET%의 가련한 입술을 예 하고 있다고 하는사실은, %마스터를% 아프게 날카로워지게 한다……
ELSEIF CFLAG:MASTER:장난 == 2
	;청소
	IF LOCAL
		PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:LOCAL%의 안에 한껏 사정하고 나서 %TINKO(PLAYER)%를 뽑아,
		PRINTFORML 정액의 부착한 %TINKO(PLAYER)%를 %CALLNAME:TARGET%의 입에 찔러 넣어 %TINKO(PLAYER)%를 청소시켰다…
	ELSEIF NOWEX:MASTER:사정
		IF LOCAL:1 == TARGET
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:TARGET%의\@ MASTER_POSE(SET_V,SET_P,1) ?  바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 입에 찔러 넣고
		ELSEIF LOCAL
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아, %CALLNAME:TARGET%의 입에 찔러 넣고
		ELSE
			PRINTFORML 잔 채로 범해지고 있는 %CALLNAME:TARGET%의 입에 %TINKO(PLAYER)%를 찔러 넣고
		ENDIF
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 목구멍에 꽉 눌렀다…
	ELSE
		PRINTFORML %조교자는% 자고 있는 %CALLNAME:TARGET%의 입에 %TINKO(PLAYER)%를 찔러 넣어 혀에 문지르도록(듯이) 허리를 움직였다…
	ENDIF
ELSE
	;청소
	IF LOCAL
		PRINTFORML %CALLNAME:LOCAL%의 안에 한껏 사정하고 나서 %TINKO(PLAYER)%를 뽑아,
		PRINTFORML %TINKO(PLAYER)%에 부착한 정액을%CALLNAME:TARGET%에 빨고 받게 했다…
		SIF TFLAG:8 && !TCVAR:TARGET:만취
			PRINTFORML %타겟은% 요도에 남은 정액까지 남김없이 깨끗하게 하고 나서\@ ABL:정액중독 > 4 ?  아쉬움을 아끼는것 같이 # \@%TINKO(PLAYER)%로부터 입술을 떼어놓고 …
	ELSEIF NOWEX:MASTER:사정
		IF LOCAL:1 == TARGET
			PRINTFORML 사정의 직전에\@ MASTER_POSE(SET_V,SET_P,1) ?  바기나(질) # 애널 \@로부터 %TINKO(PLAYER)%를 뽑아 %CALLNAME:TARGET%에 삼켜져 있는…
		ELSEIF LOCAL
			PRINTFORML 사정의 직전에 %CALLNAME:LOCAL%로부터 %TINKO(PLAYER)%를 뽑아 %CALLNAME:TARGET%에 삼켜져 있는…
		ELSE
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 %CALLNAME:TARGET%에 삼켜져 있는…
		ENDIF
		IF ABL:혀 > 2 && !TCVAR:TARGET:만취
			IF ABL:정액중독 > 4
				PRINTFORML %타겟은% 입술로 귀두를 싸고 나서 끈적끈적 혀를 움직여%CALLNAME:PLAYER%에 사정을 재촉하고 있다…
			ELSE
				PRINTFORML %타겟은% %TINKO(PLAYER)%의 민감한 부분에 혀를은 조생%CALLNAME:PLAYER%에 사정을 재촉하고 있다…
			ENDIF
		ELSE
			PRINTFORML %타겟은% %CALLNAME:PLAYER%의 반응을 물으면서 열심히 혀를 움직이고 있다…
		ENDIF
	ELSEIF TCVAR:TARGET:만취
		PRINTFORML 몽롱해지고 있는 %CALLNAME:TARGET%의 입에 %TINKO(PLAYER)%를 삼켜져 있는
		PRINTFORML 하고 나서 깨닫았지만 물리지 않도록……
	ELSE
		PRINTFORM %타겟은%
		;연모
		SIF TALENT:연모
			PRINT 사랑하는 것 같은 표정으로, 
		;음란
		SIF TALENT:음란
			PRINT 스스로 
		SIF CFLAG:패배자 && !TALENT:연모 && !TALENT:음란
			PRINTFORM 울상으로 
		PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%에 혀를 기게 하고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;82, 파이즈리
;-------------------------------------------------
@MESSAGE_COM82
IF SELECTCOM == PREVCOM
	IF FLAG:70 == 1 || CFLAG:MASTER:장난 == 2
		PRINTFORM %조교자는% \@FLAG:70 ? 시간을 멈춘 # 자고 있는 \@ %CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET,1)%
		IF RAND:2
			IF RAND:3 == 0
				PRINTFORM 의 유륜을 엄지로 간질이면서 
			ELSEIF RAND:2
				PRINTFORM 의 %NIPPLE_DESCRIPTION(TARGET,1)%를 손가락으로 잡아당기면서 
			ELSE
				PRINTFORM \@ TALENT:가슴사이즈 == -2 ? 를 손바닥 전체로 어루만지면서 # 를 마구 주무르면서 \@ 
			ENDIF
		ELSE
			PRINTFORM %TEXTR("를 안쪽에 대면서/를 밀착시키면서/를 단단히 조이면서/를 압박하면서")% 
		ENDIF
		SIF EXP:PLAYER:V성교경험 + EXP:PLAYER:A성교경험 && RAND:2
			PRINTFORM %TEXTR("성교라도 하는 것 같이/젖을 잉태와/마치 교미를 하고 있을까 (와) 같이")% 
		SIF RAND:2
			PRINTFORM 체력이 계속되는 대로 
		IF ABL:PLAYER:새드끼 && RAND:3 == 0
			PRINTFORM 몹시 거칠게 허리를 움직여 
		ELSE
			PRINTFORM %TEXTR("허리를 격렬하게 움직여/허리를 전후에 움직여")% 
		ENDIF
		IF RAND:2 == 0
			PRINTFORML 계속 범하고 있다…
		ELSE
			PRINTFORML 계속 범했다 …
		ENDIF
	ELSE
		;초유
		IF TALENT:가슴사이즈 == 3 && RAND:3
			PRINTFORML 규격외의%OPPAI_DESCRIPTION(TARGET,1)%는 %CALLNAME:PLAYER%의 손가락을 삼켜, 들어 올리지 않았다고 자중에 의해 데로리와 처진다…
			PRINTFORML 이므로 %타겟은% 몇 번이나 %OPPAI_DESCRIPTION(TARGET)%를 들어 올려 그 동작에 의해 %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 계속 잡아당겼다 …
		;작은 가슴
		ELSEIF TALENT:가슴사이즈 == -1
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%로 열심히 %TINKO(PLAYER)%를 계속 켜고 있다…
		;절벽
		ELSEIF TALENT:가슴사이즈 == -2
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%로 %NIPPLE_DESCRIPTION(TARGET,1)%를 필사적으로 %TINKO(PLAYER)%를 비비적거리고 있다…
		;거유
		ELSEIF TALENT:가슴사이즈 == 1
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%로 %TINKO(PLAYER)%를 감싸 켜 계속 고 있다…
		;폭유, 초유
		ELSEIF TALENT:가슴사이즈 >= 2
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%로 %TINKO(PLAYER)%를 숨기도록(듯이) 해 켜 계속 고 있다…
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%로 %TINKO(PLAYER)%를 끼워 켜 계속 고 있다…
		ENDIF
	ENDIF
	RETURN 0
ENDIF

IF FLAG:70 == 1
	PRINTFORM %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET,1)%
	;작은 가슴
	IF TALENT:가슴사이즈 == -1
		PRINTFORM 에 %TINKO(PLAYER)%를 문지르고 
	;절벽
	ELSEIF TALENT:가슴사이즈 == -2
		PRINTFORM 에 우뚝 솟은 %NIPPLE_DESCRIPTION(TARGET,1)%로 %TINKO(PLAYER)%를 문지르고 
	;평유 이상
	ELSE
		PRINTFORM 에 %TINKO(PLAYER)%를 끼워 문지르고 
	ENDIF
	PRINTL 있다…
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORM %조교자는% 자고 있는 %CALLNAME:TARGET%의 
	;작은 가슴
	IF TALENT:가슴사이즈 == -1 ;KR판 임의수정, 일판 OPPAI_DESCRIPTION  누락
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 문지르고
	;절벽
	ELSEIF TALENT:가슴사이즈 == -2
		PRINTFORM そそり立つ%NIPPLE_DESCRIPTION(TARGET,1)%に、%TINKO(PLAYER)%를 문지르고
	;거유
	ELSEIF TALENT:가슴사이즈 == 1
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼워 문지르고 
	;폭유
	ELSEIF TALENT:가슴사이즈 == 2
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼워 문지르고 
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼워 문지르고 
	ENDIF
	PRINTL 있다…
ELSE
	PRINTFORM %타겟은%
	;연모나 친애(애욕의 유무로 조금 분기)
	SIF TALENT:애욕
		PRINTFORM \@ TALENT:연모 ?  황홀한 # 녹은 \@표정을 하면서,
		SIF CFLAG:패배자 && !TALENT:연모 && !TALENT:음란
			PRINTFORM 정말로 싫은 듯한 표정으로 
	IF ABL:TARGET:가슴 > 2
	;가슴 3이상
		;가슴으로 분기 시켜 사이즈는 SELECTCASE로 나누어 본다. 크기는 달라도 그 앞에 돌기는 있어
		SELECTCASE TALENT:가슴사이즈
		;-이상
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML 포동포동 찬 고기와 고기, %OPPAI_DESCRIPTION(TARGET, 1)%의 사이에 %TINKO(PLAYER)%를 글 사이에 삽입된 어구, 잡아당긴다.
				PRINTL 촉촉하게 감싸, 부드럽고 어디까지나 딱딱한 페니스를 받아들여
				PRINTL 가라앉아 가는 것 같은 기분 좋음은, 마치 고기의 바다에 녹는 것 같은 감각이다…
			ELSE
				PRINTFORML 그 %OPPAI_DESCRIPTION(TARGET, 1)%를 꽉 눌러 한껏 %OPPAI_DESCRIPTION(TARGET)%의 중압을 더한 안에,
				PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 이끌었다.
				PRINTFORML %CALLNAME:TARGET%의 고기가 부드럽고 압도적인 볼륨으로 접해 가는 마음에, %조교자는% 삼켜져 간다…
			ENDIF
			PRINTL 육과 고기는 밀고 들어가면 밀고 들어갈수록 딱딱한 페니스를 어디까지나 받아들여 침울해질 만큼 부드럽다…
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 그 안에서 분홍색 붙은 %OPPAI_DESCRIPTION(TARGET, 1)%의 끝, %NIPPLE_DESCRIPTION(TARGET, 1)%가 때때로, 긁적긁적 장대나 칼리를 문질러 온다…
		;-이하
		CASE IS < 0
			PRINTFORML 물리적인 핸디캡을 경험과 기술로 보충하려고 %OPPAI_DESCRIPTION(TARGET, 1)%를 사용해 %TINKO(PLAYER)%에 봉사하고 있다.
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 상기 한 앞가슴의 먼저 있는 %NIPPLE_DESCRIPTION(TARGET, 1)%로 장대나 칼리를 켜도록(듯이) 정중하게 유도해 온다.
		;
		CASEELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 사이에 두어, 잡아당긴다.
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 때때로 땀흘린 %OPPAI_DESCRIPTION(TARGET, 1)%의 받아, %NIPPLE_DESCRIPTION(TARGET, 1)%에 장대나 칼리를 스치게 해 단 소리를 지른다.
		ENDSELECT
	ELSEIF ABL:TARGET:가슴 <= 2
	;가슴2 이하
		;작은 가슴
		IF TALENT:가슴사이즈 == -1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 열심히 %TINKO(PLAYER)%를
		;절벽
		ELSEIF TALENT:가슴사이즈 == -2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET,1)%과 %NIPPLE_DESCRIPTION(TARGET,1)%으로 필사적으로%TINKO(PLAYER)%를 
		;거유
		ELSEIF TALENT:가슴사이즈 == 1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 감싸
		;폭유, 초유
		ELSEIF TALENT:가슴사이즈 >= 2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 숨기도록(듯이) 해
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 끼워
		ENDIF
		PRINTL 켜고 있다…
	ELSE

	ENDIF
	
	;연모 또는 애욕
	IF IS_LOVER(TARGET) && EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 30000 && RAND:3 == 0
		PRINTFORML 기분이 흥분이라도 해 왔는지, %타겟은% 격렬하고 몸을 흔들어,
		PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 %OPPAI_DESCRIPTION(TARGET)%로 철저하게 잡아당기면서 , 열중하고 있어와 같이
		PRINTFORML 자신의 파이즈리는 어때?　기분이 좋은가?
		PRINTFORML 또 %SAMEN_DESCRIPTION(PLAYER)%를 내는지?　걸치는지?　부어 넣는지?
		PRINTFORML 와 쾌락에 마음을 맡긴 요염한 양상으로 물어 봐 온다…
	ENDIF
	SELECTCASE ABL:TARGET:가슴
	CASE IS < 2
		PRINTFORM %CALLNAME:TARGET%에는 나쁘지만, 더듬거린 봉사로
		SIF TALENT:가슴사이즈 >= 1
			PRINT 시각적으로는 어쨌든
		PRINTL 너무 기분 좋지 않다…
	CASE IS  > 2
		PRINTFORML %타겟은% 소화된 봉사로 %조교자를% 기쁨…
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%의 봉사는 %조교자를% 기분 잘 시키고 있다…
	ENDSELECT
ENDIF
;-------------------------------------------------
;83, 가랑이
;-------------------------------------------------
@MESSAGE_COM83
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 가랑이에 %TINKO(PLAYER)%를 사이에 두게 해
	;욕정의 PALAMLV4 이상
	SIF PALAM:윤활 >= PALAMLV:3
		PRINT 질퍽질퍽하게 젖었다
	;작은 체형
	IF TALENT:체형 < 0
		PRINT 어린 균열
	ELSE
		PRINT 음순
	ENDIF
	PRINTFORML 에 %TINKO(PLAYER)%를 문질렀다…
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 가랑이에는 삼미선, 
	;음란한가 창부＋욕정의 PALAMLV5 이상
	IF TALENT:음란 && PALAM:욕정 >= PALAMLV:5
		PRINTFORM 쾌락에 빠지고 자른 표정으로 %조교자를% 응시하면서 
	;연모나 친애＋욕정의 PALAMLV5 이상
	ELSEIF TALENT:연모 && PALAM:욕정 >= PALAMLV:5
		PRINT 당장이라도 녹을 것 같은 표정을 띠면서 
	;욕정의 PALAMLV4 이상
	ELSEIF PALAM:욕정 >= PALAMLV:4
		PRINT 입길로부터 군침을 흘리면서 
	;연모나 친애
	ELSEIF TALENT:연모
		PRINTFORM 도취해 버린 눈동자로 %조교자를% 응시하면서 
	;음란한가 창부
	ELSEIF TALENT:음란
		PRINTFORM 음란한 눈초리로 도발하면서 
	ELSEIF CFLAG:패배자
		PRINTFORM 정말로 싫은 듯한 표정으로 
	;수치심 없음
	ELSEIF TALENT:수치심 < 0
		PRINTFORM 대담하게 허리를 움직이면서 
	;감정결여
	ELSEIF TALENT:감정결여
		PRINTFORM 희미하게 상기 한 얼굴로 
	ELSE
		PRINT 부끄러운 듯이 
	ENDIF
	;작은 체형
	IF TALENT:체형 < 0
		PRINT 어린 균열을 
	ELSE
		PRINT 음순을 
	ENDIF
	PRINTFORML %TINKO(PLAYER)%에 문지르고 있다…
ENDIF
;-------------------------------------------------
;84, 거품춤
;-------------------------------------------------
@MESSAGE_COM84
PRINTFORM %타겟은% 소프를 거품이 일게 해 자신에게 바르면서,
;연모나 친애
SIF TALENT:연모
	PRINT 황홀해 하는 표정을 하면서
;작은 체형
IF TALENT:체형 < 0
	PRINTFORM %CALLNAME:PLAYER%에 작은 몸을 한껏 문질러
;거유인가 폭유＋작은 체형이 아니다
ELSEIF TALENT:가슴사이즈 > 0
	PRINTFORM %CALLNAME:PLAYER%에 %OPPAI_DESCRIPTION(TARGET)%과 포근한 피부를 문질러
ELSE
	PRINTFORM %CALLNAME:PLAYER%에 전신을 문질러
ENDIF
PRINTL 애무했다…
;-------------------------------------------------
;85, 다리 호된 훈련
;-------------------------------------------------
@MESSAGE_COM85
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 발바닥에 %TINKO(PLAYER)%를 칠하고 있다…
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORML %조교자는% 자고 있는 %CALLNAME:TARGET%의 발바닥에 %TINKO(PLAYER)%를 칠하고 있다…
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 발바닥으로
	;새드
	SIF TALENT:새드
		PRINT 강요하면서
	;연모나 친애
	SIF TALENT:연모
		PRINT 열심히
	PRINTFORML 켜고 있다…
ENDIF
;-------------------------------------------------
;86, 다리 빨고 강제
;-------------------------------------------------
@MESSAGE_COM86
;야외플레이중
IF TEQUIP:야외플레이
	;주
	IF TIME == 0
		PRINT 맑은 하늘아래에서,
	;야
	ELSE
		PRINT 달빛안,
	ENDIF
	;겁장이, 자존심 높음, 수줍음의 언젠가
	IF TALENT:담력 < 0 || TALENT:자존심 > 0 || TALENT:수치심 > 0
		PRINTL 이 치태를 누군가에게 보여지지 않는 것인지 날아 목꼬치면서
	ELSE
		PRINTL 풍으로 어루만질 수 있어 맨살을 살짝 진동시켜
	ENDIF
;미약유효
ELSEIF TCVAR:춘약
	PRINT 시선을 공중에 감돌게 하면서
ENDIF
PRINTFORML %타겟은% 내민%CALLNAME:PLAYER%의 다리에 혀를 기게 하고 있다…
;-------------------------------------------------
;88, 진공펠라
;-------------------------------------------------
@MESSAGE_COM88
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 뿌리까지 질네,
	PRINTL 그대로 격렬할 기세로 음경에 들러 붙고 있다…
;-------------------------------------------------
;89, 고무정음
;-------------------------------------------------
@MESSAGE_COM89
IF FLAG:70
	PRINTFORML %CALLNAME:TARGET%의 입 중(안)에서 사용 종료 콘돔을 뒤집어,\@ TFLAG:고무내 > 150 ?  대량의 # \@%SAMEN_DESCRIPTION(PLAYER)%를 맛있는 음식 했다…
	PRINTFORML %CALLNAME:PLAYER%의 맛을 확실히 기억해 줄 수 있도록, %조교자는% 콘돔을 뒤집어 뺨의 뒤에 문질러 한 방울도 남김없이 %SAMEN_DESCRIPTION(PLAYER)%를 맛 대면시켰다…
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORML %CALLNAME:TARGET%의 입 중(안)에서 사용 종료 콘돔을 뒤집어,\@ TFLAG:고무내 > 150 ?  대량의 # \@%SAMEN_DESCRIPTION(PLAYER)%를 맛있는 음식 했다…
	PRINTFORML %CALLNAME:PLAYER%의 맛을 확실히 기억해 줄 수 있도록, %조교자는% 콘돔을 뒤집어 뺨의 뒤에 문질러 한 방울도 남김없이 %SAMEN_DESCRIPTION(PLAYER)%를 맛 대면시켰다…
ELSE
	PRINTFORM %CALLNAME:TARGET%의 뺨에,
	SELECTCASE TFLAG:고무내
		CASE IS > 300
			PRINT 벌 있듯이
		CASE IS > 150
			PRINT 끝에 한껏
	ENDSELECT
		PRINTFORML 정자가 모인 콘돔을 꽉 눌렀다……
	;정액중독 5 미만
	IF ABL:정액중독 < 5
		PRINTFORML 흠칫흠칫 입을 연%CALLNAME:TARGET%의 혀 위에 정액을 털어 놓으면(자), %타겟은% 그대로 삼킴했다……
	ELSE
		PRINTFORML %타겟은% 황홀해 하는 얼굴로, 입안에 흘려 넣어진 고무 수상한 정액을 씹음 하도록(듯이) 맛보고 있다……
		SIF ABL:정액중독 >= 7
			PRINTFORML 정액을 모두 마셨다 뒤에도, %타겟은% 아쉬움에 뒤집은 콘돔을 빨고 있었다……
	ENDIF
ENDIF
;-------------------------------------------------
;90, 애널애무된다
;-------------------------------------------------
@MESSAGE_COM90
IF TEQUIP:PLAYER:0 > 1
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 애널을 옷 위로부터
ELSE
	PRINTFORM %타겟은% %CALLNAME:PLAYER%의 애널을
ENDIF

IF ABL:PLAYER:A감각 <= 1
	PRINT 손가락으로 쓰다듬듯이
ELSEIF ABL:PLAYER:A감각 <= 3
	PRINT 풀도록(듯이)
ELSE
	PRINT 손가락을 나사 붐비도록(듯이) 해
ENDIF

PRINTFORML 애무했다…

IF ABL:PLAYER:A감각 <= 1
	PRINTFORML %조교자는% 그 감촉에 아직 위화감을 느끼고 있다…
ELSEIF ABL:PLAYER:A감각 <= 3
	PRINTFORML %조교자는% 이물감에 상체를 진동시키고 있다…
ELSE
	PRINTFORML %조교자는% 황홀한 표정이 되는 대로 되어 있다…
ENDIF

;-------------------------------------------------
;91, 애널빨기된다
;-------------------------------------------------
@MESSAGE_COM91
PRINTFORML %타겟은% %CALLNAME:PLAYER%의 애널에 입술을 기게 했다
IF ABL:봉사정신 >= 3
	PRINTFORML 주름을 빨고 펼쳐지며, 항강안까지도 범해져%조교자는% 무심코 몸을 진동시켰다…
ELSE
	PRINTFORML 민감한 점막을 빨아 %조교자는% 몸을 진동시켰다…
ENDIF
;-------------------------------------------------
;96,パイズリする
;-------------------------------------------------
@MESSAGE_COM96
IF FLAG:70 == 1
	PRINTFORM %조교자는% 時間を止めた%CALLNAME:TARGET%の%TINKO(TARGET)%を、
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORM %조교자는% 眠っている%CALLNAME:TARGET%の%TINKO(TARGET)%を、
ELSE
	PRINTFORM %조교자는% %CALLNAME:TARGET%の%TINKO(TARGET)%を、
ENDIF
PRINTFORML 自分の%OPPAI_DESCRIPTION(PLAYER,1)%に\@ TALENT:PLAYER:가슴사이즈 <= -1? 懸命になって擦り付け# はさませて擦り上げ\@た…
;-------------------------------------------------
;120, 커닐링구스강제
;-------------------------------------------------
@MESSAGE_COM120
PRINTFORML %CALLNAME:TARGET%에 %CALLNAME:PLAYER%의 음순을 빨게 했다…
;-------------------------------------------------
;121, 조개 우려라
;-------------------------------------------------
@MESSAGE_COM121
IF FLAG:70 == 1
	PRINTFORM 시간을 멈춘%CALLNAME:TARGET%의 성기에 성기를 문질렀다…
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있는 %CALLNAME:TARGET%의 성기에 성기를 문질렀다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%과 %타겟은% 서로의 성기를 켜 대면시켰다…
ENDIF
;-------------------------------------------------
;122, 조수를 범하게 한다
;-------------------------------------------------
@MESSAGE_COM122
PRINTFORML %CALLNAME:TARGET%에 %조교자를% 안게 하는 일로 했다…
;-------------------------------------------------
;123, 풋잡한다
;-------------------------------------------------
@MESSAGE_COM123
PRINTFORM %조교자는%
SIF FLAG:70 == 1
	PRINTFORM 시간을 멈추었다
SIF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있다
PRINTFORM %CALLNAME:TARGET%의 %TINKO(TARGET)%를
;조교자가 새드
SIF TALENT:PLAYER:새드
	PRINT 짓밟도록(듯이)
PRINTL 다리로 시고 있다 …

;-------------------------------------------------
;124, 수음한다
;-------------------------------------------------
@MESSAGE_COM124
PRINTFORM %조교자는%
SIF TALENT:연인
	PRINT 사랑하는 것 같은 표정으로,
SIF FLAG:70 == 1
	PRINT 시간을 멈추었다
SIF CFLAG:MASTER:장난 == 2
	PRINT 자고 있다
PRINTFORM %CALLNAME:TARGET%의 %TINKO(TARGET)%를
IF TEQUIP:33 || TEQUIP:34
	PRINT 에이프런으로 싸 호된 훈련
ELSEIF TEQUIP:0 > 1
	PRINT 속옷 넘어로 스침
ELSE
	PRINT 손에 들어 손가락을 걸어 호된 훈련
ENDIF
PRINTL 올렸다…
;-------------------------------------------------
;125, 애무시킨다
;-------------------------------------------------
@MESSAGE_COM125
PRINTFORM %조교자는%
SIF FLAG:70 == 1
	PRINTFORM 시간을 멈추었다
PRINTFORML %CALLNAME:TARGET%의 수를 잡아 스스로의 몸에 이끌었다…
;-------------------------------------------------
;126, 젖치기
;-------------------------------------------------
@MESSAGE_COM126
PRINTFORML %CALLNAME:PLAYER%과 %타겟은% 서로의 가슴을 서로 비볐다 …
;-------------------------------------------------
;140, 이라마치오
;-------------------------------------------------
@MESSAGE_COM140
IF CFLAG:MASTER:장난 == 2
	PRINTFORML %조교자는% 일으키지 않게 신중하게 %CALLNAME:TARGET%의 목구멍까지 %TINKO(PLAYER)%를 찔러 넣었다…
ELSE
	PRINTFORML %조교자는% 난폭하게 %CALLNAME:TARGET%의 목까지 찔러 넣었다…
ENDIF
;-------------------------------------------------
;145, 애널 봉사 강제
;-------------------------------------------------
@MESSAGE_COM145
PRINTFORML %조교자는%, %CALLNAME:TARGET%에 애널을 빨게 했다…

;-------------------------------------------------
;150, 가슴의 골짜기를 범한다
;-------------------------------------------------
@MESSAGE_COM150
IF SELECTCOM == PREVCOM
	PRINTFORM %조교자는%
	SIF FLAG:70 == 1
		PRINTFORM 시간을 멈추었다
	SIF CFLAG:MASTER:장난 == 2
		PRINTFORM 자고 있다
	PRINTFORM %CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET, 1)%
	IF RAND:2
		IF RAND:3 == 0
			PRINTFORM 의 유륜을 엄지로 간질이면서
		ELSEIF RAND:2
			PRINTFORM 의 %NIPPLE_DESCRIPTION(TARGET, 1)%를 손가락으로 잡아당기면서
		ELSE
			PRINTFORM 를 비비어 하기 만
		ENDIF
	ELSE
		IF RAND:4 == 0
			PRINTFORM 를 안쪽에 대면서
		ELSEIF RAND:3 == 0
			PRINTFORM 를 밀착시키면서
		ELSEIF RAND:2
			PRINTFORM 를 단단히 조이면서
		ELSE
			PRINTFORM 를 압박하면서
		ENDIF
	ENDIF

	IF EXP:PLAYER:V성교경험 + EXP:PLAYER:A성교경험 && RAND:2
		IF RAND:3 == 0
			PRINT 성교라도 하는 것 같이
		ELSEIF RAND:2
			PRINTFORM 젖을 잉태와
		ELSE
			PRINT 마치 교미를 하고 있을까 (와) 같이
		ENDIF
	ENDIF
	SIF RAND:2
		PRINTFORM 체력이 계속되는 대로
	IF ABL:PLAYER:새드끼 && RAND:3 == 0
		PRINTFORML 몹시 거칠게 허리를 움직여
	ELSEIF RAND:2
		PRINTFORML 허리를 격렬하게 움직여
	ELSE
		PRINTFORML 허리를 전후에 움직여
	ENDIF
	
	IF RAND:3
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%를
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%의 골짜기를
	ENDIF
	
	IF RAND:2 == 0
		PRINTFORML 계속 범하고 있다…
	ELSE
		PRINTFORML 계속 범했다 …
	ENDIF
	RETURN 0
ENDIF

PRINTFORM %조교자는%
SIF FLAG:70 == 1
	PRINTFORM 시간을 멈추었다
SIF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있다
PRINTFORM %CALLNAME:TARGET%의
;작은 가슴
IF TALENT:가슴사이즈 == -1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 문지르고
;거유
ELSEIF TALENT:가슴사이즈 == 1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼우게 해 스침 인상
;폭유
ELSEIF TALENT:가슴사이즈 == 2
	PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼우게 해 스침 인상
;초유
ELSEIF TALENT:가슴사이즈 == 3
	PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 돌진해 스침 인상
ELSE
	PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 끼우게 해 스침 인상
ENDIF
PRINTL …

PRINTFORM %조교자는% %CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET)%
IF RAND:2
	IF RAND:3 == 0
		PRINTFORM 의 유륜을 엄지로 간질이면서
	ELSEIF RAND:2
		PRINTFORM 의 %NIPPLE_DESCRIPTION(TARGET, 1)%를 손가락으로 잡아당기면서
	ELSE
		PRINTFORM 를 비비어 하기 만
	ENDIF
ELSE
	IF RAND:4 == 0
		PRINTFORM 를 안쪽에 대면서
	ELSEIF RAND:3 == 0
		PRINTFORM 를 밀착시키면서
	ELSEIF RAND:2
		PRINTFORM 를 단단히 조이면서
	ELSE
		PRINTFORM 를 압박하면서
	ENDIF
ENDIF

IF EXP:PLAYER:V성교경험 + EXP:PLAYER:A성교경험 && RAND:2
	IF RAND:3 == 0
		PRINT 성교라도 하는 것 같이
	ELSEIF RAND:2
		PRINTFORM 젖을 잉태와
	ELSE
		PRINT 마치 교미를 하고 있을까 (와) 같이
	ENDIF
ENDIF

IF ABL:PLAYER:새드끼 && RAND:3 == 0
	PRINTFORMW 몹시 거칠게 허리를 움직였다…
ELSEIF RAND:2
	PRINTFORMW 허리를 격렬하게 움직였다…
ELSE
	PRINTFORMW 허리를 전후에 움직였다…
ENDIF

IF !FLAG:70 && CFLAG:MASTER:장난 != 2
	PRINTFORM %타겟은% 가슴의 골짜기를 범해지고
	;연모나 친애(애욕의 유무로 조금 분기)
	IF TALENT:애욕
		PRINTFORML \@ TALENT:연모 ?  황홀한 # 녹은 \@표정을 하고 있다…
	ELSE
		PRINTFORML 라고 있다…
	ENDIF
	IF ABL:PLAYER:기교 > 2
	;플레이어의 기교 3이상
		;가슴으로 분기 시켜 사이즈는 SELECTCASE로 나누어 본다. 크기는 달라도 그 앞에 돌기는 있어
		SELECTCASE TALENT:가슴사이즈
		;-이상
		CASE IS > 0
			IF RAND:2 == 0
				PRINTFORML 포동포동 찬 고기와 고기, %OPPAI_DESCRIPTION(TARGET, 1)%의 사이에 %TINKO(PLAYER)%를 글 사이에 삽입된 어구, 잡아당긴다.
				PRINTL 촉촉하게 감싸, 부드럽고 어디까지나 딱딱한 페니스를 받아들여
				PRINTL 가라앉아 가는 것 같은 기분 좋음은, 마치 고기의 바다에 녹는 것 같은 감각이다…
			ELSE
				PRINTFORML 그 %OPPAI_DESCRIPTION(TARGET, 1)%에 %TINKO(PLAYER)%를 꽉 눌러 한껏 %OPPAI_DESCRIPTION(TARGET)%의 중압을 더한 안에,
				PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 찔러 넣는다.
				PRINTFORML %CALLNAME:TARGET%의 고기가 부드럽고 압도적인 볼륨으로 접해 가는 마음에, %조교자는% 삼켜져 간다…
			ENDIF
			PRINTL 육과 고기는 밀고 들어가면 밀고 들어갈수록 딱딱한 페니스를 어디까지나 받아들여 침울해질 만큼 부드럽다…
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 그 안에서 분홍색 붙은 %OPPAI_DESCRIPTION(TARGET, 1)%의 끝, %NIPPLE_DESCRIPTION(TARGET, 1)%가 때때로, 긁적긁적 장대나 칼리를 문질러 온다…
		;-이하
		CASE IS < 0
			PRINTFORML 물리적인 핸디캡을 경험과 기술로 보충하려고 %OPPAI_DESCRIPTION(TARGET, 1)%를 사용해 %TINKO(PLAYER)%에 봉사하고 있다.
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 상기 한 앞가슴의 먼저 있는 %NIPPLE_DESCRIPTION(TARGET, 1)%로 장대나 칼리를 켜도록(듯이) 정중하게 유도해 온다.
		;
		CASEELSE
			PRINTFORML %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 사이에 두어, 잡아당긴다.
			SIF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000
				PRINTFORML 때때로 땀흘린 %OPPAI_DESCRIPTION(TARGET, 1)%의 받아, %NIPPLE_DESCRIPTION(TARGET, 1)%에 장대나 칼리를 스치게 해 단 소리를 지른다.
		ENDSELECT
	ELSEIF ABL:PLAYER:기교 <= 2
	;가슴2 이하
		;작은 가슴
		IF TALENT:가슴사이즈 == -1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 열심히 %TINKO(PLAYER)%를
		;거유
		ELSEIF TALENT:가슴사이즈 == 1
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 감싸
		;폭유, 초유
		ELSEIF TALENT:가슴사이즈 >= 2
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 숨기도록(듯이) 해
		ELSE
			PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%로 %TINKO(PLAYER)%를 끼워
		ENDIF
		PRINTL 켜고 있다…
	ELSE

	ENDIF
	;연모 또는 애욕
	IF IS_LOVER(TARGET) && EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 30000 && RAND:3 == 0
		PRINTFORML 기분이 흥분이라도 해 왔는지, %타겟은% 격렬하고 몸을 흔들어,
		PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%를 %OPPAI_DESCRIPTION(TARGET)%로 철저하게 잡아당기면서 , 열중하고 있어와 같이
		PRINTFORML 자신의 파이즈리는 어때?　기분이 좋은가?
		PRINTFORML 또 %SAMEN_DESCRIPTION(PLAYER)%를 내는지?　걸치는지?　부어 넣는지?
		PRINTFORML 와 쾌락에 마음을 맡긴 요염한 양상으로 물어 봐 온다…
	ENDIF
ENDIF

;-------------------------------------------------
;180, 로션,
;-------------------------------------------------
@MESSAGE_COM180
GETBIT STAIN:1, 7
IF RESULT
	PRINTFORML %CALLNAME:TARGET%에 초콜릿을 마구 칠했다…
ELSEIF TALENT:TARGET:체형 == -5
	PRINTFORML %타겟을% 로션 절임으로 했다
	PRINTFORML %CALLNAME:TARGET%에 있어 1회분이라도 빠질 정도의 양이다
ELSE
	PRINTFORML %CALLNAME:TARGET%에 로션을 마구 칠했다…
ENDIF
;-------------------------------------------------
;181, 미약,
;-------------------------------------------------
@MESSAGE_COM181
IF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있는 %CALLNAME:TARGET%에
	SIF ABL:PLAYER:기교 >= 3
		PRINT 입으로
	PRINT 천천히
	PRINTFORML 미약을 먹였다
	PRINTFORML %타겟은% 잔 채로 피부를 홍조 시켜 가랑이도 글자도 글자와 움직이고 있다…
ELSE
	PRINTFORM %CALLNAME:TARGET%에
;조교자의 기교 LV3 이상
SIF ABL:PLAYER:기교 >= 3
	PRINT 입으로
;순종 LV2 이하
SIF ABL:순종 <= 2
	PRINT 억지로
	PRINTFORML 미약을 먹였다
	PRINTFORML %타겟은% 난폭한 한숨 돌려 피부를 홍조 시키고 있다…
ENDIF
;-------------------------------------------------
;182, 이뇨제,
;-------------------------------------------------
@MESSAGE_COM182
IF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있는 %CALLNAME:TARGET%에
	SIF ABL:PLAYER:기교 >= 3
		PRINT 입으로
	PRINT 천천히
	PRINTFORML 이뇨제를 먹였다
	PRINTFORML %타겟은% 요의를 느끼고 있는 건가 가랑이도 글자도 글자와 움직이고 있다…
ELSE
PRINTFORM %CALLNAME:TARGET%에
;조교자의 기교 LV3 이상
SIF ABL:PLAYER:기교 >= 3
	PRINT 입으로
;순종 LV2 이하
SIF ABL:순종 <= 2
	PRINT 억지로
PRINTFORML 이뇨제를 먹였다
PRINTFORML %타겟은% 난폭한 한숨 돌려, 눈물을 머금어 요의에 참고 있다…
ENDIF
;-------------------------------------------------
;183, 비디오카메라,
;-------------------------------------------------
@MESSAGE_COM183
;해제시
IF !TEQUIP:비디오촬영
	PRINTFORML %CALLNAME:TARGET%의 촬영을 그만두었다…
ELSE
	PRINTFORML 비디오카메라로 %CALLNAME:TARGET%의 촬영을 시작했다…
ENDIF
;-------------------------------------------------
;184, 야외플레이,
;-------------------------------------------------
@MESSAGE_COM184
;해제시
IF !TEQUIP:야외플레이
	PRINTFORML 방으로 돌아왔다…
ELSE
	PRINTFORML %CALLNAME:TARGET%에 목걸이를 입어 밖으로 데리고 나갔다…
ENDIF
;-------------------------------------------------
;185, 조수를 범하는,
;-------------------------------------------------
@MESSAGE_COM185
;전회의 조교가 조수를 범하는이어, 뽑지 않는 플래그가 있는 경우
IF TFLAG:60 && PREVCOM == 56
	PRINTFORML %CALLNAME:TARGET%에 과시하면서, %CALLNAME:ASSI%를 계속 범했다 …
ELSE
	PRINTFORM %CALLNAME:TARGET%에 과시하면서, %CALLNAME:ASSI%의
	IF EXP:ASSI:V경험 == 0
		PRINT 처음을
	ELSE
		PRINT 바기나(질)을
	ENDIF
	PRINTL 관철해 갔다…
ENDIF
;-------------------------------------------------
;186, 수치플레이,
;-------------------------------------------------
@MESSAGE_COM186
;해제시
IF !TEQUIP:수치플레이
	PRINTFORML %타겟을% 따라 거울의 앞에서 떨어졌다…
ELSE
	PRINTFORML %타겟을% 거울의 앞에 데려 왔다…
ENDIF
;-------------------------------------------------
;187, 목욕탕플레이,
;-------------------------------------------------
@MESSAGE_COM187
IF CFLAG:MASTER:현재위치 == OMANEKIBEYA()
	;해제시
	IF !TEQUIP:목욕탕플레이
		PRINTFORML 목욕탕으로부터 돌아왔다…
		STR:98 = %CALLNAME:TARGET%의 방
	ELSE
		PRINTFORML %타겟을% 목욕탕에 데려갔다…
		STR:98 = %CALLNAME:TARGET%의 욕실
	ENDIF
ELSE
	;해제시
	IF !TEQUIP:목욕탕플레이
		PRINTFORML 풀장을 정리했다…
	ELSE
		PRINTFORML 풀장에 온수를 쳤다…
	ENDIF
ENDIF
;-------------------------------------------------
;188, 샤워,
;-------------------------------------------------
@MESSAGE_COM188
IF !TEQUIP:샤워
	PRINTFORML %CALLNAME:TARGET%에의 샤워를 멈추었다…
ELSE
	PRINTFORML %CALLNAME:TARGET%에 샤워를 받게 했다…
ENDIF
;-------------------------------------------------
;189, 새댁플레이,
;-------------------------------------------------
@MESSAGE_COM189
IF !TEQUIP:새댁플레이
	PRINTFORML %CALLNAME:TARGET%의 에이프런을 탈이 했다…
ELSE
	PRINTFORML %CALLNAME:TARGET%에 에이프런을 붙였다…
ENDIF
;-------------------------------------------------
;190, 에이프런을 하는,
;-------------------------------------------------
@MESSAGE_COM190
IF !TEQUIP:에이프런
	PRINTFORML 에이프런을 제외했다
ELSE
	PRINTFORML 에이프런을 대었다
ENDIF
;-------------------------------------------------
;191, 수면제,
;-------------------------------------------------
@MESSAGE_COM191
IF CFLAG:MASTER:장난 == 2
	PRINTFORML 자고 있는 %CALLNAME:TARGET%의 애널에 천천히 수면제를 밀어넣었다
	PRINTFORML %타겟은% 직장으로부터 흡수한 수면제에 의해 혼수 상태로 빠졌다
ELSE
	PRINTFORML 시간이 멈추어 있는 %CALLNAME:TARGET%의 애널에 천천히 수면제를 밀어넣었다
ENDIF
;-------------------------------------------------
;192, 배란유발제,
;-------------------------------------------------
@MESSAGE_COM192
IF CFLAG:MASTER:장난 == 2
	PRINTFORML 자고 있는 %CALLNAME:TARGET%의 입으로 천천히 배란유발제를 먹였다
ELSE
	PRINTFORML %CALLNAME:TARGET%에게 배란 유발제를 먹였다…
ENDIF