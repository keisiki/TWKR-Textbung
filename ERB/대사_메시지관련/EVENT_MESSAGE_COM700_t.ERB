;-------------------------------------------------
;700, 애무(위)
;-------------------------------------------------
@MESSAGE_COM700
IF FLAG:70 == 1
	PRINTFORM 시간이 멈춘 세계에서 
	PRINTFORM %조교자는% 스스로의 
	;작은 체형
	IF TALENT:PLAYER:체형 == -2
		PRINT 아이와 같은 
	ELSEIF TALENT:PLAYER:체형 == -1
		PRINT 몸집이 작은 
	ELSEIF TALENT:PLAYER:체형 == -5
		PRINT 인형과 같은 
	ENDIF
	PRINTL 몸을 열심히 애무했다…
	IF NOWEX:PLAYER:C절정 || NOWEX:PLAYER:V절정 || NOWEX:PLAYER:A절정 || NOWEX:PLAYER:B절정
		IF TEQUIP:PLAYER:50 == TARGET || TEQUIP:PLAYER:51 == TARGET
			PRINTFORML %TINKO(TARGET)%를 삽입당하고 있는 \@ TEQUIP:PLAYER:50 == PLAYER ? 질 # 애널 \@은 %CALLNAME:PLAYER%의 의사와는 상관없이 부들부들 떨면서 쾌락을 탐내고 있다…
		ELSEIF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
			PRINTFORM %TINKO(PLAYER)%를 삽입하고 있는 %CALLNAME:TARGET%의\@ TEQUIP:50 == TARGET ? 질 # 애널 \@을 사용해서 
			;남자
			IF !(TALENT:2 & 1)
				PRINTFORML %조교자는% 절정의 쾌감에 몸을 파르르 떨면서 %TINKO(PLAYER)%로부터 정액을 흘리고 있다…
			ELSE
				PRINTFORML %조교자는% 절정의 쾌감에 몸을 파르르 떨면서 보지로부터 애액을 흘리고 있다…
			ENDIF
		;남자
		ELSEIF !(TALENT:2 & 1)
			PRINTFORML %조교자는% 절정의 쾌감에 몸을 파르르 떨면서 %TINKO(PLAYER)%로부터 정액을 흘리고 있다…
		ELSE
			PRINTFORML %조교자는% 절정의 쾌감에 몸을 파르르 떨면서 보지로부터 애액을 흘리고 있다…
		ENDIF
	ELSE
		IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
			PRINTFORML %조교자는% 꼼짝도 하지 않는 %CALLNAME:TARGET%의 \@ TEQUIP:50 == PLAYER ? 질 # 애널 \@을 범하면서, 
			PRINTFORML 마치 눈앞의 %타겟은% 오나홀일 뿐이라고 말하는 것 같이, 스스로의 몸을 만지작거려 위로하고 있다…
		ELSE
			PRINTFORML %조교자는% 시간을 멈추었을 때 그대로의 표정으로 있는 %타겟을% 거들떠보지도 않고, 스스로의 몸을 만지작거려 위로하고 있다…
		ENDIF
	ENDIF
ELSE
	;키스를 하는 경우
	SIF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
		PRINTFORM %타겟과% 연결된 채로 
	PRINTFORM %조교자는% 자신의 
	;작은 체형
	IF TALENT:PLAYER:체형 == -2
		PRINT 아이와 같은 
	ELSEIF TALENT:PLAYER:체형 == -1
		PRINT 몸집이 작은 
	ELSEIF TALENT:PLAYER:체형 == -5
		PRINT 인형과 같은 
	ENDIF
	PRINTL 몸을 열심히 애무했다…
	IF NOWEX:C절정 || NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정
		IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
			PRINTFORML %CALLNAME:TARGET%의 \@ TEQUIP:50 == PLAYER ? 질 # 애널 \@은 %조교자가% 찔러넣어 댈 때마다 흠칫흠칫 수축하며 
		ELSE
			PRINTFORML %타겟은% %조교자가% 몸에 닿을 때마다 흠칫흠칫 떨며 
		ENDIF
		IF EXP_UP(27, PLAYER)
			PRINTFORML 쾌락에 녹은 얼굴로 %CALLNAME:PLAYER%에 혀를 쑥 내밀어 빨아지고 있다…
		ELSEIF EXP_UP(27, TARGET)
			PRINTFORML 쾌락에 녹은 얼굴로 %CALLNAME:PLAYER%의 입술을 요구해 왔다…
		ELSE
			PRINTFORML 주어지는 쾌락에 버틸 수 없다는 듯한 표정을 하고 있다…
		ENDIF
	ELSE
		IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
			PRINTFORM %CALLNAME:TARGET%는 %TINKO(PLAYER)%를 단단히 조이면서 
		ELSE
			;PRINTFORM %조교자는% %CALLNAME:PLAYER%에게 다가와 찰싹 달라붙으면서 
			PRINTFORM %조교자는% 스스로의 손으로 
		ENDIF
		IF EXP_UP(27, PLAYER)
			PRINTFORML %CALLNAME:PLAYER%의 입술을 요구해 왔다…
		ELSE
			PRINTFORML 주는 쾌락에 몸을 진동시켜 반응하고 있다…
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;702, 셀프 펠라(위)
;-------------------------------------------------
@MESSAGE_COM702
PRINTFORM %조교자는% 자신의 
;욕정의 PALAMLV4 이상
SIF PALAM:욕정 >= PALAMLV:4
	PRINT 딱딱하고 우뚝 발기한 
PRINTFORML %TINKO(PLAYER)%를 입에 넣고 빨아댔다…
;-------------------------------------------------
;703, 지삽(위)
;-------------------------------------------------
@MESSAGE_COM703
IF FLAG:70 == 1
	PRINTFORM %조교자는% 시간이 멈춘 %CALLNAME:PLAYER%의 
	;하반신착의
	SIF TEQUIP:0 & 2
		PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%안에 손을 넣어 
	;처녀
	IF TALENT:처녀 == 1
		PRINT 남자를 모르는 
	;윤활 LV4 이상
	ELSEIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖어 있는 
	ENDIF
	;작은 체형의 유무로 분기
	IF TALENT:체형 == -2
		PRINT 어린 균열
	ELSE
		PRINT 보지
	ENDIF
	;바이브의 유무로 분기
	IF TEQUIP:바이브
		PRINTFORML 에 삽입된, 요염하게 꾸불꾸불 진동하는 바이브를 앞뒤로 넣었다 뺐다 했다
	ELSE
		PRINTFORML 에 손가락을 천천히 삽입했다…
		IF NOWEX:V절정
			PRINTFORML %조교자는% 그 표정 그대로 몸이 절정해버려서, %CALLNAME:PLAYER%의 손가락을 애액으로 적시고 있다…
		ELSE
			PRINTFORML %조교자는% 꼼짝도 하지 못한 채 질내를 희롱당하고 있다…
		ENDIF
	ENDIF
ELSE
	;하반신착의
	SIF TEQUIP:0 & 2
		PRINTFORM %SHOW_BOTTOM(PLAYER, 2)% 안에 손을 넣어 
	;PRINTFORM %CALLNAME:PLAYER%의
	;처녀
	IF TALENT:처녀 == 1
		PRINT 남자를 모르는 
	;윤활 LV4 이상
	ELSEIF PALAM:윤활 >= PALAMLV:4
		PRINT 흠뻑 젖어 있는 
	ENDIF
	;작은 체형의 유무로 분기
	IF TALENT:체형 == -2
		PRINT 어린 균열
	ELSE
		PRINT 보지
	ENDIF
	;바이브의 유무로 분기
	IF TEQUIP:바이브
		PRINTFORML 에 삽입된, 요염하게 꾸불꾸불 진동하는 바이브를 앞뒤로 넣었다 뺐다 했다
	ELSE
		PRINTFORML 에 손가락을 천천히 삽입했다…
		IF NOWEX:V절정
			IF TALENT:처녀 == 1
				PRINTFORML %조교자는% 삽입한 손가락을 실룩실룩거리며 단단히 조이고 있다…
			ELSEIF TALENT:처녀 == -1
				PRINTFORML 벌써 처녀를 빼앗겼다는 것을 자각하지도 못한 채, %조교자는% 스스로의 육체의 반응에 당황하면서도 절정을 맞이했다…
			ELSE
				PRINTFORML %조교자는% 입에서 타액을 흘리면서, 스스로 허리를 흔들어 쾌락을 탐내고 있다…
			ENDIF
		ELSE
			IF TALENT:처녀 == 1
				PRINTFORML %조교자는% 서투른 감촉에 몸을 경직시켰다…
			ELSE
				PRINTFORML %조교자는% 질내에서 꿈틀거리는 손가락의 감촉에 달콤한 교성을 내뱉었다…
			ENDIF
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;705, 애널애무(위)
;-------------------------------------------------
@MESSAGE_COM705
SIF FLAG:70 == 1
	PRINTFORM 시간이 정지된 
;애널바이브
IF TEQUIP:애널바이브
	SIF FLAG:70 == 1
		PRINTFORM %CALLNAME:PLAYER%의 애널에 찔러 넣은, 
	PRINTFORML 조금씩 떨리는 애널바이브를 앞뒤로 넣었다 뺐다 했다…
;애널비즈
ELSEIF TEQUIP:애널비즈
	SIF FLAG:70 == 1
		PRINTFORM %CALLNAME:PLAYER%의 애널에 삽입된 
	PRINTFORML 비즈의 끈을 잡아당겨 가볍고 넣고 빼고 했다…
;관장기＋플러그
ELSEIF TEQUIP:관장
	IF FLAG:70 == 1
		PRINTFORML %CALLNAME:PLAYER%의 관장액으로 가득 찬 애널을 꽉 막은 플러그를 빙글빙글 돌려 자극했다…
	ELSE
		PRINTFORML 배설감에 떨리는 애널을 플러그로 빙글빙글 자극했다…
	ENDIF
;확장벌룬
ELSEIF TEQUIP:확장벌룬
	SIF FLAG:70 == 1
		PRINTFORM %CALLNAME:PLAYER%의 
	PRINTFORML 애널을 한계까지 넓히고 있는 벌룬을 앞뒤로 움직여주었다…
;애널전극
ELSEIF TEQUIP:애널전극
	IF FLAG:70 == 1
		PRINTFORML %CALLNAME:PLAYER%의 애널에 찔러넣은, 희미하게 소리를 내는 전극을 빙글빙글 반죽하듯 돌렸다
	ELSE
		PRINTFORML 희미하게 소리를 내는 전극으로 빙글빙글 애널을 반죽하듯 자극했다…
	ENDIF
ELSEIF TEQUIP:50 == PLAYER
	PRINTFORML %조교자를% 범하면서 애널에 장난을 했다…
ELSE
	PRINTFORM %조교자는% 애널을 농락당했다
	;A경험 100이상＋욕망의 PALAMLV4 이상
	SIF PALAM:욕정 >= PALAMLV:4 && EXP:A경험 >= EXPLV:3
		PRINT 그리고는, 손가락끝으로 직장까지 휘저어졌다
	PRINTL …
ENDIF
;-------------------------------------------------
;706, 가슴애무(위)
;-------------------------------------------------
@MESSAGE_COM706

;시간정지시
SIF FLAG:70 == 1
	PRINTFORM 시간이 멈춘 세계에서 
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	IF FLAG:70 == 1
		PRINTFORM %조교자를% 범한 채로 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%에게 연결된 채로 
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%의 
ENDIF
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL, 17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:PLAYER:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL, EQUIP:PLAYER:LOCAL)%너머로 
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:가슴사이즈 <= -1
	CALL PrintMilkedNipple(TARGET, "")
	IF TEQUIP:유두빨기
		IF RAND:3 == 0
			PRINTL 를 살짝 깨물었다…
		ELSEIF RAND:2 == 0
			PRINTL 를 물고 빨며, 싫은 소리를 내면서 괴롭혀댔다…
		ELSE
			PRINTL 를 입에 넣으면서, 유륜의 주위를 애태우듯이 빨았다…
		ENDIF
	ELSE
		PRINTFORML %TEXTR("를 주무르고 돌렸다…/를 혀로 핥으며 미끌미끌 비볐다…/를 혀로 굴려댔다…/를 강약을 조절해가며 애무했다…/를 유륜을 간질였다…/를 깨물어주었다…/를 잡아당겼다…")%
	ENDIF
ELSEIF TALENT:가슴사이즈 >= 1
	IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && RAND:4 == 0
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER, 1)%를 아래에서 
		PRINT 손으로 들어올리면서, 
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:3 == 0
				PRINT 쾌락에 대한 기대에 차 몸을 떨며 
			ELSEIF RAND:2
				PRINT 쾌락을 이제나저제나 하고 기다리며 
			ELSE
				PRINT 
			ENDIF
		ENDIF
		PRINTFORM %NIPPLE_DESCRIPTION(PLAYER, 1)%
		PRINTL 를 잡아당겨 세웠다…
	ELSEIF RAND:3 == 0 && ABL:B감각 >= 3
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:3 == 0
				PRINT 쾌락에 대한 기대에 차 몸을 떨며 
			ELSEIF RAND:2
				PRINT 쾌락을 이제나저제나 하고 기다리며 
			ELSE
				PRINT 
			ENDIF
		ENDIF
		PRINTFORM %OPPAI_DESCRIPTION(PLAYER, 1)%를, 
		IF TALENT:PLAYER:새드
			IF RAND:2 == 0
				PRINT 쥐어 짜듯이 
			ELSE
				PRINT 부서질 만큼 주물러서 
			ENDIF
		ENDIF
		IF RAND:3 == 0 && TALENT:PLAYER:새드
			PRINTL 전체를 손에 쥐고, 꽉 쥐어짜듯 비비벼댔다…
			PRINTFORML 비벼진 %OPPAI_DESCRIPTION(PLAYER)%는, 물이 가득 찬 가죽물주머니처럼 흐믈흐믈해졌다…
		ELSEIF RAND:2 == 0 && TALENT:PLAYER:새드
			PRINTL 억지로 쥐어짜, 타테나가의 범종과 같이 형태를 비뚤어지게 하게 했다…
		ELSE
			PRINTL 헝태가 변할정도로 세게 주물렀다…
		ENDIF
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5 && RAND:3 == 0
			IF RAND:3 == 0 && TALENT:음유 && TALENT:마조 &&  EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 30000
				PRINTFORML 젖짜기 가축인 %조교자는% %CALLNAME:PLAYER%로부터 느껴지는 쾌감과 
				PRINTL 쾌열에, 주물러져버려서 칠칠맞지 못하게 모양이 흐트러진 모습을 보이면서 기쁨의 교성을 흘리고 있다…
			ELSEIF RAND:2 == 0 && ABL:마조끼 >= 5 || TALENT:마조
				PRINTFORML %조교자는% %CALLNAME:PLAYER%로부터 전해지는 가슴의 쾌감에 
				PRINTL 잔뜩 풀어져 타액을 흘리는 표정을 보이면서 기쁨의 교성을 흘리고있다…
			ELSEIF ABL:마조끼 >= 3
				PRINTFORML %CALLNAME:PLAYER%로부터 전해져오는 달콤한 쾌락에 %조교자는% 
				PRINTL 거북할 정도로 음란한 표정을 하며 봉제인형처럼 몸에 힘이풀려 주저앉았다…
			ENDIF
		ENDIF
	ELSEIF RAND:2 == 0
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER, 1)%에 
		PRINT 손가락으로 비비며 
		IF TALENT:가슴사이즈 >= 1 && RAND:2
			PRINTL 가슴골을 만끽했다…
		ELSE
			PRINTL 포동포동한 감촉을 즐겼다…
		ENDIF
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%에 
		PRINTL 손가락이 파묻히는 감촉을 즐겼다…
	ENDIF
ELSE
	IF RAND:2
		IF RAND:4 == 0
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 들어올리듯이 어루만졌다.
		ELSEIF RAND:3 == 0
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 덥석 쥐어, 주물러댔다.
		ELSEIF RAND:2 == 0
			PRINTFORM %OPPAI_DESCRIPTION(PLAYER)%
			IF RAND:2 == 0
				PRINT 의 탄력을 즐기면서 
			ELSE
				PRINT 를 
			ENDIF
			PRINTL 마음껏 유린했다.
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%의 부드러움을 즐겼다.
		ENDIF
	ELSE
		IF RAND:2
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%전체를 감싸듯이 손으로 어루만졌다.
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 유린했다.
		ENDIF
	ENDIF
ENDIF
;모유
IF TALENT:모유체질 && RAND:2 == 0
	IF TALENT:가슴사이즈 <= -1 && RAND:2
		PRINTFORMW %CALLNAME:PLAYER%의 손이 유방에 가라앉을 때마다, %NIPPLE_DESCRIPTION(PLAYER)%로부터 모유가 배어나오고 있다…
	ELSE
		PRINTFORMW %CALLNAME:PLAYER%의 손에 의해 가슴의 모양이 바뀔 때마다, %NIPPLE_DESCRIPTION(PLAYER)%로부터 모유가 배어나오고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;707, 유두 꾸짖고(위)
;-------------------------------------------------
@MESSAGE_COM707
;시간정지시
SIF FLAG:70 == 1
	PRINTFORM 시간이 정지된 
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	IF FLAG:70 == 1
		PRINTFORM %조교자를% 범한 채로 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%과 연결된 채로 
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%의 
ENDIF
CALL PrintMilkedNipple(TARGET, "의복묘사부")
IF TEQUIP:유두빨기
	IF RAND:3 == 0
		PRINTL 를 살짝 깨물었다…
	ELSEIF RAND:2 == 0
		PRINTL 를 물고 빨며, 싫은 소리를 내면서 괴롭혀댔다…
	ELSE
		PRINTL 를 입에 넣으면서, 유륜의 주위를 애태우듯이 빨았다…
	ENDIF
ELSE
	PRINTFORML %TEXTR("를 주무르고 돌렸다…/를 혀로 핥으며 미끌미끌 비볐다…/를 혀로 굴려댔다…/를 강약을 조절해가며 애무했다…/를 유륜을 간질였다…/를 깨물어주었다…/를 잡아당겼다…")%
ENDIF
;모유
IF TALENT:모유체질 && RAND:3 == 0
	IF TALENT:가슴사이즈 <= -1 && RAND:2
		PRINTFORMW %CALLNAME:PLAYER%의 손이 %OPPAI_DESCRIPTION(PLAYER)%에 주무르자, %NIPPLE_DESCRIPTION(PLAYER)%에서 모유가 배어나오고 있다…
	ELSE
		PRINTFORMW %CALLNAME:PLAYER%의 손에 의해 %OPPAI_DESCRIPTION(PLAYER)%의 손에 의해 가슴의 형태가 바뀔 정도로 주물러지자, %NIPPLE_DESCRIPTION(PLAYER)%에서 모유가 배어나오고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;708, 음부개장(위)
;-------------------------------------------------
@MESSAGE_COM708
IF FLAG:70 == 1
	PRINTFORM 시간이 멈춘%CALLNAME:PLAYER%의 
	;작은 체형
	IF TALENT:체형 < 0
		PRINT 어린아이처럼 통통하고 굳게 닫힌 보지에 손을 대고
	ELSE
		PRINT 작지만 야한 균열에 손가락을 밀어넣고
	ENDIF
	PRINTL , 잘 보이도록 좌우로 펼쳤다
	IF TCVAR:102
		PRINTFORML 노출된 보지 입구에서, 방금 사정했던 정액이 주륵 흘러내렸다…
	ELSEIF ABL:V감각 > 7
		PRINTFORML 충분히 개발된 %CALLNAME:PLAYER%의 질 내부는 자궁구가 훤히 들여다보일 정도로 질내가 확장되어 애액이 흐르고 있다…
	ELSEIF PALAM:욕정 > PALAMLV:3 || PALAM:윤활 > PALAMLV:3
		PRINTFORM 노출된 보지는 
		SIF PALAM:윤활 > PALAMLV:3
			PRINTFORM 흠뻑 젖어 
		SIF PALAM:욕정 > PALAMLV:3
			PRINTFORM 쾌락을 바라는 듯이 움찔움찔 조여들고 
		PRINTFORML 있다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%에 명령받은 %조교자는% 
	;노출벽
	IF ABL:노출벽 == 0
		PRINTFORM 눈물을 글썽이며 이쪽을 노려보았지만, 마지못한다는 듯한 모습으로 자신의 
		;작은 체형
		IF TALENT:체형 == -2
			PRINT 어린아이처럼 통통하고 굳게 닫힌 보지에 
		ELSE
			PRINT 작지만 야한 균열에 
		ENDIF
		PRINTL 손가락을 꽂아 넣어 좌우로 펼쳐 보였다
		SIF TCVAR:102
			PRINTFORML 노출된 보지 입구에서, 방금 사정했던 정액이 주륵 흘러내렸다…
	ELSEIF ABL:노출벽 == 1
		PRINTFORML 얼굴이 새빨개지면서도, 흠칫흠칫, 성기를 분명히 펼쳐 보였다
		SIF TCVAR:102
			PRINTFORML 노출된 보지 입구에서, 방금 사정했던 정액이 주륵 흘러내렸다…
	ELSEIF ABL:노출벽 == 2
		PRINTFORML 자신의 아랫배 둔덕에 손을 올리고, 검지와 중지로 브이싸인을 만들듯, %CALLNAME:PLAYER%에게 자신의 보지를 전부 보여주었다
		IF TCVAR:102
			PRINTFORML 노출된 보지 입구에서, 방금 사정했던 정액이 주륵 흘러내렸다…
		ELSE
			PRINTFORML 활짝 열린 여성의 음부로부터 마주한 야한 구멍은, 받아들여야 할 물건을 바라며 기다리는 것 처럼, 애액으로 젖어 음란하게 꿈틀거리고 있다…
		ENDIF
	ELSEIF ABL:노출벽 == 3
		PRINTFORML 기쁜듯한 표정으로, 한손으로는 자신의 음란한 성기를 펼쳐보이며 빈손으로는 클리토리스를 만지작거리기 시작했다
		IF TCVAR:102
			PRINTFORML 노출된 보지 입구에서, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%의 손이 클리토리스를 자극할 때마다, 보지에서 애액이 방울져 떨어졌다…
		ENDIF
	ELSEIF ABL:노출벽 == 4
		PRINTFORML 양손으로 보지뿐만 아니라, 그 음란한 구멍까지 벌리고 있다
		IF TCVAR:102
			PRINTFORML %CALLNAME:PLAYER%의 손이 보지를 한층 더 양쪽으로 펼치자, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML 질내가 보고 있다는 사실에 느끼기라도 하는 것인지,  %CALLNAME:PLAYER%의 뺨은 은은하게 홍조를 띠어가고 있다
		ENDIF
	ELSEIF ABL:노출벽 >= 5 && TALENT:음란 == 0
		PRINTFORML 양손으로 보지와 음란한 구멍에 손가락을 밀어넣고는 %CALLNAME:PLAYER%에게 과시하듯 넓게 벌렸다
		IF TCVAR:102
			PRINTFORML 넓게 벌려진 소녀의 음란한 구멍은, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%의 손이 소녀의 음란한 구멍을 넓게 벌리자, 질벽이 뻐끔뻐끔 움직이며 애액을 흘리고있다…
		ENDIF
	;노출벽 LV5 이상＋음란 or창부
	ELSEIF ABL:노출벽 >= 5 && TALENT:음란
		PRINTFORML 양손으로 보지와 음란한 구멍에 손가락을 밀어넣고는 과시하듯 넓게 벌렸다
		IF TCVAR:102
			PRINTFORML 꿈틀거리는 질내에서, 방금 사정했던 정액이, 주르륵 하고, 칠칠맞지 못하게 흘러내렸다…
		ELSE
			PRINTFORML 부드러워 보이는 질내는, 한껏 애액으로 젖어서 이쪽을 유혹하는 것 처럼, 실룩실룩 움찔거리고 있다…
		ENDIF
	ENDIF
	;조교자가 부끄러워해＋조교경험 200 이하
	IF TALENT:PLAYER:수치심 > 0 && EXP:PLAYER:조교경험 < 200
		PRINTFORML 스스로 치욕스런 명령을 내린 %조교자는%
		PRINTL 희미한 죄악감과 수치심이 달아올라 붉게 물든 얼굴을 살짝 돌려 외면하고 있다
	ENDIF
ENDIF
;-------------------------------------------------
;709, 수음(위)
;-------------------------------------------------
@MESSAGE_COM709
PRINTFORM %조교자는% 
;비디오촬영중
SIF TEQUIP:비디오촬영
	PRINT 비디오카메라의 앞에서 
;노출벽 LV3 이상
SIF ABL:노출벽 >= 3
	PRINT 과시하듯이, 
;바이브＋애널바이브
IF TEQUIP:바이브 && TEQUIP:애널바이브
	PRINT 보지와 애널에 삽입된  2개의 바이브를 스스로 피스톤질 하면서 
;바이브
ELSEIF TEQUIP:바이브
	PRINT 보지에 삽입된 바이브를 스스로 피스톤질 하면서 
;애널바이브
ELSEIF TEQUIP:애널바이브
	PRINT 애널에 삽입된  바이브를 스스로 피스톤질 하면서 
ELSEIF TEQUIP:50 >= 0 
	PRINTFORM %CALLNAME:(TEQUIP:50)%에 범해지면서 
ELSEIF TEQUIP:51 >= 0 
	PRINTFORM %CALLNAME:(TEQUIP:51)%에 애널을 범해지면서 
ELSE
	;PRINT 보지를 스스로의 손가락으로 만지작거리면서 
	PRINTFORM %TINKO(PLAYER)%를 스스로의 손가락으로 만지작거리면서 
ENDIF
IF TEQUIP:50 >= 0 || TEQUIP:51 >= 0
	PRINTFORML %NIPPLE_DESCRIPTION(PLAYER)%과 클리토리스를 애무했다…
ELSE
	PRINTL 쾌감을 맛보고 있다…
ENDIF
;바이브＋애널바이브＋V감각 LV5 이상＋A감각 LV5 이상＋C감각 LV5 이상＋B감각 LV5 이상＋욕정의 PALAMLV4 이상
IF TEQUIP:바이브 && TEQUIP:애널바이브 && ABL:V감각 >= 5 && ABL:A감각 >= 5 && ABL:C감각 >= 5 && ABL:B감각 >= 5 && PALAM:욕정 >= PALAMLV:4
	PRINTFORML %조교자는% 입에서 타액을 흘리며, 꿈틀꿈틀 몸을 경련하며 달콤한 울음소리를 내고 있다
	PRINTFORML 절정에서 헤어나올 수 없는 듯, 머리가 새하얘지고 있다…
;바이브＋V감각 LV5 이상＋C감각 LV5 이상＋B감각 LV5 이상＋욕정의 PALAMLV4 이상
ELSEIF TEQUIP:바이브 && ABL:V감각 >= 5 && ABL:C감각 >= 5 && ABL:B감각 >= 5 && PALAM:욕정 >= PALAMLV:4
	PRINTFORML %조교자는% 애액이 흩날리는 것도 상관없이, 바이브를 격렬하고 피스톤질 하고 있다
	PRINTFORML 이제 다른 일을 신경쓸 여유도 없다…
;애널바이브＋A감각 LV5 이상＋C감각 LV5 이상＋B감각 LV5 이상＋욕정의 PALAMLV4 이상
ELSEIF TEQUIP:애널바이브 && ABL:A감각 >= 5 && ABL:C감각 >= 5 && ABL:B감각 >= 5 && PALAM:욕정 >= PALAMLV:4
	PRINTFORML %조교자는% 부들부들 몸을 떨면서, 애널에 삽입된 바이브를 빠르게 왕복시켰다
	PRINTFORML 장벽이 꾸물꾸물 올려질때 마다 겉에서 보기에도 배가 꿀렁꿀렁 움직여대며 몇번이나 절정하고 있다…
;자위중독 LV3 이상＋C감각 LV5 이상＋B감각 LV5 이상＋욕정의 PALAMLV4 이상
ELSEIF ABL:자위중독 >= 3 && ABL:C감각 >= 5 && ABL:B감각 >= 5 && PALAM:욕정 >= PALAMLV:4
	PRINTFORML %조교자는% 교성을 내뱉으면서 집중해서 스스로의 민감한 부위를 계속 마찰시키고 있다
	PRINTFORML 멈추고 싶어도 손이 마음대로 움직여 멈추지 않는다…
ENDIF
;-------------------------------------------------
;711, 유두빨기
;-------------------------------------------------
@MESSAGE_COM711
SIF FLAG:70 == 1
	PRINTFORM 시간이 멈추어진 
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	IF FLAG:70 == 1
		PRINTFORM %조교자를% 범한 채로 
	ELSE
		PRINTFORM %CALLNAME:PLAYER%과 연결된 채로 
	ENDIF
ELSE
	PRINTFORM %CALLNAME:PLAYER%의 
ENDIF
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL, 17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:PLAYER:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL, EQUIP:PLAYER:LOCAL)%너머로 
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:모유체질 && RAND:3 == 0
	IF RAND:3
		PRINT 우유가 흐르는 
	ELSEIF RAND:2
		PRINT 모유가 흐르는 
	ELSE
		PRINT 밀크가 흐르는 
	ENDIF
ENDIF
PRINTFORM %OPPAI_DESCRIPTION(PLAYER)%의
IF RAND:3 == 0
	PRINTL 말단에 있는, 
ELSEIF RAND:2
	PRINTL 정상에 있는, 
ELSE
	PRINTL 
ENDIF
PRINTFORM %NIPPLE_DESCRIPTION(PLAYER, 1)%
IF RAND:3 == 0
	PRINTL 를 살짝 깨물었다…
ELSEIF RAND:2 == 0
	PRINTL 를 물고 빨며, 싫은 소리를 내면서 괴롭혀댔다…
ELSE
	PRINTL 를 입에 넣으면서, 유륜의 주위를 애태우듯이 빨았다…
ENDIF

IF ABL:PLAYER:친밀 >= 10 && !FLAG:70
	SELECTCASE ABL:PLAYER:B감각 
	CASE 1
		PRINTFORM %조교자는% 쓴 웃음을 지으면서도, %OPPAI_DESCRIPTION(PLAYER)%에 달라붙는 %CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다
	CASE 2 TO 5
		PRINTFORM %조교자는% %OPPAI_DESCRIPTION(PLAYER)%를 들이마셔지는 쾌감을 견디듯이 신음했다
	CASE 6 TO 9
		PRINTFORM %조교자는% %OPPAI_DESCRIPTION(PLAYER)%를 들이마셔지는 쾌감을 더는 견딜 수 없게 되자 달콤한 교성을 내질렀다
	CASEELSE
		PRINTFORM %조교자는% %OPPAI_DESCRIPTION(PLAYER)%를 들이마셔지는 쾌감에 꿈틀 꿈틀과 반응해, 전신을 활처럼 휘어대며 허덕이고 있다
	ENDSELECT
ENDIF
;-------------------------------------------------
;712, 가슴주무르기(위)
;-------------------------------------------------
@MESSAGE_COM712
PRINTFORM %조교자는% 
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL, 17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:PLAYER:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL, EQUIP:PLAYER:LOCAL)%너머로 
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:가슴사이즈 >= 1
	IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && RAND:4 == 0
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 아래에서 
		PRINT 손으로 들어올리면서, 
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:3 == 0
				PRINT 쾌락에 대한 기대에 떨며 
			ELSEIF RAND:2
				PRINT 쾌락을 이제나저제나 하고 기다려 바라며 
			ELSE
				PRINT 
			ENDIF
		ENDIF
		PRINTFORM %NIPPLE_DESCRIPTION(PLAYER, 1)%
		PRINTL 를 잡아당겨 돌출시켰다…
	ELSEIF RAND:3 == 0 && ABL:B감각 >= 3
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5
			IF RAND:3 == 0
				PRINT 쾌락에 대한 기대에 떨며 
			ELSEIF RAND:2
				PRINT 쾌락을 이제나저제나 하고 기다려 바라며 
			ELSE
				PRINT 
			ENDIF
		ENDIF
		PRINTFORM %OPPAI_DESCRIPTION(PLAYER, 1)%를, 
		IF TALENT:PLAYER:새드 || TALENT:PLAYER:마조
			IF RAND:2 == 0
				PRINT 부서질 만큼 주물러서 
			ELSE
				PRINT 쥐어 짜듯이 
			ENDIF
		ENDIF
		IF RAND:3 == 0 && TALENT:PLAYER:새드 || TALENT:PLAYER:마조
			PRINTL 전체를 손에 쥐고, 꽉 쥐어짜듯 비벼댔다…
			PRINTFORML 비벼진 %OPPAI_DESCRIPTION(PLAYER)%는, 물이 가득 찬 가죽 물주머니처럼 흐믈흐믈해졌다…
		ELSEIF RAND:2 == 0 && TALENT:PLAYER:새드 || TALENT:PLAYER:마조
			PRINTL 억지로 쥐어짜, 타테나가의 범종과 같이 형태를 비뚤어지게 하게 했다…
		ELSE
			PRINTL 헝태가 변할정도로 세게 주물렀다…
		ENDIF
		IF EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 10000 && ABL:B감각 <= 5 && RAND:3 == 0
			IF RAND:3 == 0 && TALENT:음유 && TALENT:마조 &&  EX:B절정 * 10000 + PALAM:쾌B + PALAM:욕정 >= 30000
				PRINTFORML 젖짜기 가축인 %조교자는% %OPPAI_DESCRIPTION(PLAYER)%로부터 느껴지는 쾌감과 
				PRINTFORML 쾌열에, 주물러져버려서 칠칠맞지 못하게 모양이 흐트러진 모습을 보이면서 기쁨의 교성을 흘리고 있다…
			ELSEIF RAND:2 == 0 && ABL:마조끼 >= 5 || TALENT:마조
				PRINTFORML %조교자는% %OPPAI_DESCRIPTION(PLAYER)%로부터 전해지는 유열의 쾌감에 
				PRINTFORML 칠칠치 못한 표정을 지으며 녹고 있다…
			ELSEIF ABL:마조끼 >= 3
				PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%로부터 전해져오는 달콤한 쾌락에 %조교자는% 
				PRINTFORML 거북할 정도로 음란한 표정을 띠며 녹초처럼 몸을 탈진시켰다…
			ENDIF
		ENDIF
	ELSEIF RAND:2 == 0
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%인 부드러운 살덩어리에 
		PRINT 손가락을 파묻으며 
		IF TALENT:가슴사이즈 >= 1 && RAND:2
			PRINTL 강하게 애무했다…
		ELSE
			PRINTL 포동포동한 감촉을 즐겼다…
		ENDIF
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%에, 
		PRINTL 손가락이 파묻히는 감촉을 즐겼다…
	ENDIF
ELSE
	IF RAND:2
		IF RAND:4 == 0
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 들어올리며 어루만지며 
		ELSEIF RAND:3 == 0
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 덥석 잡아 주무르며 
		ELSEIF RAND:2 == 0
			PRINTFORM %OPPAI_DESCRIPTION(PLAYER)%를 
			SIF RAND:2
				PRINT 튕겨 탄력을 즐기면서 
			PRINTL 마음껏 유린했다…
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%의 부드러움을 즐겼다.
		ENDIF
	ELSE
		IF RAND:2
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)% 전체를 감싸듯이 손으로 어루만졌다.
		ELSE
			PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%를 유린했다.
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;713, 애널개장(위)
;-------------------------------------------------
@MESSAGE_COM713
	;노출벽
	IF ABL:노출벽 < 3
		PRINTFORM 스스로 
		;작은 체형
		IF TALENT:체형 == -2
			PRINT 아직 덜 여문 작은 애널에 
		ELSE
			PRINT 애널에 
		ENDIF
		PRINTL 손가락을 꽂아 넣어 좌우로 펼쳐 보였다
		SIF TCVAR:104
			PRINTFORML 노출된 애널에서, 조금 전 사정한 정액이, 쪼르륵 흘러내려서 온다…
	ELSEIF ABL:노출벽 < 5
		PRINTFORML 자신의 아랫배 둔덕에 손을 올리고, 검지와 중지로 브이싸인을 만들듯, 자신의 애널를 전부 보여주었다
		IF TCVAR:104
			PRINTFORML 노출된 애널 입구에서, 방금 사정했던 정액이 주륵 흘러내렸다…
		ELSE
			PRINTFORML 활짝 열린 여성의 음부로부터 마주한 야한 구멍은, 받아들여야 할 물건을 바라며 기다리는 것 처럼, 애액으로 젖어 음란하게 꿈틀거리고 있다…
		ENDIF
	ELSEIF ABL:노출벽 < 7
		PRINTFORML 기쁜듯한 표정으로, 한손으로는 자신의 음란한 성기를 펼쳐보이며 빈손으로는 클리토리스를 만지작거리기 시작했다
		IF TCVAR:104
			PRINTFORML 노출된 애널 입구에서, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%의 손이 클리토리스를 자극할 때마다, 애널에서 장액이 방울져 떨어졌다…
		ENDIF
	ELSEIF ABL:노출벽 < 10
		PRINTFORML 양손으로 애널뿐만 아니라, 그 음란한 구멍까지 벌리고 있다
		IF TCVAR:104
			PRINTFORML %CALLNAME:PLAYER%의 손이 애널를 한층 더 양쪽으로 펼치자, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML 직장을 보고 있다는 사실에 느끼기라도 하는 것인지, %CALLNAME:PLAYER%의 뺨은 은은하게 홍조를 띠어가고 있다
		ENDIF
	ELSEIF ABL:노출벽 >= 10 && TALENT:음란 == 0
		PRINTFORML 양손으로 애널와 음란한 구멍에 손가락을 밀어넣고는 %CALLNAME:PLAYER%에게 과시하듯 넓게 벌렸다
		IF TCVAR:104
			PRINTFORML 넓게 벌려진 소녀의 음란한 구멍은, 방금 사정했던 정액이, 음란하게 주르륵 흘러내렸다…
		ELSE
			PRINTFORML %CALLNAME:PLAYER%의 손이 소녀의 음란한 구멍을 넓게 벌리자, 질벽이 뻐끔뻐끔 움직이며 애액을 흘리고있다…
		ENDIF
	;노출벽 LV10 이상＋음란 or창부
	ELSEIF ABL:노출벽 >= 10 && TALENT:음란
		PRINTFORML 양손으로 애널와 음란한 구멍에 손가락을 밀어넣고는 과시하듯 넓게 벌렸다
		IF TCVAR:104
			PRINTFORML 꿈틀거리는 질내에서, 방금 사정했던 정액이, 주르륵 하고, 칠칠맞지 못하게 흘러내렸다…
		ELSE
			PRINTFORML 부드러워 보이는 질내는, 한껏 애액으로 젖어서 이쪽을 유혹하는 것 처럼, 실룩실룩 움찔거리고 있다…
		ENDIF
	ENDIF
	;조교자가 부끄러워해＋조교경험 200 이하
	IF TALENT:PLAYER:수치심 > 0 && EXP:PLAYER:조교경험 < 200
		PRINTFORML 스스로 치욕스런 명령을 내린 %조교자는%
		PRINTL 희미한 죄악감과 수치심이 달아올라 붉게 물든 얼굴을 살짝 돌려 외면하고 있다
	ENDIF
;-------------------------------------------------
;740, 로터(위)
;-------------------------------------------------
@MESSAGE_COM740
	IF TEQUIP:0 & 4
		PRINTFORM %조교자는%
		PRINTFORML 하반신에 %SHOW_BOTTOM(PLAYER, 3)% 너머로 진동하는 로터를 꽉 눌렀다…
		RETURN 0
	ENDIF
	IF TEQUIP:0 & 2
		PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%를 젖혀 노출시켰다
	ELSEIF TEQUIP:0 & 1
		PRINTFORM %SHOW_BOTTOM(PLAYER, 1)%를 걷어 올려 노출시켰다
	ENDIF
	PRINTFORM 자신의
	;남자＋욕정의 PALAMLV3 이상
	IF !(TALENT:2 & 1) && (PALAM:욕정 >= PALAMLV:3)
		PRINTFORML 딱딱하고 우뚝 발기한 %TINKO(PLAYER)%에 진동하는 로터를 꽉 눌렀다…
	;남자
	ELSEIF !(TALENT:2 & 1)
		PRINTFORML %TINKO(PLAYER)%에 진동하는 로터를 꽉 눌렀다…
	;양성＋욕정의 PALAMLV3 이상
	ELSEIF (TALENT:2 & 3) == 3 && PALAM:욕정 >= PALAMLV:3
		PRINTL 점액으로 범벅이 된 민감한 곳에 진동하는 로터를 꽉 눌렀다…
	;후타나리
	ELSEIF (TALENT:2 & 3) == 3
		PRINTL 민감한 곳에 진동하는 로터를 꽉 눌렀다…
	;욕정의 PALAMLV3 이상
	ELSEIF PALAM:욕정 >= PALAMLV:3
		PRINTL 딱딱하게 발기한 클리토리스에 진동하는 로터를 꽉 눌렀다…
	ELSE
		PRINTL 클리토리스에 진동하는 로터를 꽉 눌렀다…
	ENDIF
;-------------------------------------------------
;741, E마사지기(위)
;-------------------------------------------------
@MESSAGE_COM741
	PRINTFORML 자신의 보지에 \@ TEQUIP:0 > 1 ? %SHOW_BOTTOM(PLAYER)%너머로  # \@진동하고 있는 전기 마사지기를 꾸욱 눌렀다
	PRINTFORM %조교자는%
	;C감각 LV3 미만＋욕정의 PALAMLV3 미만
	IF ABL:C감각 < 3 && PALAM:욕정 < PALAMLV:3
		PRINT 매우 간지러워하고 있다
	;C감각 LV3 미만＋욕정의 PALAMLV3 이상
	ELSEIF ABL:C감각 < 3 && PALAM:욕정 >= PALAMLV:3
		PRINT , …말하기 힘들 정도였다
	;연모나 친애＋C감각 LV4 이상＋욕정의 PALAMLV3 미만
	ELSEIF TALENT:연모 && ABL:C감각 >= 4 && PALAM:욕정 < PALAMLV:3
		PRINT 애타게 기다린 것처럼 다리를 열어, 받아들이고 있다
	;연모나 친애＋C감각 LV4 이상＋욕정의 PALAMLV3 이상＋욕정의 PALAMLV5 미만
	ELSEIF TALENT:연모 && ABL:C감각 >= 4 && PALAM:욕정 >= PALAMLV:3 && PALAM:욕정 < PALAMLV:5
		PRINTFORM 쾌락에 타액을 흘리면서, 몇 번이나 사랑스러운 사람의 이름을 불렀다
	;연모나 친애＋C감각 LV4 이상＋욕정의 PALAMLV5 이상
	ELSEIF TALENT:연모 && ABL:C감각 >= 4 && PALAM:욕정 >= PALAMLV:5
		PRINTFORM 자신의 몸을 끌어안으면서 절정의 황홀감에 잠겨있다
	;C감각 LV3 이상＋욕정의 PALAMLV3 미만
	ELSEIF ABL:C감각 >= 3 && PALAM:욕정 < PALAMLV:3
		PRINT 강렬한 쾌감에 얼굴을 붉히고 있다
	;C감각 LV3 이상＋욕정의 PALAMLV3 이상＋욕정의 PALAMLV5 미만
	ELSEIF ABL:C감각 >= 3 && PALAM:욕정 >= PALAMLV:3 && PALAM:욕정 < PALAMLV:5
		PRINT 정신나간 눈으로 쾌락을 탐내고 있다
	;C감각 LV3 이상＋욕정의 PALAMLV5 이상
	ELSEIF ABL:C감각 >= 3 && PALAM:욕정 >= PALAMLV:5
		PRINT 당장이라도 절정에 이르러 버릴 것 같은 모습이다
	ENDIF
	PRINTL …
;-------------------------------------------------
;742, 클리캡(위)
;-------------------------------------------------
@MESSAGE_COM742
;해제시
IF !TEQUIP:클리캡
	PRINTFORML 음핵에 전동 클리캡을 붙였다
ELSE
	PRINTFORML 보지의 끝에 전동 클리캡을 달았다
ENDIF
;-------------------------------------------------
;743, 오나홀(위)
;-------------------------------------------------
@MESSAGE_COM743
;해제시
IF !TEQUIP:오나홀
	PRINTFORML %TINKO(PLAYER)%에게서 오나홍을 빼주었다
ELSE
	PRINTFORML 자신의 %TINKO(PLAYER)%를 오나홀에 삽입했다
ENDIF
;-------------------------------------------------
;744, 바이브(위)
;-------------------------------------------------
@MESSAGE_COM744
	;해제시
	IF !TEQUIP:바이브
		PRINTFORML 자신의 보지에서 전동 바이브를 천천히 뽑아 냈다
		;V경험 200이상＋연모나 친애
		IF EXP:V경험 > 200
			PRINTFORM %조교자는%, 질내의 상실감을 참기 힘들어 눈물과 타액을 흘리고 
		;V경험 150이상＋연모나 친애
		ELSEIF EXP:V경험 > 150
			PRINTFORM %CALLNAME:PLAYER%의 질내에서 흘러나오던 애액이 긁어져나와 방울지며 떨어져서, 바닥에 작은 웅덩이를 만들고 
		;V경험 120이상＋연모나 친애
		ELSEIF EXP:V경험 > 120
			PRINTFORM 바이브가 뽑아진 %CALLNAME:PLAYER%의 보지에서 애액이 뚝뚝 방울지며 떨어지고 
		;V경험 70이상＋연모나 친애
		ELSEIF EXP:V경험 > 70
			PRINTFORM %조교자는% 머뭇머뭇 가랑이를 조이고 
		;V경험 40이상
		ELSEIF EXP:V경험 > 40
			PRINTFORM %조교자는% 뜨거운 한숨을 토해내고 
		;V경험 20이상
		ELSEIF EXP:V경험 > 20
			PRINTFORM %조교자는% 느슨해진 보지에서 애액을 흘리고 
		;그 이외
		ELSE
			PRINTFORM %조교자는% 안도한 것 같은 표정을 하고 
		ENDIF
		PRINTL 있다…
	ELSEIF TCVAR:100 == PLAYER
		PRINTFORML %CALLNAME:PLAYER%로부터 %TINKO(PLAYER)%를 뽑아 내자 조금 전까지 범하고 있던 \@ TALENT:체형 < 0 ? 어린 균열 # 보지 \@에 바이브를 단숨에 삽입했다
			IF EXP:V경험 > 100
				IF TALENT:연모
					PRINTFORML %조교자는% 일순간 항의하는 것 같은 시선을 던졌지만, 곧 바로 바이브가 가져오는 쾌감에 녹아내리는 듯 \@ TALENT:체형 < 0 ? 사랑스러운 # 음란한 \@ 교성을 내기 시작했다…
				ELSE
					PRINTFORML %조교자는% 일순간 안심하는 것 같은 표정을 띠었지만, 곧 바로 바이브가 가져오는 쾌감에 녹아내리는 듯 \@ TALENT:체형 < 0 ? 사랑스러운 # 음란한 \@ 교성을 내기 시작했다…
				ENDIF
			ELSEIF TALENT:연모
				PRINTFORML %조교자는% 바이브가 가져오는 쾌감에 녹아내리는 듯 \@ TALENT:체형 < 0 ? 사랑스러운 # 음란한 \@ 교성을 내면서도, %CALLNAME:MASTER%의 고간에 뜨거운 시선을 던지기 시작했다…
			ELSEIF EXP:V경험 > 20
				PRINTFORML %조교자는% 바이브가 가져오는 쾌감에 녹아내리는 듯 \@ TALENT:체형 < 0 ? 사랑스러운 # 음란한 \@ 교성을 내고 있다…
			ELSE
				PRINTFORML %조교자는% 서투른 바이브의 자극에 몸을 비틀고 있다…
			ENDIF
	ELSE
		SIF ABL:V감각 >= 4 && TALENT:음란
			PRINT 허리를 들며 애원 하고 있는 
		PRINTFORM 자신의 
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%를 젖혀, 
		;윤활 LV5 이상으로 한층 더 추가
		SIF PALAM:윤활 >= PALAMLV:5
			PRINT 한껏 
		;윤활 LV3 이상으로 한층 더 추가
		SIF PALAM:윤활 >= PALAMLV:3
			PRINT 점액을 흘리며 
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 균열에 
		ELSE
			PRINT 보지에
		ENDIF
		PRINTL 바이브의 끝을 비비다가 천천히 삽입해 간다…
		PRINTFORM %조교자는%
		;처녀＋음란한가 창부
		IF TALENT:처녀 == 1 && TALENT:음란
			PRINT 자신이 처녀인 것을 잊어버린 것 같은 미소를 보이고 
		;처녀＋연모나 친애
		ELSEIF TALENT:처녀 == 1 || (TALENT:처녀 == -1) && TALENT:연모
			PRINTFORM 조금 외로운 것 같은 눈으로 %조교자를% 보고 
		;처녀＋음란
		ELSEIF TALENT:처녀 == 1 && TALENT:음란
			PRINT 처녀인 것도 상관없이, 허리를 흔들어 애원을 하고 
		;처녀＋일선을넘지않음
		ELSEIF TALENT:처녀 == 1 || (TALENT:처녀 == -1) && TALENT:일선을넘지않음
			PRINT 무엇인가를 단념한 표정으로 눈에 차오르는 눈물을 글썽이고 
		;처녀
		ELSEIF (TALENT:처녀 == 1 || TALENT:처녀 == -1)
			PRINT 흠칫흠칫 들썩이며, 상실의 공포에 무서워하고 
		;V경험 200이상＋음란한가 창부
		ELSEIF EXP:V경험 > 200 && TALENT:음란
			PRINT 삽입에 대한 기대만으로 부들부들 허리를 진동시키고 
		;V경험 150이상＋음란한가 창부
		ELSEIF EXP:V경험 > 150 && TALENT:음란
			PRINTFORM 한계까지 녹아내린 것 같은 눈으로 %조교자를% 응시하며, 스스로 음순을 문지르고 
		;V경험 120이상＋음란한가 창부
		ELSEIF EXP:V경험 > 120 && TALENT:음란
			PRINT 황홀한 표정으로 기다릴 수 없다는 듯 허리를 흔들고 
		;V경험 70이상＋음란한가 창부
		ELSEIF EXP:V경험 > 70 && TALENT:음란
			PRINTFORM 갖고 싶다는 듯이 눈물을 글썽이며 %CALLNAME:PLAYER%쪽을 보고 
		;V경험 40이상
		ELSEIF EXP:V경험 > 40
			PRINT 바이브가 스치는 감촉에 흥분해버린 한숨을 무심코 내뱉고 
		;V경험 20이상
		ELSEIF EXP:V경험 > 20
			PRINT 바이브가 스치는 감촉에 몸을 비틀며 애액을 흘리고 
		;그 이외
		ELSE
			PRINT 바이브가 스치는 감촉에 보지를 움찔거리고 
		ENDIF
		PRINTL 있다…
		PRINTFORM %조교자는% 자신의 
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는 
		PRINTFORML 성기에 바이브를 밀어넣어 왕복시켰다
	ENDIF
;-------------------------------------------------
;745, 애널바이브(위)
;-------------------------------------------------
@MESSAGE_COM745
	;해제시
	IF !TEQUIP:애널바이브
		PRINTFORML 애널로부터 애널바이브를 뽑았다
		PRINTFORM %조교자는% 
		;A경험 180이상＋음란한가 창부
		IF EXP:A경험 > 180 && TALENT:음란
			PRINTFORM 보지와 입으로부터 달콤한 액체를 흘리면서, %HIP(PLAYER)%를 계속 넓히고 
		;A경험 140이상＋음란한가 창부
		ELSEIF EXP:A경험 > 140 && TALENT:음란
			PRINT 촉촉한 눈으로 간절히 애원하며 엉덩이를 내밀고 
		;A경험 100이상＋음란한가 창부
		ELSEIF EXP:A경험 > 100 && TALENT:음란
			PRINTFORM 점액 투성이로 준비된 %HIP(PLAYER)%를 쑥 내밀고 
		;A경험 70이상＋음란한가 창부
		ELSEIF EXP:A경험 > 70 && TALENT:음란
			PRINTFORM 조금 아쉬운 듯이 %HIP(PLAYER)%를 흔들고 
		;A경험 40이상
		ELSEIF EXP:A경험 > 40
			PRINT 엉덩이도 입도 눈도, 어딘가 안심했다는 표정을 띠고 
		;A경험 20이상
		ELSEIF EXP:A경험 > 20
			PRINT 강제적인 배설감에 뺨을 붉게 물들이고 
		;그 이외
		ELSE
			PRINT 강렬한 자극에 괴로운 것 같은 표정으로 참고 
		ENDIF
		PRINTFORML 있다…
	ELSE
		PRINTFORM 자신의
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%를 젖혀, 
		PRINTFORML 애널에 애널바이브를 밀어넣었다…
		PRINTFORM %조교자는%
		;A경험 180이상＋음란한가 창부
		IF EXP:A경험 > 180 && TALENT:음란
			PRINTFORM 한계까지 녹은 표정으로, 스스로의 %HIP(PLAYER)%를 넓히고 
		;A경험 140이상＋음란한가 창부
		ELSEIF EXP:A경험 > 140 && TALENT:음란
			PRINT 삽입을 애타게 기다리는 듯한에 허리를 흔들고 
		;A경험 100이상＋음란한가 창부
		ELSEIF EXP:A경험 > 100 && TALENT:음란
			PRINTFORM 뺨을 붉혀 %HIP(PLAYER)%를 기대에 흔들고 
		;A경험 70이상＋음란한가 창부
		ELSEIF EXP:A경험 > 70 && TALENT:음란
			PRINT 좁은 구멍에게 줄 수 있는 바이브의 자극에 몸을 떨고 
		;A경험 40이상
		ELSEIF EXP:A경험 > 40
			PRINT 기대와 부정이 뒤섞이고 애원하는 것 같은 복잡한 표정을 하고 
		;A경험 20이상
		ELSEIF EXP:A경험 > 20
			PRINT 수줍음에 물든 얼굴을 숙이고 
		;그 이외
		ELSE
			PRINT 부정의 구멍을 범해지는 치욕과 굴욕감 투성이가 된 표정을 하고 
		ENDIF
		PRINTL 있다…
		PRINTFORM %CALLNAME:PLAYER%의 
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 되어있는 
		PRINTFORML 애널에 바이브를 꽂아 왕복시켰다
	ENDIF
;-------------------------------------------------
;746, 애널비즈(위)
;-------------------------------------------------
@MESSAGE_COM746
IF !TEQUIP:애널비즈
	PRINTFORML 애널로부터 애널비즈을 기세 좋게 뽑아 냈다…
ELSE
	PRINTFORML %조교자는% 애널에 애널비즈를 하나씩 삼키게 해 갔다…
ENDIF
;-------------------------------------------------
;747, 유두캡(위)
;-------------------------------------------------
@MESSAGE_COM747
PRINTFORM %조교자는% %NIPPLE_DESCRIPTION(PLAYER, 1)%
IF !TEQUIP:유두캡
	PRINTL 로부터 클립 로터를 떼어냈다
ELSE
	PRINTL 에 클립 로터를 붙였다
ENDIF
;-------------------------------------------------
;748, 착유기(위)
;-------------------------------------------------
@MESSAGE_COM748
;해제시
IF !TEQUIP:착유기
	PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%로부터 착유기를 떼어냈다
ELSE
	PRINTFORML %OPPAI_DESCRIPTION(PLAYER)%에 착유기를 붙였다
ENDIF
;-------------------------------------------------
;749, 유두로터(위)
;-------------------------------------------------
@MESSAGE_COM749
PRINTFORML %조교자는% %NIPPLE_DESCRIPTION(PLAYER, 1)%에 진동하는 로터를 꽉 눌렀다…

