
;-------------------------------------------------
;100, 스팬킹
;-------------------------------------------------
@MESSAGE_COM100
IF FLAG:70 == 1
	IF TEQUIP:50 == PLAYER
		PRINTFORM 시간을 멈춘%타겟을% 격렬하게 범하면서
	ELSEIF TEQUIP:51 == PLAYER
		PRINTFORM 시간을 멈춘%CALLNAME:TARGET%의 국화구멍을 격렬하게 범하면서

	ELSEIF TEQUIP:0 & 1 && (EQUIP:치마|| EQUIP:하반신윗도리 || EQUIP:원피스|| EQUIP:기모노)
		PRINTFORM 시간을 멈춘%CALLNAME:TARGET%의 %SHOW_BOTTOM(TARGET, 1)%를 넘기고
	ELSE
		PRINTFORM 시간을 멈춘%CALLNAME:TARGET%의
	ENDIF
	SIF EQUIP:TARGET:레오타드|| EQUIP:TARGET:보디슈트
		PRINTFORM %SHOW_하반신속옷(TARGET, 2)%에 싸였다
	PRINTFORML %HIP(TARGET)%를 손바닥에서 강하게 두드렸다
	PRINTFORML %CALLNAME:TARGET%의 엉덩이는 새빨갛게 부어 갔다…
ELSE
	IF TEQUIP:50 == PLAYER
		PRINTFORM %타겟을% 격렬하게 범하면서
	ELSEIF TEQUIP:51 == PLAYER
		PRINTFORM %CALLNAME:TARGET%의 국화구멍을 격렬하게 범하면서
	ELSEIF TEQUIP:0 & 1 && (EQUIP:치마|| EQUIP:하반신윗도리 || EQUIP:원피스|| EQUIP:기모노)
		PRINTFORM %CALLNAME:TARGET%의 %SHOW_BOTTOM(TARGET, 1)%를 넘기고
	ELSE
		PRINTFORM %CALLNAME:TARGET%의
	ENDIF
	SIF EQUIP:TARGET:레오타드|| EQUIP:TARGET:보디슈트
		PRINTFORM %SHOW_하반신속옷(TARGET, 2)%에 싸였다
	PRINTFORML %HIP(TARGET)%를 손바닥에서 강하게 두드렸다
	PRINTFORM 몇 번이나 몇 번이나 얻어맞고 있는 동안에,
	;남자＋마조끼 LV2 이상
	IF !(TALENT:2 & 1) && (ABL:마조끼 >= 2)
		PRINTFORML %CALLNAME:TARGET%의 고간은 딱딱해져 갔다…
	;마조끼 LV2 이상
	ELSEIF ABL:마조끼 >= 2 && TEQUIP:50 == -1
		PRINTFORML %CALLNAME:TARGET%의 고간은 자신의 애액으로 젖고 시작하고 있었다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 엉덩이는 새빨갛게 부어 갔다…
	ENDIF
ENDIF
;-------------------------------------------------
;101, 채찍
;-------------------------------------------------
@MESSAGE_COM101
PRINTFORM 채찍을 크게 치켜들고 마음껏 거절해 내리면, %CALLNAME:TARGET%의 피부에 새빨간 근육이 기본도 새겨져 갔다…
;-------------------------------------------------
;102, 초
;-------------------------------------------------
@MESSAGE_COM102
IF FLAG:70 == 1
	PRINTFORM %조교자는% 초에 불을 등불와 열기로우를, 시간을 그쳐라
	;밧줄 등으로 긴박중
	SIF TEQUIP:밧줄
		PRINT 밧줄로 묶인 
	PRINTFORM %CALLNAME:TARGET%의 
	;조교자가 새드
	IF TALENT:PLAYER:새드
		PRINT 민감한 부분을 집중적으로
	ELSE
		;(eraElona용 소질[알비노]의 대신 ), 고통에 약하다를 본다
		IF TALENT:통각 > 1
			PRINT 새하얀 맨살에 
		ELSE
			PRINT 몸 위에 
		ENDIF
	ENDIF
	PRINTL 떨어뜨렸다
ELSE
	PRINTFORM %조교자는% 초에 불을 등불와 열기로우를
	;밧줄 등으로 긴박중
	SIF TEQUIP:밧줄
		PRINT 긴박 되어 움직일 수 없다
	PRINTFORM %CALLNAME:TARGET%의
	;조교자가 새드
	IF TALENT:PLAYER:새드
		PRINT 민감한 부분을 집중적으로
	ELSE
		;(eraElona용 소질[알비노]의 대신 ), 고통에 약하다를 본다
		IF TALENT:통각 > 1
			PRINT 새하얀 맨살에
		ELSE
			PRINT 체 위에
		ENDIF
	ENDIF
	PRINTL 늘어뜨렸다
ENDIF
;-------------------------------------------------
;103, 바늘
;-------------------------------------------------
@MESSAGE_COM103
SIF FLAG:70 == 1
	PRINTFORM 시간을 멈추었다
PRINTFORM %CALLNAME:TARGET%의 유두나 클리토리스에
;조교자가 바늘질이나 새드
IF TALENT:PLAYER:바늘질 || TALENT:PLAYER:새드
	PRINTL 가차 없이바늘을 찔렀다…
ELSE
	PRINTL 바늘을 찔러 세웠다…
ENDIF
;-------------------------------------------------
;104, 눈가리개
;-------------------------------------------------
@MESSAGE_COM104
;해제시
IF !TEQUIP:눈가리개
	PRINTFORML %CALLNAME:TARGET%로부터 눈가리개를 떼어 주었다
ELSE
	PRINTFORML %CALLNAME:TARGET%에 눈가리개를 대게 했다
ENDIF
;-------------------------------------------------
;105, 밧줄
;-------------------------------------------------
@MESSAGE_COM105
;해제시
IF !TEQUIP:밧줄
	PRINTFORML %CALLNAME:TARGET%로부터 밧줄을 풀었다
ELSE
	PRINTFORML %타겟을% 밧줄로 묶었다
ENDIF
;-------------------------------------------------
;106, 볼개그
;-------------------------------------------------
@MESSAGE_COM106
;해제시
IF !TEQUIP:볼개그
	PRINTFORML %CALLNAME:TARGET%로부터 볼개그를 취해 주었다
ELSE
	PRINTFORML %CALLNAME:TARGET%에 볼개그를 감째 시켰다
ENDIF
;-------------------------------------------------
;107, 구속플레이
;-------------------------------------------------
@MESSAGE_COM107
;해제시
IF !TEQUIP:구속
	PRINTFORML %CALLNAME:TARGET%의 구속을 해제했다
ELSE
	PRINTFORML %CALLNAME:TARGET%의 구속을 시도했다
ENDIF
;-------------------------------------------------
;108, 젖스팬킹
;-------------------------------------------------
@MESSAGE_COM108
IF FLAG:70 == 1
	SIF FLAG:70 == 1
		PRINTFORM 시간을 멈추었다
	IF TEQUIP:50 == PLAYER
		PRINTFORM %타겟을% 격렬하게 범하면서
	ELSEIF TEQUIP:51 == PLAYER
		PRINTFORM %CALLNAME:TARGET%의 국화구멍을 격렬하게 범하면서
	ELSE
		PRINTFORM %CALLNAME:TARGET%의
	ENDIF
	IF TEQUIP:0 & 1 && EQUIP:기모노
		PRINTFORM %SHOW_TOP(TARGET, 1)%를은 만
	ELSEIF TEQUIP:0 & 1 && (EQUIP:원피스|| EQUIP:상반신윗도리1)
		PRINTFORM %SHOW_TOP(TARGET, 2)% 위로부터
	ELSEIF TEQUIP:0 & 1 && EQUIP:상반신윗도리2
		PRINTFORM %SHOW_TOP(TARGET, 1)%를은 만
	ELSE
	ENDIF
	SIF EQUIP:TARGET:레오타드|| EQUIP:TARGET:보디슈트
		PRINTFORM %SHOW_상반신속옷(TARGET, 2)%에 싸였다
	;작은 가슴인가 절벽
	IF TALENT:TARGET:가슴사이즈 < 0
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%의 %NIPPLE_DESCRIPTION(TARGET, 1)%
		PRINTL 를 강하고 손톱 연주했다…
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%
		PRINTL 를 튕기듯이 손바닥에서 두드렸다……
	ENDIF
	PRINTFORM 몇 번이나 몇 번이나 얻어맞고 있는 동안에,
	;남자＋마조끼 LV2 이상
	IF !(TALENT:성별 & 1) && (ABL:마조끼 >= 2)
		PRINTFORML %CALLNAME:TARGET%의 고간은 딱딱해져 갔다…
	;마조끼 LV2 이상
	ELSEIF ABL:마조끼 >= 2 && TEQUIP:50 == -1
		PRINTFORML %CALLNAME:TARGET%의 고간은 자신의 애액으로 젖고 시작하고 있었다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET)%는 새빨갛게 부어 갔다…
	ENDIF
ELSE
	IF TEQUIP:50 == PLAYER
		PRINTFORM %타겟을% 격렬하게 범하면서
	ELSEIF TEQUIP:51 == PLAYER
		PRINTFORM %CALLNAME:TARGET%의 국화구멍을 격렬하게 범하면서
	ELSE
		PRINTFORM %CALLNAME:TARGET%의
	ENDIF
	IF TEQUIP:0 & 1 && EQUIP:기모노
		PRINTFORM %SHOW_TOP(TARGET, 1)%를은 만
	ELSEIF TEQUIP:0 & 1 && (EQUIP:원피스|| EQUIP:상반신윗도리1)
		PRINTFORM %SHOW_TOP(TARGET, 2)% 위로부터
	ELSEIF TEQUIP:0 & 1 && EQUIP:상반신윗도리2
		PRINTFORM %SHOW_TOP(TARGET, 1)%를은 만
	ELSE
	ENDIF
	SIF EQUIP:TARGET:레오타드|| EQUIP:TARGET:보디슈트
		PRINTFORM %SHOW_상반신속옷(TARGET, 2)%에 싸였다
	;작은 가슴인가 절벽
	IF TALENT:TARGET:가슴사이즈 < 0
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%의 %NIPPLE_DESCRIPTION(TARGET, 1)%
		PRINTL 를 강하고 손톱 연주했다…
	ELSE
		PRINTFORM %OPPAI_DESCRIPTION(TARGET, 1)%
		PRINTL 를 튕기듯이 손바닥에서 두드렸다…
	ENDIF
	PRINTFORM 몇 번이나 몇 번이나 얻어맞고 있는 동안에,
	;남자＋마조끼 LV2 이상
	IF !(TALENT:성별 & 1) && (ABL:마조끼 >= 2)
		PRINTFORML %CALLNAME:TARGET%의 고간은 딱딱해져 갔다…
	;마조끼 LV2 이상
	ELSEIF ABL:마조끼 >= 2 && TEQUIP:50 == -1
		PRINTFORML %CALLNAME:TARGET%의 고간은 자신의 애액으로 젖고 시작하고 있었다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 %OPPAI_DESCRIPTION(TARGET)%는 새빨갛게 부어 갔다…
	ENDIF
ENDIF
;-------------------------------------------------
;141,배빵
;-------------------------------------------------
@MESSAGE_COM141
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 배에 주먹을 날렸다
	IF EXP:V확장경험 >= 20
		PRINTFORML %CALLNAME:TARGET%의 성기로부터는 꽃잎이 초과해, 뻥 벌려져 있는 질구의 고향은 자궁 입구가 보이고 있다
	ELSEIF EXP:V확장경험 >= 10
		PRINTFORML %CALLNAME:TARGET%의 질은 펼쳐진 채로 좀처럼 돌아오지 않는다
	ELSEIF EXP:V확장경험 >= 5
		PRINTFORML %CALLNAME:TARGET%의 질은 뻥 입을 열고 있다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 질은 천천히 벌렁거려면서 원래대로 돌아갔다
	ENDIF
ELSE
	PRINTFORML %CALLNAME:TARGET%의 배에 %CALLNAME:PLAYER%의 주먹이 정통으로 꽃혔다
	PRINTFORM 몇 번이나 몇 번이나 얻어맞고 있는 동안에,
	IF ABL:마조끼 >= 5
		PRINTFORML %CALLNAME:TARGET%의 표정에는 고통스러워 함과 동시에 쾌락또한 분명히 있었다
	ELSEIF ABL:마조끼 >= 3
		PRINTFORML %CALLNAME:TARGET%의 표정에 약간이나마 야릇한 느낌이 든다
	ELSEIF ABL:마조끼 >= 1
		PRINTFORML %CALLNAME:TARGET%의 표정은 고통으로 가득차 가고 있었다
	ELSE
		PRINTFORML %타겟은% 고통에 넋을 놓은채 그저 끝나기만을 기다리고 있었다
	ENDIF
ENDIF
;-------------------------------------------------
;142, 애널피스트
;-------------------------------------------------
@MESSAGE_COM142
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 항문을 주먹으로 써 돌렸다
	IF EXP:A확장경험 >= 20
		PRINTFORML %CALLNAME:TARGET%의 항문으로부터는 꽃잎이 초과해, 장안이 훤히 들여다 보이게 하고 있는
	ELSEIF EXP:A확장경험 >= 10
		PRINTFORML %CALLNAME:TARGET%의 항문은 펼쳐진 채로 좀처럼 돌아오지 않는다
	ELSEIF EXP:A확장경험 >= 5
		PRINTFORML %CALLNAME:TARGET%의 항문은 뻥 입을 열고 있다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 항문은 꽉과 입을 다물어 원래대로 돌아갔다
	ENDIF
ELSE
	PRINTFORML %CALLNAME:TARGET%의 항문은 %CALLNAME:PLAYER%의 주먹으로 써 돌려졌다
	IF ABL:마조끼 >= 5
		PRINTFORML %CALLNAME:TARGET%의 항문으로부터는 꽃잎이 초과해, 장안이 훤히 들여다 보이게 하고 있는
	ELSEIF ABL:마조끼 >= 3
		PRINTFORML %CALLNAME:TARGET%의 항문은 펼쳐진 채로 좀처럼 돌아오지 않는다
	ELSEIF ABL:마조끼 >= 1
		PRINTFORML %CALLNAME:TARGET%의 항문은 뻥 입을 열고 있다
	ELSE
		PRINTFORML %타겟은%, 자신의 항문을 손가락으로 펼쳐 황홀로 하고 있다
	ENDIF
ENDIF
;-------------------------------------------------
;143, 두 구멍 피스트
;-------------------------------------------------
@MESSAGE_COM143
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 질과 항문을 주먹으로 써 돌렸다
	IF EXP:V확장경험 >= 10 && EXP:A확장경험 >= 10
		PRINTFORML %CALLNAME:TARGET%의 질구와 항문은 뻥입을 열어 점액에 젖는 육 벽이 훤히 들여다 보이게 하고 있는…
	ELSE
		PRINTFORML %타겟은% 질내와 장내를 벌름거리면서 점액을 흘려 보내고 있다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:TARGET%의 질과 항문은 %CALLNAME:PLAYER%의 주먹으로 써 돌려졌다
	IF EXP:V확장경험 >= 10 && EXP:A확장경험 >= 10
		PRINTFORML %타겟은%, 완전히 펼쳐진 질구와 항문의 주위를 양손의 손가락으로 어루만지고 돌리고 있다
	ELSE
		PRINTFORML %타겟은% 양 다리를 벌리고 커어억 충분하고 하고 있어, 다 펼쳐진 질구와 항문으로부터 점액이 흘려 보내지고 있었다…
	ENDIF
ENDIF
;-------------------------------------------------
;144, 방뇨
;-------------------------------------------------
@MESSAGE_COM144
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈춘 채로 %CALLNAME:TARGET%의 요도구멍을 자극했다…
	PRINTFORML 시간을 제지당하고 인내를 할 수 없는 %타겟은%, 자극되는 대로 오줌싸개 해 버렸다…
;야외플레이중
ELSEIF TEQUIP:야외플레이
	PRINTFORML %타겟은% 수치와 굴욕감에 눈물을 머금으면서, 풀숲 중(안)에서 방뇨했다…
;수치플레이중
ELSEIF TEQUIP:수치플레이
	PRINTFORML %타겟은% 거울에 비치는 자신을 보지 않게 하면서 주저앉아 넣어 방뇨하기 시작했다…
;작은 체형
ELSEIF TALENT:체형 < 0
	PRINTFORML %타겟은% %CALLNAME:PLAYER%에 어린 아이와 같이 양 다리를 움켜 쥐고 거론되어 방뇨했다…
ELSE
	PRINTFORML %타겟은% 수치에 뺨을 붉히면서, 그 자리에 주저앉아 넣어 방뇨하기 시작했다…
ENDIF
;-------------------------------------------------
;146, 관장기＋플러그
;-------------------------------------------------
@MESSAGE_COM146
IF FLAG:70 == 1
	;해제시
	IF !TEQUIP:관장
		PRINTFORML 시간이 정지된%타겟은% 항문마개를 뽑아져 참을 수도 하지 못하고 장내의 오물을 항문으로부터 파종 가라앉혔다…
	ELSE
		PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 국화구멍에 관장액을 흘려 넣어, 마개를 했다…
	ENDIF
ELSE
	;해제시
	IF !TEQUIP:관장
		PRINTFORML %타겟은% 항문마개를 뽑아져 장내의 오물을 항문으로부터 파종 가라앉혔다
		;마조끼
		IF ABL:마조끼 == 0
			PRINTFORML %타겟은% 고민과 치욕의 표정을 띠고 있다
		ELSEIF ABL:마조끼 == 1
			PRINTFORML %타겟은% 비지땀을 띄워 아직도 아랫배에 남는 배설감에 견디고 있다
		ELSEIF ABL:마조끼 == 2
			PRINTFORML %타겟은% 부끄러워하면서도, 해방감에 안심했다 표정을 띠고 있다
		ELSEIF ABL:마조끼 == 3
			PRINTFORML %타겟은% 항문을 통과하는 배설물의 감각에 때때로 음란한 허덕여소리를 높이고 있다
		ELSEIF ABL:마조끼 == 4
			PRINTFORML %타겟은% 황홀한 표정을 띠며 배설감을 즐기고 있다
		ELSEIF ABL:마조끼 >= 5
			PRINTFORML %타겟은% 배설감과 치욕을 마음껏 맛보고 있다
			PRINTFORML %CALLNAME:TARGET%의 표정은 쾌감에 야무지지 못하게 느슨해져, 항문으로부터 오물내는 것과 동시에, 질구로부터도 애액을 뚝뚝하고 방울져 떨어지게 하고 있다
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 국화구멍에 관장액을 흘려 넣어, 마개를 했다
		;마조끼
		IF ABL:마조끼 == 0
			PRINTFORML %타겟은% 고민의 표정을 띠고 있다
		ELSEIF ABL:마조끼 == 1
			PRINTFORML %타겟은% 비지땀을 띄워 배설감에 견디고 있다
		ELSEIF ABL:마조끼 == 2
			PRINTFORML %타겟은% 괴로운 듯한 표정을 하면서도, 얼굴을 홍조 시키고 머뭇머뭇 엉덩이를 털고 있다
		ELSEIF ABL:마조끼 == 3
			PRINTFORML %타겟은%, 배설감에 시달리면서도, 때때로 황홀한 표정을 띠고 있다
		ELSEIF ABL:마조끼 == 4
			PRINTFORML %타겟은%, 황홀한 표정을 띠며 배설감과 복통을 즐기고 있다
		ELSEIF ABL:마조끼 >= 5
			PRINTFORML %타겟은%, 배설감을 마음껏 맛보고 있다
			PRINTFORML %CALLNAME:TARGET%의 표정은 쾌감에 야무지지 못하게 느슨해져, 그 입과 질구로부터는 뚝뚝하고 군침을 방울져 떨어지게 하고 있다
		ENDIF
	ENDIF
ENDIF
;-------------------------------------------------
;147, 확장벌룬
;-------------------------------------------------
@MESSAGE_COM147
IF FLAG:70 == 1
	;해제시
	IF !TEQUIP:확장벌룬
		PRINTFORML 시간을 멈춘%타겟이% 펼쳐진 국화구멍로부터 풍선을 뽑아 주었다…
		;A감각
		IF EXP:A확장경험 >= 20
			PRINTFORML %타겟이% 펼쳐진 항문으로부터 장안이 훤히 들여다 보이게 하고 있는
		ELSEIF EXP:A확장경험 >= 10
			PRINTFORML %CALLNAME:TARGET%의 항문은 펼쳐진 채로 좀처럼 돌아오지 않는 것 같다
		ELSEIF EXP:A확장경험 >= 5
			PRINTFORML %CALLNAME:TARGET%의 항문은 끄는 다하면서 천천히 닫아 갔다
		ELSE
			PRINTFORML %CALLNAME:TARGET%의 항문은 천천히 닫아 갔다
		ENDIF
	ELSE
		PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 국화구멍에 에어벌룬을 삽입해, 공기를 보냈다…
	ENDIF
ELSE
	;해제시
	IF !TEQUIP:확장벌룬
		PRINTFORML %타겟이% 펼쳐진 국화구멍로부터 풍선을 뽑아 주었다…
		;A감각
		IF ABL:A감각 == 0
			PRINTFORML %CALLNAME:TARGET%의 항문은 천천히 닫아 갔다
		ELSEIF ABL:A감각 == 1
			PRINTFORML %CALLNAME:TARGET%의 항문은 끄는 다하면서 천천히 닫아 갔다
		ELSEIF ABL:A감각 == 2
			PRINTFORML %CALLNAME:TARGET%의 항문은 뻥 입을 열고 있다
		ELSEIF ABL:A감각 == 3
			PRINTFORML %CALLNAME:TARGET%의 항문은 펼쳐진 채로 좀처럼 돌아오지 않는 것 같다
		ELSEIF ABL:A감각 == 4
			PRINTFORML %CALLNAME:TARGET%의 항문으로부터 장안이 훤히 들여다 보이게 하고 있는
		ELSEIF ABL:A감각 >= 5
			PRINTFORML %타겟은%, 자신의 항문을 손가락으로 펼쳐 황홀로 하고 있다
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 국화구멍에 에어벌룬을 삽입해, 공기를 보냈다…
	ENDIF
ENDIF
;-------------------------------------------------
;148, 애널전극
;-------------------------------------------------
@MESSAGE_COM148
;해제시
IF !TEQUIP:애널전극
	PRINTFORML %CALLNAME:TARGET%의 아누스로부터 전극을 뽑아 주었다…
ELSE
	PRINTFORML %CALLNAME:TARGET%의 국화구멍에 전극을 삽입해 스위치를 넣었다…
ENDIF