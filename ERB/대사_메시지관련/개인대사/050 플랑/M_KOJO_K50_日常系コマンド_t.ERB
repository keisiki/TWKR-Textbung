;※※※※※※※※※※※※※※※※※※※※※※※※※
;일상계 커맨드
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;300, 회화
;==================================================
@M_KOJO_MESSAGE_COM_K50_300
;커맨드 공통 메시지(TRAIN_MESSAGE) 
;삭제하면(자) 「~이야기를 했다.」라고 가가 사라집니다
CALL TRAIN_MESSAGE
;말메인 호출
CALL M_KOJO_MESSAGE_COM_K50_300_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_300_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;마이나스가 되어도 괜찮아라고 생각한다
	;IF !CFLAG:1502 && CFLAG:신뢰도 >= 500 && ABL:교양 > 2 && MONEY >= 300000
	;IF !CFLAG:1502 && CFLAG:신뢰도 >= 500 && ABL:교양 > 2
	;	TRYCALLFORM M_KOJO_MESSAGE_COM_K50_진학
	;	RETURN 1
	;ENDIF
	
	IF 텃밭가능(CFLAG:MASTER:현재위치) && !GETBIT (TCVAR:350, 1)
		SETBIT TCVAR:350, 1
		;SELECTCASE PLANT_TYPE_0
		SELECTCASE PLANT_TYPE
			CASE 151
				PRINTFORMW 「아, 양배추밭.」
				PRINTFORML 「가정 채소밭을 이렇게 해버린다는 %UNICODE(0x2661)%」
				PRINTFORMW 「오라버니, 아기 갖고 싶은거야?」
				IF CFLAG:생해금 <= 2 && TALENT:연인
					IF CFLAG:16:신뢰도 < 500
						PRINTFORML 「그렇지만 누님(언니)가 안된다고」
					ELSE
						PRINTFORML 
						PRINTFORML 「좋아」
						PRINTFORMW 「하지만 %CALLNAME:TARGET%도 오라버니의 아기 갖고 싶은 것」
						PRINTFORMW 「우후훗!이봐 오라버니!양배추밭에서 잡아!」
						CFLAG:생해금 = 2
					ENDIF
				ELSE
					PRINTDATAL
						DATALIST
							DATAFORM 「%CALLNAME:TARGET%에게는 아직 빠르다고 생각해」
							;에 어조를 맞댐쳇 호칭
							DATAFORM 「팥찰밥이 오지 않으면 무리야는 쳇이 말했었는걸」
						ENDLIST
						DATALIST
							DATAFORM 「오라버니, 맡기고」
							DATAFORM 「억지로는 싫어요? 우후후♪」
						ENDLIST
						DATALIST
							DATAFORM 「욕구·해·있고·의··이군요? ·형·양?」
							DATAFORM 「%CALLNAME:TARGET%에는 전망이니까」
							DATAFORM 「우후후. 아직 안 돼~ %UNICODE(0x2661)%」
						ENDLIST
					ENDDATA
				ENDIF
			CASE 163
				PRINTFORML 「누님(언니)도 옛날에는  푸르렀던 것일까 해들 」
			CASE 164
				PRINTFORML 「수박씨~수박씨~쑥갓-수박씨∼」
				PRINTFORML 「수박 산미응 어루만지고 먹도록 해요!」
				PRINTFORMDW %타겟은%에 확 하며 웃었다
			CASE 177
				PRINTFORML 「이 포도가 잡히면(자) 와인을 만듭시다」
				PRINTFORML 「%CALLNAME:TARGET%군요. 포도 밟아 해 보고 싶었어」
				PRINTFORML 「우후후. 기다려지구나♪」
				PRINTFORMD %타겟은% 치마를 걷어 올리고
				
				IF TALENT:MASTER:마조
					PRINTFORMDL 와 %CALLNAME:MASTER%의 고간에 시선을 보내면서 제자리 걸음을 하는 행동을 했다
				ELSE
					PRINTFORMDL 면서 떠드는 게 있다
				ENDIF
			CASE 187
				PRINTFORML 「유리∼♪유리 유리∼♪」
				PRINTFORML 「응 응 오라버니. 유리의 뿌리는 먹을 수 있어!」
				SIF TALENT:MASTER:성별 == 3
					PRINTFORMDL %CALLNAME:TARGET%의 %CALLNAME:MASTER%의 고간을 보는 눈이 이상하다…
			CASE 188
				IF TCVAR:미약
					PRINTFORML 「응 응 오라버니」
					PRINTFORML 「당근에 레몬을 걸면(자) 미약이 된대!그렇지만 미약 이라고에?」
				ELSE
					PRINTFORML 「당근씨♪무우씨♪표고버섯씨♪우엉씨♪구멍~의 하늘 있었다응감색씨♪」
					PRINTFORML 「비비 꼬고-가 다닌 후우-나무♪」
				ENDIF
		ENDSELECT
	ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	
	IF TFLAG:193 == 1
		PRINTFORMDW %타겟은% %CALLNAME:MASTER%의 말에 음음 크게 수긍하고 있다
		RETURN 1
	ELSEIF TFLAG:193 == -1
		PRINTFORMDW %타겟은% %CALLNAME:MASTER%의 말에 작게 고개를 갸웃하고 있다
		RETURN 1
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;여기에 외교 문서 있어 주세요

ENDIF
RETURN 1
;==================================================
;301, 차를 넣는
;==================================================
@M_KOJO_MESSAGE_COM_K50_301
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_301_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_301_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	
	IF TFLAG:193 == -1
		PRINTFORMW 「열!」
		PRINTFORMDW %CALLNAME:TARGET%의 혀에는 뜨거웠던 것 같다
		PRINTFORMW 「혀가 얼얼 한다」
		RETURN 1
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;302, 스킨십
;==================================================
@M_KOJO_MESSAGE_COM_K50_302
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_302_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_302_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	SELECTCASE CFLAG:300
		CASE 13,24,39
			PRINTFORML 「아 목욕으로 꽉 꽉 큐-♪」
			PRINTFORML 「윙윙 해-응만큼♪」
	ENDSELECT
	;연모 후기
	IF TALENT:연모
		IF !RAND:10
			PRINTFORMDW %타겟은% %CALLNAME:MASTER%의 수를 잡으면(자) 빙글과 일회전 했다
			PRINTFORML 「오라버니」
			PRINTFORMDW %CALLNAME:TARGET%의 진지한 시선이 %마스터를% 관철한다!
			PRINTFORMDW 깨닫으면%CALLNAME:TARGET%의 팔이 %CALLNAME:MASTER%의 허리를 안고 있다…
			PRINTFORML 「후훗」
			PRINTFORMDW 기습에 보이는 %CALLNAME:TARGET%의 고귀한 측면으로 %CALLNAME:MASTER%의 고동은 고되었다…
			PRINTFORML 
			PRINTFORML 「댄스 옛 개도 누님(언니)가 숙녀역으로 %타겟은% 신사역 바보 리나의」
			PRINTFORMW 「누님(언니)뿐 간사하다」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;303, 사과한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_303
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_303_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_303_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「네? 지금 뭐든지 한다 라고 했어?」
		PRINTFORML 「오라버니」
		PRINTFORML 「지금 뭐든지 한다 라고 했어요? 우후후 %UNICODE(0x2661)%」
		CFLAG:MASTER:우후후 = 2
		CFLAG:우후후 = 2
		RETURN 1
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;304, 일을 돕는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_304
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_304_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_304_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;고추 붙어 있다
	PRINTFORML 「저기 넘지 않아 귀엽다!」
	IF TALENT:TARGET:2 & 2
		PRINTFORMW 「그렇지만 %타겟은% 오라버니의 「고양이」에도 밀크 주고 싶다 ♪」
		PRINTFORMW %마스터는% 방의 안쪽으로 끌여들여지고 밀어 넘어뜨려졌다
		CFLAG:MASTER:우후후 = 2
		CFLAG:우후후 = 2
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;305, 무릎베개받는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_305
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_305_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_305_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;306, 배를 어루만진다
;==================================================
@M_KOJO_MESSAGE_COM_K50_306
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_306_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_306_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;307, 뺨을 집는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_307
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_307_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_307_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;308, 뺨을 쿡쿡 찌른다
;==================================================
@M_KOJO_MESSAGE_COM_K50_308
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_308_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_308_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	;IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	PRINTFORML 「!」
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	;ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;309, 머리를 어루만진다
;==================================================
@M_KOJO_MESSAGE_COM_K50_309
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_309_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_309_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「오라버니!플랑은 레이디니까 아이 취급하면 안되어!」
		PRINTFORMDW 플랑에 화가 나 버렸다……
	;연모 후기
	ELSEIF TALENT:연모
		IF !RAND:3
			PRINTFORML 「하앗……」
			PRINTFORMDW %CALLNAME:TARGET%의 깃털 입는이라고 로 하고 있다……
		ELSEIF !RAND:2
			PRINTFORML 「오라버니……」
		ELSE
			PRINTFORMW 「오라버니에 스윽스윽 되어 버렸다」
			PRINTFORMW 「무리 해 되어 버렸다」
			PRINTFORMW 「너무해요……%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			PRINTFORML 「무리 해 스윽스윽 안 돼!」
		ELSEIF !RAND:2
			PRINTFORML 「오라버니……」
			PRINTFORML 「안 돼요 ……안 돼요 ……」
			PRINTFORMDW %타겟은% 눈물짓고 있다……
		ELSE
			PRINTFORML 「오라버니 스윽스윽 하면 안 돼 …」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			PRINTFORML 「이나!」
			PRINTFORMDW %타겟은% 카리스마 흘러넘칠 자세로 %CALLNAME:MASTER%의 손바닥을 막고 있다
		ELSEIF !RAND:2
			PRINTFORML 「이나야 ……명란젓 오면 야 ……」
		ELSE
			PRINTFORML 「안 돼요 …안 돼요 …」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;310, 엉덩이를 어루만진다
;==================================================
@M_KOJO_MESSAGE_COM_K50_310
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_310_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_310_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;311, 달라붙는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_311
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_311_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_311_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	PRINTFORML 「에헤헤―♪물컹 물컹―♪」
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;312, 키스 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_312
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_312_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_312_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	IF FIRSTTIME()
		PRINTFORML 「오라버니.」
		PRINTFORMDW %타겟은% 진지한 표정으로 %마스터를% 파악했다
		PRINTFORMW 「쪽 하면(자) 아기가 생겨버리는 일이 있어」
		PRINTFORMW 「간단하게 하면 안되어?」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			PRINTFORML 「오라버니. %CALLNAME:TARGET%의 자지가 쪽 해 %UNICODE(0x2661)%라고 말해」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;임신중
	IF TALENT:임신
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			PRINTFORML 「후아…%UNICODE(0x2661)%」
			PRINTFORMW 「오라버니…%CALLNAME:TARGET%, 아기 할 수 있어버린다…%UNICODE(0x2661)%」
		;ELSEIF !RAND:2
		ELSE
			PRINTFORMW 「%CALLNAME:TARGET%. %CALLNAME:TARGET%마마가 되어 버려 …%UNICODE(0x2661)%」
		ENDIF
		RETURN 1
	;사모 중기
	;ELSEIF TALENT:사모
	;	IF !RAND:3
	;		;PRINTFORML 「」
	;	ELSEIF !RAND:2
	;		;PRINTFORML 「」
	;	ELSE
	;		;PRINTFORML 「」
	;	ENDIF
	;	RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			PRINTFORML 「오라버니…쭙 쪽 하면 안 돼요 …」
		ELSEIF !RAND:2
			PRINTFORMW 「%CALLNAME:TARGET%. %CALLNAME:TARGET%마마가 되어 버려 …」
		ELSE
			PRINTFORMW 「쭙 쪽 하면(자) 할 수 있어버리는…아기 할 수 있어버린다…」
		ENDIF
		RETURN 1
	ENDIF
ENDIF
RETURN 1
;==================================================
;313, 가슴 애무
;==================================================
@M_KOJO_MESSAGE_COM_K50_313
;CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_313_1
CALL TRAIN_MESSAGE
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_313_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	IF TIME > 360 && TIME > 1020
		PRINTFORML 「오라버니. 오점과님으로 보여져버린다? 안 돼?」
		PRINTFORMDW %타겟은% 음란한 일은 밤까지 기다렸으면 좋겠다고 말해 왔다.
		PRINTFORML 
		PRINTFORML 「안 돼요 」
		PRINTFORML 「안 돼요 」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;314, 애널 애무
;==================================================
@M_KOJO_MESSAGE_COM_K50_314
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_314_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_314_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;315, 구리 애무
;==================================================
@M_KOJO_MESSAGE_COM_K50_315
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_315_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_315_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;316, 손가락삽입
;==================================================
@M_KOJO_MESSAGE_COM_K50_316
CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_316_1
CALL M_KOJO_MESSAGE_COM_K50_313_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_316_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;317, V허리를 거절한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_317
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_317_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_317_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;318, A허리를 거절한다
;==================================================
;@M_KOJO_MESSAGE_COM_K50_318
;CALL TRAIN_MESSAGE
;CALL M_KOJO_MESSAGE_COM_K50_318_1
;RETURN RESULT

;@M_KOJO_MESSAGE_COM_K50_318_1
;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
;LOCAL = 0
;-------------------------------------------------
;IF LOCAL

;ENDIF
;RETURN 1
;==================================================
;330, 치마 걷어 올리기
;==================================================
@M_KOJO_MESSAGE_COM_K50_330
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_330_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_330_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
	;어린 여자아이의 진짜 울음. 귀염성을 내지 않게
		PRINTFORMW 「네……」
		PRINTFORMDW %타겟이% 어안이 벙벙한 표정이 순식간에 무너져 간다…
		PRINTFORMW 「이식하고 ……」
		PRINTFORML 「”아””--!」
		PRINTFORMW 「”에”있고”””가 괴롭힌”--!」
		PRINTFORMW 「아””--!아”---!」
	;범해지는 숫처녀와 같이 진지하게
	ELSE
		PRINTFORMW 「그만두어!」
		PRINTFORMW 「오라버니 그만두어!」
		PRINTFORMW 「그만두어 오라버니 그만두어!」
		PRINTFORMW 「그만두어……」
		PRINTFORMDW %CALLNAME:TARGET%의 뺨을 굵은 물방울이 탄다…
		RETURN 1
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;331, 차를 넣는(약넣은)
;==================================================
@M_KOJO_MESSAGE_COM_K50_331
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_331_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_331_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF FIRSTTIME(SELECTCOM)
		PRINTFORML 「이 맛, %CALLNAME:15%의 맛이군요」
		PRINTFORML 「제대로 했군요 오라버니」
		PRINTFORML 「대단한 대단하다」
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;350, 밀어 넘어뜨린다(시간시 장난 한다
;TFLAG:194 (0=성공 1=명째가 있다　2=야외는 싫어 입니다만　3=많은 사람 플레이는 용서　4=합의가 있어도 무드가 충분하지 않다)
;TCVAR:껴안고첫회　1=보통으로 합의 Get　2=먼저 키스 정도 해라　3=미약or발정에는 이길 수 없었어요 …　4=분한…각인조차 없으면 히″쿠히″군
;5=억지로 재촉당한다고 거절할 수 없는(소질 겁쟁이 소유 전용) 6=반드시 싫지만은 않다　7=안 돼　8=논외
;==================================================
@M_KOJO_MESSAGE_COM_K50_350
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_350_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_350_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;351, 데리고 나간다
;==================================================
@M_KOJO_MESSAGE_COM_K50_351
;데리고 나가에는 TRAIN_MESSAGE가 없다
CALL M_KOJO_MESSAGE_COM_K50_351_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_351_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF FIRSTTIME()
		PRINTFORML 「어떻게 된거야 오라버니?」
		PRINTFORML 「……혹시」
		PRINTFORML 「혼자서 화장실에 갈 수 없는거야?」
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;352, 고백한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_352
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_352_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_352_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;380, 프로포즈
;==================================================
@M_KOJO_MESSAGE_COM_K50_380
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_380_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_380_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF TFLAG:193 == 1
	    PRINTFORMW ――그것은 지금보다 미래의 시간.
    PRINTFORMW 홍마관의 한 방.
    PRINTFORMW 거기에 %CALLNAME:MASTER%의 일가가 살고 있다.
    PRINTFORMW 「자─아, 이리 와 이리 와」
    PRINTFORMW 어제, 처음으로 걸을 수 있게 된 갓난아기가, 모친을 목표로 해 아장아장 걸어 간다.
    PRINTFORMW 「도─착♪」
    PRINTFORMW 플랑은 자신의 사랑하는 아이를 안아 올려 사랑스러운 듯 뺨 문대기를 했다.
    PRINTFORMW 「이것 봐, 벌써 완전히 걸음마 잘해」
    PRINTFORMW 그것에 맞추어, 갓난아기도 웃는다.
    PRINTFORMW %마스터는% 그 흐뭇해지는 광경에 눈을 가늘게 하며 바라보고 있다.
    PRINTFORMW 「자─아, 파파가 있는 곳까지는 갈 수 있을까?」
    PRINTFORMW 손 놓아진 갓난아기는 %CALLNAME:MASTER%의 쪽으로 걷기 시작한다.
    PRINTFORMW 아장아장 오른쪽에 왼쪽에, 그것은 가는 곳을 불안하게 하는 걸음 방법으로, 
    PRINTFORMW %마스터는%, 힘내라, 라고 중얼거렸다.
    PRINTFORMW 그러나 그 도중에 갓난아기는 주저 앉아… 울기 시작해버렸다.
    PRINTFORMW 「아아, 자 자 괜찮아?」
    PRINTFORMW %CALLNAME:MASTER%이 일어나는 것 보다 빠르게 플랑은 갓난아기를 안아 올린다.
    PRINTFORMW %마스터는% 답답하게 허리를 내려 앉아, 가족의 모습을 쳐다보면서 조금 전까지 했던 형수와의 회화를 생각해 내고 있었다.
    PRINTFORMW 「그래서, 어떻게 하기로 정했다고?」
    PRINTFORMW 「몇 번이나 말하지만 당신이 결심만 하면 젊음은 되찾을 수 있을 거고, 몇 십 배의 수명도 얻을 수 있어」
    PRINTFORMW 「뭐어 혼혈이 되니깐 말이지, 영원은 보증할 수 없지만 그 아이가 충분히 성장할 때까지는 지켜볼 수는 있는 거겠지」
    PRINTFORMW 「다행히 우리집에는 지식인도 있고 말이야? 뭐 어렵게 생각하지 않아도 어떻게든 되지 않겠어?」
    PRINTFORMW 최초야 말로, 그 권유에는 당황했다.
    PRINTFORMW 사람으로서 죽음을 취할 것인가, 플랑과의 영원과 어느쪽을 선택하냐고 말하면, 헤매는 건 당연하다.
    PRINTFORMW 그러나 결론은 머지않아 나왔다.
    PRINTFORMW 생각해보니, 자신은 흡혈귀가 되어 있었던 것이다.
    PRINTFORMW 자신의 몸이 다른 것이 된 이질감에 공포도 느꼈다.
    PRINTFORMW 그러나 그 감각에도 익숙해져, 지금에는 보다 순수한 흡혈귀에 가까워지려 하고 있다.
    PRINTFORMW 「자─아 울지 말아야지. 함께 놀아줄 테니깐」
    PRINTFORMW 상냥하게 어린아이를 달래는 플랑.
    PRINTFORMW 그 모습에, 무심코 얼굴이 풀려 버린다.
    PRINTFORMW 지금 이 광경을 보면, 자신을 옳았던 것이라고 확신할 수 있다.
    PRINTFORMW 「아기도 같이, 세 명이서 놀자아? 응?」
    PRINTFORMW 「우후후」
    PRINTFORMW 자, 이제부터는 무엇을 할까.
    WAIT 

	ENDIF
ENDIF
RETURN 1
;==================================================
;353, 장난 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_353
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_353_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_353_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;354, 동침 하는(상대는 일어나 이불에 부르는 느낌) 개시측
;==================================================
@M_KOJO_MESSAGE_COM_K50_354
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_354_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_354_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;355, 때 멈추고(지문의 개변 정도로 두어야 함
;==================================================
@M_KOJO_MESSAGE_COM_K50_355
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_355_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_355_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;360, 몸을 맡긴다
;==================================================
@M_KOJO_MESSAGE_COM_K50_360
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_360_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_360_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;363, 읽어 들려주고([유치]소유)
;==================================================
@M_KOJO_MESSAGE_COM_K50_363
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_363_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_363_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;364, 콩장작(스이카, 유기, 스칼렛 자매용
;==================================================
@M_KOJO_MESSAGE_COM_K50_364
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_364_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_364_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;365, 모리야신사에 이동(사나에, 카나코, 스와코용
;==================================================
@M_KOJO_MESSAGE_COM_K50_365
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_365_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_365_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;402, 취침(동침 때)
;==================================================
@M_KOJO_MESSAGE_COM_K50_402
;취침에 TRAIN_MESSAGE는 없다
CALL M_KOJO_MESSAGE_COM_K50_402_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_402_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;403, 휴식
;==================================================
@M_KOJO_MESSAGE_COM_K50_403
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_403_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_403_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;405, 나간다(동행시 데이트에 나간다
;==================================================
@M_KOJO_MESSAGE_COM_K50_405
;나가는에 TRAIN_MESSAGE는 없다
CALL M_KOJO_MESSAGE_COM_K50_405_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_405_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;410, 청소
;==================================================
@M_KOJO_MESSAGE_COM_K50_410
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_410_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_410_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;411, 전투훈련
;==================================================
@M_KOJO_MESSAGE_COM_K50_411
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_411_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_411_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;412, 공부
;==================================================
@M_KOJO_MESSAGE_COM_K50_412
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_412_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_412_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;413, 요리
;==================================================
@M_KOJO_MESSAGE_COM_K50_413
;요리에 TRAIN_MESSAGE는 없다
CALL M_KOJO_MESSAGE_COM_K50_413_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_413_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;처음
	;기입 체크
	LOCAL:1 = 1
	;-------------------------------------------------
	IF LOCAL:1 && FIRSTTIME(SELECTCOM)
		;흡혈귀의 성 지식
		PRINTFORML 「아얏!」
		PRINTFORMDW 요리중, %타겟은% 손가락을 잘라 버렸다
		PRINTFORML 
		PRINTFORMDW %마스터가% 상처를 들이마시면(자)%타겟은% 허둥지둥 당황하기 시작했다
		PRINTFORML 「오, 오라버니?」
		PRINTFORMW 「%타겟은% 흡혈귀이니까 그, 흡혈은, 번식 행위야?」
		PRINTFORMW 「이나다 어떻게 하지. %CALLNAME:TARGET%시집가기 앞인데, 오라버니에 번식되어 버렸다」
		PRINTFORML 「누님(언니) 라도 된 적 없는데 여겨진 적 없는데」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;414, 식사
;==================================================
@M_KOJO_MESSAGE_COM_K50_414
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_414_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_414_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	IF TASTE_REACTION("매운")
		PRINTFORMDL 스칼렛 컬러의 요리에 %타겟은% 식은 땀을 흘리고 있다…
		PRINTFORMW 「이, 있잖아, 오라버니」
		PRINTFORMW 「괴로운 것 같구나?」
		PRINTFORMW 「명란젓이 아니지요?」
		PRINTFORMDW 여러분들등 여기에서는 없는 것을 확실하게 약속 하면(자)%타겟은% 조심조심요리를 입에 옮겼다…
		PRINTFORML 구!
		PRINTFORML 「으로부터!」
		;시간정지 이라마치오의 추천(전진)
		PRINTFORMDL %CALLNAME:TARGET%―！와 혀를 쑥 내밀었다…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("고추냉이")
;		PRINTFORMDW %마스터는% 임신하고 있는 %CALLNAME:TARGET%에 시큼한 것을 주기로 했다…
		PRINTFORML 「응∼~!」
		PRINTFORML  「울퉁불퉁 한 고추냉이씨가 %CALLNAME:TARGET%의 코의 안쪽을 쿡쿡은 하고 있는!」
		RETURN 1
	ENDIF
	;임신중
	IF TALENT:임신
		IF TASTE_REACTION("시큼한")
			PRINTFORMDW %마스터는% 임신하고 있는 %CALLNAME:TARGET%에 시큼한 것을 주기로 했다…
			PRINTFORML 「%타겟이% 좋아하는 맛내기?」
			PRINTFORML  「아!시큼하고 맛있다♪」
			SIF TALENT:처녀 == -1
				PRINTFORMD 자신이 몸 깃들이고 있으면(자) 모르는 그녀의 몸은 스스로 감귤을 요구해
			PRINTFORML %타겟은% 행복한 표정으로 %CALLNAME:MASTER%의 손수 만든 요리를 뺨 팽팽해져 있는…
		ENDIF
		RETURN 1
	ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;415, 식사를 대접한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_415
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_415_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_415_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	IF TASTE_REACTION("매운")
		PRINTFORMDL 스칼렛 컬러의 요리에 %타겟은% 식은 땀을 흘리고 있다…
		PRINTFORMW 「이, 있잖아, 오라버니」
		PRINTFORMW 「괴로운 것 같구나?」
		PRINTFORMW 「명란젓이 아니지요?」
		PRINTFORMDW 여러분들등 여기에서는 없는 것을 확실하게 약속 하면(자)%타겟은% 조심조심요리를 입에 옮겼다…
		PRINTFORML 구!
		PRINTFORML 「으로부터!」
		;시간정지 이라마치오의 추천(전진)
		PRINTFORMDL %CALLNAME:TARGET%―！와 혀를 쑥 내밀었다…
		RETURN 1
	ENDIF
	
	IF TASTE_REACTION("단")
		IF TALENT:연인
			PRINTFORML (이 맛…)
			PRINTFORMDW %타겟은% 맛내기의 메시지를 받아들이면(자)%CALLNAME:MASTER%의 귓전으로 작게 속삭였다…
			PRINTFORML 「좋아♪둘이서가 되면 한 잔 여승 여승 %UNICODE(0x2661)%합시다」
		ELSE
			PRINTFORMDW 「밥 달다!」
		ENDIF
		RETURN 1
	ENDIF
	IF TASTE_REACTION("고추냉이")
		PRINTFORMDW %마스터는% 임신하고 있는 %CALLNAME:TARGET%에 시큼한 것을 주기로 했다…
		PRINTFORML 「응∼~!」
		PRINTFORML  「울퉁불퉁 한 고추냉이씨가 %CALLNAME:TARGET%의 코의 안쪽을 쿡쿡은 하고 있는!」
		RETURN 1
	ENDIF
	;임신중
	IF TALENT:임신
		IF TASTE_REACTION("시큼한")
			PRINTFORMDW %마스터는% 임신하고 있는 %CALLNAME:TARGET%에 시큼한 것을 주기로 했다…
			PRINTFORML 「%타겟이% 좋아하는 맛내기?」
			PRINTFORML  「아!시큼하고 맛있다♪」
			SIF TALENT:처녀 == -1
				PRINTFORMD 자신이 몸 깃들이고 있으면(자) 모르는 그녀의 몸은 스스로 감귤을 요구해
			PRINTFORML %타겟은% 행복한 표정으로 %CALLNAME:MASTER%의 손수 만든 요리를 뺨 팽팽해져 있는…
		ENDIF
		RETURN 1
	ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;416, 연주한다(동결중?
;==================================================
@M_KOJO_MESSAGE_COM_K50_416
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_416_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_416_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;417, 낮잠을 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_417
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_417_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_417_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;421, 신불에게 발원 함 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_421
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_421_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_421_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;431, 욕실로 느긋하게 쉰다
;==================================================
@M_KOJO_MESSAGE_COM_K50_431
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_431_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_431_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF TALENT:기분 == -1
		;IF !RAND:3
			PRINTFORMDL %타겟은% 목욕통에 입까지 잠겨 거품을 내고 있다…
		;ELSEIF TALENT:기분 == -1 && !RAND:2
			;예의 범절이 되지 않은 것과 어린 일은 별개  고로 파기
		;	;PRINTFORMDL %타겟은% 목욕통을 잡아 발버둥치고 있다…
		;아무것도 하지 않는 분기
		;ELSE
		;ENDIF
		RETURN 1
	ENDIF
	
	IF !RAND:10
		PRINTFORML 「오라버니!오라버니!집오리 다!」
		PRINTFORML 「팔아~!」
		PRINTFORMDW %타겟은% 문지르면서 아히르 쨩~을 꽉 눌러 왔다…
	ELSEIF !RAND:9
		PRINTFORMDW %타겟은% 수건으로 공을 만들어 가라앉혀 만들어 가라앉히고 있다…
		PRINTFORML 「뒤룩뒤룩~!」
	ELSEIF !RAND:8
		PRINTFORMW 「오라버니 오라버니!%타겟은%요. 더운 물에 잠기자면」
		PRINTFORML 「증가해!」
		PRINTFORMW 「증가해!」
		PRINTFORML 「머리를 감고 있는 때에 뒤를 지키고 있어 주어!」
		PRINTFORMW 「머리를 감고 있는 때에 뒤를 지키고 있어 주어!」
		PRINTFORML 「저기―？」
		PRINTFORMW 「이군요―？」
	ELSEIF !RAND:7
		
	ELSE
	ENDIF
	
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;440, 기다린다
;==================================================
@M_KOJO_MESSAGE_COM_K50_440
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_440_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_440_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF !RAND:3
		PRINTFORML 「아-구 단지-에 단지―♪에 천민인가 어떨까 먹어 봐라―」
		PRINTFORML 「우걱우걱」
		PRINTFORML 「 아직 익지 않는다」
		PRINTFORMDW %타겟은% %CALLNAME:MASTER%의 옆에서 짬을 때우고 있다
	ELSEIF !RAND:2
		PRINTFORML 「저기-오라버니?」
		PRINTFORMW 「………」
		PRINTFORML 
		PRINTFORMW 「레이디를 싫증시키는 것은 아니어요」
		PRINTFORML 「키릭!」
	ELSE
		PRINTFORML 「저기-군요-오라버니.」
		PRINTFORMW 「놉시다?」
		PRINTFORMW %CALLNAME:TARGET%입는 가는 실당신의 팔을 이끌어 왔다
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;441, 수업을 받는다(케이네 전용?
;==================================================
@M_KOJO_MESSAGE_COM_K50_441
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_441_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_441_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	;IF CFLAG:우후후 == 2
	;	;고추 붙어 있다
	;	IF TALENT:TARGET:2 & 2
	;		PRINTFORML 「선키-있고!바나나가 들어갔습니다∼！」
	;		PRINTFORMDW %CALLNAME:TARGET%의 바나나에 관철해지고면서 %마스터는% 짐승과 같이 소리를 높이고 있다…
	;	;고추 붙지 않다
	;	ELSE
	;		PRINTFORML 「선키-있고!바나나는 간식에 들어갑니까?」
	;		PRINTFORMDW %타겟은% 이때라는 듯이 질문했다
	;	ENDIF
	;	RETURN 1
	;ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;442, 벌채한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_442
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_442_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_442_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;데이트계 커맨드
;※※※※※※※※※※※※※※※※※※※※※※※※※
[SKIPSTART]
;현상미실장
;==================================================
;600, 데이트 회화
;==================================================
@M_KOJO_MESSAGE_COM_K50_600
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_600_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_600_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;601, 데이트 스킨십
;==================================================
@M_KOJO_MESSAGE_COM_K50_601
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_601_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_601_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;602, 데이트 키스
;==================================================
@M_KOJO_MESSAGE_COM_K50_602
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_602_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_602_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
[SKIPEND]
;==================================================
;603, 손을 잡는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_603
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_603_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_603_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;604, 산책한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_604
;산책하려면  TRAIN_MESSAGE가 없다
CALL M_KOJO_MESSAGE_COM_K50_604_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_604_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;605, 지나치는 길 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_605
;지나치는 길 하려면  TRAIN_MESSAGE가 없다
CALL M_KOJO_MESSAGE_COM_K50_605_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_605_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF !RAND:5
		PRINTFORMDW %타겟은% 머뭇머뭇 하면서 %CALLNAME:MASTER%의 옷의 옷자락을 잡으면서 눈을 치켜 뜨고 호소해 왔다
		PRINTFORMDW %CALLNAME:TARGET%의 눈동자는 눈물이 물기를 띠고 있다…
		PRINTFORMW 「……오줌」
	ELSEIF !RAND:4
	ELSEIF !RAND:3
		PRINTFORMDW 이성을 잃은 %마스터는% %타겟을% 인기가 없는 구멍가게에 질질 끌어 들이면(자)%CALLNAME:TARGET%의 입에 검고 대담한 부과자를 념 글자 붐비었다!
		MONEY:0 -= 20
	ELSEIF !RAND:2
		IF !RAND:2
			PRINTFORMDW %마스터는% 좋은 이 %CALLNAME:TARGET%에 솜사탕을 사 주었다
			MONEY:0 -= 500
		ELSE
			PRINTFORMDW %마스터는% 좋은 이 %CALLNAME:TARGET%에 날름 날름 캔디를 사 주었다
			MONEY:0 -= 500
		ENDIF
	ELSE
		PRINTFORMDW 끝내버리는 아저씨는 %마스터와% %CALLNAME:TARGET%의 장래를 애정이 깃들인 눈으로 바라보고 있다
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;610, 식사 제의를 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_610
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_610_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_610_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;==================================================
;613, 돈 벌어 한다
;==================================================
@M_KOJO_MESSAGE_COM_K50_613
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_613_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_613_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;614, 식재료의 시장
;==================================================
@M_KOJO_MESSAGE_COM_K50_614
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_614_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_614_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;615, 도시락을 먹는다
;==================================================
@M_KOJO_MESSAGE_COM_K50_615
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_615_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_615_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	IF !TALENT:처녀 && !RAND:(ABL:요리기능 + 1)
		PRINTFORMW 「저기 군요 오라버니. %CALLNAME:TARGET%좋은 신부가 될 수 있을까?」
		PRINTFORMW 「이제(벌써) 시집갈 수 없는거야? 그런 일 말해도 오라버니」
		PRINTFORMW 「그것은 누님(언니)가 결정해」
	ELSE
	PRINTDATAL
		DATALIST
			DATAFORM 「응과 응토우쉿 좋은―♪」
			DATAFORM 「입니다 찼는지 째에-개-등-라고―♪」
		ENDLIST
		DATALIST
			DATAFORM 「오라버니 오라버니!%CALLNAME:TARGET%좋은 신부가 될 수 있을까? 될 수 있을까?」
			DATAFORM 「될 수 있지요!응. 플랑 노력한다!」
		ENDLIST
		DATALIST
			DATAFORM 「오라버니, 한 면만 구운 달걀 후라이는 서니사이드 업이야」
			DATAFORM 「플랑은 오점과님은 골칫거리. 그러니까 달 사이드 업이군요」
			DATAFORM 플랑은 통솔한 머리카락을 흔들어척 하고 있다
		ENDLIST
	ENDDATA
	ENDIF
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;617, 아마미 도코로로 이끈다
;==================================================
@M_KOJO_MESSAGE_COM_K50_617
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_617_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_617_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	PRINTFORML 「오라버니!맛있다!」
	PRINTFORML 「카리스마도 내리막뱀장어구나!」
	PRINTFORML 「%UNICODE(0x2661)%」
	PRINTFORMDW %타겟은% 깃털을 삑삑 홰치게 한이면서 감미를 뺨 팽팽해져 있는
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;618, 꽃집
;==================================================
@M_KOJO_MESSAGE_COM_K50_618
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_618_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_618_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	PRINTFORML 「예쁘다」
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;620, 스즈나안
;==================================================
@M_KOJO_MESSAGE_COM_K50_620
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_620_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_620_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	IF TALENT:임신
		PRINTFORML 「오라버니. 추천 도서피요 클럽이야. 러브 러브.」
	ELSE
		PRINTFORMDL %타겟은% 무섭게 기대하고 있다…
	ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;660, 독서
;==================================================
@M_KOJO_MESSAGE_COM_K50_660
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_660_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_660_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	IF EQUIP:액세서리 == 1
		PRINTFORMDL 안경을 내기 독서에 빠지는 %CALLNAME:TARGET%의 생김새는 꽤에님이 되어 있어
		PRINTFORMDW 순진한 거동에 평상시는 그림자를 감추고 있는 그녀안의 기품을 마음껏 꺼내고 있었다
		PRINTFORML 「…무엇일까 오라버니」
		IF TALENT:TARGET:2 & 2
			PRINTFORMDW %타겟은% 나쁜 코노에 책을 살며시 닫았다…
		ELSEIF ABL:교양 < 3
			PRINTFORMDW %타겟은% 좋은 코노에 책을 살며시 닫았다…
		ENDIF
	ELSE
		PRINTFORMDW 어느 새인가 %타겟은% 안경을 쓰고 있었다
		EQUIP:액세서리 = 1
	ENDIF
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;698, 방에 들어간다
;==================================================
@M_KOJO_MESSAGE_COM_K50_698
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_698_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_698_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;699, 밖으로 나온다
;==================================================
@M_KOJO_MESSAGE_COM_K50_699
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_COM_K50_699_1
RETURN RESULT

@M_KOJO_MESSAGE_COM_K50_699_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1

;※※※※※※※※※※※※※※※※※※※※※※※※※
;파생 커맨드
;※※※※※※※※※※※※※※※※※※※※※※※※※
;==================================================
;60, 귀이개
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_60
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_60_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_60_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;61, 안아 베개
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_61
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_61_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_61_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		;IF !RAND:3
			;PRINTFORML 「」
		;ELSEIF !RAND:2
			;PRINTFORML 「」
		;ELSE
			;PRINTFORML 「」
		;ENDIF
		PRINTFORML 「응 큐 …」
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;62, 이마 쪽―
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_62
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_62_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_62_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 0
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;63, 좀 더 키스 한다
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_63
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_63_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_63_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	IF TALENT:임신
	;	PRINTFORML 「」
		RETURN 1
	ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	PRINTFORML (오라버니도 참, 정말로 %타겟을% 잉태 싶다…)
	;연모 후기
	IF TALENT:연모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:사모
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			;PRINTFORML 「」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
;==================================================
;80, 손을 잡아 당긴다
;==================================================
@M_KOJO_MESSAGE_SCOM_K50_80
CALL TRAIN_MESSAGE
CALL M_KOJO_MESSAGE_SCOM_K50_80_1
RETURN RESULT
@M_KOJO_MESSAGE_SCOM_K50_80_1

;-------------------------------------------------
;기입 체크(=0, 비표시, 1, 표시)
LOCAL = 1
;-------------------------------------------------
IF LOCAL
	;시간정지중은 무언 말하게 할 때는;을 지워 「대사」를 가필의 일
	IF FLAG:70 == 1 && CFLAG:TARGET:344 == 0
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;수면중
	IF CFLAG:수면 == 1
		RETURN 1
	ENDIF
	;시간정지 이외로 시라후궳 없는 경우
	IF !SHIRAHU(TARGET)
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;상대 주도 밀어 넘어뜨려지고 안
	IF CFLAG:우후후 == 2
		;고추 붙어 있다
		IF TALENT:TARGET:2 & 2
			;PRINTFORML 「」
		;고추 붙지 않다
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	ENDIF
	;당신 주도 덮치기중
	IF CFLAG:우후후 == 1
		;PRINTFORML 「」
		RETURN 1
	ENDIF
	;-------------------------------------------------
	;특수 분기 사용시에는 코멘트 아웃을 해방해 주세요
	;-------------------------------------------------
	;;임신중
	;IF TALENT:임신
	;	PRINTFORML 「」
	;	RETURN 1
	;ENDIF
	;;정액 보테
	;IF !TALENT:임신 && EX:TARGET:질내정액 >= 1000
	;	PRINTFORML 「」
	;	RETURN 1
	;-------------------------------------------------
	;이하 일상 우선 랜덤 3범위
	;-------------------------------------------------
	;연모 후기
	IF TALENT:16:연모
		IF !RAND:3
			PRINTFORML 「누님(언니)를 슬퍼하게 하면(자)%CALLNAME:TARGET%안 돼」
			PRINTFORMDW %타겟은% 눈에 눈물을 모으고 호소해 왔다
		ELSEIF !RAND:2
			PRINTFORML 「안된 오라버니!」
		ELSE
			PRINTFORML 「오라버니 안 돼 …」
		ENDIF
		RETURN 1
	;사모 중기
	ELSEIF TALENT:16:사모
		IF !RAND:3
			PRINTFORML 「누님(언니)로 보여지면(자) 질투심 되어버리는 원이군요」
		ELSEIF !RAND:2
			;PRINTFORML 「」
		ELSE
			;PRINTFORML 「」
		ENDIF
		RETURN 1
	;그 이외
	ELSE
		IF !RAND:3
			PRINTFORML 「기다려 오라버니!%CALLNAME:TARGET%오지 않아로 위법한의!」
		ELSEIF !RAND:2
			PRINTFORML 「오라버니!기다려!기다려!」
		ELSE
			PRINTFORML 「이끌면 안 돼요 …오라버니…」
			PRINTFORMW 「오라버니 잡혀 제소되어버린다…」
		ENDIF
		RETURN 1
	ENDIF

ENDIF
RETURN 1
