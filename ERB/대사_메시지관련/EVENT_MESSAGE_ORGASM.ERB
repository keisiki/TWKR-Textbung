
;-------------------------------------------
;触手の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_E
;触手が射精
SIF TFLAG:15 == 1
	PRINTFORML %CALLNAME:TARGET%에 얽힌 촉수가 끈적거리는 체액을 뿜어냈다…
;触手が大量射精
SIF TFLAG:15 == 2
	PRINTFORML %CALLNAME:TARGET%의 전신에 얽힌 촉수가 일제히 끈적거리는 체액을 뿜어냈다…
;テキストを読むためのウエイト
;表示があるときのみ
SIF TFLAG:15
	WAIT
;-------------------------------------------
;奴隷の焦らしのテキスト
;-------------------------------------------
@PALAM_MESSAGE_F
PRINTFORMW %타겟이% 금방이라도 갈 것 같은 때를 가늠해서, %마스터는% 애무의 손을 멈추어 버렸다
SIF !FLAG:70
	PRINTFORMW %타겟은% 원망스러운 눈빛을 보내면서, 마지막 한 방을 요구하며 애타게 몸을 비비고 있다…

;-------------------------------------------
;奴隷の絶頂時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_B
#DIM OMORASHI

IF TCVAR:이뇨제 || TALENT:오줌싸개버릇
	OMORASHI = 1
ELSE
	OMORASHI = 0
ENDIF

LOCAL =  NOWEX:C절정 + NOWEX:V절정 + NOWEX:A절정 + NOWEX:B절정 + NOWEX:사정 + NOWEX:M절정
SIF !(LOCAL + NOWEX:방뇨)
	RETURN 0
;絶頂
IF FLAG:70 == 1
	PRINTFORM 시간을 멈춘 %타겟은% 
	SIF NOWEX:분유
		PRINTFORM %OPPAI_DESCRIPTION(TARGET)%의 %NIPPLE_DESCRIPTION(TARGET)%에서 모유를 흘리면서, 
	SIF LOCAL >= 5
		PRINT 음순에서 투명한 물줄기를 내뿜으면서, 
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%에서 %조사처리(SAMEN_DESCRIPTION(TARGET,1),"를")% 분출하면서, 
	SIF NOWEX:분유 || LOCAL >= 5 || NOWEX:사정
		PRINT 
	PRINTL 시간 정지에서 해방되고 있다
	SIF NOWEX:사정 && TALENT:TARGET:2 > 1
		PRINTFORM %TINKO(TARGET)%
	SIF NOWEX:C절정 && TALENT:TARGET:2 == 1
		PRINT 클리토리스
	SIF NOWEX:C절정 && (NOWEX:V절정 || NOWEX:A절정 || NOWEX:B절정 || NOWEX:M절정)
		PRINT 와 
	SIF NOWEX:V절정
		PRINT 질내
	SIF NOWEX:V절정 && (NOWEX:A절정 || NOWEX:B절정|| NOWEX:M절정)
		PRINT 와 
	SIF NOWEX:A절정
		PRINT 아날
	SIF NOWEX:A절정 && (NOWEX:B절정|| NOWEX:M절정)
		PRINT 과 
	SIF NOWEX:M절정
		PRINT 입술
	SIF NOWEX:M절정 && NOWEX:B절정
		PRINT 과 
	SIF NOWEX:B절정
		PRINTFORM %NIPPLE_DESCRIPTION(TARGET,0)%
	PRINTL 를 진동시키며 절정에 이르렀다

	SIF NOWEX:V절정 && TEQUIP:50 == PLAYER
		PRINTFORML 절정한 질내는 움찔움찔 경련하며, 삽입된 %TINKO(PLAYER)%에 달라붙어 온다…
	SIF NOWEX:A절정 && TEQUIP:51 == PLAYER
		PRINTFORML 절정한 애널은, 찔러 넣어진 %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱하며 단단히 조여 온다…

	;絶頂とおもらし
	IF LOCAL >= 3 && TALENT:오줌싸개버릇
		PRINTFORML %CALLNAME:TARGET%의 몸은 쾌락으로 인해 요도구를 느슨하게 하여 방뇨하고 있다…
	ENDIF
	
	;絶頂と噴乳
	IF LOCAL >= 3 && NOWEX:분유 && TEQUIP:유두빨기
		PRINTFORML %마스터는% %CALLNAME:TARGET%의 %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 빨아 올려, 흘러나오는 모유를 마시고 있다…
	ELSEIF NOWEX:분유 && TEQUIP:유두빨기
		PRINTFORML %마스터는% %CALLNAME:TARGET%의 %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 빨아올리고, 배어나오는 모유를 혀로 핥고 있다…
	ENDIF
	
ELSEIF CFLAG:MASTER:장난 == 2
	PRINTFORM 자고 있는 %타겟은% 
	SIF LOCAL >= 5
		PRINT 음순에서 투명한 물줄기를 내뿜으 
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%에서 %조사처리(SAMEN_DESCRIPTION(TARGET,1),"를")% 분출하 
	SIF NOWEX:분유 || LOCAL >= 5 || NOWEX:사정
		PRINT 면서,
	PRINTL 온몸을 떨며 절정에 이르렀다

	SIF NOWEX:V절정 && TEQUIP:50 == PLAYER
		PRINTFORML 절정한 질내는 움찔움찔 경련하며, %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 짜내려고 %TINKO(PLAYER)%에 달라붙어 온다…
	SIF NOWEX:A절정 && TEQUIP:51 == PLAYER
		PRINTFORML 절정한 애널은, 찔러 넣어진 %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱하며 단단히 조여 온다…

	;絶頂と噴乳とおもらし
	CALL PrintMilkAndOmorashi(LOCAL, OMORASHI)
ELSE
	PRINTFORM %타겟은% \@ TCVAR:억지로? 눈에 눈물이 글썽이며 # \@
	SIF LOCAL >= 5
		PRINT 음순에서 투명한 물줄기를 내뿜으며
	SIF NOWEX:사정
		PRINTFORM %TINKO(TARGET)%에서 %조사처리(SAMEN_DESCRIPTION(TARGET,1),"를")% 분출하며
	PRINTFORML \@ LOCAL < 5?  등을 크게 젖히고# \@, 온몸을 떨며 \@ SYNCED_ORGASM(TARGET)? %마스터와% 동시에  # \@절정에 이르렀다

	IF NOWEX:사정
		IF TEQUIP:PLAYER:V섹스 == TARGET
			PRINTFORM %CALLNAME:PLAYER%의 질내에 %CALLNAME%의 %조사처리(SAMEN_DESCRIPTION(TARGET,1),"가")% 쏟아졌다…
		ELSEIF TEQUIP:PLAYER:A섹스 == TARGET
			PRINTFORM %CALLNAME:PLAYER%의 아날에 %CALLNAME%의 %조사처리(SAMEN_DESCRIPTION(TARGET,1),"가")% 쏟아졌다…
		ENDIF
	ENDIF

	;絶頂と噴乳とおもらし
	CALL PrintMilkAndOmorashi(LOCAL, OMORASHI)
ENDIF

;テキストを読むためのウエイト
;表示があるときのみ
IF LOCAL + NOWEX:방뇨 > 0
	WAIT
	SIF FLAG:대사색깔
		TRYCALLFORM M_KOJO_COLOR_K{NO:TARGET}
ENDIF

;-------------------------------------------
;調教者の射精時のテキスト
;-------------------------------------------
@PALAM_MESSAGE_A
VARSET LOCAL

LOCAL = NOWEX:MASTER:C절정 + NOWEX:MASTER:V절정 + NOWEX:MASTER:A절정 + NOWEX:MASTER:B절정 + NOWEX:MASTER:M절정
IF LOCAL >= 1
	PRINTFORM %마스터는% 
	IF NOWEX:MASTER:오중절정 || NOWEX:MASTER:사중절정
		PRINTFORML 온몸을 덮치는 강렬한 쾌감의 폭풍에 목소리를 높이며 미친 듯이 날뛰었다
	ELSE
		LOCAL:1 = 0
		IF NOWEX:MASTER:C절정
			IF TALENT:MASTER:성별 & 2
				PRINT 페니스
			ELSE
				PRINT 크리토리스
			ENDIF
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:V절정
			SIF LOCAL:1
				PRINT 와 
			PRINT 질
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:A절정
			SIF LOCAL:1
				PRINT 와 
			PRINT 아날
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:B절정
			SIF LOCAL:1
				PRINT 와 
			IF TALENT:MASTER:성별 & 1
				PRINT 가슴
			ELSE
				PRINT 젖꼭지
			ENDIF
			LOCAL:1 = 1
		ENDIF
		IF NOWEX:MASTER:M절정
			SIF LOCAL:1
				PRINT 와 
			PRINT 입술
			LOCAL:1 = 1
		ENDIF
		IF LOCAL >= 5
			PRINTFORML 를 덮치는 강렬한 절정감에 머리가 새하얗게 되는 것을 느꼈다
		ELSE
			PRINTFORML 에 대한 쾌감으로 절정하여 몸을 떨었다
		ENDIF
	ENDIF
ENDIF

;TFLAG:1 射精箇所 (ビット0=콘돔 1=膣内 2=アナル 3=手淫 4=口淫 5=パイズリ 6=素股 7=足コキ 8=体表 9=アナル奉仕
;20=手淫フェラ 21=パイズリフェラ22=シックスナイン 24=子宮口 25=疑似 26=授乳プレイ

;콘돔
IF TCVAR:PLAYER:콘돔 == 1
	IF FLAG:70 == 1
		PRINTFORML %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"가")% 토해낸 \@ NOWEX:MASTER:사정 >1 ? 대량의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 콘돔이 채워져 간다…
		IF GETBIT(TFLAG:1,1)
			SIF ABL:TARGET:질내사정중독
				PRINTFORML 기대했던 것을 얻을 수 없었던 %CALLNAME:(TARGET)%의 질내가, 불만으로 쑤셨다…
		ELSEIF GETBIT(TFLAG:1,2)
			SIF ABL:TARGET:항내사정중독
				PRINTFORML 기대했던 것을 얻을 수 없었던 %CALLNAME:(TARGET)%의 아누스가, 불만으로 쑤셨다…
		ENDIF
	ELSEIF CFLAG:MASTER:장난 == 2
		PRINTFORML %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"가")% 토해낸 \@ NOWEX:MASTER:사정 >1 ? 대량의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 콘돔이 채워져 간다…
		PRINTFORM %TINKO(PLAYER)%에서 빼낸, 
		SELECTCASE NOWEX:MASTER:사정
			CASE IS > 2
				PRINT 터저버릴 듯이 
			CASE 2
				PRINT 끝부분에 잔뜩 
		ENDSELECT
		PRINTFORML 정자가 쌓인 콘돔을 이불 옆에 놓았다…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"가")% 토해낸 \@ NOWEX:MASTER:사정 >1 ? 대량의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 콘돔이 채워져 간다…
		PRINTFORM %TINKO(PLAYER)%에서 빼낸, 
		SELECTCASE NOWEX:MASTER:사정
			CASE IS > 2
				PRINT 터저버릴 듯이 
			CASE 2
				PRINT 끝부분에 잔뜩 
		ENDSELECT
		PRINTFORML 정자가 쌓인 콘돔을 %CALLNAME:(TARGET)%에게 과시했다…
		;膣内射精
		IF GETBIT(TFLAG:1,1)
			IF (TALENT:TARGET:사모 || TALENT:TARGET:연모 || TALENT:TARGET:음란 || TALENT:TARGET:복종) && CFLAG:TARGET:생해금 != 2
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% %조교자를% 기분좋게 만든 증거를 보고 만족스럽다…
			ELSEIF CFLAG:생해금 != 2 && !TALENT:TARGET:무지
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% %조교자가% 피임한 것에 대해 안도하는 것 같다…
			ENDIF
			SIF ABL:질내사정중독
				PRINTFORML 기대했던 것을 얻을 수 없었던 %CALLNAME:(TARGET)%의 질내가, 불만으로 쑤셨다…
		ELSEIF GETBIT(TFLAG:1,2)
			SIF (TALENT:TARGET:사모 || TALENT:TARGET:연모 || TALENT:TARGET:음란 || TALENT:TARGET:복종)
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% %조교자를% 기분좋게 만든 증거를 보고 만족스럽다…
			SIF ABL:항내사정중독
				PRINTFORML 기대했던 것을 얻을 수 없었던 %CALLNAME:(TARGET)%의 아누스가, 불만으로 쑤셨다…
		ENDIF
	ENDIF
	IF CFLAG:TARGET:자동고무
		PRINTFORML %마스터는% 콘돔을 다시 씌\@ TEQUIP:MASTER:콘돔? 웠다# 우려 했지만, 예비가 없었다\@
	ENDIF
	WAIT
	RETURN
;個別処理
;子宮口責め
ELSEIF GETBIT(TFLAG:1,24)
	IF NOWEX:TARGET:V절정
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
			;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁마저 침범되는 열락에 강렬한 절정을 맞이하고 있다
						PRINTFORML 질 주름과 자궁구와 자궁벽에게 빨려져, 동시에 끝에 다다른 %조교자는% %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 박아 넣었다
						PRINTFORML 미숙하고 작은 자궁의 구석구석 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 채워질 때마다 움찔움찔 %CALLNAME:(TARGET)%의 질 내부가 경련한다
						PRINTFORMW %조사처리(CALLNAME:(TARGET),"은")% 시간을 멈춘 채, \@ NOWEX:MASTER:사정 > 1 ? 아랫배가 작게 불룩해질 정도의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 자궁에 쏟아 부어졌다…
					ELSE
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁을 쿡쿡 찌르는 쾌감에 단숨에 절정했다
						PRINTFORML 간발의 차도 없이, %조교자는% %TINKO(PLAYER)%의 끝을 안쪽으로 꾸욱꾸욱 밀어 넣어 정액을 뿜어냈다
						PRINTFORML %CALLNAME:(TARGET)%의 미숙하고 작은 자궁은 순식간에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 채워지고
						PRINTFORMW 역류하는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 쿡쿡 찔러지는 쾌감에 억지로 절정 당하고 있다
					PRINTFORML 꽉하고 음부를 강렬하게 조이면서, 꼬옥하고 달라붙는 자궁구에, %CALLNAME:MASTER%도 끝에 다다른다
					PRINTFORML 아이와 같이 미숙한 %CALLNAME:(TARGET)%의 좁은 질내를 %조사처리(TINKO(PLAYER),"로")% 채우고, 귀두를 자궁에 밀어붙여 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다
					PRINTFORMW 작은 자궁과 질 내에서 도저히 받아들일 수 없는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML 질 내부를 강렬하게 단단히 조여 자궁구 마저, 느슨하게 입을 열어 귀두를 물어버리니 %조사처리(CALLNAME:(TARGET),"가")% 절정 하고 있다
					PRINTFORML 그 쾌감에 이끌려, %조교자는% 질벽을 %TINKO(PLAYER)%에 달라붙어 자궁구로 귀두를 무는 『기분좋게 사정할 수 있는 구멍』에 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고
					PRINTFORMW 시간을 멈춘 『기분좋게 사정할 수 있는 구멍』으로 되어 있는 %CALLNAME:(TARGET)%의 자궁내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 마음껏 쏟아 부었다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 자궁에 채워져간다
				ELSE
					PRINTFORML 자궁구를 쿡쿡 찌르는 감촉에 끝에 다다른 %CALLNAME:(TARGET)%의 질내가, 강렬한 경련과 함께 단단히 조여온다
					PRINTFORML %마스터를% 거부하듯이 굳게 닫힌 %CALLNAME:(TARGET)%의 자궁구에, %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱 밀어넣으며 사정하고,
					PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 자궁을 침략했다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW %마스터는% 정복감에 도취되면서, 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
				ENDIF
			;---------------------------------
			;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 시간이 멈춰지면서, 절정에 경련하는 %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 강하게 찔러넣은 %마스터는%,
				PRINTFORMW 달라붙어 오는 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")%, 질내를 움찔움찔하고 경련하며 절정하고 있다
				PRINTFORML %조교자는% 자궁구에 난 미세한 구멍에 귀두 구멍을 밀착시켜
				PRINTFORMW 직접 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 쏟아 부었다…
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:장난 == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁마저 침범되는 열락에 강렬한 절정을 맞이하고 있다
						PRINTFORML 질 주름과 자궁구와 자궁벽에게 빨려져, 동시에 끝에 다다른 %조교자는% %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 박아 넣었다
						PRINTFORML 미숙하고 작은 자궁의 구석구석 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 채워질 때마다 움찔움찔 %CALLNAME:(TARGET)%의 질 내부가 경련한다
						PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 잠든 채로, \@ NOWEX:MASTER:사정 > 1 ? 아랫배가 작게 불룩해질 정도의  # \@%조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 자궁에 쏟아 부어졌다…
					ELSE
						PRINTFORML %CALLNAME:(TARGET)%의 질내는 자궁을 쿡쿡 찌르는 쾌감에 단숨에 절정했다
						PRINTFORML 간발의 차도 없이, %조교자는% %TINKO(PLAYER)%의 끝을 안쪽으로 꾸욱꾸욱 밀어 넣어 정액을 뿜어냈다
						PRINTFORML %CALLNAME:(TARGET)%의 미숙하고 작은 자궁은 순식간에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 채워지고
						PRINTFORMW 역류하는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML %CALLNAME:(TARGET)%의 질내는, 자궁구를 쿡쿡 찔러지는 쾌감에 억지로 절정 당하고 있다
					PRINTFORML 꽉하고 음부를 강렬하게 조이면서, 꼬옥하고 달라붙는 자궁구에, %CALLNAME:MASTER%도 끝에 다다른다
					PRINTFORML 아이와 같이 미숙한 %CALLNAME:(TARGET)%의 좁은 질내를 %조사처리(TINKO(PLAYER),"로")% 채우고, 귀두를 자궁에 밀어붙여 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다
					PRINTFORMW 작은 자궁과 질 내에서 도저히 받아들일 수 없는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML 질 내부를 강렬하게 단단히 조여 자궁구 마저, 느슨하게 입을 열어 귀두를 물어버리니 %조사처리(CALLNAME:(TARGET),"가")% 절정 하고 있다
					PRINTFORML 그 쾌감에 이끌려, %조교자는% 질벽을 %TINKO(PLAYER)%에 달라붙어 자궁구로 귀두를 무는 『기분좋게 사정할 수 있는 구멍』에 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고
					PRINTFORMW 잠든 채로 범해지는 『기분좋게 사정할 수 있는 구멍』으로 되어 있는 %CALLNAME:(TARGET)%의 자궁내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 마음껏 쏟아 부었다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 자궁에 채워져간다
				ELSE
					PRINTFORML 자궁구를 쿡쿡 찌르는 감촉에 끝에 다다른 %CALLNAME:(TARGET)%의 질내가, 강렬한 경련과 함께 단단히 조여온다
					PRINTFORML %마스터를% 거부하듯이 굳게 닫힌 %CALLNAME:(TARGET)%의 자궁구에, %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱 밀어넣으며 사정하고,
					PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 자궁을 침략했다…
					SIF NOWEX:MASTER:사정 > 1
						PRINTFORMW %마스터는% 정복감에 도취되면서, 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
				ENDIF
			;---------------------------------
			;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 잠들면서, 절정에 경련하는 %CALLNAME:(TARGET)%의 질 깊숙히
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% 강하게 찔러넣은 %마스터는%,
				PRINTFORMW 달라붙어 오는 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ELSE
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")%, 질내를 움찔움찔하고 경련하며 절정하고 있다
				PRINTFORML %조교자는% 자궁구에 난 미세한 구멍에 귀두 구멍을 밀착시켜
				PRINTFORMW 직접 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 쏟아 부었다…
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:자궁구개발경험 >= 300
				IF TEQUIP:TARGET:체위2 == 1
					PRINTFORML 마지막에 마음껏 허리를 내밀어, %CALLNAME:(TARGET)%의 태내 깊숙히 고여있던 욕망을 쏟아냈다…
					PRINTFORML 자궁 안에 뜨거운 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 쏟아넣는 %조사처리(CALLNAME:(TARGET),"가")%, 아름다운 육체을 떨며 절정한다
					PRINTFORML 흠칫 흠칫 허벅지를 경련시켜, 필사적으로 %CALLNAME:PLAYER%에게 매달리는 %CALLNAME:(TARGET)%
					;PRINTFORML %CALLNAME:(TARGET)%は両脚をつま先までぴんと張り詰めさせ、雌肉をわななかせて射精を受け止めている…
					PRINTFORMW %조교자는% 단단히 %조사처리(CALLNAME:(TARGET),"를")% 꼭 끌어안고 밀착상태를 유지한 채 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 계속 쏟아 부었다…
				ELSE
					PRINTFORML 입가에 침을 흘리면서 달콤한 통곡을 올리고, 끝에 다다른 %CALLNAME:(TARGET)%
					PRINTFORML 강렬한 조임과 복잡한 경련에 이끌려 %조교자는% 솟구치는 사정감을 이겨내지 못하고, %조사처리(TINKO(PLAYER),"로")% 자궁구를 관통한 채
					PRINTFORMW 직접 %CALLNAME:(TARGET)%의 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다
					SIF TALENT:TARGET:동물귀 && TEQUIP:TARGET:체위2 == 2
						PRINTFORML 짐승 자세에서의 교배가, %CALLNAME:(TARGET)%에게 최상의 쾌락을 가져오고 있다……
				ENDIF
			ELSE
				PRINTFORML 움찔움찔 온몸을 떨면서, %조사처리(CALLNAME:(TARGET),"는")% 절정에 몸을 맡기고 있다
				PRINTFORML %조교자는% 경련하는 %CALLNAME:(TARGET)%의 질 안쪽에 딱 맞게 %조사처리(TINKO(PLAYER),"를")% 고정시켜
				PRINTFORMW 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ENDIF
			IF TCVAR:TARGET:억지로
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 태내 깊숙이 퍼지는 점액의 감촉에 녹아든 듯한 표정을 지은 뒤 정신을 차리고 떨고 있다…
			ELSE
				PRINTFORML …
				PRINTFORML 
				IF CFLAG:TARGET:생해금 < 2 && 妊娠可否(TARGET) && !TALENT:TARGET:무지
					PRINTFORMW 쾌락에 흐린 머리로, %조사처리(CALLNAME:(TARGET),"는")% 어렴풋이 임신가능성을 생각했다……
				ELSE
					PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 자궁을 채우는 뜨거운 감촉에, 강렬한 절정감과 여자의 행복을 느끼고 있다…
				ENDIF
				PRINTFORMW …
			ENDIF
		ENDIF
	ELSE
		;---------------------------------
		;時間停止
		;---------------------------------
		IF FLAG:70 == 1
			;---------------------------------
				;時間停止・鬼畜
			;---------------------------------
			IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 자궁 깊숙이 육봉을 집어넣어 사정하였다
						PRINTFORML 토해진 %조사처리(SAMEN_DESCRIPTION(PLAYER),"는")% 자궁구를 육봉으로 막아 도망갈 곳이 없어, 모두 %CALLNAME:(TARGET)%의 미숙하고 작은 자궁으로 흘러들어간다
						PRINTFORMW %조사처리(CALLNAME:(TARGET),"은")% 시간을 멈춘 채, \@ NOWEX:MASTER:사정 > 1 ? 아랫배가 작게 불룩해질 정도의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 자궁에 쏟아 부어졌다…
					ELSE
						PRINTFORML %조교자는% %TINKO(PLAYER)%의 끝을 %CALLNAME:(TARGET)%의 자궁구에 박아 넣은 채 사정했다
						PRINTFORML %CALLNAME:(TARGET)%의 미숙하고 작은 자궁은 순식간에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 채워지고
						PRINTFORMW 역류하는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML 아이와 같이 미숙한 %CALLNAME:(TARGET)%의 좁은 질내를 %조사처리(TINKO(PLAYER),"로")% 채우고, 귀두를 자궁에 밀어붙여 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다
					PRINTFORMW 작은 자궁과 질 내에서 도저히 받아들일 수 없는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML  %조교자는% 질벽을 %TINKO(PLAYER)%에 달라붙어 자궁구로 귀두를 무는 『기분좋게 사정할 수 있는 구멍』에 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고
					PRINTFORMW 시간을 멈춘 『기분좋게 사정할 수 있는 구멍』으로 되어 있는 %CALLNAME:(TARGET)%의 자궁내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 마음껏 쏟아 부었다…
				ELSE
					PRINTFORML %마스터를% 거부하듯이 굳게 닫힌 %CALLNAME:(TARGET)%의 자궁구에, %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱 밀어넣으며 사정하고,
					PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% \@ CFLAG:TARGET:생리주기 == 7 ? 위험일의 # \@자궁을 침략했다…
				ENDIF
				SIF NOWEX:MASTER:사정 > 1
					PRINTFORMW %마스터는% 정복감에 도취되면서, 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			;---------------------------------
				;時間停止・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 시간이 멈춰지면서, 절정에 경련하는 %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 강하게 찔러넣고
				PRINTFORMW 달라붙어 오는 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ELSE
				PRINTFORML %조교자는% 자궁구에 난 미세한 구멍에 귀두 구멍을 밀착시켜
				PRINTFORMW 직접 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 쏟아 부었다…
			ENDIF
		;---------------------------------
		;睡眠姦
		;---------------------------------
		ELSEIF CFLAG:MASTER:장난 == 2
			;---------------------------------
			;睡眠姦・鬼畜
			;---------------------------------
			IF EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50
				;---------------------------------
				;小柄体型
				;---------------------------------
				IF TALENT:TARGET:체형 == -2
					IF EXP:TARGET:자궁구개발경험 >=300
						PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 자궁 깊숙이 육봉을 집어넣어 사정하였다
						PRINTFORML 토해진 %조사처리(SAMEN_DESCRIPTION(PLAYER),"는")% 자궁구를 육봉으로 막아 도망갈 곳이 없어, 모두 %CALLNAME:(TARGET)%의 미숙하고 작은 자궁으로 흘러들어간다
						PRINTFORMW %조사처리(CALLNAME:(TARGET),"은")% 잠든 채, \@ NOWEX:MASTER:사정 > 1 ? 아랫배가 작게 불룩해질 정도의 # \@%조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 자궁에 쏟아 부어졌다…
					ELSE
						PRINTFORML %조교자는% %TINKO(PLAYER)%의 끝을 %CALLNAME:(TARGET)%의 자궁구에 박아 넣은 채 사정했다
						PRINTFORML %CALLNAME:(TARGET)%의 미숙하고 작은 자궁은 순식간에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 채워지고
						PRINTFORMW 역류하는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다
					ENDIF
				;---------------------------------
				;幼児体型
				;---------------------------------
				ELSEIF TALENT:TARGET:체형 == -1
					PRINTFORML 아이와 같이 미숙한 %CALLNAME:(TARGET)%의 좁은 질내를 %조사처리(TINKO(PLAYER),"로")% 채우고, 귀두를 자궁에 밀어붙여 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다
					PRINTFORMW 작은 자궁과 질 내에서 도저히 받아들일 수 없는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 털이 없는 균열로부터 흘러넘치고 있다…
				;---------------------------------
				;それ以外
				;---------------------------------
				ELSEIF EXP:TARGET:자궁구개발경험 >= 300
					PRINTFORML  %조교자는% 질벽을 %TINKO(PLAYER)%에 달라붙어 자궁구로 귀두를 무는 『기분좋게 사정할 수 있는 구멍』에 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고
					PRINTFORMW 잠든 채 범해지는 『기분좋게 사정할 수 있는 구멍』으로 되어 있는 %CALLNAME:(TARGET)%의 자궁내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 마음껏 쏟아 부었다…
				ELSE
					PRINTFORML %마스터를% 거부하듯이 굳게 닫힌 %CALLNAME:(TARGET)%의 자궁구에, %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱 밀어넣으며 사정하고,
					PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"로")% 자궁을 침략했다…
				ENDIF
				SIF NOWEX:MASTER:사정 > 1
					PRINTFORMW %마스터는% 정복감에 도취되면서, 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			;---------------------------------
				;睡眠姦・通常
			;---------------------------------
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML 자고 있는 %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 깊게 찔러 넣고
				PRINTFORMW 달라붙어 오는 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ELSE
				PRINTFORML %조교자는% 자궁구에 난 미세한 구멍에 귀두 구멍을 밀착시켜
				PRINTFORMW 직접 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 쏟아 부었다…
			ENDIF
		;---------------------------------
		;シラフ
		;---------------------------------
		ELSE
			IF EXP:TARGET:자궁구개발경험 >= 300
				IF TEQUIP:TARGET:체위2 == 1
					PRINTFORML 마지막에 마음껏 허리를 내밀어, %CALLNAME:(TARGET)%의 태내 깊숙히 고여있던 욕망을 쏟아냈다…
					PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 두 다리로 발끝까지 덮어버리고, 암컷의 고기를 부들부들 떨며 사정을 받아 들이고 있다…
						;%CALLNAME:(TARGET)%は両脚をつま先までぴんと張り詰めさせ、雌肉をわななかせて射精を受け止めている…
					PRINTFORML %조교자는% 단단히 %조사처리(CALLNAME:(TARGET),"를")% 꼭 끌어안고 밀착상태를 유지한 채 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 계속 쏟아 부었다…
				ELSE
					PRINTFORML %조교자는% 솟구치는 사정감을 이겨내지 못하고, %조사처리(TINKO(PLAYER),"로")% 자궁구를 관통한 채
					PRINTFORML 직접 %CALLNAME:(TARGET)%의 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다
				ENDIF
			ELSE
				PRINTFORML %조교자는% \@ TCVAR:TARGET:억지로? 싫어하는 # \@%CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 고정하고
				PRINTFORMW 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ENDIF
			IF TCVAR:TARGET:억지로
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 태내 깊숙이 퍼지는 점액의 감촉에 떨고 있다…
			ELSEIF TALENT:TARGET:연모
				PRINTFORML …
				PRINTFORML 
				PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 자궁을 채우는 뜨거운 감촉에, 여자의 행복을 느끼고 있다…
			ELSEIF CFLAG:TARGET:생해금 < 2 && 妊娠可否(TARGET) && !TALENT:TARGET:무지
				PRINTFORMW %CALLNAME:(TARGET)%의 뇌리에 임신가능성이 스쳐 지나갔다……
			ELSE
				PRINTFORMW …
			ENDIF
		ENDIF
	ENDIF
;シックスナイン
ELSEIF GETBIT(TFLAG:1,22)
	IF NOWEX:TARGET:C절정 || NOWEX:TARGET:M절정
		IF FLAG:70 == 1
			IF NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML \@ GETBIT(TALENT:TARGET:성별,1) ? %조사처리(TINKO((TARGET)),"와")% # 클리토리스와 \@ 입안을 떨면서 %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다르고 있다
			ELSEIF NOWEX:TARGET:C절정 && !NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 \@ GETBIT(TALENT:TARGET:성별,1) ? %조사처리(TINKO((TARGET)),"가")% # 클리토리스가 \@ 절정에 떨고 있다
			ELSEIF !NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 입안이 절정에 떨고 있다
			ENDIF
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORMW 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"가")% %CALLNAME:PLAYER%의 입안에 사정함과 동시에 %CALLNAME:PLAYER%도 %CALLNAME:(TARGET)%의 입안에 사정했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"는")% %TINKO((TARGET))% 만을 움찔움찔 떨면서 %CALLNAME:PLAYER%의 입안에 사정했다
					PRINTFORMW %조교자는% %조사처리(SAMEN_DESCRIPTION((TARGET),1),"를")% 입으로 받으면서 %CALLNAME:(TARGET)%의 입에 %조사처리(TINKO(PLAYER),"를")% 찔러 넣었다…
				ELSE
					PRINTFORMW 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"는")% %TINKO((TARGET))% 만을 움찔움찔 떨면서 %CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF HAS_PENIS(TARGET)
					PRINTFORM %조교자는% 시간이 멈춘 %CALLNAME:(TARGET)%의 남성기를 빨면서 
				ELSE
					PRINTFORM %조교자는% 시간이 멈춘 %CALLNAME:(TARGET)%의 음부에 혀를 내밀면서 
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에 사정했다…
				PRINTFORMW %조교자는% 벌렁거리는 여성기에 달라붙으면서, %CALLNAME:(TARGET)%의 목구멍까지 %조사처리(TINKO(PLAYER),"를")% 찔러 넣고 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 흘려 넣어 간다…
			ENDIF
		ELSE
			IF NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML \@ GETBIT(TALENT:TARGET:성별,1) ? %조사처리(TINKO((TARGET)),"와")% # 클리토리스와 \@ 입안을 떨면서 %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다르고 있다
			ELSEIF NOWEX:TARGET:C절정 && !NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 \@ GETBIT(TALENT:TARGET:성별,1) ? %조사처리(TINKO((TARGET)),"가")% # 클리토리스가 \@ 절정에 떨고 있다
			ELSEIF !NOWEX:TARGET:C절정 && NOWEX:TARGET:M절정
				PRINTFORML %CALLNAME:(TARGET)%의 입안이 절정에 떨고 있다
			ENDIF
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORML %조사처리(CALLNAME:(TARGET),"와")% %조교자는% 동시에 서로의 입안에 사정했다…
				PRINTFORMW 두 명은 서로의 페니스를 뿌리까지 물고 서로 쾌락을 확인했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% %CALLNAME:PLAYER%보다 먼저 사정해 버렸다…
					PRINTFORMW %조교자는% %조사처리(SAMEN_DESCRIPTION((TARGET),1),"를")% 입으로 받으면서 %CALLNAME:(TARGET)%의 입에 %조사처리(TINKO(PLAYER),"를")% 찔러 넣었다
				ELSE
					PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% %CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %조교자는% %CALLNAME:(TARGET)%의 남성기를 빨면서 
				ELSE
					PRINTFORM %조교자는% %CALLNAME:(TARGET)%의 음부에 혀를 내밀면서 
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에 사정했다…
				PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 하반신에의 자극과 입안의 열락에 흠칫흠칫 반응하며 뿜어져 나오는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 꿀꺽거리고 있다…
			ENDIF
		ENDIF
	ELSE
		IF FLAG:70 == 1
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORMW 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"가")% %CALLNAME:PLAYER%의 입안에 사정함과 동시에 %CALLNAME:PLAYER%도 %CALLNAME:(TARGET)%의 입안에 사정했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER & 2
					PRINTFORML 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"는")% %TINKO((TARGET))% 만을 움찔움찔 떨면서 %CALLNAME:PLAYER%의 입안에 사정했다
					PRINTFORMW %조교자는% %조사처리(SAMEN_DESCRIPTION((TARGET),1),"를")% 입으로 받으면서 %CALLNAME:(TARGET)%의 입에 %조사처리(TINKO(PLAYER),"를")% 찔러 넣었다…
				ELSE
					PRINTFORMW 시간이 멈춘 %조사처리(CALLNAME:(TARGET),"는")% %TINKO((TARGET))% 만을 움찔움찔 떨면서 %CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %조교자는% 시간이 멈춘 %CALLNAME:(TARGET)%의 남성기를 빨면서 
				ELSE
					PRINTFORM %조교자는% 시간이 멈춘 %CALLNAME:(TARGET)%의 음부에 혀를 내밀면서 
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에 사정했다…
				PRINTFORMW %조교자는% 벌렁거리는 여성기에 달라붙으면서, %CALLNAME:(TARGET)%의 목구멍까지 %조사처리(TINKO(PLAYER),"를")% 찔러 넣고 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 흘려 넣어 간다…
			ENDIF
		ELSE
			IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
				PRINTFORML %조사처리(CALLNAME:(TARGET),"와")% %조교자는% 동시에 서로의 입안에 사정했다…
				PRINTFORMW 두 명은 서로의 페니스를 뿌리까지 물고 서로 쾌락을 확인했다…
			ELSEIF NOWEX:TARGET:사정
				IF TALENT:PLAYER:2 & 2
					PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% %CALLNAME:PLAYER%보다 먼저 사정해 버렸다…
					PRINTFORMW %조교자는% %조사처리(SAMEN_DESCRIPTION((TARGET),1),"를")% 입으로 받으면서 %CALLNAME:(TARGET)%의 입에 %조사처리(TINKO(PLAYER),"를")% 찔러 넣었다
				ELSE
					PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% %CALLNAME:PLAYER%의 입안에 사정했다…
				ENDIF
			ELSE
				IF TALENT:TARGET:2 & 2
					PRINTFORM %조교자는% %CALLNAME:(TARGET)%의 남성기를 빨면서 
				ELSE
					PRINTFORM %조교자는% %CALLNAME:(TARGET)%의 음부에 혀를 내밀면서 
				ENDIF
				PRINTFORML %CALLNAME:(TARGET)%의 입안에 사정했다…
				PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 하반신에의 자극에 흠칫흠칫 반응하며 뿜어져 나오는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 꿀꺽거리고 있다…
			ENDIF
		ENDIF
	ENDIF
;授乳プレイ
ELSEIF GETBIT(TFLAG:1,26)
	PRINTFORML 사정이 시작되자 %조사처리(CALLNAME:(TARGET),"는")% %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% %CALLNAME:(MASTER)%의 입으로 밀어붙이면서, 더욱 격렬하게 %조사처리(TINKO(PLAYER),"를")% 훑어 내면서 
	
	IF ABL:TARGET:정액중독 >= 5 && ABL:TARGET:기교 > 3
		PRINTFORML 황홀한 표정으로 \@ NOWEX:PLAYER:사정 > 1 ? 대량으로 뿜어져 나오는 #토해내는 \@ %CALLNAME:PLAYER%의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 받고 있다…
	ELSEIF NOWEX:PLAYER:사정 == 1
		PRINTFORML \@ NOWEX:PLAYER:사정 > 1 ? 대량으로 뿜어져 나오는 #토해내는 \@ %CALLNAME:PLAYER%의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 받고 있다…
	ELSE
		PRINTFORML %CALLNAME:(TARGET)%의 얼굴과 손을, \@ NOWEX:PLAYER:사정 > 1 ? 대량으로 뿜어져 나오는 #토해내는 \@ %CALLNAME:PLAYER%의 %SAMEN_DESCRIPTION(PLAYER)%에 물들게 했다…
	ENDIF 
	PRINTFORM %조사처리(CALLNAME:(TARGET),"는")% 
	SIF NOWEX:TARGET:B절정
		PRINTFORM 동시에 젖꼭지에서 시작되는 절정의 감각에 몸부림치며 
	;噴乳は地の文に任せた
	PRINTFORMW 그 후로도 천천히 %CALLNAME:MASTER%의 %TINKO(PLAYER)%의 맥동이 멈출 때까지 애무를 계속했다…
;パイズリフェラ
ELSEIF GETBIT(TFLAG:1,21)
	IF NOWEX:TARGET:B절정 || NOWEX:TARGET:M절정
		IF FLAG:70 == 1
			PRINTFORML %CALLNAME:(TARGET)%의 %조사처리(OPPAI_DESCRIPTION((TARGET)),"로")% %조사처리(TINKO(PLAYER),"를")% 훑어 내면서 %CALLNAME:(TARGET)%의 얼굴에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 뿌렸다…
			IF NOWEX:TARGET:B절정 && !NOWEX:TARGET:M절정
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% \@ TALENT:TARGET:가슴사이즈 >= 0 ? 감싸고 있는 # 비비고 있는 \@ %조사처리(OPPAI_DESCRIPTION((TARGET)),"가")% 
			ELSEIF !NOWEX:TARGET:B절정 && NOWEX:TARGET:M절정
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% 물고 있는 입안이 
			ELSE
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% \@ TALENT:TARGET:가슴사이즈 >= 0 ? 감싸고 있는 # 비비고 있는 \@ %조사처리(OPPAI_DESCRIPTION((TARGET)),"와")% 
				PRINTFORML 귀두를 물고 있는 입안이 
			ENDIF
			PRINTFORML 절정에 떨고 있다
		ELSE
			PRINTFORML %CALLNAME:(TARGET)%의 %OPPAI_DESCRIPTION((TARGET))%에 끼우게 한 %TINKO(PLAYER)%의 끝으로부터 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 뿜어져 나왔다…
			IF NOWEX:TARGET:B절정 && !NOWEX:TARGET:M절정
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% \@ TALENT:TARGET:가슴사이즈 >= 0 ? 감싸고 있는 # 비비고 있는 \@ %조사처리(OPPAI_DESCRIPTION((TARGET)),"로")% 
			ELSEIF !NOWEX:TARGET:B절정 && NOWEX:TARGET:M절정
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% 물고 있는 입안이 입안으로부터의 쾌감에 
			ELSE
				PRINTFORML %조사처리(TINKO(PLAYER),"를")% \@ TALENT:TARGET:가슴사이즈 >= 0 ? 감싸고 있는 # 비비고 있는 \@ %조사처리(OPPAI_DESCRIPTION((TARGET)),"와")% 
				PRINTFORML 귀두를 물고 있는 입안이 
			ENDIF
			PRINTFORML 절정한 %조사처리(CALLNAME:(TARGET),"는")% 넑을 잃고 있다
		ENDIF
	ELSE
		IF FLAG:70 == 1
			PRINTFORMW %CALLNAME:(TARGET)%의 %조사처리(OPPAI_DESCRIPTION((TARGET)),"로")% %조사처리(TINKO(PLAYER),"를")% 훑어 내면서 %CALLNAME:(TARGET)%의 얼굴에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 뿌렸다…
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)%의 %OPPAI_DESCRIPTION((TARGET))%에 끼우게 한 %TINKO(PLAYER)%의 끝으로부터 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 뿜어져 나왔다…
		ENDIF
	ENDIF
;手淫フェラ
ELSEIF GETBIT(TFLAG:1,20)
	IF NOWEX:TARGET:M절정
		PRINTFORML 입안을 채워지는 쾌감에 절정한 %CALLNAME:(TARGET)%의 목을 향해서, %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다…
	ELSE
		PRINTFORML %CALLNAME:(TARGET)%의 목을 향해서, %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 배출했다…
	ENDIF
	;精飲
	IF EXP_UP(15,TARGET) == NOWEX:PLAYER:사정 * 3
		PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 사정의 기세에 숨이 막히면서도 제대로 귀두를 물고
		PRINTFORMW 짜내는 듯한 손놀림으로 줄기 부분을 훑어 내고 있다…
		;RETURN 1
	ELSE
		IF TCVAR:TARGET:정음경험 == 0
			PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 놀라 %조사처리(TINKO(PLAYER),"로")%부터 입을 떼어버렸다
			PRINTFORMW 얼굴을 %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 더럽히면서 처음 보는 맛에 조금 당황하고 있다…
		ELSE
			PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 내민 혀로 요도구를 자극하면서
			PRINTFORMW 짜내는 듯한 손놀림으로 줄기 부분을 훑어 내고 있다……
		ENDIF
		;RETURN 2
	ENDIF
;部位別処理
;膣内
ELSEIF TCVAR:PLAYER:12 == 1
	IF TFLAG:50 == 13
		PRINTFORML %조교자는% %CALLNAME:(TFLAG:152)%의 질내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 내자 일단 %조사처리(TINKO(PLAYER),"를")% 뽑고 나서
		PRINTFORML 사정이 멈추기도 전에 %CALLNAME:(TARGET)%에 삽입하여 마지막 한 방울까지 쏟아부었다…
		SIF NOWEX:TARGET:V절정
			PRINTFORML 질 내부를 찔려지는 쾌감과 정액을 쏟아 부어지는 열락에, %조사처리(CALLNAME:(TARGET),"는")% 온 몸을 떨며 끝에 다다르고 있다
	;時間停止or睡眠姦
	ELSEIF FLAG:70 || CFLAG:MASTER:장난 == 2
		IF TFLAG:50 == 7
			IF (FLAG:70 && EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50) || (CFLAG:MASTER:장난 == 2 && EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50)
				IF EXP:TARGET:자궁구개발경험 >= 50
					PRINTFORML %조교자는% 솟구치는 사정감에 저항하지 않고, %조사처리(TINKO(PLAYER),"를")% 깊게 찔러 넣었다…
					PRINTFORMW \@ FLAG:70 ? 시간이 멈춰 져서 아무것도 인식할 수 없는 # 자고 있는 \@ %CALLNAME:(TARGET)%의 자궁구를 관통한 채로, 직접 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
				ELSE
					PRINTFORML %조교자는% \@ FLAG:70 ? 시간이 멈춰 져서 아무것도 인식할 수 없는 # 자고 있는 \@ %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 고정하고
					PRINTFORMW 일절 주저 없이 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
				ENDIF
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				PRINTFORML \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@ %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 깊게 찔러 넣고
				PRINTFORMW 달라붙어 오는 자궁구에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ELSE
				PRINTFORML \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@%CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 고정하고
				PRINTFORMW 자궁 내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			ENDIF
			SIF NOWEX:TARGET:V절정
				PRINTFORML \@ FLAG:70 ? 시간이 멈춘 # 본인이 잠든 \@ 동안, 그래도 절정에 흠칫흠칫 떨리는 질 내부는, 한층 더 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 원하고 있다
		ELSE
			IF (FLAG:70 && EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50) || (CFLAG:MASTER:장난 == 2 && EXP:TARGET:수면간경험 >= EXP:TARGET:애정경험 + 50)
				IF TALENT:TARGET:체형 == -2
					SIF NOWEX:TARGET:V절정
						PRINTFORML %CALLNAME:(TARGET)%의 몸은 좁은 질 내부와 자궁구를 찔려지는 감촉에 끝에 다다른다
					IF EXP:TARGET:자궁구개발경험 >= 50
						PRINTFORML %마스터는% 작은 %CALLNAME:(TARGET)%의 질에 억지로 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고, %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다
						PRINTFORML 토해낸 %조사처리(SAMEN_DESCRIPTION(PLAYER),"는")% 순식간에 작은 질 내부를 채우고, 미숙하게 개발되어 버린 자궁구로 흘러 들어갔다
						PRINTFORMW %마스터는% 외형 그대로의 어린 성기의 강렬한 조임을 즐기며 마지막 한 방울까지 쏟아부었다…
					ELSE
						PRINTFORML %마스터는% 작은 %CALLNAME:(TARGET)%의 질에 억지로 %조사처리(TINKO(PLAYER),"를")% 뿌리까지 밀어넣고, %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다
						PRINTFORMW 토해낸 %조사처리(SAMEN_DESCRIPTION(PLAYER),"는")% 순식간에 작은 질 내부를 채우고, 자연스럽지 않게 넓혀진 결합부로부터 흘러넘치고 있다… 
							;ギチギチに広げられた結合部からあふれている… 
					ENDIF
				ELSEIF EXP:TARGET:자궁구개발경험 >= 50
					SIF NOWEX:TARGET:V절정
						PRINTFORML 절정한 %CALLNAME:(TARGET)%의 질 내부가, 움찔움찔 경련하며 꽉 조여 정액을 재촉해 온다
					PRINTFORML 일절 주저 없이 %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 흩뿌렸다
					PRINTFORML %SAMEN_DESCRIPTION(PLAYER)% 변기가 된 질 구멍은 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 열심히 빨아올려 움찔움찔 뛰어오르는 %조사처리(TINKO(PLAYER),"를")% 부드럽게 조여준다
					PRINTFORMW %마스터는% 『기분좋게 사정할 수 있는 구멍』을 사용해 기분 좋게 성욕 처리를 했다…
				ELSE
					SIF NOWEX:TARGET:V절정
						PRINTFORML 끝에 다다른 %CALLNAME:(TARGET)%의 질내가, %조사처리(TINKO(PLAYER),"를")% 훑어내듯이 음란하게 꿈틀거렸다
					IF EX:TARGET:처녀상실체크
						PRINTFORML %마스터는% 가차 없이 질내에서 사정하여, 누구에게도 오염되지 않았던 처녀질에 마음껏 정액으로 마킹 했다…
					ELSE
						PRINTFORML %마스터는% 주저 없이 고기 오나홀로 하고 있는 %CALLNAME:(TARGET)%의 질내에 사정했다
					ENDIF
					PRINTFORML \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@ %조사처리(CALLNAME:(TARGET),"는")%, 질 구멍을 오나홀 대신 사용되며 질내사정되고 있다…
				ENDIF
			ELSEIF TALENT:TARGET:체형 == -2
				SIF NOWEX:TARGET:V절정
					PRINTFORML %CALLNAME:(TARGET)%의 몸은, 좁은 질 내부와 자궁구를 찔려지는 감촉에 끝에 다다른다
				PRINTFORML \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@ %CALLNAME:(TARGET)%의 질내에 사정했다
				PRINTFORMW 어린 아이처럼 작은 질내는 순식간에 채워져 결합부에서 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 흘러나오고 있다…
			ELSEIF EXP:TARGET:자궁구개발경험 >= 50
				SIF NOWEX:TARGET:V절정
					PRINTFORML %CALLNAME:(TARGET)%의 질 내부는, 자궁구를 두두리는 쾌감에 절정 했다
				PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 질 깊숙히 사정했다
				PRINTFORMW \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@ %CALLNAME:(TARGET)%의 자궁구는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 원하는 듯이 달라붙어 온다…
			ELSE
				SIF NOWEX:TARGET:V절정
					PRINTFORML 강렬한 조임이 %TINKO(PLAYER)%에 더해지고 %CALLNAME:(TARGET)%의 절정을 전해온다
				PRINTFORML \@ NOWEX:TARGET:V절정 ? 동시에  #  \@%조교자는% %CALLNAME:(TARGET)%의 질내에 사정했다
				PRINTFORMW \@ FLAG:70 ? 시간이 멈춰진 # 자고 있는 \@ %조사처리(CALLNAME:(TARGET),"는")% 저항도 못하고 질내에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 주입되어 간다…
			ENDIF
		ENDIF
	;通常
	ELSE
		IF NOWEX:TARGET:V절정
			PRINTFORML 온몸을 떨고, 높고 날카로운 기쁨의 소리를 지르며, %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다른다
			PRINTFORML %조사처리(TINKO(PLAYER),"를")% 꾸욱꾸욱하고 질 내부가 조이고, 꾸물꾸물하며 질 주름이 빨아들여, 정액을 재촉하고 있다
		ENDIF
		IF TFLAG:50 == 7
			PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 질 깊숙히 %조사처리(TINKO(PLAYER),"를")% 고정하고
			PRINTFORMW \@ CFLAG:TARGET:900 == 7 ? 위험일의  # \@자궁에 직접 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			SIF EXP:TARGET:자궁구개발경험 >= 300
				PRINTFORMW %SAMEN_DESCRIPTION(PLAYER)%의 맛을 익힌 자궁은 크게 입을 열어, 탐욕스럽게 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 들이키고 있다…
		ELSE
			PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 질내에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 쏟아 부었다…
			IF CFLAG:TARGET:900 == 7
				PRINTFORMW 위험일의 질육은 찔끔찔끔 꿈틀거리며, 정자를 가장 안쪽으로 보내려고 하고 있다…
			ELSE
				PRINTFORMW %CALLNAME:(TARGET)%의 질육은 %TINKO(PLAYER)%의 맥동에 맞추듯 경련되고, 사정중인 %TINKO(PLAYER)%에 달라붙는다…
			ENDIF
		ENDIF
		SIF TALENT:TARGET:동물귀 && TEQUIP:TARGET:체위 == 2 && NOWEX:TARGET:V절정
			PRINTFORML 암컷의 본능에 자극을 받은 %조사처리(CALLNAME:(TARGET),"는")% %조사처리(HIP((TARGET)),"를")% %CALLNAME:MASTER%의 허리에 밀어붙여 정액을 낭비하지 않도록 하고 있다
		IF ABL:TARGET:질내사정중독 > 3
			SIF NOWEX:TARGET:V절정
				PRINTFORML 절정과 동시에 질내에 사정되어, 침을 흘리는 %CALLNAME:(TARGET)%
			PRINTFORML 질내 사정의 감각을 참을 수 없는 것인지, %조사처리(CALLNAME:(TARGET),"는")% 달콤한 목소리를 내며 행복 그 자체라는 표정으로 사정을 받아 들이고 있다…
		ELSEIF TCVAR:TARGET:억지로
			PRINTFORML \@ NOWEX:TARGET:V절정 ? 순간, 녹는 듯한 표정으로 절정의 여운에 떠는  #  \@%조사처리(CALLNAME:(TARGET),"는")% 가냘픈 비명을 흘리면서 사정을 받아들이고 있다…
		ENDIF
		SIF NOWEX:MASTER:사정 > 1
			PRINTFORML %마스터는% 녹는 듯한 사정감에 허리를 움찔하면서, 오래도록 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 계속 쏟아 부었다…
	ENDIF
;アナル
ELSEIF GETBIT(TFLAG:1,2)
	IF NOWEX:TARGET:A절정
		PRINTFORML 아플 수 있을 정도로 강하게 아날을 조여 %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다른다
	ENDIF
	PRINTFORM \@ NOWEX:TARGET:A절정 ? 동시에,  #  \@%조교자는% 
	SIF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORM \@ FLAG:70 ? 시간이 멈춘 # 자고 있는 \@ 
	PRINTFORML %CALLNAME:(TARGET)%의 장내에 사정했다
	PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 흘러넘치는 장내는 미세하게 떨리며 %조사처리(TINKO(PLAYER),"를")% 꽉 조어오고 있다…
	IF !FLAG:70 && CFLAG:MASTER:장난 != 2
		IF ABL:TARGET:항내사정중독 > 3 && !FLAG:70
			PRINTFORML 절정의 여운에 몸을 흔들면서, 엉덩이 구멍을 조여 얼굴을 느슨하게 하는 %CALLNAME:(TARGET)%
			PRINTFORML 항내 사정 감각을 견딜 수 없는지, %조사처리(CALLNAME:(TARGET),"는")% 달콤한 목소리를 내며 행복 그 자체라는 표정으로 사정을 받아 들이고 있다…
		ELSEIF TCVAR:TARGET:억지로 && !FLAG:70
			PRINTFORML 억지로 끝에 다다르고 눈가에 눈물이 맺힌 %조사처리(CALLNAME:(TARGET),"는")%, 가냘픈 비명을 흘리면서 사정을 받아들이고 있다…
		ENDIF
	ENDIF
;手淫
ELSEIF GETBIT(TFLAG:1,3)
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %마스터는% %조사처리(TINKO(PLAYER),"를")% 훑고있던 %CALLNAME:(TARGET)%의 손에 귀두를 꽉 눌러 사정했다…
	ELSE
		IF CFLAG:우후후 == 2
			PRINTFORML 사정이 시작되자 %조사처리(CALLNAME:(TARGET),"는")% 한층 더 격렬하게 %조사처리(TINKO(PLAYER),"를")% 훑어내고
			PRINTFORML \@ NOWEX:PLAYER:사정 > 1 ? 대량으로 뿜어져 나오는 #토해내는 \@ %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 만족스럽게 바라보면서
			PRINTFORMW %CALLNAME:MASTER%의% TINKO(PLAYER)%의 맥동이 멈출 때까지 애무를 계속했다…
		ELSE
			IF ABL:TARGET:정액중독 >= 5 && ABL:TARGET:기교 > 3
				PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 황홀한 표정으로 혀를 내밀면서 %CALLNAME:PLAYER%의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 뒤집어쓰고 있다…
			ELSEIF NOWEX:PLAYER:사정 == 1
				PRINTFORMW %CALLNAME:(TARGET)%에게 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 덮어졌다…
			ELSE
				PRINTFORMW %CALLNAME:(TARGET)%의 얼굴과 손이 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 얼룩져 갔다…
			ENDIF
		ENDIF
	ENDIF
;フェラチオ
ELSEIF GETBIT(TFLAG:1,4)
	IF NOWEX:TARGET:M절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORML %TINKO(PLAYER)%에 구강을 문질러서, %조사처리(CALLNAME:(TARGET),"는")% 입술을 바들바들 떨며 끝에 다다르고 있다
		ELSE
			PRINTFORML 입안을 채우는 %TINKO(PLAYER)%에 황홀한 표정을 짓는 %CALLNAME:(TARGET)%
			PRINTFORML 한 박자 놓고 입술을 오므리면서 어깨를 떨며 %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다랐다
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		SELECTCASE RAND:2
			CASE 0
				PRINTFORMW %마스터는% %조사처리(TINKO(PLAYER),"를")% %CALLNAME:(TARGET)%의 목구멍까지 찔러넣어 사정했다…
			CASE 1
				PRINTFORMW %마스터는% %CALLNAME:(TARGET)%에게 귀두를 입에 물게하고 혀 위에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 토해냈다…
		ENDSELECT
	ELSE
		IF CFLAG:우후후 == 2
			PRINTFORML %마스터는% %CALLNAME:(TARGET)%의 입안에 사정했다…
			PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 입안에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 받아들이면서 혀로 귀두를 애무하고, 더 많은 사정을 재촉하고 있다…
		ELSEIF SELECTCOM == 140
			PRINTFORML %조교자는% %CALLNAME:(TARGET)%의 목의 깊숙한 곳까지 %조사처리(TINKO(PLAYER),"를")% 삽입한 채로 사정해
			PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 식도로 보냈다…
		ELSE
			PRINTFORMW %조교자는% %CALLNAME:(TARGET)%의 입안에 사정했다
			;精飲
			IF EXP_UP(15,TARGET) == NOWEX:PLAYER:사정 * 3
				IF NOWEX:PLAYER:사정 == 1
					PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 입술로 귀두를 훑어내 모든 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 입안에 받아 들인 후
					PRINTFORMW 입안의 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 목구멍으로 소리를 내며 삼켰다…
				ELSE
					PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% 귀두를 물고 입에 뿌려지는 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 삼켜 갔다…
				ENDIF
				;RETURN 1
			ELSE
				IF TCVAR:TARGET:정음경험 == 0
					PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 깜짝 놀라 %조사처리(TINKO(PLAYER),"로")%부터 입을 떼어버렸다
					PRINTFORMW 얼굴을 %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 더럽히면서 처음 보는 맛에 조금 당황했다…
				ELSEIF TCVAR:TARGET:정음경험 < 20
					PRINTFORML 사정이 끝나자 %조사처리(CALLNAME:(TARGET),"는")% 입속의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 뱉어내버렸다
					PRINTFORMW 아직 %SAMEN_DESCRIPTION(PLAYER)%의 맛에 익숙지 않은 것 같다…
				ELSE
					IF NOWEX:PLAYER:사정 == 1
						PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 입술로 귀두를 훑어내 모든 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 입안에 받아 들인 후
						PRINTFORMW 펼친 손 위에 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 뱉어내 %CALLNAME:PLAYER%에게 과시했다…
					ELSE
						PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 혀를 내밀면서 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 뿜어져 나오는 요도구를 자극하여,
						PRINTFORMW 열린 입에서는 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 주르르 흘러나오고 있다…
					ENDIF
				ENDIF
				;RETURN 2
			ENDIF
		ENDIF
	ENDIF
;パイズリ
ELSEIF GETBIT(TFLAG:1,5)
	PRINTFORM %마스터는% 
	SIF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORM \@ FLAG:70 ? 시간이 멈춘 # 잠자고 있는 \@ 
	PRINTFORM %CALLNAME:(TARGET)%의 
	IF TALENT:가슴사이즈 > 0
		PRINTFORMW %OPPAI_DESCRIPTION((TARGET))%에 %조사처리(TINKO(PLAYER),"를")% 사이에 끼워둔 채 사정했다…
	ELSEIF TALENT:가슴사이즈 < 0
		PRINTFORMW %OPPAI_DESCRIPTION((TARGET))%에 %조사처리(TINKO(PLAYER),"를")% 문지르면서 사정했다…
	ELSE
		PRINTFORMW %OPPAI_DESCRIPTION((TARGET))%에 %조사처리(TINKO(PLAYER),"를")% 사이에 끼워둔 채 사정했다…
	ENDIF
	IF NOWEX:PLAYER:사정 == 1
		IF TALENT:가슴사이즈 > 0
			PRINTFORMW %CALLNAME:(TARGET)%의 %OPPAI_DESCRIPTION((TARGET))% 골짜기에 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 쌓여간다…
		ELSE
			PRINTFORMW %CALLNAME:(TARGET)%의 가슴팍에서 얼굴까지 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"가")% 흩뿌려졌다…
		ENDIF
	ELSE
		PRINTFORMW 흩뿌려진 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% %CALLNAME:(TARGET)%의 얼굴에서 가슴팍까지 덮고 있다…
	ENDIF
	IF NOWEX:TARGET:B절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORMW 뾰족한 젖꼭지와 떨리는 %조사처리(OPPAI_DESCRIPTION((TARGET)),"가")%, %CALLNAME:(TARGET)%의 절정을 드러내고 있다
		ELSE
			PRINTFORML 꽉하고 %조사처리(OPPAI_DESCRIPTION((TARGET)),"를")% %TINKO(PLAYER)%에 밀어붙여 %조사처리(CALLNAME:(TARGET),"는")% 절정에 이르고 있다
		ENDIF
	ENDIF
;疑似
ELSEIF GETBIT(TFLAG:1,25)
	PRINTFORM %조교자는% 
	IF FLAG:70
		PRINTFORM 시간이 멈춘 
	ELSEIF CFLAG:MASTER:장난 == 2
		PRINT 자고 있는 
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 질구에 문지르던 페니스를 성기 바로 위에 붙여서 사정했다…
	PRINTFORMW %CALLNAME:TARGET%의 아랫배가 %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 더럽혀져 간다…
;素股
ELSEIF GETBIT(TFLAG:1,6)
	IF !GETBIT(TALENT:성별,1) && NOWEX:TARGET:C절정
		IF FLAG:70 || CFLAG:MASTER:장난 == 2
			PRINTFORML 툭 튀어나온 클리토리스를 %조사처리(TINKO(PLAYER),"로")% 문지르자 %조사처리(CALLNAME:(TARGET),"는")% 끝에 다다른다
		ELSE
			PRINTFORML 클리를  문지르는 감각에, %조사처리(CALLNAME:(TARGET),"는")% 몸을 떨며 끝에 다다르고 있다
		ENDIF
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %조교자는% \@ FLAG:70 ?시간이 멈춘 # 자고 있는 \@ %CALLNAME:(TARGET)%의 성기에 %조사처리(TINKO(PLAYER),"를")% 문지르면서 사정했다…
	ELSE
		PRINTFORMW %조교자는% 밀착된 성기 사이에 사정했다…
	ENDIF
;足コキ
ELSEIF GETBIT(TFLAG:1,7)
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		PRINTFORMW %조교자는% \@ FLAG:70 ?시간이 멈춘 # 자고 있는 \@ %CALLNAME:(TARGET)%의 다리에 %조사처리(TINKO(PLAYER),"를")% 문지르면서 사정했다…
	ELSE
		PRINTFORM %CALLNAME:(TARGET)%의 
		SIF TALENT:TARGET:새드
			PRINTFORM 차가운 시선을 받으면, 그 
		PRINTFORMW 다리에 \@ NOWEX:MASTER:사정 == 2 ? 대량의 # 뜨거운 \@ %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 배출했다…
	ENDIF
;泡踊り
ELSEIF GETBIT(TFLAG:1,8)
	IF NOWEX:TARGET:사정 && NOWEX:MASTER:사정
		PRINTFORML %조사처리(CALLNAME:(TARGET),"와")% %조교자는% 서로 꼭 껴안은 채로 동시에 정액을 배출했다…
		PRINTFORMW 두 사람은 흠칫흠칫 정령을 배출한 음경을 비비듯 서로의 몸을 요구하였다…
	ELSEIF NOWEX:TARGET:사정
		PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% %조사처리(TINKO(PLAYER),"로")%부터 %조사처리(SAMEN_DESCRIPTION(PLAYER),"를")% 배출하면서 %CALLNAME:PLAYER%에게 몸을 밀착시키고 있다…
	ELSE
		PRINTFORMW %조사처리(CALLNAME:(TARGET),"는")% %조교자가% 배출한 %조사처리(SAMEN_DESCRIPTION(PLAYER,1),"를")% 문질러지듯 몸을 밀착시키고 있다…
	ENDIF
;アナル奉仕
ELSEIF GETBIT(TFLAG:1,9)
	PRINTFORMW 머리를 아래로 해서 몸을 구부려진 %CALLNAME:PLAYER%의 얼굴은, 토해낸 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"로")% 뒤덮여 갔다…
;Ａ責め
ELSEIF GETBIT(TFLAG:1,12)
	PRINTFORM 허리의 안쪽이 저리는 듯한 쾌락과 함께, %CALLNAME:PLAYER%의 페니스에서 
	IF NOWEX:MASTER:사정 >= 2
		PRINTFORML 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 콸콸하고 흘러나오고 있다…
		PRINTFORMW %조교자는% 끝없이 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 흘러나가는 쾌감에 몇 번이나 허리를 떨면서 몸부림쳤다…
	ELSE
		PRINTFORMW %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 꿀럭꿀럭하고 흘러나오고 있다…
	ENDIF
;Ｖ責め
ELSEIF GETBIT(TFLAG:1,13)
	IF NOWEX:MASTER:사정 >= 2
		PRINTFORML 허리의 안쪽이 저리는 듯한 쾌락과 함께, %CALLNAME:PLAYER%의 페니스에서 대량의 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 콸콸하고 흘러나오고 있다…
		PRINTFORMW 암컷의 쾌락을 맛보면서 대량 사정으로 %조교자는% 머리가 새하얗게 되는 것을 느꼈다…
	ELSE
		PRINTFORMW 허리의 안쪽이 저리는 듯한 쾌락과 함께, %CALLNAME:PLAYER%의 페니스에서 %조사처리(SAMEN_DESCRIPTION(PLAYER),"가")% 꿀럭꿀럭하고 흘러나오고 있다…
	ENDIF
ENDIF

IF CFLAG:TARGET:우후후 == 2
	SIF !(NOWEX:TARGET:C절정 + NOWEX:TARGET:V절정 + NOWEX:TARGET:A절정 + NOWEX:TARGET:B절정 + NOWEX:TARGET:M절정)
		PRINTFORML %조사처리(CALLNAME:(TARGET),"는")% 절정에 이완된 %CALLNAME:MASTER%의 표정을 바라보며 히죽히죽거리고 있다…
	IF GROUPMATCH(TCVAR:PLAYER:사정한개소플래그,1,2)
		CALL SHIBORITORI(MASTER,(TARGET))
	ELSEIF GROUPMATCH(TCVAR:TARGET:사정한개소플래그,1,2)
		CALL SHIBORITORI((TARGET),MASTER)
	ENDIF
ENDIF

		

SIF FLAG:대사색깔
	TRYCALLFORM M_KOJO_COLOR_K{NO:TARGET}

@PrintMilkAndOmorashi(ARG, OMORASHI)
#DIM OMORASHI

;絶頂と噴乳とおもらし

SIF ARG >= 3
	PRINTS CALLNAME:TARGET
IF ARG >= 5 && TEQUIP:50 > 0
	PRINTL 의 고기 주름은 끈적거리는 액체를 떨어뜨리며, 허덕이듯이 개폐를 반복하고 있다…
ELSEIF ARG >= 5
	PRINTFORML %조사만처리(CALLNAME:TARGET,"는")% 단속적으로 절정을 반복하면서, 맥없이 탈진하고 있다…
ELSEIF ARG >= 3
	PRINTFORML %조사만처리(CALLNAME:TARGET,"는")% \@CFLAG:MASTER:장난 == 2 ?숨소리를 거칠게 하면서 #거친 숨을 몰아쉬면서 \@ 절정의 여운에 젖어있다…
ENDIF

IF (ARG >= 7 && TCVAR:이뇨제 && TALENT:오줌싸개버릇) || (ARG >= 9 && NOWEX:분유)
	PRINTFORM %타겟은% 움찔움찍 몸을 경련하고, 
ELSEIF (ARG >= 7 && OMORASHI) || (ARG >= 7 && NOWEX:분유)
	PRINTFORM 경련하는 %타겟은% 
ELSEIF (ARG >= 5 && OMORASHI) || (ARG >= 5 && NOWEX:분유)
	PRINTFORM 축 처진 %타겟은% 
ELSEIF (ARG >= 3 && OMORASHI) || (ARG >= 3 && NOWEX:분유)
	PRINTFORM 떨고있는 %타겟은% 
ELSEIF (ARG >= 1 && OMORASHI) || NOWEX:분유
	PRINTFORM %타겟은% 
ENDIF
IF NOWEX:분유
	IF ARG >= 9
		LOCALS = 흩날릴 정도로 힘차게 모유를 분출하
	ELSEIF ARG >= 7
		LOCALS = 뷰─웃 뷰─웃하며 모유를 분출하
	ELSEIF ARG >= 5
		LOCALS = 넘쳐흐를 듯이 모유를 분출하
	ELSEIF ARG >= 3
		LOCALS = 모유를 분출하
	ELSE
		LOCALS = 모유를 흘리
	ENDIF
	PRINTFORM %조사처리(NIPPLE_DESCRIPTION(TARGET),"로")%부터 %LOCALS%
	IF OMORASHI
		PRINTFORM 면서, 
	ELSE
		PRINTFORML …
	ENDIF
ENDIF
IF ARG >= 7 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 하염없이 소변을 흘리고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 소변이 흠뻑 스며들어 간다…
ELSEIF ARG >= 7 && TCVAR:이뇨제
	PRINTFORML 요도구에서 분수처럼 소변을 뿜어낸다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 소변이 흠뻑 스며들어 간다…
ELSEIF ARG >= 7 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 힘차게 물줄기를 뿜어내고, 예쁜 호를 그리게했다…
	PRINTFORML %CALLNAME:TARGET%의 이불로 소변이 흠뻑 스며들어 간다…
ELSEIF ARG >= 5 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 분수처럼 소변을 뿜어낸다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 생겼다…
ELSEIF ARG >= 5 && TCVAR:이뇨제
	PRINTFORML 요도구에서 힘차게 물줄기를 뿜어내고, 예쁜 호를 그리게했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 생겼다…
ELSEIF ARG >= 5 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 물줄기를 떨어뜨려 웅덩이를 만들었다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 큰 얼룩이 생겼다…
ELSEIF ARG >= 3 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구로부터 기세 좋게 수류를 내뿜게 해 호를 그리게 했다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 생겼다…
ELSEIF ARG >= 3 && TCVAR:이뇨제
	PRINTFORML 요도구에서 물줄기를 떨어뜨려 웅덩이를 만들었다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 생겼다…
ELSEIF ARG >= 3 && TALENT:오줌싸개버릇
	PRINTFORML 음순에서 오줌이 줄줄 새고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 얼룩이 생겼다…
ELSEIF ARG >= 1 && TCVAR:이뇨제 && TALENT:오줌싸개버릇
	PRINTFORML 요도구에서 물줄기를 떨어트렸다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 작은 얼룩이 생겼다…
ELSEIF ARG >= 1 && TCVAR:이뇨제
	PRINTFORML 음순에서 오줌이 줄줄 새고 있다…
	PRINTFORML %CALLNAME:TARGET%의 이불에 작은 얼룩이 생겼다…
ENDIF
IF TEQUIP:유두빨기 && NOWEX:분유
	SELECTCASE ARG
		CASE IS >= 9
			LOCALS = 입가에 흘리면서 모유를 꿀꺽꿀꺽 마시고 있다…
		CASE IS >= 7
			LOCALS = 입가에 흘러넘칠 정도의 모유를 마시고 있다…
		CASE IS >= 5
			LOCALS = 주르륵 하고 흘러 넘치는 모유를 마시고 있다…
		CASE IS >= 3
			LOCALS = 모유를 마시고 있다…
		CASEELSE
			LOCALS = 배어 나오는 모유를 혀로 핥고 있다…
	ENDSELECT
	PRINTFORML %마스터는% %CALLNAME:TARGET%의 %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 빨아 올려 %LOCALS%
ENDIF
