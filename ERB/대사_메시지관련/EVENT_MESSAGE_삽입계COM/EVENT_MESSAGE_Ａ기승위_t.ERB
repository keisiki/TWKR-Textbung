;-------------------------------------------------
;94, A기승위한다
;-------------------------------------------------
@MESSAGE_COM94
;뽑지 않고
IF FLAG:70 == 1
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:사정
			PRINTFORM %조교자는%
			IF ABL:PLAYER:A감각 >= 3
				PRINTFORML 스스로 허리를 추잡하게 비틀며, 직장으로부터 전해지는 쾌락을 탐내고 있다
			ELSE
				PRINTFORML 열심히 허리를 곤두세워%CALLNAME:TARGET%의 남성기에 봉사하고 있다
			ENDIF
			PRINTFORML 이윽고 시간이 멈춘 채로의 %타겟은% %TINKO(TARGET)%만을 절정시켜, %CALLNAME:PLAYER%의 직장에 정령을 쏟았다…
			;조교자도 사정
			SIF NOWEX:PLAYER:사정
				PRINTFORML 동시에 %CALLNAME:PLAYER%의 끝에서도 뜨거운 백탁이 폭, 공중을 날았다…
		;전턴으로 A질내사정
		ELSEIF TCVAR:PLAYER:104
			PRINTFORML %조교자는% 시간이 정지된 채로 사정한 %CALLNAME:TARGET%의 %TINKO(TARGET)%를 개방하는 일 없이, 다시 허리를 거절하기 시작한다
			PRINTFORML 넘쳐 나온 정액과 장액은 접합부를 적셔, 끈적끈적 더러워진 흰 실을 늘어뜨린다…
		ELSE
			PRINTFORML %조교자는% 시간을 멈춘%CALLNAME:TARGET%의 %TINKO(TARGET)%를 개방하는 일 없이, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		PRINTFORML %조교자는% 시간을 멈춘%타겟을% 위로 향해 재우고 허리 위에 걸치면(자)
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%의 틈새로부터
		IF EXP:PLAYER:A경험 >= EXPLV:4
			PRINTFORML 이미 부드럽게 녹고 자르고 있는 애널로 %TINKO(TARGET)%를 삼켰다
		ELSEIF EXP:PLAYER:A경험 >= EXPLV:3
			PRINTFORML 신무성해에 입을 연 애널로 %TINKO(TARGET)%를 삼켰다
		ELSE
			PRINTFORML 아직도 이물을 거절하려고 하는 애널로 %TINKO(TARGET)%를 삼켰다
		ENDIF
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 장벽을 도려내 범하는 감각에, %조교자는% 허덕이고 있다…
	ENDIF
ELSEIF CFLAG:MASTER:장난 == 2
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:사정
			PRINTFORM %조교자는%
			IF ABL:PLAYER:A감각 >= 3
				PRINTFORML 스스로 허리를 추잡하게 비틀며, 직장으로부터 전해지는 쾌락을 탐내고 있다
			ELSE
				PRINTFORML 열심히 허리를 곤두세워%CALLNAME:TARGET%의 남성기에 봉사하고 있다
			ENDIF
			PRINTFORML 이윽고 잔 채로 범해지고 있는 %타겟은% %TINKO(TARGET)%를 절정시켜, %CALLNAME:PLAYER%의 직장에 정령을 쏟았다…
			;조교자도 사정
			SIF NOWEX:PLAYER:사정
				PRINTFORML 동시에 %CALLNAME:PLAYER%의 끝에서도 뜨거운 백탁이 폭, 공중을 날았다…
		;전턴으로 A질내사정
		ELSEIF TCVAR:PLAYER:104
			PRINTFORML %조교자는% 잔 채로 사정한 %CALLNAME:TARGET%의 %TINKO(TARGET)%를 개방하는 일 없이, 다시 허리를 거절하기 시작한다
			PRINTFORML 넘쳐 나온 정액과 장액은 접합부를 적셔, 끈적끈적 더러워진 흰 실을 늘어뜨린다…
		ELSE
			PRINTFORML %조교자는% 자고 있는 %CALLNAME:TARGET%의 %TINKO(TARGET)%를 개방하는 일 없이, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		PRINTFORML %조교자는% 자고 있는 %타겟을% 위로 향해 재우고 허리 위에 걸치면(자)
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%의 틈새로부터
		IF EXP:PLAYER:A경험 >= EXPLV:4
			PRINTFORML 이미 부드럽게 녹고 자르고 있는 애널로 %TINKO(TARGET)%를 삼켰다
		ELSEIF EXP:PLAYER:A경험 >= EXPLV:3
			PRINTFORML 신무성해에 입을 연 애널로 %TINKO(TARGET)%를 삼켰다
		ELSE
			PRINTFORML 아직도 이물을 거절하려고 하는 애널로 %TINKO(TARGET)%를 삼켰다
		ENDIF
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 장벽을 도려내 범하는 감각에, %조교자는% 허덕이고 있다…
	ENDIF
ELSE
	IF TCVAR:PLAYER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:사정
			PRINTFORM %조교자는%
			IF ABL:PLAYER:A감각 >= 3
				PRINTFORML 스스로 허리를 추잡하게 비틀며, 직장으로부터 전해지는 쾌락을 탐내고 있다
			ELSE
				PRINTFORM 열심히 허리를 곤두세워%CALLNAME:TARGET%의
				IF TALENT:TARGET:2 & 2
					PRINTFORM %TINKO(TARGET)%
				;에 처세의 재능응
				ELSE
					PRINTFORM 페니스밴드
				ENDIF
				PRINTL 에 봉사하고 있다
			ENDIF
			PRINTFORM 이윽고 달한 %CALLNAME:TARGET%의 그것은 부들부들 경련하고, %CALLNAME:PLAYER%의 직장에 정령을 쏟았다
			;조교자도 사정
			SIF NOWEX:PLAYER:사정
				PRINTFORM 동시에 %CALLNAME:PLAYER%의 끝에서도 뜨거운 백탁이 폭, 공중을 날았다
			PRINTFORML …
		;전턴으로 A질내사정
		ELSEIF TCVAR:PLAYER:104
			PRINTFORM %조교자는% %CALLNAME:TARGET%로부터 비난과 압력을 받으면서 항문을 꽉 잡고 있다
			PRINTFORM 넘쳐 나온 정액과 장액이 접합부를 적셔, 끈적끈적 더러워진 흰 실을 늘어뜨린다…
		ELSE
			PRINTFORM %조교자는% %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 단단히 조이면서, 추잡하게 허리를 털고 있다
		ENDIF
	ELSE
		PRINTFORM %조교자는% %CALLNAME:TARGET%에
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받은 채로
		ELSE
			PRINTFORM 부탁되었던 대로
		ENDIF
		PRINTFORM 허리 위에 걸쳐, 항문을
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 의 끝에 억눌렀다
		SIF TEQUIP:PLAYER:0 & 2
			PRINTFORM %SHOW_BOTTOM(PLAYER, 2)%의 틈새로부터
		IF EXP:PLAYER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹고 자르고 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 살짝 깨무는 같게 받아 간다
		ELSEIF EXP:PLAYER:A경험 >= EXPLV:3
			PRINTFORM 터지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 될 수 없는 애널을 억지로 관철해져%마스터는% 괴로운 울음을 흘렸다
		ENDIF
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 장벽을 도려내 범하는 감각에, %조교자는% 허덕이고 있다…
	ENDIF
ENDIF