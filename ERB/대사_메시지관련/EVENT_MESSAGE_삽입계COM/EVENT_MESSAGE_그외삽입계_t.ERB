;-------------------------------------------------
;64, 역강간
;-------------------------------------------------
@MESSAGE_COM64
;역강간과 기승위 되고는 조금 뉘앙스가 다르다는 생각이 하지만, 수요가 확실할 때까지 보류합니다
;;연속
;IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
;	PRINTFORML %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 꽉 먹어 잡은 채로, 집요하게 허리를 거절해 세웠다…
;ELSE
;	PRINTFORM %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 억지로,
;	;조교자가 처녀
;	SIF TALENT:MASTER:처녀 == 1
;		PRINT 남을 받아들인 일이 없다
;	PRINTL 바기나의 안쪽까지 질불공평응이다…
;ENDIF
;역강간은 MASTER 주도인데, 지문이 TARGET 주도가 되고 있기 때문에, 절대로 변경
[SKIPSTART]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %타겟은% %CALLNAME:MASTER%에 올라탄 채,
			PRINTFORML 복받쳐 오는 충동에 맡겨 허리를 쳐 올렸다 
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTL  
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 백탁을 토해냈다
		PRINTFORML …
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %타겟은% %CALLNAME:MASTER%의 가장 깊숙한 곳에 정액을 쏟아내며, 달아오른 것이 채 식기도 전에 허리를 움직이기 시작했다
		PRINTFORML %마스터가% 질내사정의 여운에 한숨 돌릴 틈도 없이 흐트러지는 모습을 바라보며, 그 몸을 계속 흔들었다 …
	ELSE
		PRINTFORML %타겟은% 질안에서 뽑아내지도 않고 그대로 %마스터를% 안아 올려, 아래로부터 허리를 밀어올렸다…
	ENDIF
ELSE
	IF TALENT:MASTER:처녀 == 1
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 올라타 천천히 앉아
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 다리를 벌리고 올라타고, 천천히 내려앉아
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML , 남자를 모르는 어린 균열로 %TINKO(MASTER)%를 삼켜 갔다…
		;처녀
		ELSE
			PRINTFORML , 남자를 모르는 비렬로 %TINKO(MASTER)%를 삼켜 갔다…
		ENDIF
	ELSE
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸쳐,
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 다리를 벌리고 올라타고,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새에
		PRINTFORML %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 나갔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%에
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드에
		ENDIF
		PRINTFORML 전신을 꿰뚫리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF
[SKIPEND]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %타겟이% 발등에 불이 떨어진 것 같은 절박한 모습에, 입가가 느슨하게 풀어져 허리를 세우고 격렬하게 흔든다
		PRINTFORM %TINKO(TARGET)%가 질강을 후벼파는 열락에, %CALLNAME:MASTER%도 달콤하게 허덕이며 소리를 높여나갔다
		;조교자도 사정
		IF NOWEX:MASTER:11
			PRINTL  
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%도 %TINKO(MASTER)%에서, 세차게 %SAMEN_DESCRIPTION(MASTER)%를 쏟아내기 시작했다
		ENDIF
		PRINTFORML …
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 체내에 부어진 정액의 감촉에, 황홀한 표정을 지으며 허리를 흔들기 시작했다
		PRINTFORML 사정의 열락에 잠겨 있던 %CALLNAME:TARGET%지만, %CALLNAME:MASTER%의 질내에 훑어지는 쾌감에 허덕임을 멈출 수가 없었다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 %TINKO(TARGET)%를, 질 구멍으로 꾸욱꾸욱 단단히 조이면서 %마스터는% 허덕이고 있다
	ENDIF
ELSE
	IF TALENT:MASTER:처녀 == 1
		PRINTFORML %마스터는% %CALLNAME:TARGET%의 위에 다리를 벌리고 올라타, 스스로 %TINKO(TARGET)%를 질 안으로 이끌었다
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML 아직 남자를 모르는 어린 균열로 스스로 인도해, %TINKO(TARGET)%를 천천히 그 입구에 천천히 물린다…
		;처녀
		ELSE
			PRINTFORML 처음의 증거를 내부에 고이 숨긴 비렬을 양쪽으로 나누어 벌리고, %TINKO(MASTER)%를 천천히 삼켜 나갔다…
		ENDIF
	ELSE
		PRINTFORM %마스터는% %타겟을% 깔아누르고,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		PRINTFORML 자신의 %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 나갔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드
		ENDIF
		PRINTFORML 를 질강으로 휘감아 빨면서, %마스터는% 새된 소리로 뜨거운 비명을 흘리고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;66, 기승위애널
;-------------------------------------------------
@MESSAGE_COM66
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 시간을 멈추고 %타겟을% 자신에게 다리를 벌리고 올라타게 한 채, 다시 허리를 밀어 올리기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == MASTER
		PRINTFORML 장 안에서 뽑지도 않고 그대로, 꼼짝도 하지 않는%타겟을% 안아 올려, 아래로부터 허리를 밀어 올렸다…
	ELSE
		PRINTFORM %마스터는% 시간이 멈추어 있는 %CALLNAME:TARGET%의 다리를 열어, %CALLNAME:TARGET%의 밑으로 들어갔다
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스에 천천히%CALLNAME:MASTER%의 물건을 삽입해 갔다…
	ENDIF
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟은% 삽입된 채로 있는 물건을 단단히 조이며, 다시 허리를 움직이기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == MASTER
		PRINTFORML 장내로부터 빼내지도 않고  %타겟을% 안아 올려 아래로부터 허리를 밀어 올렸다…
	ELSE
		PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 다리를 벌리고 올라타,
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스로 천천히%CALLNAME:MASTER%의 물건을 삼켜 갔다…
	ENDIF
	;함락이 끝난 상태
	IF TALENT:연모
		PRINTFORM %타겟은%
		;작은 체형
		SIF TALENT:체형 < 0
			PRINT 작은 몸으로 열심히
		;거유
		SIF TALENT:가슴사이즈 == 1
			PRINT 크게 가슴을 흔들면서
		;폭유
		SIF TALENT:가슴사이즈 == 2
			PRINT 튀어 오르는 가슴에 신체마저 흔들리면서
		PRINTL 스스로 허리를 움직이고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;71, 양구멍삽입
;-------------------------------------------------
@MESSAGE_COM71
VARSET LOCAL
LOCAL:1 = MASTER_POSE(SET_V, SET_P, 1)
LOCAL:2 = MASTER_POSE(SET_A, SET_P, 1)
IF LOCAL:1
	LOCAL = LOCAL:1
ELSEIF LOCAL:2
	LOCAL = LOCAL:2
ENDIF
;LOCAL 두 구멍삽입 물러날 수 있는 사람
;연속
IF PREVCOM == SELECTCOM
	PRINTFORML %조사처리(CALLNAME:LOCAL,"는")% 앞과 뒤를 동시에 꿰뚫리는 폭력적인 쾌감에 넋을 잃고 허덕이고 있다…
	RETURN 0
;주인 W 꽂아
ELSEIF ((LOCAL:1 == 0 || LOCAL:2 == 0) && LOCAL == TARGET) && TALENT:MASTER:쌍창
	PRINTFORML %마스터는% 두 개의 자신으로 %CALLNAME:LOCAL%의 두 구멍을 단숨에 꿰둟었다…
;주인 V삽입-TARGET A삽입
ELSEIF LOCAL == LOCAL:1
	PRINTFORML %마스터는% 연결된 채로 %CALLNAME:LOCAL%의 볼기를 벌리고, %CALLNAME:TARGET%에게 뒷구멍도 범해달라고 재촉했다…
;주인 A삽입-조수 V삽입
ELSE
	PRINTFORML %마스터는% 아누스를 꿰뚫린 상태로 %CALLNAME:LOCAL%를 안아 올려, 다리가 벌어지게 하고서 %CALLNAME:TARGET%에게 앞구멍도 범해달라고 재촉했다…
ENDIF

[SKIPSTART]
;-------------------------------------------------
;74, 그라인드 한다
;-------------------------------------------------
@MESSAGE_COM75
IF ABL:MASTER:허리 < 3
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 밀어붙여 꽉 누른 채, 어색한 동작으로 좌우로 움직이기 시작했다.
ELSE
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 밀어붙여 꽉 누른 채, 천천히 の자를 쓰는 것처럼 허리를 움직였다.
ENDIF
;-------------------------------------------------
;75, 그라인드 시킨다
;-------------------------------------------------
@MESSAGE_COM75
%CALLNAME:MASTER%에게 스스로 움직이라고 명해진 %타겟은%, 얼굴을 새빨갛게 붉히며 허리를 천천히 움직이기 시작했다.
%CALLNAME:MASTER%에게 스스로 움직이라고 명해진 %타겟은%, 홍조를 띄운 얼굴로 웃음꽃을 피우며 허리를 흔들기 시작했다.
%마스터가% 말할 필요도 없이 %타겟은% 서투른 동작으로 허리를 앞뒤로 움직이기 시작했다.
%마스터가% 허리의 움직임을 멈추자 %타겟은% 곧바로 허리를 앞뒤로 움직이기 시작했다.
%타겟은% 쾌락을 더욱 탐하기 위해서일까, %마스터가% 명할 것도 없이 허리를 크게 흔들기 시작했다.
%마스터는% %CALLNAME:TARGET%에게 움직여 달라고 간청했다. %타겟은% 기학적인 미소를 지으며 허리를 흔들기 시작했다.


(허리 모습중. )
%타겟은% 허리를 움직임으로써 얻어지는 쾌락에 열중하고 있다.
%타겟은% 녹은 표정을 숨길 생각조차 안 하고, 탐욕스럽게 쾌락을 즐기기 위해 허리를 크게 움직이고 있다.
어떻게 움직이면 자신이 가장 많이 느끼는 부분이 긁히는지 이해한걸까, %타겟은% 움직일 때마다 홍조를 띄운 얼굴을 서서히 무너뜨렸다.
%타겟은% 군침이나 눈물이 흘러넘치는 따윈 신경조차 쓰지 않고, 짐승처럼 그저 계속해서 허리를 흔들고 있다.
%타겟은% 초점이 맞지 않는 눈동자로 어딘가를 응시한 채, 기계적으로 허리를 움직이고 있다.
%타겟은% 당신의 얼굴을 보면서 허리를 움직이고 있다. 자신의 쾌락보다 %마스터를% 만족시키고 싶은 것 같다.
%타겟은% 어떻게 움직여야 좋은 줄 모르는지, 허둥지둥 어색한 동작으로 허리를 흔들고 있다.
[SKIPEND]
;-------------------------------------------------
;79, 난쟁이오나홀
;-------------------------------------------------
@MESSAGE_COM79
IF TCVAR:100 == MASTER && TARGET == TFLAG:103
	IF TALENT:MASTER:2 & 2
		PRINTFORMW %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 %CALLNAME:TARGET%로 페니스를 훑기 시작했다……
	;페니스밴드를 사용하고 있는 경우
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 %CALLNAME:TARGET%로 페니스를 훑기 시작했다……
	ENDIF
	SIF !FLAG:70 && EXP:V확장경험 < 25 && CFLAG:MASTER:장난 != 2
		PRINTFORML %타겟은% 눈을 하얗게 뒤집고 넋을 잃은 채, 의미 불명의 신음 소리를 내고 있다…
ELSE
	PRINTFORM %타겟을% 움켜쥐고, 그
	;처녀
	IF TALENT:0
		PRINT 남자를 모르는 
	;윤활 LV4 이상
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINT 흠뻑 젖어 있는
	ELSEIF PALAM:4 <= PALAMLV:2
		PRINT 제대로 젖지도 않은
	ENDIF
	IF EXP:V확장경험 < 5
		PRINTFORML 작은 균열에 %TINKO(MASTER)%를 힘으로 쑤셔 넣었다
	ELSE
		PRINTL 작은 균열을 꿰뚫었다
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		IF EXP:V확장경험 < 10
			PRINTFORM %CALLNAME:TARGET%의 비소가 삐걱삐걱 싫은 소리를 내는 것도 개의치 않고,
		ELSEIF EXP:V확장경험 > 50
			PRINTFORM 거대한 페니스를 거뜬히 물었다
		ENDIF
	ELSE
		IF ABL:마조끼 > 3 && EXP:V확장경험 > 50
			PRINT 거대한 고기의 창에 꼬챙이처럼 꿰뚫리면서도 황홀한 표정으로 응시하고 있다
		ELSEIF ABL:마조끼 > 2 && EXP:V확장경험 > 25
			PRINT 입을 뻐끔뻐끔 거리면서도 뺨을 상기시키고 있다
		ELSE
			PRINT 거대한 페니스에 내장을 압박당하여 괴로운 소리로 절규하고 있다
		ENDIF
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 몸을 오나홀처럼 사용해 페니스를 훓었다……
	PRINTFORML %CALLNAME:TARGET%의 배에는 %CALLNAME:MASTER%의 페니스 형태가 그로테스크하게 떠올라, %타겟의% 작은 몸과 현저한 대비를 이뤘다……
ENDIF
;-------------------------------------------------
;92, A정상위된다
;-------------------------------------------------
@MESSAGE_COM92
;A대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 안겨져 항강을 후벼지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 떰과 동시에, %CALLNAME:MASTER%의 직장에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 A질내사정되고 있다
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 항문을 꿰뚫고 있다
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%가
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응(라고인가 조리 아이 없는데 질내사정되고 있다고 무슨 일?
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 에 휘감겨, 추잡한 점착음을 항문으로 울리고 있다
		ELSE
			PRINTFORML %TINKO(TARGET)%에 장벽을 비벼지면서 %마스터는% 허리를 앞으로 쑥 내밀었다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%에\@ ABL:새드끼 >= 3 ?  억지로 # 상냥하게 \@ 안겨졌다
		PRINTFORM %마스터는% 한쪽 다리를 %CALLNAME:TARGET%의 허리에 돌리고,
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 부드럽게 녹아 있는 애널로,
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 것 같이 받아들인다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스로 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 단단히 물어삼키고 있다
		ELSE
			PRINTFORML 아직도 이물이 익숙치 않은 애널을 꿰뚫리는 고통에, 비명을 질렀다
		ENDIF

		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 항문 입구를 후비면서 장벽을 희롱하는 감각에 , %마스터는% 허덕이고 있다…
	ENDIF
;A정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에게 깔아 눌려, 항강을 후벼파지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 떰과 동시에, %CALLNAME:MASTER%의 직장에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 항문을 꿰뚫고 있다
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%가
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 에 휘감겨, 추잡한 점착음을 항문으로 울리고 있다
		ELSE
			PRINTFORML %TINKO(TARGET)%에 장벽을 비벼지면서 %마스터는% 위를 보며 누웠다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 위를 보고
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀려 넘어져
		ELSE
			PRINTFORM 눕혀져서,
		ENDIF
		PRINTFORML %마스터는% 다리를 자신의 머리 위로 뻗었다
		PRINTFORM 스스로 엉덩이를 여는 %CALLNAME:MASTER%의,
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 것 같이 받아들인다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 익숙치 않은 애널을 꿰뚫리는 고통에, 비명을 질렀다
		ENDIF

		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 장벽을 도려내듯 범하는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;93, A후배위된다
;-------------------------------------------------
@MESSAGE_COM93
;A배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이살에 허리를 쳐박히고 있는 %마스터는%, %CALLNAME:TARGET%에 애원하듯이 항문을 움츠렸다
			PRINTFORM 꿈틀꿈틀 떨리는 페니스가 직장으로 %SAMEN_DESCRIPTION(TARGET)%를 쳐박자, %마스터는% 뜨거운 숨결을 흘렸다
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 늦게 절정에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 더럽혔다
			ELSE ;KR판 수정, 어미 처리에 따른 변경. 다른 작업자가위 쪽 지문에 어미를 통합함
				PRINTL …
			ENDIF
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에게 항문을 계속 유린당하고 있다
			PRINTFORM 정액과 장액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 격렬해져, %CALLNAME:TARGET%의 흥분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%가 말한 대로 %마스터는%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 쑥 내민 양손을 잡힌 채 # 양손으로 벽을 짚고서 \@, 애널을 후벼지는 쾌감에 허덕이고 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 양손을 쑥 내밀라고 # 양손을 벽에 대라고 \@
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁 받아,
		ENDIF
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 것 같이 받아들인다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 익숙치 않은 애널을 억지로 꿰뚫어 %마스터는% 괴로운 소리를 흘렸다
		ENDIF
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 안을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;A후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이살에 허리를 쳐박히고 있는 %마스터는%, %CALLNAME:TARGET%에 애원하듯이 항문을 움츠렸다
			PRINTFORM 꿈틀꿈틀 떨리는 페니스가 직장으로 %SAMEN_DESCRIPTION(TARGET)%를 쳐박자, %마스터는% 뜨거운 숨결을 흘렸다
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 늦게 절정에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 더럽혔다
			ELSE ;KR판 수정, 어미 처리에 따른 변경
				PRINTL …
			ENDIF
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에게 항문을 계속 유린당하고 있다
			PRINTFORM 정액과 장액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 격렬해져, %CALLNAME:TARGET%의 흥분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%가 말한 대로 %마스터는% 네발로 엎드린 자세를 취해, 애널을 후벼지는 쾌감에 허덕이고 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 네발로 엎드리라고
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁 받아,
		ENDIF
		PRINT
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹아 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 것 같이 받아들인다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 벌어지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 익숙치 않은 애널을 억지로 꿰뚫어 %마스터는% 괴로운 소리를 흘렸다
		ENDIF
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 안을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;95, 유사 삽입
;-------------------------------------------------
@MESSAGE_COM95
PRINTFORML %CALLNAME:TARGET%\@ TFLAG:193? 의 한쪽 다리를 들어올리고# 에 올라타 \@ %SLIT(TARGET)%에 %TINKO(PLAYER)%를 대고,
IF PALAM:윤활 > PALAMLV:4
	PRINTFORML 귀두가 3 분의 1도 진입하지 않을 만큼 매우 얕은 삽입으로, 질꺽이는 질구를 질척질척 파헤쳤다…
ELSE
	PRINTFORML \@ TALENT:처녀 == 1? 꼭 닫혀 있는 # 부드러운 \@ 음순에 쿠퍼액이 방울져 흘러내리는 귀두를 문질렀다…
ENDIF

SIF FLAG:70
	RETURN
IF TALENT:처녀 == 1 || TALENT:처녀 == -1
	IF PALAM:욕정 > PALAMLV:7
		PRINTFORML 불안해 하는 표정과는 정반대로 %CALLNAME:TARGET%의 허리는 안타까운 듯이 구불거리고, 유혹하듯 흔들리는 고기는 그녀의 숨겨진 소망을 대변하는것 같이
		PRINTFORML 귀두에 착 달라붙어, 틈만 있으면 안쪽으로 이끌려고 꿈틀대고 있다…
	ELSE
		PRINTFORML %타겟은% \@ TALENT:무지? 무슨 일이 벌어지고 있는지 알지도 못하면서 # 이대로 정말로 꿰뚫려 버리는 것은 아닐까 하는 \@ 불안한 표정을 띄우고 있다…
	ENDIF
ELSE
	PRINTFORML %타겟은% 어중간한 자극에 안타깝다는 듯이 허리를 뒤틀고 있다…
ENDIF
;-------------------------------------------------
;130, 정상위된다
;-------------------------------------------------
@MESSAGE_COM130
;대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 꼭 안겨져 질 안을 후벼지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 떰과 동시에, %CALLNAME:MASTER%의 자궁에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 질벽에 정액을 쳐바른다
			PRINTFORML 자궁 안까지 범한 %SAMEN_DESCRIPTION(TARGET)%가, %CALLNAME:MASTER%에게 밖에 들리지 않는 추잡한 소리를 울리고 있다…
		ELSE
			PRINTFORML %TINKO(TARGET)%에 질벽을 비벼지면서 %마스터는% 한쪽 다리와 양팔을 %CALLNAME:TARGET%에 휘감았다
			PRINTFORML 꽉 매달리는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%에\@ ABL:새드끼 >= 3 ?  억지로 #  \@ 안긴 %마스터는% 스스로 허리를 쑥 내밀어 %TINKO(TARGET)%에 질구를 비비고 있다
		PRINTFORM \@ ABL:새드끼 >= 3 ?  히죽 # 기쁜듯이 \@웃은 %타겟이%,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		PRINTFORM %SLIT()%에
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 단숨에 꿰뚫고 들어왔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에게 덮쳐져 질 안을 쑤셔지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 떰과 동시에, %CALLNAME:MASTER%의 자궁에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 질벽에 정액을 쳐바른다
			PRINTFORML 자궁 안까지 범한 %SAMEN_DESCRIPTION(TARGET)%가, %CALLNAME:MASTER%밖에 들리지 않는 추잡한 소리를 울리고 있다…
		ELSE
			PRINTFORML %TINKO(TARGET)%에 질벽을 비벼지면서 %마스터는% 위로 향하게 되었다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에 위를 보고
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀려 넘어져
		ELSE
			PRINTFORM 눕혀져,
		ENDIF
		PRINTFORML %마스터는% 몸에서 힘을 빼고 다리를 열어, 자기 손으로 음순을 나누어 벌렸다
		PRINTFORM 이쪽의 은밀한 곳을 응시하며,  군침을 삼킨 %타겟이%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		PRINTFORM %SLIT()%에
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 를 갖다 대고 찔러 넣었다……
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;131, 후배위된다
;-------------------------------------------------
@MESSAGE_COM131
;배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이살을 주물러지던 %마스터는%, 격렬하게 엉덩이를 쑥 내밀어 자궁구를 %TINKO(TARGET)%의 귀두에 눌러 붙이고
			PRINTFORML 꿈틀꿈틀 떨린 페니스가 자궁에 %SAMEN_DESCRIPTION(TARGET)%를 쳐박자 %마스터는% 뜨거운 숨결을 흘렸다
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 늦게 절정에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 더럽혔다
			ELSE ;KR판 수정, 어미 처리에 따른 변경
				PRINTL …
			ENDIF
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에게 질육을 계속 유린당하고 있다
			PRINTFORML 정액과 애액이 질벽에 발려 비벼지는 열락에 몸을 맡기는 %CALLNAME:MASTER%
			PRINTFORM 남녀의 음액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 격렬해져, %CALLNAME:TARGET%의 흥분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%가 말한 대로 %마스터는%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 쑥 내민 양손을 잡힌 채 # 양손으로 벽을 짚은 채 \@, 질강을 후벼지는 쾌감에 허덕이고 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 양손을 쑥 내밀라고 # 양손을 벽에 대라고 \@
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁 받아,
		ENDIF
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		PRINTFORM %SLIT()%에 가져다 댄 \@ TALENT:TARGET:2 & 2 ?  %TINKO(TARGET)% # 페니스밴드 \@에 가장 안쪽까지 꿰뚫렸다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이를 주물려지던 %마스터는%, 여성의 음부에 양물을 물고 비비며 스스로 허리를 흔들고 있다
			PRINTFORML 꿈틀꿈틀 떨리는 페니스가 자궁에 %SAMEN_DESCRIPTION(TARGET)%를 쳐박자 %마스터는% 뜨거운 숨결을 흘렸다
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 늦게 절정에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 더럽혔다
			ENDIF
			PRINTL …
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에게 질육을 계속 유린당하고 있다
			PRINTFORML 정액과 애액이 질벽에 발려 비벼지는 열락에 몸을 맡기는 %CALLNAME:MASTER%
			PRINTFORM 남녀의 음액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 격렬해져, %CALLNAME:TARGET%의 흥분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%가 말한 대로 %마스터는% 네발로 엎드린 자세를 취해, 질강을 후벼지는 쾌감에 허덕이고 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에게 네발로 엎드리라고
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁 받아,
		ENDIF
		PRINT
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
		PRINTFORM %SLIT()%에 가져다 댄 \@ TALENT:TARGET:2 & 2 ?  %TINKO(TARGET)% # 페니스밴드 \@에 가장 안쪽까지 꿰뚫렸다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;132, 대면좌위된다
;-------------------------------------------------
@MESSAGE_COM132
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %CALLNAME:TARGET%에게 껴안겨진 채로 밀어올려져
		PRINTFORML %SAMEN_DESCRIPTION(TARGET)%가 질 안쪽에 세차게 뿌려지는 감촉에 몸을 떨었다
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 허공에 흩뿌렸다…
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%로부터 질육을 계속 유린당하고 있다
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%에
		ELSE
			PRINTFORM 페니스밴드에
		ENDIF
		PRINTFORML 가장 안쪽을 얻어맞을 때마다 정액의 바다가 출렁이며 자궁을 흔들어 %마스터는% 황홀해 하고 있다
		PRINTFORML 그 모습에 만족했는지, %타겟이% 보다 적극적으로 고기의 관으로 암컷의 약한 부위를 쳐 올리고 있다…
	ELSE
		PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 쇠약해지는 기색조차 보이지 않는다
		PRINTFORML 갑자기%타겟이% 꽉하고 껴안아 와, 입술을 겹치고 혀를 휘감으면서 다시 허리를 거세게 흔들기 시작했다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에 정면으로 안아 올려졌다
	SIF TEQUIP:MASTER:0 & 2
		PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로
	PRINTFORM %SLIT()%에
	IF TALENT:TARGET:2 & 2
		PRINTFORM %TINKO(TARGET)%
	;에 처세의 재능응
	ELSE
		PRINTFORM 페니스밴드
	ENDIF
	PRINTFORM 가 삽입되고
	;볼개그의 유무(예정)
	SIF !TEQUIP:MASTER:볼개그
		PRINT 입맞춤 받으며
	PRINTL 허리를 밀어 올려졌다…
	PRINTFORML 
	;응
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
	;에 처세의 재능응
	ELSE
		PRINTFORM 무기질인 페니스밴드가
	ENDIF
	PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
ENDIF

;-------------------------------------------------
;133, 배면좌위 된다
;-------------------------------------------------
@MESSAGE_COM133
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %CALLNAME:TARGET%에게 꼭 껴안겨진 채로 밀어올려져
		PRINTFORML %TINKO(TARGET)%로부터 분출하는 %SAMEN_DESCRIPTION(TARGET)%에 몸을 떨었다
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크게 떨리며, 뜨거운 %SAMEN_DESCRIPTION(TARGET)%를 허공에 흩뿌렸다…
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에게 질육을 계속 유린당하고 있다
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%에
		ELSE
			PRINTFORM 페니스밴드에
		ENDIF
		PRINTFORML 안쪽 깊은 곳을 찔릴 때마다, 결합부로부터 애액과 정자가 뒤섞인 엑기스가 흘러넘치는 것이 느껴진다
		PRINTFORML 씨뿌리기 교배의 쾌감에 몸을 맡긴 %CALLNAME:MASTER%에, %타겟이% 한층 더 아이 만들기를 위해 허리를 밀어올렸다…
	ELSE
		PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 쇠약해지는 기색조차 보이지 않는다
		PRINTFORML 갑자기 %CALLNAME:TARGET%의 손이 앞으로 돌아, 몸을 흔들면서 이쪽의 피부를 어루만져 왔다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에 등 뒤로 안아 올려졌다
	PRINTFORM %SLIT()%
	IF TEQUIP:MASTER:0 & 2
		PRINTFORM 에 %SHOW_BOTTOM(MASTER, 2)%의 틈새로 천천히
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 가 삽입되었다…
	ELSE
		PRINTFORML 를 기다리고 있던
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 천천히 꿰뚫렸다……
	ENDIF
	PRINTFORML 
	;응
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열기를 띤 %CALLNAME:TARGET%의 %TINKO(TARGET)%가
	;에 처세의 재능응
	ELSE
		PRINTFORM 무기질인 페니스밴드가
	ENDIF
	PRINTFORML 질벽을 휘젓는 감각에, %마스터는% 허덕이고 있다…
ENDIF
