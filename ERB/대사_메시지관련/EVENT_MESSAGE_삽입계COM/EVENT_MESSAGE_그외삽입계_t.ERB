;-------------------------------------------------
;64, 역강간
;-------------------------------------------------
@MESSAGE_COM64
;역강간과 기승위 되고는 조금 뉘앙스가 다르다는 생각이 하지만, 수요가 확실할 때까지 보류합니다
;;연속
;IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
;	PRINTFORML %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 꽉 먹어 잡은 채로, 집요하게 허리를 거절해 세웠다…
;ELSE
;	PRINTFORM %마스터는% %CALLNAME:TARGET%의 %TINKO(MASTER)%를 억지로,
;	;조교자가 처녀
;	SIF TALENT:MASTER:처녀 == 1
;		PRINT 남을 받아들인 일이 없다
;	PRINTL 바기나의 안쪽까지 질불공평응이다…
;ENDIF
;역강간은 MASTER 주도인데, 지문이 TARGET 주도가 되고 있기 때문에, 절대로 변경
[SKIPSTART]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %타겟은% %CALLNAME:MASTER%에 걸치게 한 채로,
			PRINTFORML 복받쳐 오는 충동에 맡겨 허리를 밀어 올렸다
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTL  
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 백탁을 토해냈다
		PRINTFORML …
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %타겟은% %CALLNAME:MASTER%의 가장 깊숙이 정액을 쏟으면(자), 뜨거워져 차가운 그치지 않는 동안에 허리를 움직이기 시작했다
		PRINTFORML %마스터가% 질내사정의 여운에 중단할 틈도 없게 흐트러지는 모습을 바라보면서, 그 몸을 계속 흔들었다 …
	ELSE
		PRINTFORML %타겟은% 질내로부터 앞지르는 일 없이 %마스터를% 안아 올려 아래로부터 허리를 밀어 올렸다…
	ENDIF
ELSE
	IF TALENT:MASTER:처녀 == 1
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸쳐, 천천히 앉아
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 걸쳐, 천천히 앉아
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML , 남자를 모르는 어린 균열로 %TINKO(MASTER)%를 삼켜 갔다…
		;처녀
		ELSE
			PRINTFORML , 남자를 모르는 비렬로 %TINKO(MASTER)%를 삼켜 갔다…
		ENDIF
	ELSE
		;역강간은 당신 주도의 당신구멍 상대장대와 생각 수정
		;PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸쳐,
		PRINTFORM %마스터는% %CALLNAME:TARGET%의 위에 걸쳐,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORML %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 갔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%에
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드에
		ENDIF
		PRINTFORML 전신을 관철해지는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF
[SKIPEND]
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %타겟이% 발등에 불이 떨어진 모습에, 입길을 느슨하게해 격렬하게 허리를 곤두세운다
		PRINTFORM %TINKO(TARGET)%가 질강을 뜨는 열락에, %CALLNAME:MASTER%도 단 허덕여소리를 높였다
		;조교자도 사정
		IF NOWEX:MASTER:11
			PRINTL  
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%도 %TINKO(MASTER)%로부터, 격렬하게 %SAMEN_DESCRIPTION(MASTER)%를 공격하기 시작했다
		ENDIF
		PRINTFORML …
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 체내에 따라진 정액의 감촉에, 황홀한 표정을 띠면서 허리를 거절하기 시작했다
		PRINTFORML 사정의 열락에 잠겨 있던%CALLNAME:TARGET%이지만, %CALLNAME:MASTER%의 질내로 해 쉬는 쾌감에 천를 멈출 수 없다
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 %TINKO(TARGET)%를, 질 구멍으로 큐 응 두근 단단히 조이면서 %마스터는% 허덕이고 있다
	ENDIF
ELSE
	IF TALENT:MASTER:처녀 == 1
		PRINTFORML %마스터는% %CALLNAME:TARGET%의 위에 걸쳐, 스스로 %TINKO(TARGET)%를 질강에 이끌어 간다
		;처녀＋작은 체형
		IF TALENT:MASTER:체형 < 0
			PRINTFORML 아직도 남자를 모르는 어린 균열을 스스로 계 와, %TINKO(TARGET)%를 천천히 입에 물고 넣었다…
		;처녀
		ELSE
			PRINTFORML 처음의 증거를 여기에(우리집, 나) 숨긴 비렬을 나누어 열어, %TINKO(MASTER)%를 천천히 삼켜 갔다…
		ENDIF
	ELSE
		PRINTFORM %마스터는% %타겟을% 짜 깔아,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORML 자신의 %SLIT()%에 %TINKO(TARGET)%를 갖다 대고 삼켜 갔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드
		ENDIF
		PRINTFORML 를 질강으로 빨면서, %마스터는% 날카로운 열명을 올리고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;66, 기승위애널
;-------------------------------------------------
@MESSAGE_COM66
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 시간을 멈춘%타겟을% 자신에게 걸치게 한 모습인 채, 다시 허리를 밀어 올리기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == MASTER
		PRINTFORML 장내로부터 앞지르는 일 없이 꼼짝도 하지 않는%타겟을% 안아 올려 아래로부터 허리를 밀어 올렸다…
	ELSE
		PRINTFORM %마스터는% 시간이 멈추어 있는 %CALLNAME:TARGET%의 다리를 열어, %CALLNAME:TARGET%의 아래에 몸을 기어들어 붐비게 했다
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스에 천천히%CALLNAME:MASTER%의 물건을 삽입하고 있는…
	ENDIF
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟은% 삽입된 채로의 물건을 단단히 조여 다시 허리를 움직이기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == MASTER
		PRINTFORML 장내로부터 앞지르는 일 없이 %타겟을% 안아 올려 아래로부터 허리를 밀어 올렸다…
	ELSE
		PRINTFORM %타겟은% %CALLNAME:MASTER%의 위에 걸치면(자),
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스로 천천히%CALLNAME:MASTER%의 물건을 삼켜 갔다…
	ENDIF
	;함락이 끝난 상태
	IF TALENT:연모
		PRINTFORM %타겟은%
		;작은 체형
		SIF TALENT:체형 < 0
			PRINT 작은 몸으로 열심히
		;거유
		SIF TALENT:가슴사이즈 == 1
			PRINT 크게 가슴을 흔들면서
		;폭유
		SIF TALENT:가슴사이즈 == 2
			PRINT 뛰는 가슴에 몸 전체로 흔들어지고면서
		PRINTL 스스로 허리를 움직이고 있다…
	ENDIF
ENDIF
;-------------------------------------------------
;71, 양구멍삽입
;-------------------------------------------------
@MESSAGE_COM71
VARSET LOCAL
LOCAL:1 = MASTER_POSE(SET_V, SET_P, 1)
LOCAL:2 = MASTER_POSE(SET_A, SET_P, 1)
IF LOCAL:1
	LOCAL = LOCAL:1
ELSEIF LOCAL:2
	LOCAL = LOCAL:2
ENDIF
;LOCAL 두 구멍삽입 물러날 수 있는 사람
;연속
IF PREVCOM == SELECTCOM
	PRINTFORML %조사처리(CALLNAME:LOCAL,"는")% 앞과 뒤를 동시에 관철해져 폭력적인 쾌감에 넋을 잃어 허덕이고 있다…
	RETURN 0
;주인 W 꽂아
ELSEIF ((LOCAL:1 == 0 || LOCAL:2 == 0) && LOCAL == TARGET) && TALENT:MASTER:쌍창
	PRINTFORML %마스터는% 2 개의 자신으로 %CALLNAME:LOCAL%의 두 구멍을 관철했다…
;주인 V삽입-TARGET A삽입
ELSEIF LOCAL == LOCAL:1
	PRINTFORML %마스터는% 연결된 채로 %CALLNAME:LOCAL%의 엉덩이를 열어, %CALLNAME:TARGET%에 뒤의 구멍도 범하도록(듯이) 재촉했다…
;주인 A삽입-조수 V삽입
ELSE
	PRINTFORML %마스터는% 아누스를 관철한 상태로 %CALLNAME:LOCAL%를 안아 다리를 열게 해 %CALLNAME:TARGET%에 앞의 구멍도 범하도록(듯이) 재촉했다…
ENDIF

[SKIPSTART]
;-------------------------------------------------
;74, 그라인드 한다
;-------------------------------------------------
@MESSAGE_COM75
IF ABL:MASTER:허리 < 3
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 꽉 누른 채로 어색한 움직임으로 좌우에 움직이기 시작했다.
ELSE
	PRINTFORML %마스터는% 허리를 %CALLNAME:TARGET%에 꽉 누른 채로 천천히의의 글자를 쓰도록(듯이) 허리를 움직였다.
ENDIF
;-------------------------------------------------
;75, 그라인드 시킨다
;-------------------------------------------------
@MESSAGE_COM75
%CALLNAME:MASTER%에 스스로 움직이도록(듯이) 명령 받았다%타겟은%, 얼굴이 새빨개져서 허리를 천천히 움직이기 시작했다.
%CALLNAME:MASTER%에 스스로 움직이도록(듯이) 명령 받았다%타겟은%, 홍조 시킨 얼굴을 피기 시작하게 한이면서 허리를 공갈협박 시작했다.
%마스터가% 말할 필요도 없이 %타겟은% 변변치않은 움직임으로 허리를 전후에 움직이기 시작하는.
%마스터가% 허리의 움직임을 그만두면(자)%타겟은% 곧바로 허리를 전후에 움직이기 시작했다.
%타겟은% 쾌락을 게으름 피우기 (위해)때문에일까, %마스터가% 명할 것도 없이 허리를 크게 거절하기 시작했다.
%마스터는% %CALLNAME:TARGET%에 움직여 주도록(듯이) 간절히원함 했다. %타겟은% 기학적인 미소를 띠면서 허리를 거절하기 시작했다.


(허리 모습중. )
%타겟은% 허리를 움직이는 것에 의해 얻을 수 있는 쾌락에 열중하고 있다.
%타겟은% 녹은 표정을 숨기는 일 없이 탐욕에 쾌락게으름 피우기 위해서(때문에) 허리를 크게 움직이고 있다.
어떻게 움직이면 자신이 제일 느끼는 곳(중)이 스치는지 이해했을 것이다, %타겟은% 움직일 때마다 홍조 한 얼굴을 서서히 무너뜨려 갔다.
%타겟은% 군침이나 눈물이 흘러넘치는 것도 신경쓰는 일 없이, 짐승처럼 단지 계속해서 허리를 계속 거절하고 있다.
%타겟은% 초점이 맞지 않는 눈동자로 어딘가를 응시한 채로, 기계적으로 허리를 움직이고 있다.
%타겟은% 당신의 얼굴을 보면서 허리를 움직이고 있다. 자신의 쾌락보다%마스터를% 만족시키고 싶은 것 같다.
%타겟은% 어떻게 움직이면 좋은 것인지 모른채, 당황하면서 어색한 움직임으로 허리를 털고 있다.
[SKIPEND]
;-------------------------------------------------
;79, 난쟁이오나홀
;-------------------------------------------------
@MESSAGE_COM79
IF TCVAR:100 == MASTER && TARGET == TFLAG:103
	IF TALENT:MASTER:2 & 2
		PRINTFORMW %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 %CALLNAME:TARGET%로 페니스를 잡아당기기 시작했다……
	;페니스밴드를 사용하고 있는 경우
	ELSE
		PRINTFORMW %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 %CALLNAME:TARGET%로 페니스를 잡아당기기 시작했다……
	ENDIF
	SIF !FLAG:70 && EXP:V확장경험 < 25 && CFLAG:MASTER:장난 != 2
		PRINTFORML %타겟은% 흰색 눈을 벗겨 의미가 없는 비명 소리를 지르고 있다…
ELSE
	PRINTFORM %타겟을% 움켜쥐고, 그
	;처녀
	IF TALENT:0
		PRINT 남자를 모르는 
	;윤활 LV4 이상
	ELSEIF PALAM:4 >= PALAMLV:4
		PRINT 흠뻑 젖어 있는
	ELSEIF PALAM:4 <= PALAMLV:2
		PRINT 제대로 젖지 않았다
	ENDIF
	IF EXP:V확장경험 < 5
		PRINTFORML 작은 균열에 %TINKO(MASTER)%를 힘으로 나사 붐비었다
	ELSE
		PRINTL 작은 균열을 관철했다
	ENDIF
	IF FLAG:70 || CFLAG:MASTER:장난 == 2
		IF EXP:V확장경험 < 10
			PRINTFORM %CALLNAME:TARGET%의 비소가 미치미치와 싫은 소리를 내는 것도 뜻에 개좌도,
		ELSEIF EXP:V확장경험 > 50
			PRINTFORM 거대한 페니스를 손쉽게 질네 넣는다
		ENDIF
	ELSE
		IF ABL:마조끼 > 3 && EXP:V확장경험 > 50
			PRINT 거대한 육 창에 꿰뚫고로 되면서도 황홀한 표정
		ELSEIF ABL:마조끼 > 2 && EXP:V확장경험 > 25
			PRINT 구를 뻐끔뻐끔 시키면서도 뺨을 상기 시킨다
		ELSE
			PRINT 거대한 페니스에 내장을 압박받고 고민의 절규를 올린다
		ENDIF
	ENDIF
	PRINTFORML %CALLNAME:TARGET%의 몸을 오나홀과 같이 사용해 페니스를 취급……
	PRINTFORML %CALLNAME:TARGET%의 배에는 %CALLNAME:MASTER%의 페니스의 형태가 그로테스크에 떠올라, %타겟이% 작은 몸과 현저한 대비를 했다……
ENDIF
;-------------------------------------------------
;92, A정상위된다
;-------------------------------------------------
@MESSAGE_COM92
;A대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 껴안을 수 있어 항강을 꾸물거려지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 진동시키면(자) 동시, %CALLNAME:MASTER%의 직장에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 A질내사정되고 있다
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 항문을 관철하고 있다
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%가
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응(라고인가 조리 아이 없는데 질내사정되고 있다고 무슨 일?
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 에 관련되어 붙어, 추잡한 점착음을 항문으로부터 미치게 하고 있다
		ELSE
			PRINTFORML %TINKO(TARGET)%에 장벽을 비비어지고면서 %마스터는% 허리를 앞에 두고 쑥 내밀었다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%에\@ ABL:새드끼 >= 3 ?  억지로 # 상냥하게 \@껴안을 수 있었다
		PRINTFORM %마스터는% 한쪽 발을%CALLNAME:TARGET%의 허리에 돌려,
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 부드럽게 녹고 자르고 있는 애널로,
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 같게 받아 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 터지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 함네 들어간다
		ELSE
			PRINTFORML 아직도 이물이 될 수 없는 애널을 관철해지는 고통에, 소리를 흘린다
		ENDIF

		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 항구를 도려내 장벽을 요감각에, %마스터는% 허덕이고 있다…
	ENDIF
;A정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 덮개피 되어 항강을 꾸물거려지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 진동시키면(자) 동시, %CALLNAME:MASTER%의 직장에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 항문을 관철하고 있다
			PRINTFORM %SAMEN_DESCRIPTION(TARGET)%가
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 에 관련되어 붙어, 추잡한 점착음을 항문으로부터 미치게 하고 있다
		ELSE
			PRINTFORML %TINKO(TARGET)%에 장벽을 비비어지고면서 %마스터는% 위로 향해 되었다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에 위로 향해
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀어 넘어뜨려져
		ELSE
			PRINTFORM 자,
		ENDIF
		PRINTFORML %마스터는% 다리를 자신의 머리 위에와 냈다
		PRINTFORM 스스로 엉덩이를 여는 %CALLNAME:MASTER%의,
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹고 자르고 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 같게 받아 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 터지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 될 수 없는 애널을 관철해져 괴로운 울음이 새었다
		ENDIF

		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 장벽을 도려내 범하는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;93, A후배위된다
;-------------------------------------------------
@MESSAGE_COM93
;A배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 고육에 허리를 부딪힐 수 있는 %마스터는%, %CALLNAME:TARGET%에 애원 하도록(듯이) 항문을 움츠렸다
			PRINTFORM 꿈틀꿈틀 떨린 페니스로부터 직장으로 %SAMEN_DESCRIPTION(TARGET)%를 박혀%마스터는% 뜨거운 숨결을 루등
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 두어 올라 채운%CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 오
			ENDIF
			PRINTL 했다…
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에 항문을 유린계속 되고 있다
			PRINTFORM 정액과 장액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 앞당겨져, %CALLNAME:TARGET%의 앙분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 쑥 내민 양손을 잡혀라 # 양손을 벽을 뒤따라 \@, 애널을 보전쾌감에 맞을 수 있는 있고로 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 양손을 쑥 내미는 # 양손을 벽을 뒤따르는 \@
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁되어
		ENDIF
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹고 자르고 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 같게 받아 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 터지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 될 수 없는 애널을 억지로 관철해져%마스터는% 괴로운 울음을 흘렸다
		ENDIF
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;A후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:101 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 고육에 허리를 부딪힐 수 있는 %마스터는%, %CALLNAME:TARGET%에 애원 하도록(듯이) 항문을 움츠렸다
			PRINTFORM 꿈틀꿈틀 떨린 페니스로부터 직장으로 %SAMEN_DESCRIPTION(TARGET)%를 박혀%마스터는% 뜨거운 숨결을 루등
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 두어 올라 채운%CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 오
			ENDIF
			PRINTL 했다…
		;전턴으로 A질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%에 항문을 유린계속 되고 있다
			PRINTFORM 정액과 장액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 앞당겨져, %CALLNAME:TARGET%의 앙분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는% 엎드린 자세로 되어, 애널을 보전쾌감에 맞을 수 있는 있고로 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 엎드린 자세로
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 는이라고 부탁되어
		ENDIF
		PRINT
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		IF EXP:MASTER:A경험 >= EXPLV:4
			PRINTFORM 이미 부드럽게 녹고 자르고 있는 애널이, %CALLNAME:TARGET%의
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 를 살짝 깨무는 같게 받아 간다
		ELSEIF EXP:MASTER:A경험 >= EXPLV:3
			PRINTFORM 터지기 시작한 아누스에 천천히
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			;에 처세의 재능응
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 가 삽입되어 온다
		ELSE
			PRINTFORML 아직도 이물이 될 수 없는 애널을 억지로 관철해져%마스터는% 괴로운 울음을 흘렸다
		ENDIF
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;95, 유사 삽입
;-------------------------------------------------
@MESSAGE_COM95
PRINTFORML %CALLNAME:TARGET%\@ TFLAG:193? 의 한쪽 발을 들어 올려# 에 덮어씌워 \@ %SLIT(TARGET)%에 %TINKO(PLAYER)%를 대고,
IF PALAM:윤활 > PALAMLV:4
	PRINTFORML 귀두가 3 분의 1도 진입하지 않을 만큼 매우 얕은 삽입으로 질퍽거리는 질구를 질척질척 파헤쳤다…
ELSE
	PRINTFORML \@ TALENT:처녀 == 1? 꼭 닫힌 # 부드러운 \@ 음순에 쿠퍼액이 방울진 귀두를 문질렀다…
ENDIF

SIF FLAG:70
	RETURN
IF TALENT:처녀 == 1 || TALENT:처녀 == -1
	IF PALAM:욕정 > PALAMLV:7
		PRINTFORML 불안한 표정과 정반대로 %CALLNAME:TARGET%의 허리는 안타까운 듯이 구불거려, 미육은 그녀의 숨긴 소원을 대변하는것 같이
		PRINTFORML 귀두에 착 달라붙어, 틈이 있으면 안쪽으로 이끌려고 꿈틀거리고 있다…
	ELSE
		PRINTFORML %타겟은% \@ TALENT:무지? 무엇을 되고 있는지 알지 못하고 # 이대로 정말로 관철해져 버리는 것은 아닐까 \@ 불안한 표정을 띄우고 있다…
	ENDIF
ELSE
	PRINTFORML %타겟은% 어중간한 자극에 안타까운 듯이 허리를 구불거리게 되어지고 있다…
ENDIF
;-------------------------------------------------
;130, 정상위된다
;-------------------------------------------------
@MESSAGE_COM130
;대면입위 된다
IF (!BEDROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 꼭 껴안을 수 있어 질강을 꾸물거려지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 진동시키면(자) 동시, %CALLNAME:MASTER%의 자궁에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 질벽에 정액을 발라 붐비어 온다
			PRINTFORML 태안까지 범한 %SAMEN_DESCRIPTION(TARGET)%가, %CALLNAME:MASTER%밖에 들리지 않는 추잡한 소리를 미치게 했다…
		ELSE
			PRINTFORML %TINKO(TARGET)%에 질벽을 비비어지고면서 %마스터는% 한쪽 발과 양팔을 %CALLNAME:TARGET%에 휘감았다
			PRINTFORML 꽉 매달리는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:TARGET%에\@ ABL:새드끼 >= 3 ?  억지로 #  \@껴안을 수 있어 %마스터는% 스스로 허리를 쑥 내밀어 %TINKO(TARGET)%에 질구를 문질러 바른다
		PRINTFORM \@ ABL:새드끼 >= 3 ?  히죽 # 기쁜듯이 \@웃은 %타겟이%,
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORM %SLIT()%에
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 단숨에 관철해 왔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;정상위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML %마스터는% %CALLNAME:TARGET%에 덮쳐져 질강을 꾸물거려지고 있다
			PRINTFORML %타겟이% 격렬하게 몸을 진동시키면(자) 동시, %CALLNAME:MASTER%의 자궁에 %SAMEN_DESCRIPTION(TARGET)%가 쏟아졌다
			;조교자도 사정. 아 헤에
			SIF NOWEX:MASTER:11
				PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 토해냈다
			PRINTFORML …
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %타겟이% 허리를 움직여, %CALLNAME:MASTER%의 질벽에 정액을 발라 붐비어 온다
			PRINTFORML 태안까지 범한 %SAMEN_DESCRIPTION(TARGET)%가, %CALLNAME:MASTER%밖에 들리지 않는 추잡한 소리를 미치게 했다…
		ELSE
			PRINTFORML %TINKO(TARGET)%에 질벽을 비비어지고면서 %마스터는% 위로 향해 되었다
			PRINTFORML 전신으로부터 힘을 빼는 %CALLNAME:MASTER%에, %CALLNAME:TARGET%의 허리가 움직이기 시작한다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%에 위로 향해
		IF ABL:새드끼 >= 3
			PRINTFORM 난폭하게 밀어 넘어뜨려져
		ELSE
			PRINTFORM 자,
		ENDIF
		PRINTFORML %마스터는% 몸이 힘을 빼 다리를 열어, 스스로의 손으로 음순을 나누어 열었다
		PRINTFORM 이쪽의 은밀한 곳을 응시하며,  군침을 삼킨%타겟이%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORM %SLIT()%에
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 를 갖다 대고 관철해 왔다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;131, 후배위된다
;-------------------------------------------------
@MESSAGE_COM131
;배면입위 된다
IF (!INROOM(CFLAG:MASTER:현재위치) && !(BATHROOM() && ITEM:20))
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이를 반죽하고 돌려지는 %마스터는%, 격렬하게 엉덩이를 쑥 내밀어 자궁구를 %TINKO(TARGET)%의 귀두에 눌러 붙이는
			PRINTFORML 꿈틀꿈틀 떨린 페니스로부터 자궁에 %SAMEN_DESCRIPTION(TARGET)%를 박혀%마스터는% 뜨거운 숨결을 루등
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 두어 올라 채운%CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 오
			ENDIF
			PRINTL 했다…
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%로부터 질육을 유린계속 되고 있다
			PRINTFORML 정액과 애액이 질벽에 발라 붐비어지는 열락에 몸을 맡기는 %CALLNAME:MASTER%
			PRINTFORM 남녀의 비액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 앞당겨져, %CALLNAME:TARGET%의 앙분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 쑥 내민 양손을 잡혀라 # 양손을 벽을 뒤따라 \@, 질강을 꾸물거려지는 쾌감에 맞을 수 있는 있고로 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로 양손을 쑥 내미는 # 양손을 벽을 뒤따르는 \@
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 부탁되어
		ENDIF
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORM %SLIT()%에 갖다 대고진\@ TALENT:TARGET:2 & 2 ?  %TINKO(TARGET)% # 페니스밴드 \@에 최안쪽까지 관철해졌다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
;후배위된다
ELSE
	;뽑지 않고
	IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
		;대상이 사정
		IF NOWEX:TARGET:11
			PRINTFORML 엉덩이를 반죽하고 돌려지는 %마스터는%, 여성의 음부에 양물을 입에 물고 붐비어 스스로 허리를 흔들고 있다
			PRINTFORML 꿈틀꿈틀 떨린 페니스로부터 자궁에 %SAMEN_DESCRIPTION(TARGET)%를 박혀%마스터는% 뜨거운 숨결을 루등
			;조교자도 사정
			IF NOWEX:MASTER:11
				PRINTL
				PRINTFORM 한 박자 두어 올라 채운%CALLNAME:MASTER%의 %TINKO(MASTER)%로부터 뜨거운 %SAMEN_DESCRIPTION(MASTER)%가 토해내져 바닥을 오
			ENDIF
			PRINTL 했다…
		;전턴으로 V질내사정
		ELSEIF TCVAR:MASTER:102
			PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%로부터 질육을 유린계속 되고 있다
			PRINTFORML 정액과 애액이 질벽에 발라 붐비어지는 열락에 몸을 맡기는 %CALLNAME:MASTER%
			PRINTFORM 남녀의 비액 투성이가 되었다
			IF TALENT:TARGET:2 & 2
				PRINTFORM %TINKO(TARGET)%
			ELSE
				PRINTFORM 페니스밴드
			ENDIF
			PRINTFORML 의 움직임이 한층 더 앞당겨져, %CALLNAME:TARGET%의 앙분을 전해 왔다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%에 말해지는 대로 %마스터는% 엎드린 자세로 되어, 질강을 꾸물거려지는 쾌감에 맞을 수 있는 있고로 있다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%로부터 엎드린 자세로
		IF ABL:새드끼 >= 3
			PRINTFORM 명령 받아,
		ELSE
			PRINTFORM 는이라고 부탁되어
		ENDIF
		PRINT
		PRINTFORML 솔직하게 따르는 %CALLNAME:MASTER%
		SIF TEQUIP:MASTER:0 & 2
			PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
		PRINTFORM %SLIT()%에 갖다 대고진\@ TALENT:TARGET:2 & 2 ?  %TINKO(TARGET)% # 페니스밴드 \@에 최심부까지 관철해졌다…
		PRINTFORML 
		;응
		IF TALENT:TARGET:2 & 2
			PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
		;에 처세의 재능응
		ELSE
			PRINTFORM 무기질인 페니스밴드가
		ENDIF
		PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
	ENDIF
ENDIF

;-------------------------------------------------
;132, 대면좌위된다
;-------------------------------------------------
@MESSAGE_COM132
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %CALLNAME:TARGET%에 꼭 껴안을 수 있던 채로 비난과 압력을 받아
		PRINTFORML %SAMEN_DESCRIPTION(TARGET)%가 질 안쪽에 두드려 붙여지는 감촉에 몸을 진동시킨다
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(MASTER)%를 허공에 가라앉혔다…
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%로부터 질육을 유린계속 되고 있다
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%에
		ELSE
			PRINTFORM 페니스밴드에
		ENDIF
		PRINTFORML 최심부를 얻어맞을 때마다 정액의 바다가 자궁으로 흔들려%마스터는% 황홀로 하고 있다
		PRINTFORML 그 모습에 만족했는지, %타겟이% 보다 적극적으로 고기의 관으로 암컷의 약소를 찌르고 왔다…
	ELSE
		PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 쇠약해지는 기색도 보이지 않는다
		PRINTFORML 갑자기%타겟이% 꽉하고 안아 따라 와, 입술을 거듭하고 혀를 휘감으며 오면서 다시 허리를 강탈 시작했다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에 정면에서 안아 올릴 수 있었다
	SIF TEQUIP:MASTER:0 & 2
		PRINTFORM %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터
	PRINTFORM %SLIT()%에
	IF TALENT:TARGET:2 & 2
		PRINTFORM %TINKO(TARGET)%
	;에 처세의 재능응
	ELSE
		PRINTFORM 페니스밴드
	ENDIF
	PRINTFORM 가 삽입되고
	;볼개그의 유무(예정)
	SIF !TEQUIP:MASTER:볼개그
		PRINT 입맞춤을 하시면서
	PRINTL 허리를 비난과 압력을 받았다…
	PRINTFORML 
	;응
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
	;에 처세의 재능응
	ELSE
		PRINTFORM 무기질인 페니스밴드가
	ENDIF
	PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
ENDIF

;-------------------------------------------------
;133, 후배위된다
;-------------------------------------------------
@MESSAGE_COM133
;뽑지 않고
IF TCVAR:MASTER:100 == TARGET && TARGET == TFLAG:103
	;대상이 사정
	IF NOWEX:TARGET:11
		PRINTFORML %마스터는% %CALLNAME:TARGET%에 꼭 껴안을 수 있던 채로 비난과 압력을 받아
		PRINTFORML %TINKO(TARGET)%로부터 분출하는 %SAMEN_DESCRIPTION(TARGET)%에 몸을 진동시켰다
		;조교자도 사정
		SIF NOWEX:MASTER:11
			PRINTFORM 동시에 달한 %CALLNAME:MASTER%의 %TINKO(MASTER)%도 크고 떨리며, 뜨거운 %SAMEN_DESCRIPTION(TARGET)%를 허공에 가라앉혔다…
	;전턴으로 V질내사정
	ELSEIF TCVAR:MASTER:102
		PRINTFORML %마스터는% 더욱더 %CALLNAME:TARGET%로부터 질육을 유린계속 되고 있다
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%에
		ELSE
			PRINTFORM 페니스밴드에
		ENDIF
		PRINTFORML 안쪽 깊은 곳을 찔러질 때마다, 결합부로부터 애액과 정자의 들어와 섞인 엑기스가 흘러넘치는 것 느낀다
		PRINTFORML 교배의 쾌감에 몸을 바치는 %CALLNAME:MASTER%에, %타겟이% 한층 더 아이 만들기를 위해 허리를 밀어 올렸다…
	ELSE
		PRINTFORML %TINKO(TARGET)%는 %CALLNAME:MASTER%의 질내로부터 빠지기는 커녕, 쇠약해지는 기색도 보이지 않는다
		PRINTFORML 갑자기%CALLNAME:TARGET%의 손이 앞에 돌아 몸을 흔들면서, 이쪽의 피부를 어루만지고 돌려 왔다…
	ENDIF
ELSE
	PRINTFORML %CALLNAME:MASTER%의 몸은 %CALLNAME:TARGET%에 등 뒤로부터 안아 올릴 수 있었다
	PRINTFORM %SLIT()%
	IF TEQUIP:MASTER:0 & 2
		PRINTFORM 에 %SHOW_BOTTOM(MASTER, 2)%의 틈새로부터 천천히
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 가 삽입되었다…
	ELSE
		PRINTFORML 를 기다리고 있던
		IF TALENT:TARGET:2 & 2
			PRINTFORM %TINKO(TARGET)%
		;에 처세의 재능응
		ELSE
			PRINTFORM 페니스밴드
		ENDIF
		PRINTFORML 로 천천히 관철해졌다…
	ENDIF
	PRINTFORML 
	;응
	IF TALENT:TARGET:2 & 2
		PRINTFORM 열을 띤%CALLNAME:TARGET%의 %TINKO(TARGET)%가
	;에 처세의 재능응
	ELSE
		PRINTFORM 무기질인 페니스밴드가
	ENDIF
	PRINTFORML 질벽을 넘김이나 나무 돌리는 감각에, %마스터는% 허덕이고 있다…
ENDIF
