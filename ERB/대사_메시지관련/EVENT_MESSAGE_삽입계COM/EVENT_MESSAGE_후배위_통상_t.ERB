
@MESSAGE_COM61통상
;배면입위
IF TFLAG:193
	;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% %CALLNAME:TARGET%의\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  양손을 이끌어 # 엉덩이를 잡아 \@,
			PRINTFORML 정액을 질내에 쏟기 (위해)때문에 격렬하게 허리를 뽑았다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아 끝내면(자),
			PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 2
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 2
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 벽에 손을 첨부나 키
				PRINTFORML 뒤로부터 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  뒤로부터 양손을 잡은 # 벽에 손을 붙게 한 \@%CALLNAME:TARGET%에 엉덩이를 쑥 내밀게 했다
			PRINTFORML 남을 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 관철했다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓았다
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올렸다
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  에 쑥 내밀게 한 양손을 잡아 # 의 양손을 벽을 뒤따르게 해 \@, 뒤로부터
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 흠뻑 젖어 있는
			;작은 체형
			IF TALENT:체형 < 0
				PRINT 어린 균열
			ELSE
				PRINT 비렬
			ENDIF
			PRINTFORML 의 최안쪽까지 찔러 넣었다…
		ENDIF
	ENDIF
ELSE
;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% 엎드린 자세로 시킨%CALLNAME:TARGET%의 허리를 잡아,
			PRINTFORML 정액을 질내에 쏟기 (위해)때문에 격렬하게 허리를 뽑았다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아 끝내면(자),
			PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 2
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			PRINTFORML 삽입한 채로 %타겟을% 엎드린 자세로시켜, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML 엎드린 자세로 시킨%CALLNAME:TARGET%의 엉덩이를 높게 주게 했다
			PRINTFORML 남을 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 관철했다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM 엎드린 자세로 시킨%CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤로부터
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 흠뻑 젖어 있는
			;작은 체형
			IF TALENT:체형 < 0
				PRINT 어린 균열
			ELSE
				PRINT 비렬
			ENDIF
			PRINTFORML 의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;322, 상대주도후배위
;-------------------------------------------------
@MESSAGE_COM322
IF TALENT:처녀 == 1
	PRINTFORML %CALLNAME:TARGET%의 소망 대로, 그 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 귀두를 가져다 대며,
	PRINTFORML 딱이라고 닫은 비순을 나누어 열어 관철해, 순결의 증거를 찢었다…
		SIF TALENT:연모
			PRINTFORML %타겟은% 아픔에 참으면서 %CALLNAME:PLAYER%에 꽉 안아 붙었다…
ELSE
	PRINTFORML %마스터는% %CALLNAME:TARGET%에 엎어졌다와 갖고 싶어하는 듯하게 벌렁거리는 질구에 귀두를 천천히 가라앉혀 갔다…
	SELECTCASE ABL:TARGET:질
		CASE 0
			PRINTFORML %TINKO()%는 좁은 육도를 길길과 비틀어 열어 눌러 진행되어, %CALLNAME:TARGET%의 깊숙한 곳까지 침입했다…
		CASE 1
			PRINTFORML 단단함을 남긴 질육을 나누어 열어, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASE 2,3
			PRINTFORML 꽉 벽이 찬 부드러운 살덩어리를 밀어 헤쳐, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASEELSE
			PRINTFORML 힘을 집중하지 않고와도, 녹고 자른%CALLNAME:TARGET%의 미육은 기쁨으로서 %마스터를% 최심부까지 불러 넣었다…
	ENDSELECT
	PRINTFORML 유타카 쓰는 육벽에 싸여 그에 적합한 장소에 들어간 %CALLNAME:MASTER%의 %TINKO()%는 기쁜듯하게 꿈틀 붙었다
ENDIF