@MESSAGE_COM60_1
;-------------------------------------------------
;60, 정상위(제정신)
;-------------------------------------------------
IF TFLAG:193
	;뽑지 않고
	IF TCVAR:V삽입관리플래그 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %타겟을%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  허리를 꼭 껴안고 # 벽에 꽉 밀어붙이고 \@격렬하게 허리를 흔들어,
			PRINTFORML 사정의 순간에 %TINKO(PLAYER)%를 질 안쪽 깊숙한 곳까지 찔러 넣었다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아내고
			PRINTFORML %TINKO(PLAYER)%가 단단함을 잃지 않은 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 1
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 뽑아내지 않고, 다시 허리를 움직이기 시작했다…
			;배면입위로부터
			ELSE
				PRINTFORML 일단 %CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑고 이쪽을 돌아 보게 하고 나서
				PRINTFORML %CALLNAME:TARGET%의 한쪽 다리를 들어 올려 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ENDIF
	ELSEIF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:TARGET%의 한쪽 다리를 들어 올려 %SLIT(TARGET)%에
		PRINTFORML 귀두를 대고 단숨에 꿰뚫었다…
	ELSE
		IF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 한쪽 다리를 들어 올려
			SIF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			PRINTFORML %SLIT(TARGET)%에 %TINKO(PLAYER)%를 대고 꿰뚫었다…
		ENDIF
		IF TALENT:연모
			IF TALENT:처녀
				PRINTFORML %타겟은% 눈가에 눈물이 맺히도록 아픔을 견디면서 %CALLNAME:PLAYER%에게 꽉 안겨들었다…
			ENDIF
		ENDIF
	ENDIF
;정상위
ELSE
	;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML 위를 보고 눕힌 %CALLNAME:TARGET%에게 위에서 덮쳐 누르고 허리를 흔들어,
			PRINTFORML 사정의 순간에 %TINKO(PLAYER)%를 질 안쪽 깊숙한 곳까지 찔러 넣었다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아내고
			PRINTFORML %TINKO(PLAYER)%가 단단함을 잃지 않은 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			PRINTFORML 질 안에서 뽑아내지 않고 %타겟을% 위로 향해 눕히고 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML 위를 보고 눕힌 %CALLNAME:TARGET%의 다리를 열어 귀두를 얕게 삽입하고 나서,
			PRINTFORML %SLIT(TARGET)%를 관철해 갔다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM 위를 보고 눕힌 %CALLNAME:TARGET%의
			SIF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고, 
			PRINTFORML %SLIT(TARGET)%에 %TINKO(PLAYER)%를 대고 꿰뚫었다…
		ENDIF
		IF TALENT:연모
			IF TALENT:처녀 && TALENT:처녀 != -1
				PRINTFORML %타겟은% 눈가에 눈물이 맺히도록 아픔을 견디면서 %CALLNAME:PLAYER%에게 꽉 안겨들었다…
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;320, 상대주도정상위
;-------------------------------------------------
@MESSAGE_COM320
IF TALENT:처녀 == 1
	PRINTFORML %CALLNAME:TARGET%의 소망 대로, 그 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 귀두를 가져다 대고,
	PRINTFORML 꼭 닫혀있는 비순을 가르고 그대로 꿰뚫어, 순결의 증거를 찢었다…
		SIF TALENT:연모
			PRINTFORML %타겟은% 눈가에 눈물이 맺히도록 아픔을 견디면서 %CALLNAME:PLAYER%에게 꽉 안겨들었다…
ELSE
	PRINTFORML %마스터는% %CALLNAME:TARGET%에 엎어져, 갖고 싶다는 듯이 벌름거리는 질구에 귀두를 천천히 가라앉혀 갔다…
	SELECTCASE ABL:TARGET:질
		CASE 0
			PRINTFORML %TINKO()%는 좁다란 고기의 길을 삐걱삐걱 비집고 나아가, %CALLNAME:TARGET%의 깊숙한 곳까지 침입했다…
		CASE 1
			PRINTFORML 단단함이 남은 질육을 가르며 나아가, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASE 2,3
			PRINTFORML 주름이 옹골차게 꽉 찬 부드러운 살덩어리를 밀어 헤치고, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASEELSE
			PRINTFORML 힘을 기울이지 않아도, 녹아내릴 것 같은 %CALLNAME:TARGET%의 미육은 희희낙락 기뻐하며 %마스터를% 가장 안쪽까지 불러 들였다…
	ENDSELECT
	PRINTFORML 따뜻하게 미끌거리는 고깃주름에 휩싸여, 마땅히 있어야 할 장소에 들어 앉은 %CALLNAME:MASTER%의 %TINKO()%는 기쁜 듯이 움찔거렸다
ENDIF