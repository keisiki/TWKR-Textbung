@MESSAGE_COM60_1
;-------------------------------------------------
;60, 정상위(시라후)
;-------------------------------------------------
IF TFLAG:193
	;뽑지 않고
	IF TCVAR:V삽입관리플래그 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %타겟을%\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  허리를 꼭 껴안아 # 벽에 꽉 눌러 \@격렬하게 허리를 거절해,
			PRINTFORML 사정의 순간에 %TINKO(PLAYER)%를 질의 깊숙한 곳까지 찔러 넣었다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아 끝내면(자),
			PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 1
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%앞지르는 일 없이 다시 허리를 움직이기 시작했다…
			;배면입위로부터
			ELSE
				PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 이쪽을 뒤돌아 보게 하고 나서
				PRINTFORML %CALLNAME:TARGET%의 한쪽 발을 들어 올려 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ENDIF
	ELSEIF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:TARGET%의 한쪽 발을 들어 올려 %SLIT(TARGET)%에
		PRINTFORML 귀두를 주어 단숨에 관철했다…
	ELSE
		IF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 한쪽 발을 들어 올려
			SIF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아,
			PRINTFORML %SLIT(TARGET)%에 %TINKO(PLAYER)%를 주어 관철했다…
		ENDIF
		IF TALENT:연모
			IF TALENT:처녀
				PRINTFORML %타겟은% 아픔에 참으면서 %CALLNAME:PLAYER%에 꽉 안아 붙었다…
			ENDIF
		ENDIF
	ENDIF
;정상위
ELSE
	;뽑지 않고
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML 위로 향해 재운%CALLNAME:TARGET%에 지워지면서 허리를 거절해,
			PRINTFORML 사정의 순간에 %TINKO(PLAYER)%를 질의 깊숙한 곳까지 찔러 넣었다…
		ELSEIF TCVAR:102
			PRINTFORML %CALLNAME:TARGET%의 질내에 정액을 쏟아 끝내면(자),
			PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			PRINTFORML 질내로부터 앞지르는 일 없이 %타겟을% 위로 향해 재워 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TALENT:처녀 == 1
			PRINTFORML 위로 향해 재운%CALLNAME:TARGET%의 다리를 열어 귀두를 얕게 삽입하고 나서,
			PRINTFORML %SLIT(TARGET)%를 관철해 갔다…
		ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
			PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
			PRINTFORML %CALLNAME:TARGET%에 삽입했다…
		ELSE
			PRINTFORM 위로 향해 재운%CALLNAME:TARGET%의
			SIF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓아, 
			PRINTFORML %SLIT(TARGET)%에 %TINKO(PLAYER)%를 주어 관철했다…
		ENDIF
		IF TALENT:연모
			IF TALENT:처녀 && TALENT:처녀 != -1
				PRINTFORML %타겟은% 아픔에 참으면서 %CALLNAME:PLAYER%에 꽉 안아 붙었다…
			ENDIF
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;320, 상대주도정상위
;-------------------------------------------------
@MESSAGE_COM320
IF TALENT:처녀 == 1
	PRINTFORML %CALLNAME:TARGET%의 소망 대로, 그 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 귀두를 가져다 대며,
	PRINTFORML 딱이라고 닫은 비순을 나누어 열어 관철해, 순결의 증거를 찢었다…
		SIF TALENT:연모
			PRINTFORML %타겟은% 아픔에 참으면서 %CALLNAME:PLAYER%에 꽉 안아 붙었다…
ELSE
	PRINTFORML %마스터는% %CALLNAME:TARGET%에 엎어졌다와 갖고 싶어하는 듯하게 벌렁거리는 질구에 귀두를 천천히 가라앉혀 갔다…
	SELECTCASE ABL:TARGET:질
		CASE 0
			PRINTFORML %TINKO()%는 좁은 육도를 길길과 비틀어 열어 눌러 진행되어, %CALLNAME:TARGET%의 깊숙한 곳까지 침입했다…
		CASE 1
			PRINTFORML 단단함을 남긴 질육을 나누어 열어, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASE 2,3
			PRINTFORML 꽉 벽이 찬 부드러운 살덩어리를 밀어 헤쳐, %CALLNAME:TARGET%의 깊숙한 곳까지 %TINKO()%를 묻었다…
		CASEELSE
			PRINTFORML 힘을 집중하지 않고와도, 녹고 자른%CALLNAME:TARGET%의 미육은 기쁨으로서 %마스터를% 최안쪽까지 불러 넣었다…
	ENDSELECT
	PRINTFORML 유타카 쓰는 육벽에 싸여 그에 적합한 장소에 들어간 %CALLNAME:MASTER%의 %TINKO()%는 기쁜듯하게 꿈틀 붙었다
ENDIF