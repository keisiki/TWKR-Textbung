;-------------------------------------------------
;67, 대면좌위
;-------------------------------------------------
@MESSAGE_COM67
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% 시간이 멈춘%타겟을% 꼭 껴안아
			PRINTFORML %CALLNAME:TARGET%의 체취에 싸이면서, 복받쳐 오는 충동에 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 4
			PRINTFORML %조교자는% 시간이 멈춘%CALLNAME:TARGET%에 키스를 하며,  혀를 걸면서 다시 허리를 강탈 시작했다…
		ELSE
			PRINTFORM 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 시간이 멈춘%CALLNAME:TARGET%의 몸을 껴안아
			PRINTFORML 입안을 맛보면서 다시 허리를 강탈 시작했다…
		ENDIF
	ELSE
		PRINTFORM 시간이 멈춘%CALLNAME:TARGET%의 몸을 안아 올려
		PRINTFORM %TINKO(PLAYER)%목표로 하고 내려
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새의
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 질구
		ELSE
			PRINT 질 구멍
		ENDIF
		PRINTFORM 에 삽입해,
		;볼개그의 유무
		SIF TEQUIP:볼개그 == 0
			PRINT 입맞춤을 하면서
		PRINTFORML %CALLNAME:TARGET%라고 하는 오나홀로 자위할 수 있도록 허리를 동요시켰다…
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% %타겟을% 꼭 껴안은 채로,
			PRINTFORML 복받쳐 오는 충동에 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 4
			PRINTFORML %타겟은% %CALLNAME:PLAYER%과 키스를 해, 혀를 걸면서 다시 허리를 강탈 시작했다…
		ELSE
			PRINTFORM 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 %CALLNAME:TARGET%의 몸을 꽉 껴안아
			PRINTFORML 혀를 걸면서 다시 허리를 강탈 시작했다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올려
		PRINTFORM 천천히 내리면서
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새의
		PRINTFORM %SLIT()%에 삽입해,
		;볼개그의 유무
		SIF TEQUIP:볼개그 == 0
			PRINT 입맞춤을 하면서
		PRINTL 허리를 동요시켰다…
	ENDIF
	;SIF TALENT:연모
	;	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 목에 손을 써 안겨들었다…
ENDIF
;-------------------------------------------------
;68, 후배위
;-------------------------------------------------
@MESSAGE_COM68
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% 시간이 멈춘%타겟을% 꼭 껴안아
			PRINTFORML %CALLNAME:TARGET%의 체취에 싸이면서, 복받쳐 오는 충동에 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 5
			PRINTFORML 시간이 멈춘%타겟을% 등 뒤로부터 꼭 껴안아 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
		ELSE
			PRINTFORML 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이, 시간이 멈춘%CALLNAME:TARGET%의 몸을 등으로부터 껴안아
			PRINTFORML 몸을 흔들거림%타겟을% 사용한 자위를 재개했다…
		ENDIF
	ELSE
		PRINTFORM 시간이 멈춘%CALLNAME:TARGET%의 몸을 안아 올려 %SLIT()%
		IF TEQUIP:0 & 2
			PRINTFORML 에 %SHOW_BOTTOM(TARGET, 2)%의 틈새로부터 삽입했다…
		ELSE
			PRINTFORML 를 기다리고 있던 %TINKO(PLAYER)%로 관철했다…
		ENDIF
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% %타겟을% 꼭 껴안은 채로,
			PRINTFORML 복받쳐 오는 충동에 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 5
			PRINTFORML %타겟을% 등 뒤로부터 꼭 껴안아 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
		ELSE
			PRINTFORML 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 %CALLNAME:TARGET%의 몸을 등으로부터 껴안아 몸을 흔들기 시작했다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올려 %SLIT()%
		IF TEQUIP:0 & 2
			PRINTFORML 에 %SHOW_BOTTOM(TARGET, 2)%의 틈새로부터 천천히 삽입했다…
		ELSE
			PRINTFORML 를 기다리고 있던 물건으로 천천히 관철했다…
		ENDIF
	ENDIF
	;SIF TALENT:연모
	;	PRINTFORML %타겟은% 키를 위로 돌리고 뒤로 젖힐 수 있어 %CALLNAME:PLAYER%로 하고 눈사태 걸렸다…
ENDIF
;-------------------------------------------------
;69, 대면좌위애널
;-------------------------------------------------
@MESSAGE_COM69
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %조교자는% 시간이 멈춘%CALLNAME:TARGET%에 키스를 하며,  혀를 걸면서 다시 허리를 강탈 시작했다…
		RETURN 0
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
			PRINTFORM 앞지르는 일 없이 시간이 멈춘%CALLNAME:TARGET%의 몸을 등으로부터 꼭 껴안아
			PRINTFORML 몸을 흔들어 %타겟을% 사용한 자위를 재개했다…
	ELSE
		PRINTFORM 시간이 멈춘%CALLNAME:TARGET%의 몸을 안아 올리고
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 억지로 비켜 놓아
		PRINTFORM %TINKO(PLAYER)%목표로 하고 내려
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORM 항문으로 삽입하면(자),
	ENDIF
	;볼개그의 유무
	SIF TEQUIP:볼개그 == 0
		PRINT 입맞춤을 하면서
	PRINTFORML %CALLNAME:TARGET%의 애널로 자위할 수 있도록 허리를 동요시켰다…
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟은% %CALLNAME:PLAYER%과 키스를 해, 혀를 걸면서 다시 허리를 강탈 시작했다…
		RETURN 0
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
			PRINTFORM 앞지르는 일 없이 %CALLNAME:TARGET%의 몸을 꽉 껴안아
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올리고
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 억지로 비켜 놓아
		PRINTFORM 천천히 내리면서
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORM 아누스로 삽입하면(자),
	ENDIF
	;볼개그의 유무
	SIF TEQUIP:볼개그 == 0
		PRINT 입맞춤을 하면서
	PRINTL 허리를 동요시켰다…
	;함락이 끝난 상태
	SIF TALENT:연모
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 목에 손을 써 안겨들었다…
ENDIF

;-------------------------------------------------
;70, 후배위애널
;-------------------------------------------------
@MESSAGE_COM70
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 시간이 멈춘%타겟을% 등 뒤로부터 꼭 껴안아 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
		PRINTFORML 앞지르는 일 없이, 시간이 멈춘%CALLNAME:TARGET%의 몸을 등으로부터 껴안아
		PRINTFORML 몸을 흔들거림%타겟을% 사용한 자위를 재개했다…
	ELSE
		PRINTFORML 시간이 멈춘%CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로부터
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 항문을 기다리고 있던 %TINKO(PLAYER)%로 천천히 관철했다…
	ENDIF
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟을% 등 뒤로부터 꼭 껴안아 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
		PRINTFORML 앞지르는 일 없이 %CALLNAME:TARGET%의 몸을 등으로부터 껴안아 몸을 흔들기 시작했다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로부터
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스를 기다리고 있던 물건으로 천천히 관철했다…
	ENDIF
	;함락이 끝난 상태
	SIF TALENT:연모
		PRINTFORML %타겟은% 키를 위로 돌리고 뒤로 젖힐 수 있어 %CALLNAME:PLAYER%로 하고 눈사태 걸렸다…
ENDIF