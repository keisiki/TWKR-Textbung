;-------------------------------------------------
;67, 대면좌위
;-------------------------------------------------
@MESSAGE_COM67
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% 시간이 멈춰진 %타겟을% 꼭 껴안고,
			PRINTFORML %CALLNAME:TARGET%의 체취에 포근하게 감싸여, 치밀어 오르는 충동에 몸을 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 4
			PRINTFORML %조교자는% 시간이 멈춰진 %CALLNAME:TARGET%에게 키스를 해,  혀를 휘감아 얽으면서 다시 허리를 격하게 흔들기 시작했다…
		ELSE
			PRINTFORM 질내에서 %TINKO(PLAYER)%를 뽑지도 않고 시간이 멈춘 %CALLNAME:TARGET%의 몸을 껴안고
			PRINTFORML 입안을 맛보면서 다시 허리를 격하게 흔들기 시작했다…
		ENDIF
	ELSE
		PRINTFORM 시간이 멈춘 %CALLNAME:TARGET%의 몸을 안아 올려
		PRINTFORM %TINKO(PLAYER)%를 목표로 내려 놓으며
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 질구
		ELSE
			PRINT 질 구멍
		ENDIF
		PRINTFORM 에 삽입해,
		;볼개그의 유무
		SIF TEQUIP:볼개그 == 0
			PRINT 입맞춤을 하면서
		PRINTFORML %CALLNAME:TARGET%라고 하는 오나홀로 자위하려고 허리를 격하게 흔들었다…
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% %타겟을% 꼭 껴안은 채로,
			PRINTFORML 치밀어 오르는 충동에 몸을 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 4
			PRINTFORML %타겟은% %CALLNAME:PLAYER%과 키스를 해, 혀를 휘감고 얽으면서 다시 허리를 격하게 흔들기 시작했다…
		ELSE
			PRINTFORM 질내로부터 %TINKO(PLAYER)%를 뽑지도 않고 그대로 %CALLNAME:TARGET%의 몸을 꽉 껴안고
			PRINTFORML 혀를 휘감아 얽으면서 다시 허리를 강하게 흔들기 시작했다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올려
		PRINTFORM 천천히 내려 놓으면서
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로
		PRINTFORM %SLIT()%에 삽입해,
		;볼개그의 유무
		SIF TEQUIP:볼개그 == 0
			PRINT 입맞춤을 하면서
		PRINTL 허리를 흔들었다…
	ENDIF
	;SIF TALENT:연모
	;	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 목에 팔을 두르고 안겨들었다…
ENDIF
;-------------------------------------------------
;68, 배면좌위
;-------------------------------------------------
@MESSAGE_COM68
IF FLAG:70 == 1
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% 시간이 멈춰진 %타겟을% 꼭 껴안고,
			PRINTFORML %CALLNAME:TARGET%의 체취에 포근하게 감싸여, 치밀어 오르는 충동에 몸을 맡겨 허리를 밀어 올렸다…
		ELSEIF TEQUIP:체위2 == 5
			PRINTFORML 시간이 멈춘 %타겟을% 등 뒤에서 꼭 껴안고 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
		ELSE
			PRINTFORML 질내로부터 %TINKO(PLAYER)%를 뽑아내지도 않고, 시간이 멈춘 %CALLNAME:TARGET%의 몸을 등에서 껴안고
			PRINTFORML 신체를 흔들어 %타겟을% 사용한 자위를 재개했다…
		ENDIF
	ELSE
		PRINTFORM 시간이 멈춰진 %CALLNAME:TARGET%의 몸을 안아 올려 %SLIT()%
		IF TEQUIP:0 & 2
			PRINTFORML 에 %SHOW_BOTTOM(TARGET, 2)%의 틈새로부터 삽입했다…
		ELSE
			PRINTFORML 를 기다리고 있던 %TINKO(PLAYER)%로 꿰뚫었다…
		ENDIF
	ENDIF
ELSE
	IF TCVAR:100 == PLAYER && TARGET == TFLAG:103
		IF NOWEX:MASTER:사정
			PRINTFORML %조교자는% %타겟을% 꼭 껴안은 채로,
			PRINTFORML 치밀어 오르는 충동에 몸을 맡겨 허리를 쳐 올렸다…
		ELSEIF TEQUIP:체위2 == 5
			PRINTFORML %타겟을% 등 뒤에서 꼭 껴안고 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
		ELSE
			PRINTFORML 질내로부터 %TINKO(PLAYER)%를 뽑아내지도 않은 채 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고, 몸을 흔들기 시작했다…
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올려 %SLIT()%
		IF TEQUIP:0 & 2
			PRINTFORML 에 %SHOW_BOTTOM(TARGET, 2)%의 틈새로 천천히 삽입했다…
		ELSE
			PRINTFORML 를 기다리고 있던 물건으로 천천히 꿰뚫었다…
		ENDIF
	ENDIF
	;SIF TALENT:연모
	;	PRINTFORML %타겟은% 등을 뒤로 젖히고 %CALLNAME:PLAYER%에게 나긋나긋한 몸을 기대고 아양을 부렸다…
ENDIF
;-------------------------------------------------
;69, 대면좌위애널
;-------------------------------------------------
@MESSAGE_COM69
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %조교자는% 시간이 멈춰진 %CALLNAME:TARGET%에게 키스를 해,  혀를 휘감아 얽으면서 다시 허리를 격하게 흔들기 시작했다…
		RETURN 0
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
			PRINTFORM 뽑아내지도 않은 채 시간이 멈춘 %CALLNAME:TARGET%의 몸을 등 뒤에서 꼭 껴안고
			PRINTFORML 몸을 흔들어 %타겟을% 사용한 자위를 재개했다…
	ELSE
		PRINTFORM 시간이 멈춰진 %CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 억지로 비켜 놓고
		PRINTFORM %TINKO(PLAYER)%목표로 하고 내려
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORM 항문으로 삽입하면서,
	ENDIF
	;볼개그의 유무
	SIF TEQUIP:볼개그 == 0
		PRINT 입맞춤을 하면서
	PRINTFORML %CALLNAME:TARGET%의 애널로 자위하려는 것 처럼 허리를 격하게 흔들었다…
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟은% %CALLNAME:PLAYER%과 키스를 해, 혀를 휘감아 얽으면서 다시 허리를 격하게 흔들기 시작했다…
		RETURN 0
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
			PRINTFORM 뽑아내지도 않고 %CALLNAME:TARGET%의 몸을 꽉 껴안고
	ELSE
		PRINTFORM %CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 억지로 비켜 놓아
		PRINTFORM 천천히 내리면서
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORM 아누스로 삽입하면서,
	ENDIF
	;볼개그의 유무
	SIF TEQUIP:볼개그 == 0
		PRINT 입맞춤을 하면서
	PRINTL 허리를 흔들었다…
	;함락이 끝난 상태
	SIF TALENT:연모
		PRINTFORML %타겟은% %CALLNAME:PLAYER%의 목에 팔을 두르며 안겨 왔다…
ENDIF

;-------------------------------------------------
;70, 배면좌위 애널
;-------------------------------------------------
@MESSAGE_COM70
IF FLAG:70 == 1
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML 시간이 멈춰진 %타겟을% 등 뒤에서 꼭 껴안고 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
		PRINTFORML 뽑아내지도 않은 채, 시간이 멈춰진 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고
		PRINTFORML 몸을 흔들거리는 %타겟을% 사용한 자위를 재개했다…
	ELSE
		PRINTFORML 시간이 멈춰진 %CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 항문을 기다리고 있던 %TINKO(PLAYER)%로 천천히 꿰뚫었다…
	ENDIF
ELSE
	;연속
	IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
		PRINTFORML %타겟을% 등 뒤로에서 꼭 껴안고 다시 %CALLNAME:TARGET%의 몸을 흔들기 시작했다…
	;뽑지 않고
	ELSEIF TCVAR:101 == PLAYER
		PRINTFORM 장내로부터
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORM %TINKO(PLAYER)%를
		ELSE
			PRINTFORM 딜도를
		ENDIF
		PRINTFORML 뽑아내지도 않은 채 %CALLNAME:TARGET%의 몸을 등 뒤에서 껴안고 몸을 흔들기 시작했다…
	ELSE
		PRINTFORML %CALLNAME:TARGET%의 몸을 안아 올려
		SIF TEQUIP:0 & 2
			PRINTFORM 비켜 놓은 %SHOW_BOTTOM(TARGET, 2)%의 틈새로
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 점액 투성이가 된 
		PRINTFORML 아누스를 기다리고 있던 물건으로 천천히 꿰꿇었다…
	ENDIF
	;함락이 끝난 상태
	SIF TALENT:연모
		PRINTFORML %타겟은% 등윌 뒤로 젖히고 %CALLNAME:PLAYER%에게 나긋나긋한 몸을 기대며 아양을 부렸다…
ENDIF