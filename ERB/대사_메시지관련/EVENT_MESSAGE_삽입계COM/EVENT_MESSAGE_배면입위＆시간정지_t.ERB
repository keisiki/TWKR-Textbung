@MESSAGE_COM61배면립정도_시간정지_뽑지않고

IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
	IF TALENT:체형 == -1
		IF NOWEX:MASTER:사정
			PRINTFORML 이키 그렇게 된%마스터는%, 오나홀로 하고 있는%CALLNAME:TARGET%의 살집의 적은 엉덩이에 한층 격렬하게 허리를 두드려 붙어라
			PRINTFORML 꽉 단단히 조이는 어린 질육으로 사정 직전의 %TINKO(PLAYER)%를 훑어내기 주었다…
		ELSEIF TCVAR:102
			PRINTFORML 시간을 멈춘 채로 %타겟이% 작은 몸에 정액을 쏟은 %마스터는%
			PRINTFORML 다시 어린 질 구멍을 오나홀로 해 허리를 거절하기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
		;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질 구멍을 고기 오나홀로 해, 다시 쾌락을 탐내기 시작했다…
		;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 1
				PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 다시 허리를 움직이기 시작했다…
			;배면입위로부터
			ELSE
				PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 유혈에 %TINKO(PLAYER)%를 삽입해, 오나홀과 같이 상하에 흔들고 있던%마스터는%
				PRINTFORML 질내에 %TINKO(PLAYER)%를 삽입한 채로 완구로도 취급하는 것 같이 %타겟이% 작은 몸을 빙글과 회전시키고
				PRINTFORML 공중에서 정지하고 있는 %CALLNAME:TARGET%의 어린 질 구멍을 뒤로부터 범하기 시작했다…
			ENDIF
		ENDIF
	ELSEIF NOWEX:MASTER:사정
		PRINTFORML %CALLNAME:TARGET%의 질 구멍을 고기 오나홀로서 사용하고 있는 %마스터는%
		PRINTFORML 정액이 복받쳐 오는 것을 느껴, 격렬하게 허리를 %CALLNAME:TARGET%의 고육에 부딪치기 시작했다…
	ELSEIF TCVAR:102
		PRINTFORML 시간을 멈춘 채로 %CALLNAME:TARGET%에 정액을 토해낸%마스터는%
		PRINTFORML 다시 쾌락을 탐내기 (위해)때문에 꼼짝도 하지 않는 %타겟을% 범하기 시작했다…
	ELSEIF TEQUIP:체위2 == 1
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질 구멍을 고기 오나홀로 해, 다시 쾌락을 탐내기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TEQUIP:체위2 == 1
			PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 다시 허리를 움직이기 시작했다…
		;배면입위로부터
		ELSE
			PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 뒤로 돌아 붐비어, 엉덩이를 쑥 내미는 것 같은 모습을 시켜 다시 삽입해
			PRINTFORML 물결치는 고육과 질내의 상태의 차이를 즐기면서 질 구멍을 범했다……
		ENDIF
	ENDIF
ELSEIF NOWEX:MASTER:사정
	PRINTFORML %조교자는% 시간이 멈춘%CALLNAME:TARGET%의 %HIP(TARGET)%를 잡아 격렬하게 허리를 모습
	PRINTFORML 사정의 순간에 %CALLNAME:TARGET%의 질 안쪽까지 %TINKO(PLAYER)%를 삽입했다…
ELSEIF TCVAR:102
	PRINTFORML 시간이 멈춘%CALLNAME:TARGET%의 질내에 정액을 쏟아 끝내면(자)
	PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
ELSEIF TEQUIP:체위2 == 2
	;조교자에 대하고 있다
	IF TALENT:PLAYER:2 & 2
		PRINTFORML 일절 저항하지 않는 %CALLNAME:TARGET%의 질내의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
	;페니스밴드를 사용하고 있는 경우
	ELSE
		PRINTFORML 일절 저항하지 않는 %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	IF TEQUIP:체위2 == 2
		PRINTFORML 꼼짝도 하지 않는 %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 앞지르는 일 없이 다시 허리를 움직이기 시작했다…
	;대면입위로부터
	ELSE
		PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 뒤로 돌아 포함
		PRINTFORML 엉덩이를 쑥 내미는 것 같은 모습을 시켜 삽입해, 다시 허리를 움직이기 시작했다…
	ENDIF
ENDIF

@MESSAGE_COM61배면립정도_시간정지_신규삽입
IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
	IF TALENT:처녀 == 1
		IF TALENT:체형 == -1
			IF NOWEX:MASTER:사정
				PRINTFORML %마스터는% 시간을 멈춘%타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해, 작은 몸자체 들어 올려
				PRINTFORML 남을 알기에는  너무 빠른 아이와 같이 내세우고 줄기에, 사정 직전의 %TINKO(PLAYER)%를 나사 포함
			ELSE
				PRINTFORML %마스터는% 시간을 멈춘%타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해, 작은 몸자체 들어 올려
				PRINTFORML 남을 알기에는  너무 빠른 아이와 같이 내세우고 줄기에 %TINKO(PLAYER)%를 나사 포함
			ENDIF
			PRINTFORML 일절 망설임 없이 격렬한 허리 사용으로 어린 질 구멍을 범하기 시작했다…
		ELSEIF NOWEX:MASTER:사정
			PRINTFORML %마스터는% 시간을 멈춘%타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해
			PRINTFORML 정액을 토해내기 (위해)때문에인 만큼, 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 사정 직전의 %TINKO(PLAYER)%를 나사 붐비었다…
		ELSE
			PRINTFORML 뒤로부터 아직도 남자를 모르는 %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 시간을 멈춘 채로 단숨에 관철했다
			PRINTFORML %마스터는% 침입자를 거절하도록(듯이) 꽉 단단히 조여 오는 질 구멍을 억지로 범하기 시작했다……
		ENDIF
	ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
		PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
		PRINTFORML %CALLNAME:TARGET%에 삽입했다…
	ELSEIF TALENT:체형 == -1
		IF NOWEX:MASTER:사정
			PRINTFORML %마스터는% 시간을 멈춘%타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해, 허리의 높이까지 들어 올리고
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 아이와 같이 내세우고 줄기에 말이야 글자 붐비어, 일절 망설임 없이 격렬한 허리 사용으로 어린 질 구멍을 범하기 시작했다…
		ELSE
			PRINTFORML 시간을 멈춘%타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 해, 허리의 높이까지 들어 올리고
			PRINTFORM 공중에서 정지하고 있는 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤로부터
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액에 젖은,
			PRINTFORML 아이와 같은 세로줄을 %TINKO(PLAYER)%로 나누어 열림
			PRINTFORML 꽉 단단히 조여 오는 어린 질 구멍을 범하기 시작했다…
		ENDIF
	ELSEIF NOWEX:MASTER:사정
		PRINTFORML %마스터는% 정액을 토해내기 (위해)때문에인 만큼 시간을 멈춘%CALLNAME:TARGET%의 엉덩이를 잡아, 폭발 직전의 %TINKO(PLAYER)%를\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 말이야 글자 붐비었다
		PRINTFORML %CALLNAME:TARGET%의 질내는 갑작스러운 침입자에게 전율 있지만, %마스터는% 상관하는 일 없이 허리를 움직이기 시작했다…
	ELSE
		PRINTFORML 시간을 멈춘%CALLNAME:TARGET%의 질을 오나홀 대신에 사용하기 (위해)때문에, 엉덩이를 쑥 내미는 것 같은 모습에 시켜
		IF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
		ELSEIF TEQUIP:0 & 1
			PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
		ENDIF
		PRINTFORM 뒤로부터
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 에 삽입해 질 구멍을 범하기 시작했다…
	ENDIF
ELSE
	IF TALENT:처녀 == 1
		PRINTFORML 시간을 멈춤 엉덩이를 쑥 내미는 것 같은 모습으로 한 %CALLNAME:TARGET%의
		PRINTFORML 남을 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 관철했다…
	ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
		PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
		PRINTFORML %CALLNAME:TARGET%에 삽입했다…
	ELSE
		PRINTFORM 시간을 멈춤 엉덩이를 쑥 내미는 것 같은 모습으로 한 %CALLNAME:TARGET%의
		IF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
		ELSEIF TEQUIP:0 & 1
			PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
		ENDIF
		PRINTFORM 뒤로부터
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 의 최안쪽까지 찔러 넣었다…
	ENDIF
ENDIF
