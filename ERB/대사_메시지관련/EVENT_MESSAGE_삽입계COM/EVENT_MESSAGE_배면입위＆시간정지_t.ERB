@MESSAGE_COM61배면입위_시간정지_뽑지않고

IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
	IF TALENT:체형 == -1
		IF NOWEX:MASTER:사정
			PRINTFORML 갈 것 같게 된 %마스터는%, 오나홀로 삼고 있는 %CALLNAME:TARGET%의 살집이 얇은 매끈한 엉덩이에 한층 격렬하게 허리를 내지르며
			PRINTFORML 꽉 단단히 조이는 어린 질육으로 사정 직전의 %TINKO(PLAYER)%를 훑어 올렸다…
		ELSEIF TCVAR:102
			PRINTFORML 시간을 멈춘 채로 %타겟의% 작은 몸에 정액을 쏟아낸 %마스터는%
			PRINTFORML 다시 어린 질 구멍을 오나홀로 삼아 허리를 흔들기 시작했다…
		ELSEIF TEQUIP:체위2 == 1
		;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질 구멍을 고기 오나홀로 삼아, 다시 쾌락을 탐하기 시작했다…
		;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:체위2 == 1
				PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 뽑아내지도 않고 다시 허리를 움직이기 시작했다…
			;배면입위로부터
			ELSE
				PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 미숙한 구멍에 %TINKO(PLAYER)%를 삽입해, 오나홀과 같이 위아래로 흔들고 있던 %마스터는%
				PRINTFORML 질내에 %TINKO(PLAYER)%를 삽입한 채로 완구라도 취급하는 것 같이 %타겟의% 작은 몸을 빙글 돌려
				PRINTFORML 공중에 정지해 있는 %CALLNAME:TARGET%의 어린 질 구멍을 뒤에서 범하기 시작했다…
			ENDIF
		ENDIF
	ELSEIF NOWEX:MASTER:사정
		PRINTFORML %CALLNAME:TARGET%의 질 구멍을 고기 오나홀로 사용하고 있던 %마스터는%
		PRINTFORML 정액이 치밀어 오르는 것을 느끼고, 격렬하게 허리를 %CALLNAME:TARGET%의 엉덩이살에 부딪치기 시작했다…
	ELSEIF TCVAR:102
		PRINTFORML 시간을 멈춘 채로 %CALLNAME:TARGET%에 정액을 토해낸 %마스터는%
		PRINTFORML 다시 쾌락을 탐하기 위해 꼼짝도 하지 않는 %타겟을% 범하기 시작했다…
	ELSEIF TEQUIP:체위2 == 1
		;조교자에 대하고 있다
		IF TALENT:PLAYER:2 & 2
			PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질 구멍을 고기 오나홀로 삼아, 다시 쾌락을 탐하기 시작했다…
		;페니스밴드를 사용하고 있는 경우
		ELSE
			PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질내로부터 뽑지도 않고, 다시 허리를 움직이기 시작했다…
		ENDIF
	ELSE
		IF TEQUIP:체위2 == 1
			PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질내에서 %TINKO(PLAYER)%를 뽑아내지도 않고 다시 허리를 움직이기 시작했다…
		;배면입위로부터
		ELSE
			PRINTFORML 일단 %CALLNAME:TARGET%에게서 %TINKO(PLAYER)%를 뽑아 낸 후 뒤로 돌아 들어가, 엉덩이를 쑥 내미는 것 같은 모습을 시키고 다시 삽입해
			PRINTFORML 물결치는 엉덩이살과 질내 상태의 차이를 즐기면서 질 구멍을 범했다……
		ENDIF
	ENDIF
ELSEIF NOWEX:MASTER:사정
	PRINTFORML %조교자는% 시간이 멈춘 %CALLNAME:TARGET%의 %HIP(TARGET)%를 붙잡고 격렬하게 허리를 흔들다
	PRINTFORML 사정의 순간에 %CALLNAME:TARGET%의 질 안쪽까지 %TINKO(PLAYER)%를 삽입했다…
ELSEIF TCVAR:102
	PRINTFORML 시간이 멈춘 %CALLNAME:TARGET%의 질내에 정액을 쏟아내고
	PRINTFORML %TINKO(PLAYER)%가 경도를 잃지 않는 동안에 다시 허리를 움직이기 시작했다…
ELSEIF TEQUIP:체위2 == 2
	;조교자에 대하고 있다
	IF TALENT:PLAYER:2 & 2
		PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 질 안 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
	;페니스밴드를 사용하고 있는 경우
	ELSE
		PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 질내로부터 뽑아내지도 않고, 다시 허리를 움직이기 시작했다…
	ENDIF
ELSE
	IF TEQUIP:체위2 == 2
		PRINTFORML 꼼짝도 하지 않는 %CALLNAME:TARGET%의 질내로부터 %TINKO(PLAYER)%를 뽑아내지도 않은 채 다시 허리를 움직이기 시작했다…
	;대면입위로부터
	ELSE
		PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑아 낸 후 뒤로 돌아 들어가
		PRINTFORML 엉덩이를 쑥 내미는 것 같은 모습을 시키고 삽입해, 다시 허리를 움직이기 시작했다…
	ENDIF
ENDIF

@MESSAGE_COM61배면입위_시간정지_신규삽입
IF EXP:MASTER:시간경험 >= EXP:TARGET:애정경험 + 50
	IF TALENT:처녀 == 1
		IF TALENT:체형 == -1
			IF NOWEX:MASTER:사정
				PRINTFORML %마스터는% 시간을 멈춘 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습을 시키고, 작은 몸 자체를 들어 올려
				PRINTFORML 남자를 알기에는 너무 빠른 아이와도 같은 세로선에, 사정 직전의 %TINKO(PLAYER)%를 힘껏 쑤셔 넣었다
			ELSE
				PRINTFORML %마스터는% 시간을 멈춘 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 만들고, 작은 몸 자체를 들어 올려
				PRINTFORML 남자를 알기에는 너무 빠른 아이와도 같은 세로선에 %TINKO(PLAYER)%를 힘껏 쑤셔 넣었다
			ENDIF
			PRINTFORML 조금도 거리낌이 없이 격렬한 허리놀림으로 어린 질 구멍을 범하기 시작했다…
		ELSEIF NOWEX:MASTER:사정
			PRINTFORML %마스터는% 시간을 멈춘 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 만들고
			PRINTFORML 오로지 정액을 토해내기 위해, 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 사정 직전의 %TINKO(PLAYER)%를 힘껏 쑤셔 넣었다…
		ELSE
			PRINTFORML 뒤에서 아직 남자를 모르는 %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 시간을 멈춘 채로 단숨에 관철했다
			PRINTFORML %마스터는% 침입자를 거부하려고 꽈악 단단히 조여 오는 질 구멍을 억지로 범하기 시작했다……
		ENDIF
	ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
		PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
		PRINTFORML %CALLNAME:TARGET%에 삽입했다…
	ELSEIF TALENT:체형 == -1
		IF NOWEX:MASTER:사정
			PRINTFORML %마스터는% 시간을 멈춘 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 만들고, 허리 높이까지 들어 올려
			PRINTFORML 사정 직전의 %TINKO(PLAYER)%를 아이와도 같은 세로선에 쑤셔 넣어, 조금도 망설이지 않고 격렬한 허리놀림으로 어린 질 구멍을 범하기 시작했다…
		ELSE
			PRINTFORML 시간을 멈춘 %타겟을% 엉덩이를 쑥 내미는 것 같은 모습으로 만들고, 허리 높이까지 들어 올려
			PRINTFORM 공중에 정지해 있는 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액에 젖은,
			PRINTFORML 아이와 같은 세로선을 %TINKO(PLAYER)%로 좌우로 나누어 열어
			PRINTFORML 꽈악하고 단단히 조여 오는 어린 질 구멍을 범하기 시작했다…
		ENDIF
	ELSEIF NOWEX:MASTER:사정
		PRINTFORML %마스터는% 오로지 정액을 토해내기 위해, 시간을 멈춘 %CALLNAME:TARGET%의 엉덩이를 붙잡고, 폭발 직전의 %TINKO(PLAYER)%를\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@에 대고 찔러 넣었다
		PRINTFORML %CALLNAME:TARGET%의 질내는 갑작스러운 침입자에 전율 했지만, %마스터는% 신경 쓰지 않고 허리를 움직이기 시작했다…
	ELSE
		PRINTFORML 시간을 멈춘 %CALLNAME:TARGET%의 질을 오나홀 대신 사용하기 위해, 엉덩이를 쑥 내미는 것 같은 모습을 시키고
		IF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
		ELSEIF TEQUIP:0 & 1
			PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
		ENDIF
		PRINTFORM 뒤에서
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 에 삽입해 질 구멍을 범하기 시작했다…
	ENDIF
ELSE
	IF TALENT:처녀 == 1
		PRINTFORML 시간을 멈춰져 엉덩이를 쑥 내미는 것 같은 모습으로 된 %CALLNAME:TARGET%의
		PRINTFORML 남자를 모르는\@ TALENT:체형 < 0 ?  어린 균열 # 비렬 \@을 %TINKO(PLAYER)%로 꿰뚫었다…
	ELSEIF MASTER_POSE(SET_V, SET_P, 1) && MASTER_POSE(SET_V, SET_P, 1) != TARGET
		PRINTFORML %CALLNAME:(MASTER_POSE(SET_V, SET_P, 1))%로부터 %TINKO(PLAYER)%를 뽑는 것과 동시에
		PRINTFORML %CALLNAME:TARGET%에 삽입했다…
	ELSE
		PRINTFORM 시간을 멈춰져 엉덩이를 쑥 내미는 것 같은 모습으로 된 %CALLNAME:TARGET%의
		IF TEQUIP:0 & 2
			PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 내려,
		ELSEIF TEQUIP:0 & 1
			PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
		ENDIF
		PRINTFORM 뒤에서
		;윤활 LV4 이상
		SIF PALAM:윤활 >= PALAMLV:4
			PRINT 흠뻑 젖어 있는
		;작은 체형
		IF TALENT:체형 < 0
			PRINT 어린 균열
		ELSE
			PRINT 비렬
		ENDIF
		PRINTFORML 의 가장 안쪽까지 찔러 넣었다…
	ENDIF
ENDIF
