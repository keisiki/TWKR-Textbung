;-------------------------------------------------
;63, 후배위애널
;-------------------------------------------------
@MESSAGE_COM63
IF FLAG:70 == 1
	IF TFLAG:193
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:체위2 == 2
				PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 아누스에 삽입한 채로 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑고 뒤로 돌아 들아가
				PRINTFORML 엉덩이를 쑥 내미는 것 같은 모습을 시키고 다시 %CALLNAME:TARGET%의 아누스를 범하기 시작했다…
			ENDIF
		ELSE
			PRINTFORM 시간을 멈춤 엉덩이를 쑥 내민 것 같은 모습으로 만든 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 가장 안쪽까지 찔러 넣었다…
		ENDIF
	ELSE
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 일체 저항하지 않는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 삽입한 채로 %타겟을% 네발로 엎드린 것 같은 몸의 자세로 만들고 나서, 다시 허리를 움직이기 시작했다…
		ELSE
			PRINTFORM 시간을 멈추고 네발로 엎드린 모습으로 만든 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 가장 안쪽 깊은 곳까지 찔러 넣었다…
		ENDIF
	ENDIF
ELSEIF CFLAG:MASTER:장난 == 2
	IF TFLAG:193
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:체위2 == 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스에 삽입한 채로 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑고 뒤로 돌아 돌아가
				PRINTFORML 엉덩이를 쑥 내민 것 같은 모습을 시키고 다시 %CALLNAME:TARGET%의 아누스를 범하기 시작했다…
			ENDIF
		ELSE
			PRINTFORM 자고 있는 %타겟을% 엉덩이를 쑥 내민 것 같은 모습으로 만들고
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 가장 안쪽 깊은 곳까지 찔러 넣었다…
		ENDIF
	ELSE
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML 자고 있는 %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 삽입한 채로 %타겟을% 엎드리게 해, 다시 허리를 움직이기 시작했다…
		ELSE
			PRINTFORM 자고 있는 %타겟을% 엎드린 자세로 만들어
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ELSE
	IF TFLAG:193
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			IF TEQUIP:체위2 == 2
				PRINTFORML %CALLNAME:TARGET%의 아누스에 삽입한 채로 다시 허리를 움직이기 시작했다…
			;대면입위로부터
			ELSE
				PRINTFORML 일단%CALLNAME:TARGET%로부터 %TINKO(PLAYER)%를 뽑고\@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  쑥 내밀게 한 양손을 잡고 # 벽에 손을 짚게 하고 \@
				PRINTFORML 뒤에서 삽입해 다시 허리를 움직이기 시작했다…
			ENDIF
		ELSE
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓았다
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올렸다
			ENDIF
			PRINTFORM %CALLNAME:TARGET%
			PRINTFORM \@ OPENPLACE(CFLAG:MASTER:현재위치) == 2 ?  에게 쑥 내밀게 한 양손을 잡고 # 의 양손을 벽에 짚게 한 다음 \@, 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ELSE
		;연속
		IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
			;조교자에 대하고 있다
			IF TALENT:PLAYER:2 & 2
				PRINTFORML %CALLNAME:TARGET%의 아누스의 감촉을 맛보면서, 다시 허리를 움직이기 시작했다…
			;페니스밴드를 사용하고 있는 경우
			ELSE
				PRINTFORML %CALLNAME:TARGET%의 장내로부터 뽑지 않고, 다시 허리를 움직이기 시작했다…
			ENDIF
		;뽑지 않고
		ELSEIF TCVAR:101 == PLAYER
			PRINTFORML 삽입한 채로 %타겟을% 네발로 엎드리게 시킨 다음, 다시 허리를 움직이기 시작했다…
		ELSE
			PRINTFORM 네발로 엎드리게 한 %CALLNAME:TARGET%의
			IF TEQUIP:0 & 2
				PRINTFORM %SHOW_BOTTOM(TARGET, 2)%를 비켜 놓고,
			ELSEIF TEQUIP:0 & 1
				PRINTFORM %SHOW_BOTTOM(TARGET, 1)%를 걷어 올려
			ENDIF
			PRINTFORM 뒤에서
			;윤활 LV4 이상
			SIF PALAM:윤활 >= PALAMLV:4
				PRINT 점액 투성이가 된 
			PRINTFORML 아누스의 최심부까지 찔러 넣었다…
		ENDIF
	ENDIF
ENDIF

;-------------------------------------------------
;323, 상대주도후배위A
;-------------------------------------------------
@MESSAGE_COM323
PRINTFORML %마스터는% %CALLNAME:TARGET%를 뒤에서 덮쳐 누르고 \@ ABL:애널 > 2?  갖고 싶다는 듯이 벌렁거리는 # \@애널에 귀두를 가져다 대,  천천히 허리를 가라앉혀 갔다……
PRINTFORML %타겟은% 달콤한 숨을 내쉬며 삽입감을 곱씹고 있다……