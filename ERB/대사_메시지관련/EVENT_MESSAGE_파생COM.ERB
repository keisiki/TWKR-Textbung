;-------------------------------------------------
;1,シックスナイン
;-------------------------------------------------
@MESSAGE_SCOM1
IF FLAG:70 == 1
	IF SELECTCOM == 3
		PRINTFORM 시간을 멈추었다%CALLNAME:TARGET%의 입에%조사처리(TINKO(PLAYER),"를")% 찔러 넣으면서,%CALLNAME:TARGET%의
		IF TEQUIP:바이브
			IF TALENT:체형 < 0
				PRINT 어린 와레메
			ELSE
				PRINT 비렬
			ENDIF
				PRINTFORML 중(안)에서 요염하게 꾸불꾸불하는 바이브래이터를 전후에 빼고 꽂기 했다…
		ELSE
			PRINTFORML \@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@(을)를 빨고 풀었다…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 시간을 멈추었다%CALLNAME:TARGET%의 입에%조사처리(TINKO(PLAYER),"를")% 찔러 넣으면서,
		;アナルバイブ
		IF TEQUIP:애널바이브
			PRINTFORML 조금씩 떨리는 어널 바이브래이터를 전후에 빼고 꽂기 했다…
		;アナルビーズ
		ELSEIF TEQUIP:애널비즈
			PRINTFORML 비즈의 끈을 잡아당겨 가볍게 출납해 주었다…
		;浣腸器＋プラグ
		ELSEIF TEQUIP:관장
			PRINTFORML 배설감에 떨리는 국혈을 플러그로 그리그리와 보전은 했다…
		;拡張バルーン
		ELSEIF TEQUIP:확장벌룬
			PRINTFORML 어널을 한계까지 넓히고 있는 벌룬을 전후에 움직여 주었다…
		;アナル電極
		ELSEIF TEQUIP:애널전극
			PRINTFORML 희미하게 소리를 내는 전극으로 그리그리와 국혈을 반죽해 돌렸다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 어널을 농
			;Ａ経験100以上＋욕망のPALAMLV3以上
			SIF PALAM:욕정 >= PALAMLV:3 && EXP:A경험 >= EXPLV:3
				PRINT (이)라고, 손가락끝으로 직장까지 휘저어
		PRINTL …
		ENDIF
	ENDIF
ELSE
	IF SELECTCOM == 3
		PRINTFORM %CALLNAME:TARGET%(와)과 서로의 성기를 서로 빨면서
		IF TEQUIP:바이브
			IF TALENT:체형 < 0
				PRINT 어린 와레메
			ELSE
				PRINT 비렬
			ENDIF
				PRINTFORML 중(안)에서 요염하게 꾸불꾸불하는 바이브래이터를 전후에 빼고 꽂기 했다…
		ELSE
			PRINTFORML 질구에 손가락을 삽입했다…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM %CALLNAME:TARGET%(와)과 서로의 성기를 서로 빨면서
		;アナルバイブ
		IF TEQUIP:애널바이브
			PRINTFORML 조금씩 떨리는 어널 바이브래이터를 전후에 빼고 꽂기 했다…
		;アナルビーズ
		ELSEIF TEQUIP:애널비즈
			PRINTFORML 비즈의 끈을 잡아당겨 가볍게 출납해 주었다…
		;浣腸器＋プラグ
		ELSEIF TEQUIP:관장
			PRINTFORML 배설감에 떨리는 국혈을 플러그로 그리그리와 보전은 했다…
		;拡張バルーン
		ELSEIF TEQUIP:확장벌룬
			PRINTFORML 어널을 한계까지 넓히고 있는 벌룬을 전후에 움직여 주었다…
		;アナル電極
		ELSEIF TEQUIP:애널전극
			PRINTFORML 희미하게 소리를 내는 전극으로 그리그리와 국혈을 반죽해 돌렸다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 어널을 농
			;Ａ経験100以上＋욕망のPALAMLV3以上
			SIF PALAM:욕정 >= PALAMLV:3 && EXP:A경험 >= EXPLV:3
				PRINT (이)라고, 손가락끝으로 직장까지 휘저어
		PRINTL …
		ENDIF
	ELSE
		PRINTFORM %CALLNAME:TARGET%(와)과
		;助手が調教中
		SIF ASSIPLAY
			PRINTFORM %CALLNAME:ASSI%하
		PRINTL 서로의 성기를 서로 빨았다…
	ENDIF
ENDIF
;-------------------------------------------------
;2,岩清水
;-------------------------------------------------
@MESSAGE_SCOM2
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:TARGET%(을)를 얼굴 위에 걸치게 해 일부러 소리를 내면서 비순을 철올렸다…
ELSE
	PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:TARGET%(을)를 얼굴 위에 걸치게 해 일부러 소리를 내면서 비순을 철올렸다…
ENDIF
;-------------------------------------------------
;3,Gスポット刺激
;-------------------------------------------------
@MESSAGE_SCOM3
IF FLAG:70 == 1
	;前回がＧスポット刺激
	IF PREVCOM == 503
		PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:TARGET%의 질내의 흔함 붙은 천정을, 격렬하게 켜도록(듯이) 자극했다…
		IF EXP:V경험 > 80
			PRINTFORML %CALLNAME:TARGET%의 질내는 벌벌손가락에 관련되어 붙어, 애액을 불기 시작하고 있다…
		ENDIF
	;バイブ挿入中
	ELSEIF TEQUIP:바이브
		PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:TARGET%의 질내에서 요염하게 꾸불꾸불하는 바이브래이터를, 흔함 붙은 천정을 자극하도록(듯이) 움직였다
		;欲情のPALAMLV3以上＋Ｖ感覚LV3以上
		IF  PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
			PRINTFORML 밀에 젖은 질내는, 빼고 꽂기 되는 바이브래이터에 말이야 새와 휘감겨 온다…
		;欲情のPALAMLV3未満＋Ｖ感覚LV3以上
		ELSEIF PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
			PRINTFORML %CALLNAME:TARGET%의 질내는, 빼고 꽂기 되는 바이브래이터를 수락 쾌락에 떨고 있다…
		;欲情のPALAMLV3以上＋Ｖ感覚LV3未満
		ELSEIF PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
			PRINTFORML %CALLNAME:TARGET%의 질내는, 밀을 방울져 떨어지게 한 바이브래이터를 받아들이고 있다…
		;欲情のPALAMLV3未満＋Ｖ感覚LV3未満
		ELSEIF PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
			PRINTFORML 시간을 제지당했다%CALLNAME:TARGET%(은)는 저항도 하지 못하고 , 초심인 질내가 유린되고 있다…
		ENDIF
	ELSE
		PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:TARGET%의 질 중(안)에서 손가락을 굽혀 흔함 붙은 천정을 자극했다…
	ENDIF
ELSE
	;前回がＧスポット刺激
	IF PREVCOM == 503
		;Ｖ経験が120より多い
		IF EXP:V경험 > 120
			PRINTFORML %CALLNAME:TARGET%(은)는 군침을 흘려, 스스로 허리를 털어 쾌락을 탐내고 있다…
		;Ｖ経験が80より多い
		ELSEIF EXP:V경험 > 80
			PRINTFORML %CALLNAME:TARGET%(은)는 배안을 휘저어지는 감각에 뜨거운 한숨을 토해 번민이라고 있다…
		;Ｖ経験が50より多い
		ELSEIF EXP:V경험 > 50
			PRINTFORML %CALLNAME:TARGET%(은)는 손가락이 우글거리는 감촉에 넑을 잃고 있다…
		;Ｖ経験が30より多い
		ELSEIF EXP:V경험 > 30
			PRINTFORML %CALLNAME:TARGET%(은)는 손가락의 감촉을 즐기고 있는 것 같다…
		;Ｖ経験が0より多い
		ELSEIF EXP:V경험 > 0
			PRINTFORML %CALLNAME:TARGET%(은)는 손가락의 감촉에 한숨을 토해 참고 있다…
		ELSE
			PRINTFORML %CALLNAME:TARGET%(은)는 질내의 이물감에 무서워하고 있다…
		ENDIF
	;バイブ挿入中＋欲情のPALAMLV3以上＋Ｖ感覚LV3以上
	ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
		PRINTFORML %CALLNAME:TARGET%의 질은, 밀에 젖은 바이브래이터에 말이야 새와 휘감기고 있다…
	;バイブ挿入中＋欲情のPALAMLV3未満＋Ｖ感覚LV3以上
	ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
		PRINTFORML %CALLNAME:TARGET%(은)는 바이브래이터의 자극에 번민이라고 있다…
	;バイブ挿入中＋欲情のPALAMLV3以上＋Ｖ感覚LV3未満
	ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
		PRINTFORML %CALLNAME:TARGET%(은)는 뺨을 붉혀 때때로 소리를 높이고 있다…
	;バイブ挿入中＋欲情のPALAMLV3未満＋Ｖ感覚LV3未満
	ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
		PRINTFORML %CALLNAME:TARGET%(은)는 조금 괴로운 듯한 표정을 하고 있다…
	;バイブ挿入中
	ELSEIF TEQUIP:바이브
		PRINTFORML %CALLNAME:PLAYER%(은)는 교묘하게 바이브래이터를 조종해, 민감한 천정을 놀렸다…
	ELSE
		PRINTFORML %CALLNAME:PLAYER%하%CALLNAME:TARGET%의 질 중(안)에서 손가락을 굽혀 흔함 붙은 천정을 자극했다…
	ENDIF
ENDIF
;-------------------------------------------------
;4,乱れ牡丹
;-------------------------------------------------
@MESSAGE_SCOM4
;連続
IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
	PRINTFORML %CALLNAME:TARGET%의 결합부와 가슴에 다시 손을 기게 해 과시하도록(듯이) 허리를 움직였다…
;抜かず
ELSEIF TCVAR:100 == PLAYER
	PRINTFORM %CALLNAME:TARGET%의 신체를 배후로부터 삽입한 채로 안아 올리고 다리를 크게 벌어지게 했다…
ELSE
	PRINTFORM %CALLNAME:TARGET%의 신체를 안아 올려 천천히

	PRINTFORML %SLIT(, 1)%에 삽입하면(자), 다리를 크게 벌어지게 했다…
ENDIF
;露出癖
IF ABL:노출벽 >= 5
	PRINTFORML %CALLNAME:TARGET%(은)는 거울에 비치는 치태를 녹은 눈으로 응시하면서 환희가 소리를 높이고 있다…
ELSEIF ABL:노출벽 >= 4
	PRINTFORML %CALLNAME:TARGET%(은)는 수치와 쾌락에 빠진 것 같은 표정으로 거울안의 치태를 응시하고 있다…
ELSEIF ABL:노출벽 >= 2
	PRINTFORML %CALLNAME:TARGET%(은)는 얼굴을 수치에 물들이면서 때때로 음란한 소리를 높이고 있다…
ELSEIF ABL:노출벽 >= 1
	PRINTFORML %CALLNAME:TARGET%(은)는 손으로 얼굴을 덮어 가리면서 수치에 신체를 진동시키고 있다…
ENDIF
;-------------------------------------------------
;5,手淫フェラ
;-------------------------------------------------
@MESSAGE_SCOM5

IF TFLAG:194 == 0
	PRINTFORML %CALLNAME:TARGET%(은)는 첨단을 질천민 상태로%CALLNAME:PLAYER%의%조사처리(TINKO(PLAYER),"를")% 잡아,
	PRINTL 그대로 격렬하게 줄기의 부분을 훑어 냈다…
	SIF ABL:기교 > 3
		PRINTL 한층 더 빈 손으로 음낭을 상냥하게 비비어 풀고 있다…
ELSE
	PRINTFORML %CALLNAME:TARGET%하%CALLNAME:PLAYER%의%조사처리(TINKO(PLAYER),"를")% 집어 첨단을 입으로 질와
	PRINTL 줄기의 부분을 훑어 내면서 귀두를 혀로 간질였다…
	SIF ABL:기교 > 3
		PRINTL 한층 더 빈 손으로 음낭을 상냥하게 비비어 풀고 있다…
ENDIF
;-------------------------------------------------
;8,６９パイズリ
;-------------------------------------------------
@MESSAGE_SCOM8
PRINTFORM %CALLNAME:TARGET%하
;貧乳
IF TALENT:가슴사이즈 < 0
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%그리고%조사처리(TINKO(PLAYER),"를")%
;巨乳
ELSEIF TALENT:가슴사이즈 == 1
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%그리고%조사처리(TINKO(PLAYER),"를")% 감싸
;爆乳
ELSEIF TALENT:가슴사이즈 == 2
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%그리고%조사처리(TINKO(PLAYER),"를")% 숨기도록(듯이) 해
ELSE
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%그리고%조사처리(TINKO(PLAYER),"를")% 끼워
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 혀가 가져오는 쾌감에 번농 되면서도 열심히 켜고 있다…
IF ABL:TARGET:가슴 < 2
	PRINTFORM %CALLNAME:TARGET%의 봉사는 더듬거리고,%CALLNAME:TARGET%에는 나쁘겠지만
	SIF TALENT:가슴사이즈 >= 1
		PRINT 시각적으로는 차치하고
	PRINTL 별로 기분 좋지 않다…
ELSEIF ABL:TARGET:가슴 > 2
	PRINTFORML %CALLNAME:TARGET%(은)는 교묘한 봉사로%CALLNAME:PLAYER%(을)를 기쁨…
ENDIF
;-------------------------------------------------
;9,ダブルフェラ
;-------------------------------------------------
@MESSAGE_SCOM9
LOCAL = MASTER_POSE(SET_M,SET_P,1)
IF FLAG:70 == 1
	PRINTFORML %CALLNAME:PLAYER%(은)는 시간을 멈추었다%CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%의 입안에 대신하는 대신한다%조사처리(TINKO(PLAYER),"를")% 삽입해 범했다…
ELSE
	PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%하
	;奴隷と助手が恋慕か親愛
	SIF TALENT:연모 && TALENT:LOCAL:연모
		PRINTFORM 넑을 잃은 눈을 치켜 뜨고 봄으로%CALLNAME:PLAYER%(을)를 올려보면서,
	PRINTFORML %CALLNAME:PLAYER%의%TINKO(PLAYER)%에 둘이서 혀를 기게 하고 있다…
ENDIF
;-------------------------------------------------
;10,ダブル素股
;-------------------------------------------------
@MESSAGE_SCOM10
LOCAL = MASTER_POSE(SET_C,SET_P,1)
PRINTFORML %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%의 성기로%조사처리(TINKO(PLAYER),"를")% 사이에 두어, 두 명의 사이에 잡아당겼다…
;-------------------------------------------------
;11,ダブルパイズリ
;-------------------------------------------------
@MESSAGE_SCOM11
LOCAL = MASTER_POSE(SET_B,SET_P,1)
PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%하
;奴隷と助手が恋慕か親愛
SIF TALENT:연모 && TALENT:LOCAL:연모
	PRINTFORM 넑을 잃은 눈을 치켜 뜨고 봄으로%CALLNAME:PLAYER%(을)를 올려보면서,
PRINT 서로의
;奴隷と助手が巨乳か爆乳
IF TALENT:가슴사이즈 > 0 && TALENT:LOCAL:가슴사이즈 > 0
	PRINT 풍만한
;奴隷と助手が貧乳か絶壁
ELSEIF TALENT:가슴사이즈 < 0 && TALENT:LOCAL:가슴사이즈 < 0
	PRINT 얇다
ENDIF
PRINTFORML 가슴을 거듭해 맞추어%CALLNAME:PLAYER%의%조사처리(TINKO(PLAYER),"를")% 잡아당기고 있다…

;-------------------------------------------------
;12,パイズリフェラ
;-------------------------------------------------
@MESSAGE_SCOM12
IF FLAG:70 == 1
	PRINTFORM %마스터는% 시간을 멈추었다%CALLNAME:TARGET%의 입에 귀두를 함먹이키,
	;巨乳
	IF TALENT:가슴사이즈 == 1
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%그리고%조사처리(TINKO(PLAYER),"를")% 사이에 두어 허리를 움직이기 시작했다…
	;爆乳
	ELSEIF TALENT:가슴사이즈 == 2
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%그리고%조사처리(TINKO(PLAYER),"를")% 사이에 두어 허리를 움직이기 시작했다…
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%그리고%조사처리(TINKO(PLAYER),"를")% 사이에 두어 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%하
	;恋慕か親愛
	SIF TALENT:연모
		PRINT 넑을 잃은 표정을 하면서,
	;貧乳
	IF TALENT:가슴사이즈 == -1
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%뿐만이 아니게 혀도 사용해 열심히%TINKO(PLAYER)%전체를 자극하고 있었다…
	;巨乳
	ELSEIF TALENT:가슴사이즈 == 1
		PRINTFORML %조사처리(TINKO(PLAYER),"를")%%OPPAI_DESCRIPTION(TARGET,1)%그리고 감싸, 남은 귀두를 빨아 자극하고 있다…
	;爆乳
	ELSEIF TALENT:가슴사이즈 == 2
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%그리고%조사처리(TINKO(PLAYER),"를")% 숨겨 버려, 때때로 혀를 넣어, 열심히%조사처리(TINKO(PLAYER),"를")% 빨고 있다…
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%그리고%조사처리(TINKO(PLAYER),"를")% 끼워, 남은 부분을 혀로, 자극하고 있다…
	ENDIF
	IF ABL:TARGET:가슴 < 2
		PRINTFORM %CALLNAME:TARGET%의 봉사는 더듬거리고,%CALLNAME:TARGET%에는 나쁘겠지만
		SIF TALENT:가슴사이즈 >= 1
			PRINT 시각적으로는 차치하고
		PRINTL 별로 기분 좋지 않다…
	ELSEIF ABL:TARGET:가슴 > 2
		PRINTFORML %CALLNAME:TARGET%(은)는 교묘한 봉사로%CALLNAME:PLAYER%(을)를 기쁨…
	ENDIF
ENDIF
;-------------------------------------------------
;13,交互挿入
;-------------------------------------------------
@MESSAGE_SCOM13
LOCAL = MASTER_POSE(SET_V,SET_P,1)
IF FLAG:70 == 1
	;現在2人以上の処女を同ターンで奪うことは出来ないため、処女関連はここ一つにまとめる
	IF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:LOCAL%의 질내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아 그 남자를 모르는 \@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@을 함께 관철했다…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT (을)를 서로 마주 보게 한에 거듭해
		;正常位＋正常位
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT (을)를 위로 향해 늘어놓아
		ELSE
			PRINT (을)를 납죽 엎드림으로 해,
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 질내의 감촉의 차이를 맛보면서
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 질내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아
			IF RAND:2 == 0
				PRINT 그
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 흠뻑 젖어있는
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@도
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 거듭할 수 있었다
				ELSE
					PRINT 줄섰다
				ENDIF
				PRINT 2개(살)의
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 유
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@(을)를
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(을)를 서로 마주 보게 한에 거듭해 그\@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@(을)를
				ELSE
					PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 균열 \@(을)를 밀착시키면(자),
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 균열 \@(을)를 늘어놓아
			ENDIF
		ENDIF
		PRINTL 교대로 관철했다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE
	;現在2人以上の処女を同ターンで奪うことは出来ないため、処女関連はここ一つにまとめる
	IF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:LOCAL%의 질내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아 그 남자를 모른다\@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@도 함께 관철했다…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 의 몸을 밀착시켜,
		;正常位＋正常位
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT (을)를 위로 향해 늘어놓아
		ELSE
			PRINT 의 엉덩이를 이쪽으로 향하게 해
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 질내의 감촉의 차이를 맛보면서
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 질내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아
			IF RAND:2 == 0
				PRINT 그
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 유
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@도
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 거듭할 수 있었다
				ELSE
					PRINT 줄섰다
				ENDIF
				PRINT 2개(살)의
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 유
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@(을)를
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(을)를 얼싸안게 해, 그\@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 균열 \@(을)를
				ELSE
					PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 균열 \@(을)를 밀착시키면(자),
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 균열 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 균열 \@(을)를 늘어놓아
			ENDIF
		ENDIF
		PRINTL 교대로 관철했다…
	ENDIF

	IF TALENT:연모 && TALENT:LOCAL:연모
		WAIT
		PRINTFORML %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(은)는 교대로 삽입되면서,%CALLNAME:MASTER%의%조사처리(TINKO(PLAYER),"를")% 졸라 왔다…
	ELSEIF EXP:레즈경험 >= 1000 && EXP:LOCAL:레즈경험 >= 1000
		WAIT
		PRINTFORML %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(은)는 서로의 혀를 서로 걸면서,%CALLNAME:MASTER%의%조사처리(TINKO(PLAYER),"를")% 받아들이고 있다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF


;-------------------------------------------------
;16,アナル交互挿入
;-------------------------------------------------
@MESSAGE_SCOM16
LOCAL = MASTER_POSE(SET_A,SET_P,1)
IF FLAG:70 == 1	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT (을)를 서로 마주 보게 한에 거듭해
		;正常位＋正常位
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT (을)를 위로 향해 늘어놓아
		ELSE
			PRINT (을)를 납죽 엎드림으로 해,
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 장내의 감촉의 차이를 맛보면서
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 장내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아
			IF RAND:2 == 0
				PRINT 그
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 눌 붙었다
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@도
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 세로에 줄섰다
				ELSE
					PRINT 옆에 줄섰다
				ENDIF
				PRINT 2개(살)의
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 눌 붙었다
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@(을)를
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(을)를 서로 마주 보게 한에 거듭해 그\@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@(을)를
				ELSE
					PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 비렬 \@(을)를 세로에 늘어놓게 하면(자),
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 비렬 \@(을)를 옆에 늘어놓아
			ENDIF
		ENDIF
		PRINTL 교대로 관철했다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%
		;正常位＋後背位
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 의 몸을 밀착시켜,
		;正常位＋正常位
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT (을)를 위로 향해 늘어놓아
		ELSE
			PRINT 의 엉덩이를 이쪽으로 향하게 해
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 장내의 감촉의 차이를 맛보면서
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 장내에 삽입한 채로%CALLNAME:TARGET%(을)를 껴안아
			IF RAND:2 == 0
				PRINT 그
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 흠뻑 젖은
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@도
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 거듭할 수 있었다
				ELSE
					PRINT 줄섰다
				ENDIF
				PRINT 2개(살)의
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 눌 붙었다
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@(을)를
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(을)를 얼싸안게 해, 그\@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 어린 균열 # 비렬 \@(을)를
				ELSE
					PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 비렬 \@(을)를 세로에 늘어놓게 하면(자),
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의\@ TALENT:체형 < 0 ? 어린 균열 # 비렬 \@(와)과%CALLNAME:LOCAL%의\@ TALENT:LOCAL:체형 < 0 ? 어린 균열 # 비렬 \@(을)를 옆에 늘어놓아
			ENDIF
		ENDIF
		PRINTL 교대로 관철했다…
	ENDIF

	IF TALENT:연모 && TALENT:LOCAL:연모
		WAIT
		PRINTFORML %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(은)는 교대로 삽입되면서,%CALLNAME:MASTER%의%조사처리(TINKO(PLAYER),"를")% 졸라 왔다…
	ELSEIF EXP:레즈경험 >= 1000 && EXP:LOCAL:레즈경험 >= 1000
		WAIT
		PRINTFORML %CALLNAME:TARGET%(와)과%CALLNAME:LOCAL%(은)는 서로의 혀를 서로 걸면서,%CALLNAME:MASTER%의%조사처리(TINKO(PLAYER),"를")% 받아들이고 있다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF

;-------------------------------------------------
;17,乳首同時吸い
;-------------------------------------------------
@MESSAGE_SCOM17
SIF FLAG:70 == 1
	PRINTFORM 시간이 멈추었다
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	IF FLAG:70 == 1
		PRINTFORM %CALLNAME:TARGET%(을)를 범한 채로
	ELSE
		PRINTFORM %CALLNAME:TARGET%(와)과 연결된 채로
	ENDIF
ELSE
	PRINTFORM %CALLNAME:TARGET%의
ENDIF
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%의 위로부터
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:모유체질 && RAND:3 == 0
	IF RAND:3
		PRINT 자유를 배이게 한다
	ELSEIF RAND:2
		PRINT 모유를 배이게 한다
	ELSE
		PRINT 밀크를 배이게 한다
	ENDIF
ENDIF
IF TALENT:TARGET:가슴사이즈 >= 1
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%(을)를 대어
	PRINTFORM 2개(살)의%NIPPLE_DESCRIPTION(TARGET,1)%
	SELECTCASE RAND:4
	CASE 0
		PRINTL (을)를 동시에 입에 넣어 감 씹어 하면서 희롱하면서 흡인했다……
	CASE 1
		PRINTL 에 동시에 빨아 붙어, 싫은 것 같은 소리를 내면서 들이마셨다……
	CASE 2
		PRINTL (을)를 동시에 입에 넣으면서, 유윤의 주위를 초조하게 하도록(듯이) 빨고 들이마셨다……
	CASEELSE
		PRINTL (을)를 동시에 입에 넣어, 입안에서 조정이라고 자극했다……
	ENDSELECT
ELSE
	PRINTFORM %OPPAI_DESCRIPTION(TARGET)%의
	IF RAND:3 == 0
		PRINTL 앞의,
	ELSEIF RAND:2
		PRINTL 정상에 있는,
	ELSE
		PRINTL 
	ENDIF
	PRINTFORM %NIPPLE_DESCRIPTION(TARGET,1)%
	SELECTCASE RAND:3
	CASE 0
		PRINTL (을)를 교대로 입에 넣어 감 씹어 하면서 희롱하면서 흡인했다……
	CASE 1
		PRINTL 에 교대로 빨아 붙어, 싫은 것 같은 소리를 내면서 들이마셨다……
	CASEELSE
		PRINTL (을)를 교대로 입에 넣으면서, 유윤의 주위를 초조하게 하도록(듯이) 빨고 들이마셨다……
	ENDSELECT
ENDIF
IF ABL:TARGET:친밀 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:B감각 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%(은)는 쓴웃음하면서도,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%(은)는 쾌감을 견디면서,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 상냥하게 껴안았다
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%(은)는 쾌감에 허덕이면서,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 강하게 껴안았다
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%(은)는 쾌감에 참지 못할 모습으로%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%에 해가 보고 붙어,
		PRINTFORML 한층 더 애무와 보다 강한 유두에의 자극을 요구해 왔다
	ENDSELECT
ENDIF

;-------------------------------------------------
;18,後背位スパンキング
;-------------------------------------------------
@MESSAGE_SCOM18
PRINTFORM %CALLNAME:PLAYER%하%CALLNAME:TARGET%에 허리를 부딪히면서,
SIF TEQUIP:0 & 1 && (EQUIP:상반신윗도리2 || EQUIP:하반신윗도리 || EQUIP:치마 || EQUIP:상반신윗도리1)
	PRINTFORM %SHOW_BOTTOM(TARGET,1)%(을)를 넘겨
SIF EQUIP:TARGET:레오타드 || EQUIP:TARGET:보디슈트
	PRINTFORM %SHOW_下半身下着(TARGET,2)%에 싸였다
PRINTFORML %HIP(TARGET)%(을)를 화려한 소리를 울려 두드렸다

;-------------------------------------------------
;18,授乳プレイ
;-------------------------------------------------
@MESSAGE_SCOM19
PRINTFORM %CALLNAME:TARGET%하%CALLNAME:MASTER%(을)를 가슴에 껴안아
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)%의 위로부터
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:모유체질 && RAND:3 == 0
	IF RAND:3
		PRINT 자유를 배이게 한다
	ELSEIF RAND:2
		PRINT 모유를 배이게 한다
	ELSE
		PRINT 밀크를 배이게 한다
	ENDIF
ENDIF
SIF TALENT:TARGET:가슴사이즈 >= 1
	PRINTFORM %OPPAI_DESCRIPTION(TARGET)%(을)를 들어 올려,
PRINTFORML %NIPPLE_DESCRIPTION(TARGET,1)%(을)를%CALLNAME:MASTER%의 입에 넣게 한 채로%조사처리(TINKO(PLAYER),"를")% 잡아당기기 시작했다
PRINTFORM %CALLNAME:MASTER%하%CALLNAME:TARGET%에 몸을 맡겨
SELECTCASE RAND:4
CASE 0
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%(을)를 감 씹어 하면서 혀로 희롱하면서 흡인했다……
CASE 1
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%빨아 붙어, 싫은 것 같은 소리를 내면서 들이마셨다……
CASE 2
	PRINTFORML 유윤의 주위를 초조하게 하도록(듯이) 빨고 나서,%NIPPLE_DESCRIPTION(TARGET)%(을)를 들이마셨다……
CASEELSE
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%(을)를 감언이설로 굴리고 나서, 갓난아이와 같이 들이마셨다……
ENDSELECT
IF ABL:TARGET:친밀 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:B감각 
	CASE 0,1
		PRINTFORM %CALLNAME:TARGET%(은)는 쓴웃음하면서도,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다
	CASE 2 TO 5
		PRINTFORM %CALLNAME:TARGET%(은)는 쾌감을 견디면서,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 상냥하게 껴안았다
	CASE 6 TO 9
		PRINTFORM %CALLNAME:TARGET%(은)는 쾌감에 허덕이면서,%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 강하게 껴안았다
	CASEELSE
		PRINTFORML %CALLNAME:TARGET%(은)는 쾌감에 참지 못할 모습으로%OPPAI_DESCRIPTION(TARGET)%에 달라붙는다%CALLNAME:MASTER%의 머리를 긁어 안아,
		PRINTFORML 보다 강한 유두에의 자극을 요구해 왔다
	ENDSELECT
ENDIF
