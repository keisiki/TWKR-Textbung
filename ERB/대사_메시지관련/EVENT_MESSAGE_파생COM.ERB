;-------------------------------------------------
;1,식스나인
;-------------------------------------------------
@MESSAGE_SCOM1
IF FLAG:70 == 1
	IF SELECTCOM == 3
		PRINTFORM 시간을 멈추고 %CALLNAME:TARGET%의 입에 %조사처리(TINKO(PLAYER),"를")% 밀어 넣은 채, %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@
		IF TEQUIP:바이브
			PRINTFORML 의 안에서 요염하게 구불거리는 바이브를 앞뒤로 움직였다…
		ELSE
			PRINTFORML 을 핥아서 풀어주었다…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM 시간을 멈추고 %CALLNAME:TARGET%의 입에 %조사처리(TINKO(PLAYER),"를")% 밀어 넣은 채, 
		;애널바이브
		IF TEQUIP:애널바이브
			PRINTFORML 조금씩 떨리는 애널바이브를 꼽고 앞뒤로 움직였다…
		;애널비즈
		ELSEIF TEQUIP:애널비즈
			PRINTFORML 비즈 끈을 잡아당겨 가볍게 출납해주었다…
		;관장器＋プラグ
		ELSEIF TEQUIP:관장
			PRINTFORML 배설감에 떨고 있는 국혈을 플러그로 문질문질 후벼 주었다…
		;확장벌룬
		ELSEIF TEQUIP:확장벌룬
			PRINTFORML 애널을 한계까지 넓히고 있는 벌룬을 앞뒤로 움직여 주었다…
		;애널전극
		ELSEIF TEQUIP:애널전극
			PRINTFORML 희미하게 소리를 내고 있는 전극으로 문질문질 국혈을 휘저어 주었다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 애널을 만지작거
			;A경험100以上＋욕망のPALAMLV3以上
			SIF PALAM:욕정 >= PALAMLV:3 && EXP:A경험 >= EXPLV:3
				PRINT 리며, 손끝으로 직장까지 휘적거
		PRINTL 렸다…
		ENDIF
	ENDIF
ELSE
	IF SELECTCOM == 3
		PRINTFORM %타겟과% 서로의 성기를 핥아 주면서 
		IF TEQUIP:바이브
			PRINTFORML \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@의 안에서 요염하게 구불거리는 바이브를 앞뒤로 움직였다…
		ELSE
			PRINTFORML 질구에 손가락을 삽입했다…
		ENDIF
	ELSEIF SELECTCOM == 5
		PRINTFORM %타겟과% 서로의 성기를 핥아 주면서  
		;애널바이브
		IF TEQUIP:애널바이브
			PRINTFORML 조금씩 떨리는 애널바이브를 꼽고 앞뒤로 움직였다…
		;애널비즈
		ELSEIF TEQUIP:애널비즈
			PRINTFORML 비즈 끈을 잡아당겨 가볍게 출납해주었다…
		;관장器＋プラグ
		ELSEIF TEQUIP:관장
			PRINTFORML 배설감에 떨고 있는 국혈을 플러그로 문질문질 후벼 주었다…
		;확장벌룬
		ELSEIF TEQUIP:확장벌룬
			PRINTFORML 애널을 한계까지 넓히고 있는 벌룬을 앞뒤로 움직여 주었다…
		;애널전극
		ELSEIF TEQUIP:애널전극
			PRINTFORML 희미하게 소리를 내고 있는 전극으로 문질문질 국혈을 휘저어 주었다…
		ELSE
			PRINTFORM %CALLNAME:TARGET%의 애널을 만지작거
			;A경험100以上＋욕망のPALAMLV3以上
			SIF PALAM:욕정 >= PALAMLV:3 && EXP:A경험 >= EXPLV:3
				PRINT 리며, 손끝으로 직장까지 휘적거
		PRINTL 렸다…
		ENDIF
	ELSE
		PRINTFORM %타겟과% 
		;助手が調教中
		SIF ASSIPLAY
			PRINTFORM %조수는% 
		PRINTL  서로의 성기를 핥았다…
	ENDIF
ENDIF
;-------------------------------------------------
;2,이와시미즈
;-------------------------------------------------
@MESSAGE_SCOM2
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈추고 %타겟을% 얼굴 위로 걸치게 해, 일부러 소리를 내면서 비순을 훌쩍였다…
ELSE
	PRINTFORML %조교자는% %타겟을% 얼굴 위에 걸치게 해, 일부러 소리를 내면서 비순을 훌쩍였다…
ENDIF
;-------------------------------------------------
;3,Gスポット刺激
;-------------------------------------------------
@MESSAGE_SCOM3
IF FLAG:70 == 1
	;前回がG스팟자극
	IF PREVCOM == 503
		PRINTFORML %조교자는% %CALLNAME:TARGET%의 질내의 거칠거칠한 천장을, 격렬하게 문지르듯 자극했다…
		IF EXP:V경험 > 80
			PRINTFORML %CALLNAME:TARGET%의 질내는 움찔움찔 손가락을 휘감고, 애액을 뿜어내고 있다…
		ENDIF
	;바이브挿入中
	ELSEIF TEQUIP:바이브
		PRINTFORML %조교자는% %CALLNAME:TARGET%의 질내에서 요염하세 구불거리는 바이브를, 거칠거칠한 천장을 자극하도록 움직였다
		;욕정のPALAMLV3以上＋V감각LV3以上
		IF  PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
			PRINTFORML 꿀에 젖은 질내는, 전후로 움직이는 바이브에 끈적끈적 달라붙었다…
		;욕정のPALAMLV3未満＋V감각LV3以上
		ELSEIF PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
			PRINTFORML %CALLNAME:TARGET%의 질내는, 전후로 움직이는 바이브를 받아들이고 쾌락에 떨고 있다…
		;욕정のPALAMLV3以上＋V감각LV3未満
		ELSEIF PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
			PRINTFORML %CALLNAME:TARGET%의 질내는, 꿀을 뚝뚝 떨어뜨리며 바이브를 받아들이고 있다…
		;욕정のPALAMLV3未満＋V감각LV3未満
		ELSEIF PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
			PRINTFORML 시간을 멈춰진 %타겟은% 저항조차 하지 못하고, 미숙한 질내를 유린당하고 있다…
		ENDIF
	ELSE
		PRINTFORML %조교자는% %CALLNAME:TARGET%의 질 안에서 손가락을 구부려, 거칠거칠한 천장을 자극했다…
	ENDIF
ELSE
	;前回がG스팟자극
	IF PREVCOM == 503
		;V경험が120より多い
		IF EXP:V경험 > 120
			PRINTFORML %타겟은% 침을 흘리며, 스스로 허리를 흔들어 쾌락을 탐하고 있다…
		;V경험が80より多い
		ELSEIF EXP:V경험 > 80
			PRINTFORML %타겟은%는 뱃속을 휘젓는 감각에 뜨거운 숨을 내쉬며 몸부림치고 있다…
		;V경험が50より多い
		ELSEIF EXP:V경험 > 50
			PRINTFORML %타겟은% 손가락의 꿈틀거리는 감촉에 도취되어 있다…
		;V경험が30より多い
		ELSEIF EXP:V경험 > 30
			PRINTFORML %타겟은% 손가락의 감촉을 즐기는 것 같다…
		;V경험が0より多い
		ELSEIF EXP:V경험 > 0
			PRINTFORML %타겟은% 손가락의 감촉에 한숨을 내쉬며 견디고 있다…
		ELSE
			PRINTFORML %타겟은% 질내의 이물감에 무서워하고 있다…
		ENDIF
	;바이브挿入中＋욕정のPALAMLV3以上＋V감각LV3以上
	ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 >= 3
		PRINTFORML %CALLNAME:TARGET%의 질은, 꿀에 젖은 바이브에 끈적끈적 달라붙었다…
	;바이브挿入中＋욕정のPALAMLV3未満＋V감각LV3以上
	ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 >= 3
		PRINTFORML %타겟은% 바이브의 자극에 몸부림치고 있다…
	;바이브挿入中＋욕정のPALAMLV3以上＋V감각LV3未満
	ELSEIF TEQUIP:바이브 && PALAM:욕정 >= PALAMLV:3 && ABL:V감각 < 3
		PRINTFORML %타겟은% 뺨을 붉히며 이따금 달콤한 소리를 내고 있다…
	;바이브挿入中＋욕정のPALAMLV3未満＋V감각LV3未満
	ELSEIF TEQUIP:바이브 && PALAM:욕정 < PALAMLV:3 && ABL:V감각 < 3
		PRINTFORML %타겟은% 조금 괴로운 표정을 짓고 있다…
	;바이브挿入中
	ELSEIF TEQUIP:바이브
		PRINTFORML %조교자는% 능숙하게 바이브를 조종해, 민감한 천장을 희롱했다…
	ELSE
		PRINTFORML %조교자는% %CALLNAME:TARGET%의 질 안에서 손가락을 구부려, 거칠거칠한 천장을 자극했다…
	ENDIF
ENDIF
;-------------------------------------------------
;4,미다레보탄
;-------------------------------------------------
@MESSAGE_SCOM4
;連続
IF SELECTCOM == PREVCOM && ASSIPLAY == TFLAG:150
	PRINTFORML %CALLNAME:TARGET%의 결합부와 가슴에 다시 손을 얹고, 과시하듯 허리를 움직였다…
;抜かず
ELSEIF TCVAR:100 == PLAYER
	PRINTFORM %CALLNAME:TARGET%의 몸을 등 뒤에서 삽입한 채 안아올려 다리를 크게 벌리게 했다…
ELSE
	PRINTFORML %CALLNAME:TARGET%의 몸을 안아올려, 천천히 %SLIT(, 1)%에 삽입하고, 다리를 크게 벌리게 했다…
ENDIF
;노출벽
IF ABL:노출벽 >= 5
	PRINTFORML %타겟은% 거울에 비치는 치태를 황홀한 눈으로 바라보며 환희의 목소리를 내고 있다…
ELSEIF ABL:노출벽 >= 4
	PRINTFORML %타겟은% 수치와 쾌락에 젖은 듯한 표정으로 거울 속의 치태를 응시한다…
ELSEIF ABL:노출벽 >= 2
	PRINTFORML %타겟은% 얼굴을 수치로 물들이면서 이따금 요염한 목소리를 낸다…
ELSEIF ABL:노출벽 >= 1
	PRINTFORML %타겟은% 손으로 얼굴을 가리며 수치에 몸을 떨고 있다…
ENDIF
;-------------------------------------------------
;5,수음 펠라
;-------------------------------------------------
@MESSAGE_SCOM5

IF TFLAG:194 == 0
	PRINTFORML %타겟은% 끝부분을 입에 문 상태로 %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"를")% 잡고,
	PRINTL 그대로 격렬하게 줄기 부분을 훑어 냈다…
	SIF ABL:기교 > 3
		PRINTL 거기에 한층 더, 빈 손으로는 음낭을 부드럽게 주물러 주고 있다…
ELSE
	PRINTFORML %타겟은% %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"를")% 손에 쥐고 끝부분을 입에 물고
	PRINTL 줄기 부분을 쓸어주며 귀두를 혀로 간질였다…
	SIF ABL:기교 > 3
		PRINTL 거기에 한층 더, 빈 손으로는 음낭을 부드럽게 주물러 주고 있다…
ENDIF
;-------------------------------------------------
;8,69파이즈리
;-------------------------------------------------
@MESSAGE_SCOM8
PRINTFORM %타겟은% %OPPAI_DESCRIPTION(TARGET)%로 %조사처리(TINKO(PLAYER),"를")% 
;貧乳
IF TALENT:가슴사이즈 < 0
	PRINTFORML 감싸려 노력하며
;巨乳
ELSEIF TALENT:가슴사이즈 == 1
	PRINTFORML 감싸고
;爆乳
ELSEIF TALENT:가슴사이즈 == 2
	PRINTFORML 숨기려는듯 감싸고
ELSE
	PRINTFORML 끼우고
ENDIF
PRINTFORML %CALLNAME:PLAYER%의 혀가 가져오는 쾌감에 농락당하면서도 열심히 문지르고 있다…
IF ABL:TARGET:가슴 < 2
	PRINTFORM %CALLNAME:TARGET%의 봉사는 더듬더듬 서툴렀고, %CALLNAME:TARGET%에게는 미안하지만 
	SIF TALENT:가슴사이즈 >= 1
		PRINT 시각적으로는 어쨌든 
	PRINTL 그다지 기분 좋지 않았다…
ELSEIF ABL:TARGET:가슴 > 2
	PRINTFORML %타겟은% 교묘한 봉사로 %조교자를% 기쁘게 했다…
ENDIF
;-------------------------------------------------
;9,더블 펠라
;-------------------------------------------------
@MESSAGE_SCOM9
LOCAL = MASTER_POSE(SET_M,SET_P,1)
IF FLAG:70 == 1
	PRINTFORML %조교자는% 시간을 멈추고 %타겟과% %CALLNAME:LOCAL%의 입안을 번갈아 가며 %조사처리(TINKO(PLAYER),"를")% 삽입해 범했다…
ELSE
	PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 
	;奴隷と助手が연모か親愛
	SIF TALENT:연모 && TALENT:LOCAL:연모
		PRINTFORM 황홀한 눈빛으로 %조교자를% 올려다보며, 
	PRINTFORML %CALLNAME:PLAYER%의 %TINKO(PLAYER)%에 둘이서 혀를 쓰고 있다…
ENDIF
;-------------------------------------------------
;10,더블 스마타
;-------------------------------------------------
@MESSAGE_SCOM10
LOCAL = MASTER_POSE(SET_C,SET_P,1)
PRINTFORML %타겟과% %CALLNAME:LOCAL%의 성기로 %조사처리(TINKO(PLAYER),"를")% 끼우고, 두 사람 사이에서 훑어 주었다…
;-------------------------------------------------
;11,더블 파이즈리
;-------------------------------------------------
@MESSAGE_SCOM11
LOCAL = MASTER_POSE(SET_B,SET_P,1)
PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 
;奴隷と助手が연모か親愛
SIF TALENT:연모 && TALENT:LOCAL:연모
	PRINTFORM 황홀한 눈빛으로 %조교자를% 올려다보며, 
PRINT 서로의 
;奴隷と助手が巨乳か爆乳
IF TALENT:가슴사이즈 > 0 && TALENT:LOCAL:가슴사이즈 > 0
	PRINT 풍만한 
;奴隷と助手が貧乳か絶壁
ELSEIF TALENT:가슴사이즈 < 0 && TALENT:LOCAL:가슴사이즈 < 0
	PRINT 궁상맞은 
ENDIF
PRINTFORML 가슴을 포개고 %CALLNAME:PLAYER%의 %조사처리(TINKO(PLAYER),"를")% 훑어주고 있다…

;-------------------------------------------------
;12,파이즈리 펠라
;-------------------------------------------------
@MESSAGE_SCOM12
IF FLAG:70 == 1
	PRINTFORM %마스터는% 시간을 멈추고 %CALLNAME:TARGET%의 입에 귀두를 물게 해, 
	;巨乳
	IF TALENT:가슴사이즈 == 1
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%에 %조사처리(TINKO(PLAYER),"를")% 끼우고 허리를 움직이기 시작했다…
	;爆乳
	ELSEIF TALENT:가슴사이즈 == 2
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%에 %조사처리(TINKO(PLAYER),"를")% 끼우고 허리를 움직이기 시작했다…
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%에 %조사처리(TINKO(PLAYER),"를")% 끼우고 허리를 움직이기 시작했다…
	ENDIF
ELSE
	PRINTFORM %타겟은% 
	;연모か親愛
	SIF TALENT:연모
		PRINT 황홀한 표정을 지으며, 
	;貧乳
	IF TALENT:가슴사이즈 == -1
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%뿐만 아니라 혀도 사용해 열심히 %TINKO(PLAYER)% 전체를 자극하고 있다…
	;巨乳
	ELSEIF TALENT:가슴사이즈 == 1
		PRINTFORML %조사처리(TINKO(PLAYER),"를")% %조사처리(OPPAI_DESCRIPTION(TARGET,1),"로")% 감싸고, 남은 귀두를 혀로 핥으며 자극하고 있다…
	;爆乳
	ELSEIF TALENT:가슴사이즈 == 2
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%에 %조사처리(TINKO(PLAYER),"를")% 유육 속에 감추고, 가끔 혀를 넣어, 열심히 %조사처리(TINKO(PLAYER),"를")% 훑어 올리고 있다…
	ELSE
		PRINTFORML %OPPAI_DESCRIPTION(TARGET,1)%에 %조사처리(TINKO(PLAYER),"를")% 끼우고, 남은 부분을 혀로, 자극하고 있다…
	ENDIF
	IF ABL:TARGET:가슴 < 2
		PRINTFORM %CALLNAME:TARGET%의 봉사는 더듬더듬 서툴렀고, %CALLNAME:TARGET%에게는 미안하지만 
		SIF TALENT:가슴사이즈 >= 1
			PRINT 시각적으로는 어쨌든 
		PRINTL 그다지 기분 좋지 않았다…
	ELSEIF ABL:TARGET:가슴 > 2
		PRINTFORML %타겟은% 교묘한 봉사로 %조교자를% 기쁘게 해다…
	ENDIF
ENDIF
;-------------------------------------------------
;13,교대로 삽입
;-------------------------------------------------
@MESSAGE_SCOM13
LOCAL = MASTER_POSE(SET_V,SET_P,1)
IF FLAG:70 == 1
	;現在2人以上の처녀を同ターンで奪うことは出来ないため、처녀関連はここ一つにまとめる
	IF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:LOCAL%의 질내에 삽입한 채로 %타겟을% 끌어안고서, 그 남자를 모르는 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@도 함께 꿰뚫었다…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")%
		;정상위＋후배위
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 마주보게 한 다음 겹쳐 쌓고, 
		;정상위＋정상위
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT 위를 보게 해 나란히 놓고, 
		ELSE
			PRINT 네발로 엎드리게 하고, 
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 질 안 감촉의 차이를 맛보면서
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 질내에 삽입한 채 %타겟을% 끌어안고서, 
			IF RAND:2 == 0
				PRINTFORM 그 \@ PALAM:윤활 >= PALAMLV:4? 흠뻑 젖은 # \@
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@도 
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 겹쳐진 
				ELSE
					PRINT 늘어선 
				ENDIF
				PRINT 두 개의 
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 흠뻑 젖은 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@을 
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")% 마주보게 하고, 그 \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@을 
				ELSE
					PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 균열 # 비열 \@을 밀착시키고, 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 균열 # 비열 \@을 늘어놓고, 
			ENDIF
		ENDIF
		PRINTL 교대로 꿰뚫었다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE
	;現在2人以上の처녀を同ターンで奪うことは出来ないため、처녀関連はここ一つにまとめる
	IF TALENT:처녀 == 1
		PRINTFORML %CALLNAME:LOCAL%의 질내에 삽입한 채로 %타겟을% 끌어안고, 그 남자를 모르는 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@도 함께 꿰뚫었다…
	;連続
	ELSEIF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %타겟과% %CALLNAME:LOCAL%
		;정상위＋후배위
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 의 몸을 밀착시키고, 
		;정상위＋정상위
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			;TWKR 조사처리용 추가 열
			PRINTFORM %조사만처리(CALLNAME:LOCAL,"를")% 
			PRINT 위를 보게 해 나란히 놓고, 
		ELSE
			PRINT 의 엉덩이를 이쪽을 향하게 하고, 
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 명의 질 안 감촉의 차이를 맛보면서 
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 질내에 삽입한 채 %타겟을% 끌어안고서, 
			IF RAND:2 == 0
				PRINT 그 
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 흠뻑 젖은 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@도 
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 겹쳐진 
				ELSE
					PRINT 늘어선 
				ENDIF
				PRINT 두 개의 
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 흠뻑 젖은 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@을 
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")% 서로 껴안게 해, 그 \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 균열 # 비열 \@을 
				ELSE
					PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 균열 # 비열 \@을 밀착시키고, 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 균열 # 비열 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 균열 # 비열 \@을 늘어놓고, 
			ENDIF
		ENDIF
		PRINTL 교대로 꿰뚫었다…
	ENDIF

	IF TALENT:연모 && TALENT:LOCAL:연모
		WAIT
		PRINTFORML %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 번갈아가며 삽입되면서, %CALLNAME:MASTER%의 %조사처리(TINKO(PLAYER),"를")% 서로 조른다…
	ELSEIF EXP:레즈경험 >= 1000 && EXP:LOCAL:레즈경험 >= 1000
		WAIT
		PRINTFORML %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 서로의 혀를 휘감으며, %CALLNAME:MASTER%의 %조사처리(TINKO(PLAYER),"를")% 받아들이고 있다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF


;-------------------------------------------------
;16,애널교대로 삽입
;-------------------------------------------------
@MESSAGE_SCOM16
LOCAL = MASTER_POSE(SET_A,SET_P,1)
IF FLAG:70 == 1	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")%
		;정상위＋후배위
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 마주보게 겹치고, 
		;정상위＋정상위
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			PRINT 위를 보게 해 나란히 늘어놓고, 
		ELSE
			PRINT 네발로 엎드리게 하고, 
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 사람의 장 안 감촉의 차이를 맛보면서 
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 장내에 삽입한 채 %타겟을% 끌어안고서, 
			IF RAND:2 == 0
				PRINT 그 
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 미끈미끈한 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@도 
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 위아래로 늘어선 
				ELSE
					PRINT 옆으로 늘어선 
				ENDIF
				PRINT 두 개의 
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 미끈미끈한 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@을 
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")% 마주보게 하고, 그 \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@을 
				ELSE
					PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 구멍 # 항문 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 구멍 # 항문 \@을 위아래로 늘어놓고, 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 구멍 # 항문 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 구멍 # 항문 \@을 옆으로 늘어놓고, 
			ENDIF
		ENDIF
		PRINTL 교대로 꿰뚫었다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ELSE	;連続
	IF TFLAG:50 == TFLAG:151 && TARGET == TFLAG:152
		PRINTFORM %타겟과% %CALLNAME:LOCAL%
		;정상위＋후배위
		IF ((TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 2) || (TEQUIP:체위 == 2 && TEQUIP:LOCAL:체위2 == 1))
			PRINT 의 몸을 밀착시키고, 
		;정상위＋정상위
		ELSEIF TEQUIP:체위 == 1 && TEQUIP:LOCAL:체위2 == 1
			;TWKR 조사처리용 추가 열
			PRINTFORM %조사만처리(CALLNAME:LOCAL,"를")% 
			PRINT 위를 보게 해 나란히 늘어놓고, 
		ELSE
			PRINT 의 엉덩이를 이쪽을 향하게 하고, 
		ENDIF
		SIF TALENT:MASTER:2 & 2
			PRINT 두 사람의 장 안 감촉의 차이를 맛보면서 
		PRINTL 다시 삽입을 반복했다…
	ELSE
		IF RAND:2 == 0
			PRINTFORM %CALLNAME:LOCAL%의 장내에 삽입한 채 %타겟을% 끌어안고서, 
			IF RAND:2 == 0
				PRINT 그 
				SIF PALAM:윤활 >= PALAMLV:4
					PRINT 미끈미끈한 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@도 
			ELSE
				IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
					PRINT 위아래로 늘어선 
				ELSE
					PRINT 옆으로 늘어선 
				ENDIF
				PRINT 두 개의 
				SIF PALAM:윤활 >= PALAMLV:4 && PALAM:LOCAL:윤활 >= PALAMLV:4
					PRINT 미끈미끈한 
				PRINTFORM \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@을 
			ENDIF
		ELSE
			IF TEQUIP:체위 == 2 && (TEQUIP:LOCAL:체위2 == 1 || TEQUIP:LOCAL:체위2 == 4)
				IF RAND:2 == 0
					PRINTFORM %타겟과% %조사처리(CALLNAME:LOCAL,"를")% 서로 껴안게 해서, 그 \@ ( TALENT:체형 < 0 && TALENT:LOCAL:체형 < 0 ) ? 미숙한 구멍 # 항문 \@을 
				ELSE
					PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 구멍 # 항문 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 구멍 # 항문 \@을 위아래로 늘어놓고, 
				ENDIF
			ELSE
				PRINTFORM %CALLNAME:TARGET%의 \@ TALENT:체형 < 0 ? 미숙한 구멍 # 항문 \@과 %CALLNAME:LOCAL%의 \@ TALENT:LOCAL:체형 < 0 ? 미숙한 구멍 # 항문 \@을 옆으로 늘어놓고, 
			ENDIF
		ENDIF
		PRINTL 교대로 꿰뚫었다…
	ENDIF

	IF TALENT:연모 && TALENT:LOCAL:연모
		WAIT
		PRINTFORML %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 번갈아가며 삽입되면서, %CALLNAME:MASTER%의 %조사처리(TINKO(PLAYER),"를")% 서로 조른다…
	ELSEIF EXP:레즈경험 >= 1000 && EXP:LOCAL:레즈경험 >= 1000
		WAIT
		PRINTFORML %타겟과% %조사처리(CALLNAME:LOCAL,"는")% 서로의 혀를 휘감으며, %CALLNAME:MASTER%의 %조사처리(TINKO(PLAYER),"를")% 받아들이고 있다…
	ENDIF

	;前回の共同参加者の保存
	TFLAG:152 = LOCAL
ENDIF

;-------------------------------------------------
;17,乳首同時吸い
;-------------------------------------------------
@MESSAGE_SCOM17
SIF FLAG:70 == 1
	PRINTFORM 시간이 멈춘 
IF TEQUIP:50 == PLAYER || TEQUIP:51 == PLAYER
	PRINTFORM %CALLNAME:TARGET%\@FLAG:70 ?%조사만처리(CALLNAME:TARGET,"를")% 범하면서 #%조사만처리(CALLNAME:TARGET,"와")% 이어진 채 \@ 
ELSE
	PRINTFORM %CALLNAME:TARGET%의 
ENDIF
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)% 위로  
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:모유체질 && RAND:3 == 0
	PRINTFORM %조사처리(TEXTR("암컷젖/모유/밀크"),"를")% 밴 
ENDIF
IF TALENT:TARGET:가슴사이즈 >= 1
	PRINTFORML %조사처리(OPPAI_DESCRIPTION(TARGET),"를")% 모아올려, 
	PRINTFORML 두 개의 %조사처리(NIPPLE_DESCRIPTION(TARGET,1),"를")% %TEXTR("동시에 입에 머금어 살짝 깨물면서 희롱해가며 빨아들였다/동시에 물고 늘어져, 추잡한 소리를 내가며 빨아들였다/동시에 입에 머금고, 유륜 주위를 애태우듯이 핥아가며 빨았다/동시에 입에 머금고, 입안에서 마주 비벼 문대면서 자극했다")%……
ELSE
	PRINTFORML %OPPAI_DESCRIPTION(TARGET)%의 %TEXTR("앞의, /끝에 있는, /")%
	PRINTFORML %조사처리(NIPPLE_DESCRIPTION(TARGET,1),"를")% %TEXTR("번갈아 입에 머금어 살짝 깨물면서 희롱해가며 빨아들였다/번갈아 물고 늘어져, 추잡한 소리를 내가며 빨아들였다/번갈아 입에 머금고, 유륜 주위를 애태우듯이 핥아가며 빨았다")%……
ENDIF
IF ABL:TARGET:친밀 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:B감각 
	CASE 0,1
		PRINTFORM %타겟은% 쓴웃음을 지으면서도, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다
	CASE 2 TO 5
		PRINTFORM %타겟은% 쾌감을 견디면서, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 상냥하게 껴안았다
	CASE 6 TO 9
		PRINTFORM %타겟은% 쾌감에 허덕이면서, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 힘껏 부둥켜안았다
	CASEELSE
		PRINTFORML %타겟은% 쾌감을 견디지 못하는 듯 %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%에게 매달려,
		PRINTFORML 한층 더해진 애무와 더욱 강한 유두에의 자극을 요구해 왔다
	ENDSELECT
ENDIF

;-------------------------------------------------
;18,후배위스팽킹
;-------------------------------------------------
@MESSAGE_SCOM18
PRINTFORM %조교자는% %CALLNAME:TARGET%에 허리를 쳐박으면서, 
SIF TEQUIP:0 & 1 && (EQUIP:치마 || EQUIP:하반신윗도리 || EQUIP:원피스 || EQUIP:기모노)
	PRINTFORM %조사처리(SHOW_BOTTOM(TARGET,1),"를")% 들추고 
SIF EQUIP:TARGET:레오타드 || EQUIP:TARGET:보디슈트
	PRINTFORM %SHOW_하반신속옷(TARGET,2)%에 감싸인 
PRINTFORML %조사처리(HIP(TARGET),"를")% 요란한 소리를 내며 후려쳤다

;-------------------------------------------------
;19,授乳プレイ
;-------------------------------------------------
@MESSAGE_SCOM19
PRINTFORM %타겟은% %마스터를% 가슴에 끌어안고, 
IF TEQUIP:1 == 0
	PRINT 
ELSE
	FOR LOCAL,17,6,-1
		IF GROUPMATCH(LOCAL, 13, 12, 11)
			CONTINUE
		ENDIF
		IF EQUIP:TARGET:LOCAL 
			PRINTFORM %CLOTHNAME(LOCAL,EQUIP:TARGET:LOCAL)% 너머로 
			BREAK
		ENDIF
	NEXT
ENDIF
IF TALENT:모유체질 && RAND:3 == 0
	PRINTFORM %조사처리(TEXTR("암컷젖/모유/밀크"),"를")% 배어내었다
ENDIF
SIF TALENT:TARGET:가슴사이즈 >= 1
	PRINTFORM %조사처리(OPPAI_DESCRIPTION(TARGET),"를")% 들어 올려, 
PRINTFORML %조사처리(NIPPLE_DESCRIPTION(TARGET,1),"를")% %CALLNAME:MASTER%의 입에 물려준 채 %조사처리(TINKO(PLAYER),"를")% 훑어주기 시작했다
PRINTFORM %마스터는% %CALLNAME:TARGET%에게 몸을 맡기고, 
SELECTCASE RAND:4
CASE 0
	PRINTFORML %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 살짝 깨물면서 혀로 희롱해가며 빨아들였다……
CASE 1
	PRINTFORML %NIPPLE_DESCRIPTION(TARGET)%에 물고 늘어져, 추잡한 소리를 내가며 빨아들였다……
CASE 2
	PRINTFORML 유륜 주위를 애태우듯이 핥아가며, %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 들이마셨다……
CASEELSE
	PRINTFORML %조사처리(NIPPLE_DESCRIPTION(TARGET),"를")% 혀끝으로 굴리고 나서, 갓난아기처럼 들이마셨다……
ENDSELECT
IF ABL:TARGET:친밀 >= 10 && !FLAG:70
	SELECTCASE ABL:TARGET:B감각 
	CASE 0,1
		PRINTFORM %타겟은% 쓴웃음을 지으면서도, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 상냥하게 어루만졌다
	CASE 2 TO 5
		PRINTFORM %타겟은% 쾌감을 견디면서, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 상냥하게 껴안았다
	CASE 6 TO 9
		PRINTFORM %타겟은% 쾌감에 허덕이면서, %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 힘껏 부둥켜안았다
	CASEELSE
		PRINTFORML %타겟은% 쾌감을 견디지 못하는 듯 %OPPAI_DESCRIPTION(TARGET)%에 달라붙은 %CALLNAME:MASTER%의 머리를 끌어안고,
		PRINTFORML 보다 강한 유두에의 자극을 요구해 왔다
	ENDSELECT
ENDIF
