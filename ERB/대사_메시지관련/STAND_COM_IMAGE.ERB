;立ち絵 추가 리소스 세트 관련
@DISPLAY_COM_IMAGE(리소스명, COMNO)
#DIMS 리소스명
#DIM COMNO
IF !FLAG:스탠딩종류 && CHECK_NUDE(NO:TARGET)
	리소스명 = 立ち絵_裸_COM_{NO:TARGET}_{COMNO}
	;PRINTFORML うふふ画像存在チェック%リソース名%
	IF SPRITECREATED(리소스명)
		CALL 画像表示(리소스명, 0, 0, 0, 100)
		CALL ピクセル自動改行(デフォルトキャラ画像横幅)
		CALL 画像描画終了
	ELSE
		;PRINTFORML 存在しません
	ENDIF
ELSE
	;PRINTFORML 立ち絵追加リソース表示しません
ENDIF
