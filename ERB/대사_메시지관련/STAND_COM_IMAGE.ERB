;立ち絵 추가 리소스 세트와 보테 관련
@DISPLAY_COM_IMAGE(COMNO)
#DIMS 리소스명
#DIM COMNO
리소스명 '= ""
IF !FLAG:스탠딩종류 && CHECK_NUDE(NO:TARGET)
	리소스명 = 立ち絵_裸_COM_{NO:TARGET}_{COMNO}
	;ELSEIF SHOW_BOTE(NO:TARGET)
	리소스명 = ボテ_{NO:TARGET}
ENDIF
SIF SPRITECREATED(리소스명)
	CALL PRINT_IMAGE(리소스명)

@SHOW_BOTE(CHARA)
#FUNCTION
#DIM CHARA
SIF CFLAG:CHARA:의상일시변경
	RETURNF 0
SIF CFLAG:CHARA:우후후
	RETURNF 0
SIF TEQUIP:CHARA:상반신착의상황 == 0 || TEQUIP:CHARA:하반신착의상황 == 0
	RETURNF 0
SIF CFLAG:CHARA:임신경과일수 < 40 || SELECTCOM != 306
	RETURNF 0
RETURNF 1

@IS_BUNNY(CHARA)
#FUNCTION
#DIM CHARA
SIF CHECK_NUDE(CHARA)
	RETURNF 0
SIF CFLAG:CHARA:의상일시변경 == 55
	RETURNF 1
SIF CFLAG:CHARA:기본복장세트 == 55 && CFLAG:CHARA:의상일시변경==0
	RETURNF 1
RETURNF 0
