@조교전서비스
$LOOP
VARSET LOCAL
DRAWLINE
PRINTFORML 어떤 서비스를 이용할까 ?    | 현재 조교 포인트 : {조교포인트}p
DRAWLINE
PRINTFORML [000] 체력 업(5p)
PRINTFORML [001] 기력 업(5p)
PRINTFORML [002] 메로메로(5p)
PRINTFORML [003] 권능:지배(5p)
PRINTFORML [004] 스탯 초기화(10p)
PRINTFORML [999] 돌아간다
INPUT
DRAWLINE
IF RESULT == 999
    RETURN
ELSEIF RESULT == 0
    IF 조교포인트 < 5
        PRINTFORML 조교포인트가 부족합니다
    ELSE
        MAXBASE:MASTER:체력 += 300
        PRINTFORML 당신의 체력이 300 상승했습니다
        조교포인트 -= 5
    ENDIF
ELSEIF RESULT == 1
    IF 조교포인트 < 5
        PRINTFORML 조교포인트가 부족합니다
    ELSE
        MAXBASE:MASTER:기력 += 300
        PRINTFORML 당신의 기력이 300 상승했습니다
        조교포인트 -= 5
    ENDIF
ELSEIF RESULT == 2
    IF 조교포인트 < 5
        PRINTFORML 조교포인트가 부족합니다
    ELSE
        PRINTL 누구를?
        DRAWLINE
        FOR LOCAL,1,CHARANUM
            PRINTFORMLC [{LOCAL,3}] %CALLNAME:LOCAL,10% 호:{CFLAG:LOCAL:호감도}
            LOCAL:1++
            IF LOCAL:1 == 3
                PRINTL
            ENDIF
        NEXT
        PRINTL
        PRINTL [999] 돌아간다
        INPUT
        IF RESULT == 999
            GOTO LOOP
        ELSE
            IF INRANGE(RESULT,1,CHARANUM)
                CFLAG:RESULT:호감도 += 500
                PRINTFORML %CALLNAME:RESULT%의 호감도가 500 상승했습니다
            ELSE
                GOTO LOOP
            ENDIF
        ENDIF
        조교포인트 -= 5
    ENDIF
ELSEIF RESULT == 3
    IF 조교포인트 < 5
        PRINTFORML 조교포인트가 부족합니다
    ELSE
        PRINTL 누구를?
        DRAWLINE
        FOR LOCAL,1,CHARANUM
            PRINTFORM [{LOCAL,3}] %CALLNAME:LOCAL,14% 종속:{CFLAG:LOCAL:종속도,3} 
            LOCAL:1++
            IF LOCAL:1 == 4
                PRINTL
                LOCAL:1 = 0
            ENDIF
        NEXT
        PRINTL
        PRINTL [999] 돌아간다
        INPUT
        IF RESULT == 999
            GOTO LOOP
        ELSE
            LOCAL = RESULT
            IF INRANGE(LOCAL,1,CHARANUM)
                IF CFLAG:LOCAL:종속도 < 100
                    CALL 종속도상승(RESULT,5)
                    PRINTFORML %CALLNAME:LOCAL%의 종속도가 5\%p 상승했습니다
                ELSE
                    GOTO LOOP
                ENDIF
            ELSE
                GOTO LOOP
            ENDIF
        ENDIF
        조교포인트 -= 5
    ENDIF
ELSEIF RESULT == 4
    IF 조교포인트 < 10
        PRINTFORML 조교포인트가 부족합니다
    ELSE
        CALL 레벨리셋
        IF RESULT != 1
            조교포인트 -= 10
        ENDIF
    ENDIF
ENDIF
GOTO LOOP


