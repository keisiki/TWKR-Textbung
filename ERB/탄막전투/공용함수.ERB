@PRINT_SPELLNAME(ARG)
CALLFORMF T_SKILL{ARG}_STR("스킬명")
IF T_시전자 == MASTER
    SETCOLOR 0xFFFFFF
ELSE
    SETCOLOR 0xFF4444
ENDIF
PRINTFORML %RESULTS%
RESETCOLOR

@T_행동력회복(ARG,ARGS,ARG:2 = 1)
IF ARGS == "행동"
    T_행동기회:ARG:T_통상행동 += ARG:2
ELSEIF ARGS == "이동"
    T_행동기회:ARG:T_이동행동 += ARG:2
ELSE
    THROW 행동력 회복 함수 코딩실수
ENDIF