@IS_COOLDOWN(ARG)
#FUNCTION
SIF ARG > 9999
    RETURNF -1
IF 사용가능스킬:ARG == 1
    RETURNF 1
ELSE
    RETURNF 0
ENDIF

@사용가능스킬목록생성
VARSET 사용가능스킬
FOR LOCAL,1,T_스킬한계값
    CALL CAN_T_SKILL(사고패턴당사자,LOCAL)
    사용가능스킬:LOCAL = RESULT
NEXT